import os

BASE = "/Users/pf/workspace/brainscience/content/studynote/09_security/04_endpoint_security"

files = {
346: ("346_jop.md", "346. JOP (Jump-Oriented Programming)", """
## 핵심 인사이트 (3줄 요약)
> 1. **본질**: JOP(Jump-Oriented Programming)은 ROP(Return-Oriented Programming)의 변형으로, `ret` 명령어 대신 `jmp` 명령어로 끝나는 가젯 체인을 활용하여 CFI/Shadow Stack을 우회하는 코드 재사용 공격이다.
> 2. **가치**: Shadow Stack이나 Return Address Protection이 적용된 환경에서도 `jmp`·`call`을 기반으로 공격 체인을 구성할 수 있어, 더 정교한 방어 계층이 필요하다.
> 3. **판단 포인트**: JOP 방어는 CFI(Control Flow Integrity) + IBT(Indirect Branch Tracking)의 조합으로 간접 분기 목표를 화이트리스트 기반으로 제한하는 것이 핵심이다.

---

## Ⅰ. 개요 및 필요성

JOP(Jump-Oriented Programming)은 2011년 Tyler Bletsch 등이 발표한 기법으로, ROP의 `ret` 의존성을 제거하기 위해 `jmp` 명령어로 끝나는 가젯을 연결한다. ROP에 대한 방어로 Shadow Stack이 도입되자, 공격자들은 리턴 주소를 건드리지 않고 `jmp reg` 형태의 가젯만으로 체인을 구성하는 방식으로 대응했다.

JOP 체인의 핵심은 디스패처(Dispatcher) 가젯이다. 디스패처는 가젯 카탈로그(스택이나 특정 메모리 영역)에서 다음 가젯 주소를 읽어 `jmp`로 이동하는 역할을 한다. 이 구조 덕분에 스택의 리턴 주소를 변조하지 않아도 되므로 Stack Canary와 Shadow Stack을 동시에 우회할 수 있다.

JOP는 CTF(Capture The Flag) 문제와 실제 공격 코드에서 모두 사용되며, 특히 커널 공격에서 JIT(Just-In-Time) 스프레이와 결합된 형태로 등장한다.

📢 **섹션 요약 비유**: JOP는 `ret`이라는 귀가 신호 대신 `jmp`라는 직접 이동 신호만으로 공연자(가젯)들을 이어주는 방식—감시원(Shadow Stack)이 귀가만 체크하므로, 아예 귀가하지 않는 이동 경로를 만든 것이다.

---

## Ⅱ. 아키텍처 및 핵심 원리

### JOP 구성 요소

| 구성 요소 | 설명 | 예시 |
|:---|:---|:---|
| 디스패처(Dispatcher) | 다음 가젯 주소를 로드·점프 | `mov rax,[rdi]; jmp rax` |
| 기능 가젯(Functional) | 실제 동작 수행 후 jmp | `mov [rbx],rax; jmp rdx` |
| 초기화 가젯(Initiator) | 레지스터 설정 | `pop rdi; jmp rax` |
| 가젯 카탈로그 | 가젯 주소 배열 | 힙 or .data 영역에 배치 |

```
┌──────────────────────────────────────────────────────┐
│                  JOP 실행 흐름                        │
├──────────────────────────────────────────────────────┤
│  취약점 트리거                                        │
│      │                                               │
│      ▼                                               │
│  Initiator Gadget ──→ 레지스터(rdi) = 카탈로그 주소  │
│      │                                               │
│      ▼                                               │
│  Dispatcher Gadget ──→ 카탈로그[i] 주소 → jmp        │
│      │                                               │
│      ▼                                               │
│  Functional Gadget 1 → jmp → Dispatcher             │
│      │                                               │
│      ▼                                               │
│  Functional Gadget 2 → jmp → ... (체인 계속)         │
└──────────────────────────────────────────────────────┘
```

📢 **섹션 요약 비유**: JOP는 스택 대신 힙에 있는 전화번호부(가젯 카탈로그)를 보고 다음 공연자에게 직접 뛰어가는(jmp) 방식—경비원(Shadow Stack)은 전화번호부를 확인하지 않는다.

---

## Ⅲ. 비교 및 연결

| 기법 | 분기 타입 | Shadow Stack 우회 | CFI 우회 | 대표 방어 |
|:---|:---|:---|:---|:---|
| ROP | ret | 어려움 | 부분 | Shadow Stack, CFI |
| JOP | jmp | 가능 | 부분 | IBT, CFI |
| COP | call | 가능 | 부분 | IBT+CFI |
| COOP | vtable call | 가능 | 어려움 | Fine-grained CFI |

ROP에 비해 JOP는 Shadow Stack을 우회하지만, Intel CET의 IBT(Indirect Branch Tracking)가 활성화되면 인가된 `ENDBR64` 명령어가 없는 가젯은 분기 목표가 될 수 없어 차단된다.

📢 **섹션 요약 비유**: ROP는 뒷문(ret)으로 탈출, JOP는 창문(jmp)으로 탈출—Shadow Stack은 뒷문만 잠갔으니 창문을 통해 빠져나오는 셈이다.

---

## Ⅳ. 실무 적용 및 기술사 판단

JOP 방어를 위해서는 Intel CET의 IBT 활성화가 가장 효과적이며, 컴파일러에서 `-fcf-protection=full` 옵션으로 `ENDBR64` 명령어를 삽입한다. CFI(Control Flow Integrity)가 추가로 적용되면 합법적인 간접 분기 목표를 제한할 수 있다.

기술사 답안에서는 ROP와 함께 "코드 재사용 공격(Code Reuse Attack)"의 두 가지 주요 기법으로 묶어 서술하고, 방어 계층으로 ASLR+PIE(가젯 주소 은닉), CET/IBT(간접 분기 제한), CFI(제어 흐름 무결성)의 3단계를 제시한다.

📢 **섹션 요약 비유**: JOP를 막으려면 창문마다 ENDBR 자물쇠(IBT)를 달아야 한다—자물쇠 없는 창문으로는 뛰어들 수 없다.

---

## Ⅴ. 기대효과 및 결론

IBT와 CFI가 결합된 환경에서는 JOP 체인 구성에 필요한 합법적 가젯의 수가 극적으로 감소한다. 특히 fine-grained CFI는 각 간접 분기의 유효 목표를 코드 분석 시점에 제한하여, 실용적인 JOP 체인 구성을 사실상 불가능하게 만든다.

장기적으로 메모리 안전 언어(Rust, Go, Swift)로의 전환이 ROP/JOP 공격의 근본 원인인 메모리 오류를 원천 차단하는 최선의 방어이다.

📢 **섹션 요약 비유**: 창문에 모두 자물쇠를 달고(IBT), 허용된 방문자 명단(CFI)을 만들면, 아무리 창문으로 뛰어들려 해도 들어올 방법이 없다.

---

### 📌 관련 개념 맵
| 개념 | 관계 | 설명 |
|:---|:---|:---|
| ROP (Return-Oriented Programming) | 유사 기법 | ret 기반 코드 재사용 공격 |
| COP (Call-Oriented Programming) | 유사 기법 | call 기반 코드 재사용 공격 |
| IBT (Indirect Branch Tracking) | 주요 방어 | Intel CET의 간접 분기 추적 |
| CFI (Control Flow Integrity) | 주요 방어 | 합법적 제어 흐름만 허용 |
| NX/DEP | 관련 방어 | 실행 불가 메모리 설정 |

### 👶 어린이를 위한 3줄 비유 설명
- JOP는 나쁜 사람이 `점프(jmp)`를 이용해 여러 코드 조각을 이어 달리는 공격이야.
- 경비원(Shadow Stack)은 `돌아오기(ret)`만 감시해서, 점프로 이동하면 모르고 지나쳐.
- 그래서 모든 점프 목표에 특별 표시(ENDBR)를 달아 표시 없는 곳은 점프 못 하게 막아야 해!
"""),

347: ("347_cop.md", "347. COP (Call-Oriented Programming)", """
## 핵심 인사이트 (3줄 요약)
> 1. **본질**: COP(Call-Oriented Programming)은 `call` 명령어로 끝나는 가젯을 체인으로 연결하는 코드 재사용 공격으로, 함수 호출 ABI(Application Binary Interface)를 악용하여 ROP/JOP보다 자연스러운 실행 흐름을 위장한다.
> 2. **가치**: `call`은 반환 주소를 스택에 쌓으므로 기존 CFI 도구가 합법적 호출로 오인하기 쉽고, 객체지향 프로그램의 vtable 콜과 결합하면 COOP(Counterfeit Object-Oriented Programming)으로 발전한다.
> 3. **판단 포인트**: COP 방어는 IBT(Indirect Branch Tracking) + 타입 기반 CFI(Type-safe CFI)의 결합이 필요하며, 단순 Shadow Stack만으로는 차단이 어렵다.

---

## Ⅰ. 개요 및 필요성

COP(Call-Oriented Programming)은 2014년 Nicolas Carlini 등이 제안한 기법으로, `call reg` 혹은 `call [mem]`으로 끝나는 가젯을 연결하여 ROP 체인과 유사한 공격을 구성한다. `call` 명령어는 스택에 반환 주소를 push하므로, 기존 Shadow Stack 기반 방어를 단독으로 우회하기보다 이를 함께 고려한 복잡한 체인 설계가 필요하다.

COP는 특히 C++ 프로그램의 가상 함수 디스패치(vtable dispatch)와 결합될 때 강력해진다. COOP(Counterfeit Object-Oriented Programming)은 가짜 C++ 객체를 만들어 vtable 포인터를 조작하고, 가상 함수 호출 흐름 전체를 공격자가 제어하는 고급 기법이다.

📢 **섹션 요약 비유**: COP는 "함수 호출(call)"이라는 정상적인 회의 소집 방식을 악용해—보안 요원이 "합법적 회의"로 보는 사이에 비밀 공작을 진행하는 것이다.

---

## Ⅱ. 아키텍처 및 핵심 원리

### COP 가젯 유형

| 가젯 유형 | 예시 | 역할 |
|:---|:---|:---|
| Call-preceded 가젯 | `mov rax,rbx; call rcx` | 레지스터 조작 후 간접 호출 |
| Dispatcher call | `call [rdi+8]` | vtable 방식 디스패치 |
| Epilogue 가젯 | `add rsp,N; call [rsp+M]` | 스택 프레임 이동 |
| COOP 가젯 | `call [rax+vtable_offset]` | 가짜 객체 vtable 활용 |

```
┌─────────────────────────────────────────────────────┐
│                COP 실행 흐름                         │
├─────────────────────────────────────────────────────┤
│  취약점 트리거 (vtable 포인터 또는 함수 포인터 덮기) │
│      │                                              │
│      ▼                                              │
│  call rcx ──→ 가젯 1 실행 ──→ call rdx             │
│      │                                              │
│      ▼                                              │
│  가젯 2 실행 ──→ call [rax+0x18] (vtable dispatch)  │
│      │                                              │
│      ▼                                              │
│  목표 동작 완료 (쉘 실행, 파일 쓰기 등)              │
└─────────────────────────────────────────────────────┘
```

📢 **섹션 요약 비유**: COP의 vtable 악용은 가짜 이사회 구성원 명단을 회의록에 삽입하여, 모든 결의가 합법인 것처럼 보이게 하는 수법이다.

---

## Ⅲ. 비교 및 연결

| 기법 | 분기 메커니즘 | 탐지 난이도 | 방어 핵심 |
|:---|:---|:---|:---|
| ROP | ret | 낮음 | Shadow Stack, CFI |
| JOP | jmp | 중간 | IBT, CFI |
| COP | call | 높음 | IBT + 타입 CFI |
| COOP | vtable call | 매우 높음 | Fine-grained CFI |

COOP는 C++ 런타임 타입 정보(RTTI)를 우회하기 위해 가짜 객체를 힙에 배치하는 Heap Spray와 결합되는 경우가 많다.

📢 **섹션 요약 비유**: ROP가 뒷문, JOP가 창문, COP는 정문(함수 호출)—가장 의심받지 않는 경로를 사용하므로 탐지가 가장 어렵다.

---

## Ⅳ. 실무 적용 및 기술사 판단

C++ 기반 소프트웨어에서 COP/COOP 방어를 위해서는 VTV(Virtual Table Verification) 또는 타입 기반 CFI(Type-safe CFI)가 필요하다. Clang의 `-fsanitize=cfi-vcall` 옵션은 가상 함수 호출 시 타입 검증을 삽입하여 COOP 체인을 차단한다.

기술사 답안에서는 ROP/JOP/COP를 "코드 재사용 공격 트리오"로 묶고, 방어 계층을 ASLR+PIE(주소 은닉), IBT(분기 제한), CFI(흐름 검증), Shadow Stack(반환 보호)의 4단계로 정리한다.

📢 **섹션 요약 비유**: 정문(함수 호출) 보안을 강화하려면 신분증(타입 검증)과 사전 방문자 명단(CFI 화이트리스트)이 모두 필요하다.

---

## Ⅴ. 기대효과 및 결론

IBT와 타입 기반 CFI가 결합된 환경에서는 COP/COOP 체인 구성에 필요한 합법적 가젯이 극적으로 감소하며, COOP를 위한 가짜 객체 배치도 CFI 타입 검증에 걸린다. 장기적으로 메모리 안전 언어와 데이터 불변성 보장이 공격 근본 원인을 제거한다.

📢 **섹션 요약 비유**: 정문에 안면 인식(CFI)+방문자 명단(IBT)을 모두 달면, 아무리 정장(합법적 call)을 입고 와도 등록되지 않은 사람은 입장 불가다.

---

### 📌 관련 개념 맵
| 개념 | 관계 | 설명 |
|:---|:---|:---|
| JOP (Jump-Oriented Programming) | 유사 기법 | jmp 기반 코드 재사용 |
| COOP | 파생 기법 | vtable 기반 COP 고급화 |
| IBT (Indirect Branch Tracking) | 주요 방어 | 간접 분기 목표 제한 |
| CFI (Control Flow Integrity) | 주요 방어 | 제어 흐름 무결성 보장 |
| VTV (Virtual Table Verification) | 방어 기술 | C++ vtable 무결성 검증 |

### 👶 어린이를 위한 3줄 비유 설명
- COP는 나쁜 사람이 "회의 소집(call)"처럼 보이는 신호를 써서 몰래 공격을 진행해.
- 특히 C++ 프로그램의 가짜 안내판(vtable)을 바꿔 원하는 방으로 유도하는 방식이야.
- 막으려면 모든 회의 소집 때 신분증(타입 검증)과 사전 등록 명단(CFI)을 확인해야 해!
"""),

348: ("348_return_to_libc.md", "348. Return-to-libc 공격", """
## 핵심 인사이트 (3줄 요약)
> 1. **본질**: Return-to-libc는 스택 오버플로우로 리턴 주소를 libc의 함수(`system()`, `execve()`)로 덮어, 공격자가 직접 작성한 코드 없이 운영체제 기능을 실행하는 NX/DEP 우회 기법이다.
> 2. **가치**: 1997년 최초 문서화 이후 ROP의 이론적 전신(前身)으로, 현재도 ASLR 없는 32비트 환경이나 정보 누출(info leak)이 가능한 환경에서 유효하다.
> 3. **판단 포인트**: ASLR이 없으면 libc 함수 주소가 고정되어 공격이 직관적이지만, ASLR+PIE+Full RELRO 환경에서는 libc 베이스 주소 누출이 선행되어야 한다.

---

## Ⅰ. 개요 및 필요성

Return-to-libc 공격은 스택 버퍼 오버플로우로 저장된 리턴 주소를 `system()` 등 libc 함수의 주소로 교체하고, 인자(`/bin/sh` 문자열 주소)를 스택에 적절히 배치하여 쉘을 실행한다. 직접 쉘코드를 작성하지 않으므로 NX(No-Execute) 비트와 DEP(Data Execution Prevention)를 우회한다.

전통적인 공격 절차는: ① 스택 오버플로우로 리턴 주소를 `system()` 주소로 덮기 → ② 리턴 주소 다음 4바이트에 `exit()` 주소(크래시 방지), 그 다음 4바이트에 `/bin/sh` 주소 배치 → ③ `system("/bin/sh")` 호출로 쉘 획득.

64비트 ABI에서는 인자가 레지스터(rdi, rsi, rdx...)로 전달되어 단순 스택 배치만으로는 부족하고, `pop rdi; ret` 같은 ROP 가젯을 먼저 실행해야 하므로 Return-to-libc와 ROP가 융합된 형태를 사용한다.

📢 **섹션 요약 비유**: Return-to-libc는 자기 칼이 없어도 남의 주방(libc)에 있는 칼(`system()`)을 빌려 요리(공격)하는 방식—빌린 칼에는 "내 코드"라는 출처가 없다.

---

## Ⅱ. 아키텍처 및 핵심 원리

### 스택 레이아웃 (32비트 기준)

| 스택 위치 | 내용 | 설명 |
|:---|:---|:---|
| `[saved_rbp+8]` | `system()` 주소 | 오버플로우로 덮은 리턴 주소 |
| `[saved_rbp+12]` | `exit()` 주소 | system() 리턴 후 정상 종료용 |
| `[saved_rbp+16]` | `/bin/sh` 주소 | system()의 첫 번째 인자 |

```
┌──────────────────────────────────────────────┐
│        Return-to-libc 스택 구조 (32비트)      │
├──────────────────────────────────────────────┤
│  [높은 주소]                                 │
│  "/bin/sh" 주소   ← system()의 arg           │
│  exit() 주소      ← system() 리턴 주소       │
│  system() 주소    ← 오버플로우로 덮은 ret addr│
│  saved EBP (덮임)                            │
│  버퍼 내용 (A*N)                             │
│  [낮은 주소]                                 │
└──────────────────────────────────────────────┘
```

📢 **섹션 요약 비유**: 편지(리턴 주소)의 수신인을 `system()` 으로 바꾸고, 편지 봉투 안에 `/bin/sh`라는 쪽지를 넣어두면 편지를 받자마자 쉘이 열린다.

---

## Ⅲ. 비교 및 연결

| 기법 | NX 우회 | ASLR 필요 | 복잡도 |
|:---|:---|:---|:---|
| 쉘코드 직접 실행 | 불가 | 무관 | 낮음 |
| Return-to-libc | 가능 | 주소 고정 필요 | 낮음 |
| ROP | 가능 | ASLR+PIE도 극복 가능 | 높음 |
| SROP (Sigreturn ROP) | 가능 | 어려움 | 매우 높음 |

Return-to-libc는 ASLR 도입 이후 단독으로는 어려워졌으나, 정보 누출 취약점(format string, side channel)과 결합하면 libc 주소를 leak하여 공격을 재개할 수 있다.

📢 **섹션 요약 비유**: ASLR은 남의 주방(libc) 위치를 무작위로 바꾸는 것—위치를 알아야(info leak) 빌릴 수 있다.

---

## Ⅳ. 실무 적용 및 기술사 판단

Return-to-libc 방어를 위한 실무 대책은 ① ASLR+PIE 활성화(libc 주소 무작위화), ② Full RELRO(GOT 쓰기 방지), ③ Stack Canary(리턴 주소 덮기 탐지), ④ FORTIFY_SOURCE(오버플로우 함수 강화 버전으로 대체)의 4중 방어이다.

기술사 답안에서는 Return-to-libc를 "NX/DEP 우회의 역사적 출발점이자 ROP의 전신"으로 정의하고, 방어 계층 진화(쉘코드→ret2libc→ROP→JOP/COP)를 흐름으로 서술한다.

📢 **섹션 요약 비유**: 주방 위치를 매번 바꾸고(ASLR), 주방 문을 잠그면(Full RELRO), 칼 빌리기가 불가능해진다.

---

## Ⅴ. 기대효과 및 결론

Return-to-libc 공격 이해는 현대 메모리 공격의 계보와 방어 기술 진화를 파악하는 데 핵심적이다. 이 기법의 등장이 ASLR 도입을 촉진했고, ASLR 우회 시도가 ROP를 낳았으며, ROP 방어로 Shadow Stack이 등장하는 연쇄 반응이 현대 CPU 보안 기능의 발전을 이끌었다.

📢 **섹션 요약 비유**: Return-to-libc는 사이버 보안의 군비 경쟁에서 "첫 번째 투석기"였고, 그 대응으로 성벽(ASLR), 해자(CFI), 성문 경비(Shadow Stack)가 차례로 세워졌다.

---

### 📌 관련 개념 맵
| 개념 | 관계 | 설명 |
|:---|:---|:---|
| NX/DEP | 우회 대상 | 스택·힙 실행 방지 |
| ASLR | 방어 기법 | libc 주소 무작위화 |
| ROP | 발전형 | ret2libc의 일반화 |
| Stack Canary | 탐지 기법 | 리턴 주소 덮기 탐지 |
| GOT (Global Offset Table) | 관련 구조 | libc 함수 주소 저장소 |

### 👶 어린이를 위한 3줄 비유 설명
- Return-to-libc는 나쁜 사람이 자기 무기 없이 학교 창고(libc)에 있는 도구를 몰래 빌려 장난치는 방식이야.
- 창고 위치(libc 주소)를 매일 바꾸면(ASLR) 어디 있는지 찾기 어려워져.
- 그래도 누군가 창고 위치를 알려주면(info leak) 여전히 위험하니, 창고 문도 잠가야(CFI) 해!
"""),

349: ("349_heap_spray.md", "349. 힙 스프레이 (Heap Spray)", """
## 핵심 인사이트 (3줄 요약)
> 1. **본질**: 힙 스프레이(Heap Spray)는 공격자가 힙 메모리 대부분을 쉘코드·NOP 슬라이드로 채워, ASLR로 주소를 예측할 수 없어도 무작위로 실행 흐름을 유도하면 명령이 실행될 확률을 높이는 기법이다.
> 2. **가치**: 브라우저·PDF 리더처럼 스크립트로 대량 객체를 할당할 수 있는 환경에서 특히 유효하며, UAF(Use-After-Free)·Type Confusion과 결합하면 신뢰성 높은 익스플로잇이 된다.
> 3. **판단 포인트**: Heap Spray 방어는 힙 무작위화(ASLR 강화)·NX 힙·JIT 컴파일러 격리·메모리 압박 탐지로 다층 구성하며, JavaScript 엔진의 JIT 스프레이 변형에는 JIT 하드닝이 추가로 필요하다.

---

## Ⅰ. 개요 및 필요성

힙 스프레이는 2001년 Neel Mehta, Halvar Flake 등이 처음 문서화한 기법으로, 스크립트 언어(JavaScript, ActionScript)나 OLE 자동화로 수백~수천 개의 대형 문자열·배열을 할당하여 힙을 원하는 패턴으로 가득 채운다. NOP 슬라이드로 둘러싸인 페이로드가 힙의 많은 부분을 차지하면, 제어 흐름이 힙의 어느 곳으로 튀어도 NOP 슬라이드를 타고 내려와 페이로드를 실행하게 된다.

브라우저 UAF(Use-After-Free) 취약점 익스플로잇에서 힙 스프레이는 해제된 객체의 메모리를 공격자 데이터로 빠르게 재점유(reclaim)하는 용도로 사용된다. 특히 Internet Explorer, Adobe Flash, PDF 처리 엔진에서 실제 사고로 이어졌다.

📢 **섹션 요약 비유**: 힙 스프레이는 광활한 들판(힙)에 낙하산 병력(페이로드)을 가득 뿌려—어디에 착지해도 아군이 있어 전투(코드 실행)가 가능하게 하는 전략이다.

---

## Ⅱ. 아키텍처 및 핵심 원리

### 힙 스프레이 페이로드 구조

| 구성 요소 | 내용 | 크기 |
|:---|:---|:---|
| NOP 슬라이드 | `\x90\x90...` | 수십~수백 KB |
| 쉘코드/페이로드 | 실제 공격 코드 | 수백 바이트~수 KB |
| 반복 패턴 | NOP+페이로드 블록 반복 | 블록 × N회 |
| 목표 주소 | 예측 가능한 힙 영역 | 예: 0x0c0c0c0c |

```
┌─────────────────────────────────────────────────────┐
│             힙 스프레이 메모리 배치                  │
├─────────────────────────────────────────────────────┤
│ [힙 낮은 주소]                                      │
│  NOP NOP NOP ... NOP [쉘코드] NOP NOP ... [쉘코드]  │
│  NOP NOP NOP ... NOP [쉘코드] NOP NOP ... [쉘코드]  │
│  (수백 MB 반복)                                     │
│  ↑ 제어 흐름이 여기로 오면 NOP를 타고 쉘코드 도달  │
│ [힙 높은 주소]                                      │
└─────────────────────────────────────────────────────┘
```

📢 **섹션 요약 비유**: NOP 슬라이드는 활주로—어디서 착륙해도 미끄러져 쉘코드(격납고)에 도착하게 설계됐다.

---

## Ⅲ. 비교 및 연결

| 기법 | 목적 | ASLR 필요 여부 | 결합 공격 |
|:---|:---|:---|:---|
| 힙 스프레이 | 주소 예측 대신 확률 향상 | 우회 기법 | UAF, Type Confusion |
| 힙 펑수이 | 힙 레이아웃 정밀 제어 | 우회 기법 | Double Free, UAF |
| JIT 스프레이 | JIT 코드 캐시에 페이로드 | JIT 캐시 활용 | JS 엔진 취약점 |

힙 스프레이는 확률적 접근이지만, 힙 펑수이(Heap Feng Shui)는 힙의 구조를 정밀하게 제어하여 결정론적(deterministic) 공격을 가능하게 한다.

📢 **섹션 요약 비유**: 힙 스프레이가 "씨앗을 뿌리는" 전략이라면, 힙 펑수이는 "정원을 설계하는" 전략이다.

---

## Ⅳ. 실무 적용 및 기술사 판단

현대 브라우저는 힙 스프레이 방어를 위해 ① 힙 주소 무작위화(Heap ASLR), ② NX 힙(힙 메모리 실행 방지), ③ JIT 컴파일러 코드 무작위화, ④ 메모리 압박 탐지(할당 폭발 감지)를 구현한다. Chrome의 V8 엔진과 Firefox의 SpiderMonkey는 JIT 페이지를 별도 메모리 영역에 분리하고 W^X(Write XOR Execute) 원칙을 적용한다.

기술사 답안에서는 힙 스프레이를 "ASLR 우회의 확률적 접근"으로 정의하고, 방어 대책을 NX+힙무작위화+JIT격리의 3층으로 서술한다.

📢 **섹션 요약 비유**: 들판에 지뢰(힙 가드 페이지)를 무작위로 심고, 밤에 불을 끄면(힙 ASLR) 씨앗(스프레이)이 어디 떨어질지 예측할 수도 없고, 착지해도 폭발한다.

---

## Ⅴ. 기대효과 및 결론

현대 브라우저 보안의 발전—샌드박스, Site Isolation, JIT 하드닝, MiraclePtr—은 힙 스프레이와 UAF 공격에 대한 대응으로 추진되었다. 힙 스프레이 방어 기술의 발전이 브라우저 보안 전체의 성숙도를 높이는 핵심 동력 중 하나였다.

📢 **섹션 요약 비유**: 힙 스프레이 방어 역사는 "씨 뿌리는 자(공격자)"와 "들판을 계속 바꾸는 농부(방어자)" 사이의 긴 게임으로, 결국 더 스마트한 들판 설계(격리, 샌드박스)가 승리했다.

---

### 📌 관련 개념 맵
| 개념 | 관계 | 설명 |
|:---|:---|:---|
| UAF (Use-After-Free) | 결합 공격 | 해제 메모리 재점유에 힙 스프레이 활용 |
| 힙 펑수이 | 발전 기법 | 결정론적 힙 레이아웃 제어 |
| ASLR | 방어 기법 | 힙 시작 주소 무작위화 |
| NX (No-Execute) | 방어 기법 | 힙 실행 방지 |
| JIT 스프레이 | 변형 기법 | JIT 코드 캐시를 스프레이에 활용 |

### 👶 어린이를 위한 3줄 비유 설명
- 힙 스프레이는 나쁜 사람이 컴퓨터 메모리(힙)에 나쁜 코드를 가득 채우는 거야.
- 어디에 코드가 실행되든 나쁜 코드가 나올 만큼 많이 채워두는 방법이야.
- 막으려면 메모리 위치를 계속 바꾸고(ASLR), 거기서 코드 실행 자체를 막아야(NX) 해!
"""),

350: ("350_heap_feng_shui.md", "350. 힙 펑수이 (Heap Feng Shui)", """
## 핵심 인사이트 (3줄 요약)
> 1. **본질**: 힙 펑수이(Heap Feng Shui)는 특정 순서로 메모리 할당·해제를 수행하여 힙의 레이아웃을 공격자가 원하는 형태로 정밀하게 조작하는 기법으로, 확률적 힙 스프레이와 달리 결정론적(deterministic) 공격을 가능하게 한다.
> 2. **가치**: UAF(Use-After-Free), Double Free, Type Confusion 취약점의 신뢰성을 높이기 위해 힙 레이아웃을 예측 가능하게 만드는 전처리 단계로 사용된다.
> 3. **판단 포인트**: 힙 펑수이 방어는 힙 청크 격리(Guard Page), 할당자 무작위화(ASLR heap), 타입 안전 할당자(PartitionAlloc) 도입으로 레이아웃 예측을 어렵게 하는 것이 핵심이다.

---

## Ⅰ. 개요 및 필요성

힙 펑수이(Heap Feng Shui)는 2007년 Alexander Sotirov가 Internet Explorer 브라우저 익스플로잇에서 처음 문서화한 기법이다. "풍수(風水)"라는 이름처럼 힙 메모리 공간을 공격자 의도에 맞게 "배치"하는 기술이다. 특정 크기의 객체들을 전략적으로 할당·해제하여 메모리 조각화를 유도하고, 취약한 객체와 공격 목표 객체를 인접하게 배치한다.

예를 들어 UAF 공격에서 힙 펑수이를 활용하면: ① 크기 A 객체 100개 할당 → ② 짝수 번째 해제 → ③ 취약한 크기 A 객체 할당(비어 있는 짝수 자리 중 하나에 배치됨) → ④ 취약점 트리거로 해제 → ⑤ 공격 페이로드로 같은 크기 블록 재할당. 이 과정에서 힙 펑수이가 없으면 ⑤의 재할당 위치가 예측 불가능하다.

📢 **섹션 요약 비유**: 힙 펑수이는 체스판(힙) 위의 말(객체)을 원하는 위치로 배치한 다음 게임을 시작하는 것—체스를 두기 전에 이미 승리 포지션을 만들어 놓는다.

---

## Ⅱ. 아키텍처 및 핵심 원리

### 힙 레이아웃 조작 단계

| 단계 | 작업 | 목적 |
|:---|:---|:---|
| 1. 홀 생성 | 특정 크기 객체 할당 후 일부 해제 | 원하는 크기의 빈 청크 생성 |
| 2. 정렬 | 추가 할당으로 힙 단편화 최소화 | 타겟 객체 위치 예측 가능화 |
| 3. 배치 | 취약 객체와 공격 목표 객체 인접 배치 | 오버플로우/UAF 영향 범위 제어 |
| 4. 트리거 | 취약점 발생 | 인접 객체 데이터 조작 |

```
┌─────────────────────────────────────────────────────┐
│          힙 펑수이 레이아웃 조작 예시                │
├─────────────────────────────────────────────────────┤
│  초기:  [A][B][A][B][A][B][A][B]  (A: 타겟 크기)   │
│                                                     │
│  짝수   [A][ ][A][ ][A][ ][A][ ]  (B 해제)         │
│  해제:                                              │
│                                                     │
│  취약   [A][V][A][ ][A][ ][A][ ]  (V: 취약 객체)   │
│  배치:                                              │
│                                                     │
│  재할   [A][V][A][P][A][P][A][P]  (P: 페이로드)     │
│  당:    V 해제 후 P가 V 자리에 배치됨               │
└─────────────────────────────────────────────────────┘
```

📢 **섹션 요약 비유**: 빈 주차 공간(해제된 청크)을 미리 특정 위치에 만들어두면, 원하는 차(페이로드)가 정확히 그 자리에 주차된다.

---

## Ⅲ. 비교 및 연결

| 기법 | 결정론적 여부 | 사전 조작 | 대상 취약점 |
|:---|:---|:---|:---|
| 힙 스프레이 | 확률적 | 대량 할당 | UAF, 제어 흐름 탈취 |
| 힙 펑수이 | 결정론적 | 정밀 배치 | UAF, Double Free, 오버플로우 |
| Grooming | 결정론적 | OS/커널 힙 조작 | 커널 UAF, 커널 오버플로우 |

힙 그루밍(Heap Grooming)은 iOS/macOS 커널 익스플로잇에서 힙 펑수이와 유사하게 커널 힙을 조작하는 기법을 지칭한다.

📢 **섹션 요약 비유**: 힙 스프레이가 "씨앗 뿌리기"라면, 힙 펑수이는 "분재 가꾸기"—정밀한 손길로 원하는 형태를 만든다.

---

## Ⅳ. 실무 적용 및 기술사 판단

Chrome의 PartitionAlloc은 타입별로 힙을 분리하여 서로 다른 타입의 객체가 인접 배치되는 것을 방지한다. 이는 UAF+힙펑수이 공격에서 "재할당 단계"를 어렵게 만드는 핵심 방어이다. Windows의 Low Fragmentation Heap(LFH)도 무작위 청크 할당 순서를 도입하여 예측 가능한 레이아웃을 방해한다.

기술사 답안에서는 힙 펑수이를 "UAF 취약점의 신뢰성을 높이는 익스플로잇 프리미티브"로 정의하고, PartitionAlloc·타입 격리·힙 무작위화를 방어 삼각형으로 제시한다.

📢 **섹션 요약 비유**: PartitionAlloc은 주차장을 차종별로 분리하는 것—SUV(객체 A)와 세단(객체 B)을 같은 구역에 섞어두지 않으면, 풍수 배치가 불가능해진다.

---

## Ⅴ. 기대효과 및 결론

힙 펑수이 방어 기술의 발전은 현대 브라우저 메모리 안전성의 핵심 토대가 되었다. PartitionAlloc, MiraclePtr, XOR 링크드 리스트 등의 힙 강화 기술은 모두 결정론적 힙 조작을 어렵게 만들기 위한 노력의 산물이다. 궁극적으로 메모리 안전 언어로의 전환이 이 공격 클래스를 근본적으로 차단한다.

📢 **섹션 요약 비유**: 아무리 풍수 전문가(공격자)라도 집(힙)의 구조가 매번 바뀌고, 같은 종류끼리만 모여 살면(타입 격리) 배치를 예측할 수 없다.

---

### �� 관련 개념 맵
| 개념 | 관계 | 설명 |
|:---|:---|:---|
| UAF (Use-After-Free) | 결합 대상 | 힙 펑수이로 신뢰성 향상 |
| 힙 스프레이 | 관련 기법 | 확률적 힙 조작 |
| PartitionAlloc | 주요 방어 | 타입별 힙 분리 |
| 힙 그루밍 | 관련 기법 | 커널 힙 조작 |
| LFH (Low Fragmentation Heap) | 방어 기법 | 무작위 청크 할당 |

### 👶 어린이를 위한 3줄 비유 설명
- 힙 펑수이는 나쁜 사람이 컴퓨터 메모리 방(힙)을 미리 예쁘게 정돈해서 함정을 파는 거야.
- 마치 블록을 특정 순서로 쌓아두면 원하는 블록이 정확히 원하는 자리에 떨어지게 만드는 것처럼.
- 방어하려면 같은 종류끼리만 한 공간에 두고(PartitionAlloc) 공간 위치를 계속 바꿔야(ASLR) 해!
"""),
}

for num, (fname, title, content) in files.items():
    fpath = os.path.join(BASE, fname)
    if not os.path.exists(fpath):
        front_matter = f"""+++
weight = {num}
title = "{title}"
date = "2026-04-21"
[extra]
categories = "studynote-security"
+++
"""
        with open(fpath, 'w') as f:
            f.write(front_matter + content)
        print(f"Created: {fname}")
    else:
        print(f"Exists: {fname}")

print("Batch 1 (346-350) done.")
