import os
TARGET_DIR = "/Users/pf/workspace/brainscience/content/studynote/10_ai/05_exam_summary"
files = {}

files["362_roc_auc_math.md"] = '''+++
weight = 362
title = "362. ROC 곡선과 AUC (Receiver Operating Characteristic / Area Under Curve)"
date = "2026-04-21"
[extra]
categories = "studynote-ai"
+++

## 핵심 인사이트 (3줄 요약)
> 1. **본질**: ROC(Receiver Operating Characteristic, 수신자 조작 특성) 곡선은 분류 임계값(Threshold)을 변화시키면서 TPR(True Positive Rate, 재현율)과 FPR(False Positive Rate, 거짓 양성률)의 트레이드오프를 시각화하며, AUC(Area Under Curve)는 이 곡선 아래 면적으로 분류기의 전반적 성능을 [0,1]로 수치화한다.
> 2. **가치**: 클래스 불균형(Class Imbalance) 데이터에서 정확도(Accuracy)는 왜곡되지만 AUC는 클래스 비율에 독립적으로 분류 성능을 평가해 의료 진단, 사기 탐지의 표준 평가 지표다.
> 3. **판단 포인트**: AUC=0.5는 랜덤 분류, AUC=1.0은 완벽한 분류기이며, AUC는 "임의 양성 샘플이 임의 음성 샘플보다 높은 점수를 받을 확률"과 동치다.

---

## Ⅰ. 개요 및 필요성

암 진단 AI에서 임계값(threshold)을 낮추면 모든 환자를 양성으로 예측해 재현율(Recall=TPR)은 100%지만 거짓 양성(FP)도 폭발한다. 반대로 임계값을 높이면 확실한 케이스만 양성으로 예측해 정밀도(Precision)는 높지만 재현율이 낮아진다. 어떤 임계값이 최적인가? ROC 곡선은 임계값 전체 범위에서의 성능을 한 그래프에 담아 모델의 고유 성능을 임계값 독립적으로 평가한다.

📢 **섹션 요약 비유**: ROC 곡선은 "암 진단 기준의 엄격도 조절 다이얼"이다. 다이얼을 느슨하게 하면 더 많은 환자를 잡지만(TPR↑) 정상인도 많이 걸린다(FPR↑). 다이얼을 조이면 정상인은 안 걸리지만 환자도 놓친다. ROC 곡선은 이 다이얼의 모든 설정에서의 성능 지도다.

---

## Ⅱ. 아키텍처 및 핵심 원리

```
┌──────────────────────────────────────────────────────────┐
│  TPR, FPR 수식 및 혼동 행렬 (Confusion Matrix)           │
├──────────────────────────────────────────────────────────┤
│                 예측 양성    예측 음성                    │
│  실제 양성  │   TP          FN         │                 │
│  실제 음성  │   FP          TN         │                 │
│                                                          │
│  TPR (True Positive Rate = Sensitivity = Recall):       │
│  TPR = TP / (TP + FN)                                   │
│                                                          │
│  FPR (False Positive Rate = 1 - Specificity):           │
│  FPR = FP / (FP + TN)                                   │
│                                                          │
│  ROC 공간: X축=FPR, Y축=TPR                             │
│  AUC = ∫₀¹ TPR(FPR) d(FPR)                             │
│  = P(score(pos) > score(neg))  (Wilcoxon 통계량)        │
│                                                          │
│  임계값 변화 방향:                                      │
│  낮게 → 우상단(FPR↑, TPR↑) / 높게 → 좌하단            │
└──────────────────────────────────────────────────────────┘
```

| 지표 | 수식 | AUC 적합성 |
|:---|:---|:---|
| AUC-ROC | ∫TPR d(FPR) | 클래스 불균형 강함 |
| AUC-PR | ∫Precision d(Recall) | 양성 클래스 희귀 시 더 민감 |
| F1 Score | 2·P·R/(P+R) | 단일 임계값 기준 |
| Accuracy | (TP+TN)/N | 불균형 시 왜곡 |

📢 **섹션 요약 비유**: AUC=0.5는 "눈 감고 동전 던지기"다. 랜덤으로 양성/음성을 찍는 것과 성능이 같다. AUC=0.9는 "10번 중 9번은 실제 양성이 음성보다 높은 점수를 받는 분류기"다. 이 확률 해석이 AUC의 통계적 직관이다.

---

## Ⅲ. 비교 및 연결

AUC-PR(Precision-Recall Curve) 대 AUC-ROC: 데이터에서 양성 클래스가 극히 희귀(1% 미만)할 때(사기 탐지, 희귀 질환) AUC-ROC는 높게 나와도 실제 성능이 나쁠 수 있다. 이 경우 AUC-PR이 더 엄격한 평가 지표다. 다중 클래스에서 OvR(One-vs-Rest) 방식으로 각 클래스별 AUC를 계산해 매크로 평균을 취한다.

📢 **섹션 요약 비유**: AUC-ROC vs AUC-PR은 "전체 성적 vs 수학 성적" 비교다. 전체 과목 성적(AUC-ROC)이 90점이라도 수학(양성 클래스)이 50점일 수 있다. 중요한 과목(희귀 양성)만 집중 평가하는 것이 AUC-PR이다.

---

## Ⅳ. 실무 적용 및 기술사 판단

최적 임계값 선택: ① 유덴 지수(Youden Index) = TPR - FPR 최대점 ② 비용 민감 분류에서 오분류 비용(C_FP, C_FN) 가중 최적점 ③ F1 Score 최대점. DeLong Test: 두 모델의 AUC가 통계적으로 유의하게 다른지 검정하는 비모수 통계 검정. 구현: sklearn.metrics.roc_auc_score, roc_curve로 계산 가능.

📢 **섹션 요약 비유**: 유덴 지수로 최적 임계값 찾기는 "적군은 최대로 막고(TPR), 아군 피해는 최소로(1-FPR)" 하는 전쟁 전략 최적화다. 둘의 합(TPR-FPR)이 최대인 지점이 최선의 작전 기준선이다.

---

## Ⅴ. 기대효과 및 결론

ROC/AUC는 분류 모델의 글로벌 성능을 임계값 독립적으로 요약하는 가장 중요한 평가 지표 중 하나다. 기술사 시험에서 TPR/FPR 수식, AUC의 확률적 해석(Wilcoxon 통계량 동치), AUC-PR이 필요한 상황(불균형 데이터)을 서술하면 완성도 높은 답안이다.

📢 **섹션 요약 비유**: AUC는 의료 AI의 "면허 시험 점수"다. 단 하나의 임계값(기준)에서만 성능을 보는 게 아니라, 모든 가능한 기준에서 얼마나 잘 분류하는지의 종합 점수다. 어떤 기준을 써도 잘 작동해야 면허(AUC)가 높다.

---

### 📌 관련 개념 맵
| 개념 | 연관 키워드 | 관계 |
|:---|:---|:---|
| 혼동 행렬 (Confusion Matrix) | TP, FP, TN, FN | TPR/FPR 계산의 기반 |
| AUC-PR (Precision-Recall) | 불균형 데이터 | AUC-ROC 보완 지표 |
| 유덴 지수 (Youden Index) | 임계값 선택 | 최적 분류 기준점 |
| F1 Score | 정밀도·재현율 조화 | 단일 임계값 기준 평가 |

---

### 👶 어린이를 위한 3줄 비유 설명
1. ROC 곡선은 "암 진단 기준을 느슨하게/엄격하게 바꿀 때 성능이 어떻게 달라지는지" 그래프예요.
2. AUC는 이 그래프 아래 넓이로, 넓을수록(1에 가까울수록) AI가 더 잘 구분한다는 뜻이에요.
3. AUC=0.5는 동전 던지기 수준, AUC=1.0은 완벽한 AI예요!
'''

files["363_softmax_backprop.md"] = '''+++
weight = 363
title = "363. 소프트맥스 역전파 (Softmax Backpropagation) - 야코비안 그래디언트 공식"
date = "2026-04-21"
[extra]
categories = "studynote-ai"
+++

## 핵심 인사이트 (3줄 요약)
> 1. **본질**: 소프트맥스(Softmax) 함수의 역전파는 출력들이 서로 의존적(합이 1)이기 때문에 스칼라 미분 대신 야코비안(Jacobian) 행렬이 필요하며, i=j와 i≠j 두 케이스로 나눠 유도한다.
> 2. **가치**: 소프트맥스 + 교차 엔트로피(Cross Entropy) 손실의 결합 미분은 δ = ŷ - y (예측값 - 정답)로 극도로 단순해져, 다중 분류의 역전파 구현이 효율적이다.
> 3. **판단 포인트**: 수치 안정성(Numerical Stability)을 위해 softmax(z-max(z))처럼 최대값을 빼주는 log-sum-exp 트릭을 반드시 적용하지 않으면 exp(z) 계산에서 오버플로가 발생한다.

---

## Ⅰ. 개요 및 필요성

다중 분류 신경망의 출력층에서 소프트맥스는 각 클래스의 점수(logit)를 확률로 변환한다. 역전파 시 ∂L/∂zᵢ를 계산해야 하는데, sᵢ = exp(zᵢ)/Σexp(zⱼ)에서 sᵢ는 모든 zⱼ에 의존하므로 단순 도함수가 아닌 야코비안 행렬 ∂sᵢ/∂zⱼ (i×j 행렬)이 필요하다. 이를 교차 엔트로피와 결합하면 아름답게 단순화된다.

📢 **섹션 요약 비유**: 소프트맥스의 역전파는 "팀 점수 배분 게임의 미분"이다. 한 선수의 점수(zᵢ)가 오르면 자신의 확률이 오르고 다른 선수의 확률은 내려간다(상호의존). 이 복잡한 의존성을 야코비안 행렬로 한 번에 처리한다.

---

## Ⅱ. 아키텍처 및 핵심 원리

```
┌──────────────────────────────────────────────────────────┐
│         소프트맥스 야코비안 유도                          │
├──────────────────────────────────────────────────────────┤
│  sᵢ = exp(zᵢ) / Σⱼ exp(zⱼ)                            │
│                                                          │
│  Case 1: i = j (대각 원소)                               │
│  ∂sᵢ/∂zᵢ = sᵢ(1 - sᵢ)                                 │
│                                                          │
│  Case 2: i ≠ j (비대각 원소)                             │
│  ∂sᵢ/∂zⱼ = -sᵢ · sⱼ                                   │
│                                                          │
│  야코비안 행렬: J = diag(s) - s·sᵀ                      │
│                                                          │
│  CE + Softmax 결합 미분:                                 │
│  L = -Σᵢ yᵢ log(sᵢ)                                    │
│  ∂L/∂zᵢ = sᵢ - yᵢ = ŷᵢ - yᵢ  ← 극도로 단순!          │
│                                                          │
│  수치 안정성 트릭:                                       │
│  softmax(z) = softmax(z - max(z))  (값 동일, 안정적)   │
└──────────────────────────────────────────────────────────┘
```

| 미분 케이스 | 수식 | 의미 |
|:---|:---|:---|
| i = j | sᵢ(1-sᵢ) | 자기 자신에 대한 기울기 |
| i ≠ j | -sᵢ·sⱼ | 다른 출력의 영향 |
| CE+Softmax 결합 | ŷᵢ - yᵢ | 예측 - 정답 (단순화) |

📢 **섹션 요약 비유**: CE+Softmax 결합 그래디언트(ŷ-y)는 "점수 오차만큼 가중치 조정"이다. 예측 확률 0.7에서 정답 1.0을 빼면 -0.3이라는 수정 신호가 나온다. 이 단순한 오차 신호가 전체 신경망을 통해 역전파된다.

---

## Ⅲ. 비교 및 연결

수치 안정성 문제: exp(1000) = ∞(오버플로), exp(-1000) = 0(언더플로). softmax(z - max(z)) 트릭을 쓰면 최대값이 0이 되어 분자는 ≤1, 분모는 ≥1이 되어 항상 수치적으로 안전하다. PyTorch의 F.cross_entropy()는 내부적으로 LogSoftmax + NLLLoss로 수치 안정적으로 구현되어 있어 별도 softmax를 출력에 추가하지 않는 것이 권장된다.

📢 **섹션 요약 비유**: log-sum-exp 트릭은 "온도계 범위 조정"이다. 섭씨 1000°C를 섭씨로 재면 온도계가 터지지만(오버플로), 기준점(max)을 0°C로 맞추면 -0.3°C ~0°C 범위에서 안전하게 계산된다.

---

## Ⅳ. 실무 적용 및 기술사 판단

다중 클래스 분류에서 마지막 레이어에 소프트맥스를 붙이고 nn.CrossEntropyLoss(PyTorch)를 사용하면 수치 안정적 구현이 자동으로 된다. 반면 nn.BCEWithLogitsLoss(이진 분류)는 Sigmoid + BCE를 안정적으로 결합한다. 온도 스케일링(Temperature Scaling): softmax(z/T)에서 T>1이면 분포가 평탄화(불확실한 예측), T<1이면 날카로워짐(확신). LLM의 생성 다양성 조절에 사용.

📢 **섹션 요약 비유**: 온도 스케일링은 "AI의 자신감 조절 다이얼"이다. T=1은 보통, T=2면 "좀 더 다양하게 대답해봐(창의적)", T=0.5면 "제일 확률 높은 것만 답해(확실하게)"다. ChatGPT의 temperature 파라미터가 바로 이것이다.

---

## Ⅴ. 기대효과 및 결론

소프트맥스 역전파의 이해는 다중 분류 신경망의 학습 원리를 수학적으로 완전히 파악하는 핵심이다. 야코비안 유도(i=j, i≠j 케이스)와 CE+Softmax 결합 단순화, 수치 안정성 트릭을 함께 서술하면 딥러닝 구현 능력과 이론 이해 모두를 보여줄 수 있다.

📢 **섹션 요약 비유**: 소프트맥스 역전파는 "팀 투표의 책임 소재 계산"이다. 모든 선수가 총점을 나눠 가지므로 한 선수의 점수를 올리려면 다른 선수에게서 뺏어야 한다. 야코비안은 이 복잡한 뺏고 빼앗기는 관계를 행렬로 정확히 계산한다.

---

### 📌 관련 개념 맵
| 개념 | 연관 키워드 | 관계 |
|:---|:---|:---|
| 교차 엔트로피 (Cross Entropy) | 손실 함수 | Softmax와 결합 시 단순화 |
| 야코비안 행렬 (Jacobian) | 벡터 미분 | Softmax 역전파의 핵심 도구 |
| 온도 스케일링 | 생성 다양성 | Softmax z/T 변형 |
| LogSoftmax + NLLLoss | PyTorch 구현 | 수치 안정적 CE 구현 |

---

### 👶 어린이를 위한 3줄 비유 설명
1. 소프트맥스는 "점수를 확률로 바꿔주는 마법"이에요. 예: [3, 1, 0.5] → [0.7, 0.2, 0.1]
2. 역전파 때는 "예측 확률 - 정답"만 계산하면 되어서 아주 간단해요!
3. 단, 큰 숫자가 들어오면 컴퓨터가 폭발(오버플로)할 수 있어서 최대값을 빼는 안전 장치가 필요해요.
'''

files["364_adagrad_rmsprop.md"] = '''+++
weight = 364
title = "364. Adagrad / RMSProp 옵티마이저 - 적응적 학습률과 지수 이동 평균"
date = "2026-04-21"
[extra]
categories = "studynote-ai"
+++

## 핵심 인사이트 (3줄 요약)
> 1. **본질**: Adagrad(Adaptive Gradient Algorithm)와 RMSProp(Root Mean Square Propagation)은 각 파라미터마다 다른 학습률을 적응적으로 조정하는 옵티마이저로, 희소(Sparse) 특성이 많은 데이터에서 SGD보다 빠른 수렴을 달성한다.
> 2. **가치**: Adagrad는 자주 나타나는 특성은 학습률을 줄이고 드문 특성은 크게 유지해 텍스트 모델에 강력하지만, 학습이 진행될수록 학습률이 0에 수렴하는 치명적 소멸 문제가 있고 RMSProp이 이를 지수 이동 평균(EMA)으로 해결한다.
> 3. **판단 포인트**: Adam = RMSProp (2차 모멘트, 학습률 적응) + Momentum (1차 모멘트)의 결합이며, Adam이 실무에서 가장 널리 사용되는 이유가 여기에 있다.

---

## Ⅰ. 개요 및 필요성

기본 SGD(Stochastic Gradient Descent)는 모든 파라미터에 동일한 학습률(η)을 적용한다. 텍스트 데이터에서 "the"같은 자주 나오는 단어의 가중치는 그래디언트가 크므로 큰 업데이트가, "quasar" 같은 드문 단어는 작은 업데이트가 필요하다. Adagrad는 이를 각 파라미터의 과거 그래디언트 제곱 합(G_t)을 학습률 분모에 넣어 자동으로 조정한다.

📢 **섹션 요약 비유**: Adagrad는 "자주 쓰는 도로는 속도를 줄이고, 처음 가는 샛길은 빠르게" 학습하는 지능형 내비게이션이다. 이미 많이 업데이트된 파라미터(자주 쓴 도로)는 학습률을 줄여 안정화하고, 거의 업데이트 안 된 파라미터(새 샛길)는 빠르게 학습시킨다.

---

## Ⅱ. 아키텍처 및 핵심 원리

```
┌──────────────────────────────────────────────────────────┐
│      Adagrad vs RMSProp 수식 비교                        │
├──────────────────────────────────────────────────────────┤
│  Adagrad:                                                │
│  G_t = G_{t-1} + g_t²          (누적 제곱 합)          │
│  θ_t = θ_{t-1} - η/√(G_t+ε) · g_t                     │
│  문제: G_t는 단조 증가 → η/√(G_t) → 0 (학습률 소멸!)  │
│                                                          │
│  RMSProp:                                               │
│  E[g²]_t = ρ·E[g²]_{t-1} + (1-ρ)·g_t²  (EMA)         │
│  θ_t = θ_{t-1} - η/√(E[g²]_t+ε) · g_t                │
│  해결: EMA로 최근 그래디언트만 반영 → 학습률 안정!     │
│                                                          │
│  Adam (결합):                                           │
│  m_t = β₁·m_{t-1} + (1-β₁)·g_t  (1차 모멘트)         │
│  v_t = β₂·v_{t-1} + (1-β₂)·g_t²  (2차 모멘트)        │
│  θ_t = θ_{t-1} - η·m̂_t/√(v̂_t+ε)                    │
└──────────────────────────────────────────────────────────┘
```

| 옵티마이저 | 학습률 누적 | 소멸 문제 | 특이 사항 |
|:---|:---|:---|:---|
| SGD | 고정 | ❌ 없음 | 튜닝 어려움 |
| Adagrad | 전체 누적 | ✅ 발생 | 희소 특성에 강함 |
| RMSProp | EMA (지수) | ❌ 없음 | 비정상 시계열에 강함 |
| Adam | EMA + Momentum | ❌ 없음 | 실무 기본값 |

📢 **섹션 요약 비유**: Adagrad의 학습률 소멸은 "계단 내려가는 로봇"이다. 처음엔 큰 걸음(η)으로 빠르게 내려오지만, 걸음 수(G_t)가 쌓일수록 걸음이 점점 작아져 결국 아예 멈춰버린다(학습 정지). RMSProp은 최근 몇 걸음(EMA)만 기억해 항상 적정 속도를 유지한다.

---

## Ⅲ. 비교 및 연결

AMSGrad는 Adam의 과거 최대 2차 모멘트를 유지해 수렴 보장 문제를 해결한다. AdamW는 Adam에 가중치 감쇠(Weight Decay)를 L2 정규화와 분리하여 더 정확한 정규화를 구현한다(Transformer 훈련의 표준). Lion(Evolved Sign Momentum) 옵티마이저는 Google이 진화 알고리즘으로 발견한 Adam보다 메모리 효율적인 최신 옵티마이저다.

📢 **섹션 요약 비유**: AdamW는 "다이어트와 운동을 분리하는 체중 관리"다. 기존 Adam의 L2 정규화는 그래디언트 업데이트와 뒤섞여 효과가 감소했다. AdamW는 가중치 감쇠를 별도로 적용해 Transformer 같은 대형 모델의 정규화 효과를 극대화한다.

---

## Ⅳ. 실무 적용 및 기술사 판단

하이퍼파라미터 기본값(Adam): η=0.001, β₁=0.9, β₂=0.999, ε=1e-8. Warmup 스케줄: 학습 초기에 학습률을 0에서 천천히 올려 초기 학습 불안정성 방지(Transformer 훈련 필수). Cosine Annealing: 학습률을 코사인 커브로 감소시켜 수렴 말기 섬세한 조정. Cyclic LR: 학습률을 주기적으로 올렸다 내렸다 하며 다양한 Loss Landscape 탐색.

📢 **섹션 요약 비유**: 학습률 Warmup은 "엔진 예열"이다. 차(모델)를 처음 출발시킬 때 갑자기 최고 속도로 달리면(큰 학습률) 엔진이 망가진다. 천천히 예열(작은 학습률)한 뒤 최적 속도로 올리는 것이 Warmup이다.

---

## Ⅴ. 기대효과 및 결론

Adagrad → RMSProp → Adam으로 이어지는 적응적 학습률의 진화는 딥러닝 최적화의 핵심 발전사다. RMSProp의 EMA 아이디어가 Adam의 2차 모멘트가 되었고, Momentum이 1차 모멘트가 되어 Adam이 완성됐다. 기술사 시험에서 Adagrad 소멸 문제 → RMSProp EMA 해결 → Adam 수식까지 진화 경로를 설명하면 최고 수준의 답안이다.

📢 **섹션 요약 비유**: Adagrad→Adam 진화는 "손으로 페달 밟기 → 자전거 → 전기자동차"다. 손으로 걷기(SGD)에서 자전거 기어(Adagrad 적응 학습률), 자동 기어(RMSProp EMA), 최첨단 전기차(Adam 모멘텀+적응 학습률) 순으로 편리하고 효율적으로 진화했다.

---

### 📌 관련 개념 맵
| 개념 | 연관 키워드 | 관계 |
|:---|:---|:---|
| Adam 옵티마이저 | β₁, β₂모멘트 | RMSProp + Momentum의 결합 |
| AdamW | 가중치 감쇠 분리 | Transformer 훈련 표준 |
| 학습률 스케줄링 | Warmup, Cosine | 옵티마이저와 함께 사용 |
| 그래디언트 소실 | 학습 정지 | Adagrad의 핵심 문제 |

---

### 👶 어린이를 위한 3줄 비유 설명
1. Adagrad는 "자주 쓰는 도구는 조금씩, 새 도구는 많이" 업데이트하는 똑똑한 AI예요.
2. 하지만 시간이 지나면 모든 도구를 너무 조금씩 업데이트해서 AI가 성장을 멈추는 문제가 있어요.
3. RMSProp은 이를 고쳐서 "최근에 쓴 것만 기억"하게 해서 AI가 계속 성장할 수 있게 했어요!
'''

files["365_glove_word_embedding.md"] = '''+++
weight = 365
title = "365. GloVe (Global Vectors for Word Representation) - 전역 빈도 행렬 분해 임베딩"
date = "2026-04-21"
[extra]
categories = "studynote-ai"
+++

## 핵심 인사이트 (3줄 요약)
> 1. **본질**: GloVe(Global Vectors for Word Representation)는 전체 코퍼스의 동시 등장 행렬(Co-occurrence Matrix) X_ij를 분해하여 단어 임베딩을 학습하는 방법으로, 전역(Global) 통계를 활용해 Word2Vec의 로컬(Local) 문맥 창 한계를 극복한다.
> 2. **가치**: "왕 - 남자 + 여자 = 여왕"처럼 단어 벡터 간 산술 연산으로 의미 관계를 표현하는 능력이 Word2Vec과 동등하면서, 전역 통계를 활용해 희귀 단어와 낮은 빈도 동시 등장 패턴을 더 잘 포착한다.
> 3. **판단 포인트**: GloVe 목적 함수는 Σᵢⱼ f(X_ij)·(wᵢᵀw̃ⱼ + bᵢ + b̃ⱼ - log X_ij)²이며, 가중치 함수 f(x) = (x/x_max)^α (x ≤ x_max, else 1)로 초고빈도 동시 등장 쌍의 지배(Dominance)를 억제한다.

---

## Ⅰ. 개요 및 필요성

Word2Vec은 문맥 창(Window) 내에서만 학습하므로 전체 코퍼스의 전역 통계를 활용하지 못한다. "ice"와 "steam"은 서로 다르지만 둘 다 "water"와 자주 등장한다. 반면 "ice"는 "solid"와, "steam"은 "gas"와 더 자주 등장한다. 이 전역적 비율(X_ice,solid / X_ice,gas vs X_steam,solid / X_steam,gas)이 의미 차이를 담는다는 통찰이 GloVe의 핵심이다.

📢 **섹션 요약 비유**: Word2Vec이 "이웃집만 보는 정보원"이라면, GloVe는 "도시 전체 통계청 데이터를 분석하는 인구학자"다. 이웃집만 보면 단편적이지만, 도시 전체 통계(전역 동시 등장 행렬)를 보면 더 넓은 패턴을 포착한다.

---

## Ⅱ. 아키텍처 및 핵심 원리

```
┌──────────────────────────────────────────────────────────┐
│                GloVe 학습 파이프라인                      │
├──────────────────────────────────────────────────────────┤
│  1. 동시 등장 행렬 구성:                                 │
│     X_ij = 단어 i와 j가 윈도우 k 내 함께 등장한 횟수    │
│     (|V|×|V| 희소 행렬, V=어휘 크기)                   │
│                                                          │
│  2. GloVe 목적 함수:                                    │
│     J = Σᵢⱼ f(X_ij)·(wᵢᵀw̃ⱼ + bᵢ + b̃ⱼ - log X_ij)²  │
│                                                          │
│  3. 가중치 함수 f(x):                                   │
│     f(x) = (x/x_max)^α  if x < x_max                  │
│             1            otherwise                      │
│     (α=0.75, x_max=100 일반적 설정)                    │
│                                                          │
│  4. 최적화: AdaGrad로 wᵢ, w̃ⱼ, bᵢ, b̃ⱼ 학습           │
│  5. 최종 벡터: wᵢ + w̃ⱼ 평균 (두 벡터 평균이 더 우수) │
└──────────────────────────────────────────────────────────┘
```

| 방법 | 학습 방식 | 전역 통계 | 희귀 단어 | 계산 효율 |
|:---|:---|:---|:---|:---|
| GloVe | 행렬 분해 (목적함수) | ✅ 전역 | ✅ 보통 | 중간 |
| Word2Vec (Skip-Gram) | 로컬 예측 | ❌ 로컬 | ✅ 좋음 | 빠름 |
| FastText | 서브워드 포함 | ❌ 로컬 | ✅✅ 매우 좋음 | 빠름 |

📢 **섹션 요약 비유**: GloVe의 f(X_ij) 가중치는 "인기 단어 페널티"다. "the, a" 같은 초고빈도 단어가 모든 문맥에 나타나 행렬을 지배하지 않도록 f 함수로 빈도를 꺾어준다. 인기스타가 모든 사진에 나와도 특별 취급하지 않겠다는 공평한 가중치다.

---

## Ⅲ. 비교 및 연결

FastText(Facebook AI): 단어를 문자 n-gram으로 분해하여 "play"와 "playing"을 서로 다른 단어가 아닌 공유 서브워드(sub-word)로 처리한다. 덕분에 형태소가 풍부한 한국어, 독일어에서 GloVe/Word2Vec보다 탁월하고, 훈련 중 미등장 단어(OOV, Out-Of-Vocabulary)도 서브워드 합성으로 처리 가능하다. BERT 이후에는 문맥 기반 임베딩(Contextual Embedding)이 정적 임베딩(GloVe, Word2Vec)을 대체하고 있으나, 정적 임베딩은 계산 효율성이 압도적이다.

📢 **섹션 요약 비유**: 정적 임베딩(GloVe)은 "사전 뜻풀이"다. "bank"가 항상 동일한 벡터를 가진다(강가 bank와 은행 bank 동일). BERT 같은 동적 임베딩은 "문맥에 따라 뜻이 바뀌는 스마트 사전"으로 같은 bank도 문장에 따라 다른 벡터를 가진다.

---

## Ⅳ. 실무 적용 및 기술사 판단

사전 훈련된 GloVe 벡터(50d, 100d, 200d, 300d)는 Stanford NLP 웹사이트에서 무료로 다운로드 가능하며, 소규모 NLP 프로젝트의 임베딩 레이어 초기화에 활용된다. 한국어의 경우 Word2Vec 기반 ko.bin이나 FastText 기반 모델이 더 적합하다. gensim 라이브러리로 GloVe 벡터를 로드하고 유사도 검색에 사용한다.

📢 **섹션 요약 비유**: 사전 훈련 GloVe 벡터는 "백과사전 기증"이다. 누군가 수십억 문장으로 미리 공부한(사전 훈련) 300차원 지식(벡터)을 무료로 기증해주면, 우리 프로젝트에서 처음부터 공부하지 않고 이 지식을 그대로 가져다 쓸 수 있다.

---

## Ⅴ. 기대효과 및 결론

GloVe는 전역 통계 활용이라는 강점으로 Word2Vec과 함께 2010년대 NLP를 이끌었으며, 현재도 계산 자원이 제한된 환경에서 효과적인 임베딩 방법이다. 기술사 시험에서 GloVe 목적 함수의 f(X_ij) 역할과 log X_ij를 최소화하는 의미(비율 비교), Word2Vec과의 차이점을 서술하면 높은 점수를 받는다.

📢 **섹션 요약 비유**: GloVe의 목적 함수는 "두 단어가 함께 나타나는 빈도의 로그를 벡터 내적으로 근사"하는 것이다. "같이 자주 나오면 벡터 방향이 비슷해야 한다"는 직관을 수식으로 구현한 것으로, 단어 의미를 벡터 공간에 체계적으로 담아낸다.

---

### 📌 관련 개념 맵
| 개념 | 연관 키워드 | 관계 |
|:---|:---|:---|
| Word2Vec (Skip-Gram/CBOW) | 로컬 문맥 | GloVe의 대비 방법 |
| FastText | 서브워드 | GloVe의 개선 (형태소 처리) |
| 동시 등장 행렬 (Co-occurrence) | X_ij 행렬 | GloVe의 입력 |
| BERT | 문맥적 임베딩 | 정적 임베딩(GloVe)을 대체 |

---

### 👶 어린이를 위한 3줄 비유 설명
1. GloVe는 "책 전체에서 같이 자주 나오는 단어들을 같은 방향의 벡터로 만드는 AI"예요.
2. "얼음"과 "차갑다"가 자주 같이 나오면 이 두 단어의 벡터가 가까워져요.
3. 이렇게 학습된 벡터로 "왕 - 남자 + 여자 = 여왕" 같은 신기한 계산도 할 수 있어요!
'''

files["366_cooccurrence_matrix.md"] = '''+++
weight = 366
title = "366. 동시 등장 행렬 (Co-occurrence Matrix) - 윈도우 기반 스케일 축소"
date = "2026-04-21"
[extra]
categories = "studynote-ai"
+++

## 핵심 인사이트 (3줄 요약)
> 1. **본질**: 동시 등장 행렬(Co-occurrence Matrix)은 코퍼스에서 두 단어가 윈도우(Window) 크기 k 이내에서 함께 등장한 빈도를 행렬 X_ij로 집계하는 고차원 희소 행렬(Sparse Matrix)이며, 단어 의미를 분포 가설(Distributional Hypothesis)로 인코딩한다.
> 2. **가치**: 단순 빈도 대신 PPMI(Positive Pointwise Mutual Information)로 변환하면 "the"처럼 의미 없이 자주 등장하는 단어의 영향을 제거하고, 진짜 의미적 연관성만 포착한다.
> 3. **판단 포인트**: 어휘 크기 |V|×|V| 행렬(|V|=10만이면 100억 원소)의 메모리 문제를 SVD(Singular Value Decomposition) 또는 PPMI + Truncated SVD로 저차원 밀집 벡터로 압축한다.

---

## Ⅰ. 개요 및 필요성

분포 가설(Distributional Hypothesis): "비슷한 문맥에서 사용되는 단어는 비슷한 의미를 가진다"(John Firth, 1957). 이 가설을 수치화한 것이 동시 등장 행렬이다. "은행"이라는 단어 주변에 "돈", "이자", "대출"이 자주 등장한다면, 이 공동 등장 패턴(동시 등장 벡터)이 "은행"의 의미를 담는다. Word2Vec, GloVe, BERT 모두 이 원리를 기반으로 한다.

📢 **섹션 요약 비유**: 동시 등장 행렬은 "단어의 친구 목록"이다. "은행"의 친구가 "돈, 이자, ATM"이고, "강둑"의 친구가 "물, 낚시, 물고기"라면 두 단어는 다른 의미를 가진다. 친구 목록(동시 등장 패턴)이 단어의 신분증이다.

---

## Ⅱ. 아키텍처 및 핵심 원리

```
┌──────────────────────────────────────────────────────────┐
│         동시 등장 행렬 구성 및 PPMI 변환                 │
├──────────────────────────────────────────────────────────┤
│  윈도우 크기 k=2 예시:                                  │
│  "나는 오늘 맛있는 밥을 먹었다"                         │
│  오늘 기준: {나는(2), 맛있는(1)} 윈도우 내 등장        │
│                                                          │
│  X_ij: 단어 i 기준 단어 j의 동시 등장 횟수             │
│                                                          │
│  PMI (Pointwise Mutual Information):                    │
│  PMI(i,j) = log[P(i,j) / (P(i)·P(j))]                 │
│  = log[X_ij·N / (Σⱼ X_ij · Σᵢ X_ij)]                 │
│                                                          │
│  PPMI (Positive PMI):                                   │
│  PPMI(i,j) = max(0, PMI(i,j))                          │
│  → 음수 PMI 제거 (음수 = 이 조합이 우연보다 드문 경우) │
│                                                          │
│  차원 축소: Truncated SVD                               │
│  X ≈ U_k · Σ_k · V_kᵀ  (k 차원 근사)                 │
└──────────────────────────────────────────────────────────┘
```

| 변환 방법 | 수식 | 특성 | 주요 문제 |
|:---|:---|:---|:---|
| 원시 빈도 | X_ij | 단순 | 고빈도 단어 지배 |
| TF-IDF | - | 문서 내 중요도 | 전역 통계 무시 |
| PMI | log(P(i,j)/P(i)P(j)) | 연관성 측정 | 희귀 쌍 과대평가 |
| PPMI | max(0, PMI) | 음수 제거 | 0 과잉(희소) |

📢 **섹션 요약 비유**: PPMI는 "의미 없는 인연 제거 필터"다. "the"와 모든 단어가 함께 나오는 것은 우연(낮은 PMI)이다. PPMI는 우연 이상의 진짜 의미적 연관(양의 PMI만 남김)만 보존한다.

---

## Ⅲ. 비교 및 연결

동시 등장 행렬 기반 LSA(Latent Semantic Analysis, 잠재 의미 분석)는 TF-IDF 행렬에 Truncated SVD를 적용하여 문서-단어 행렬을 저차원으로 압축한다. 이는 GloVe 이전의 대표적 분산 표현 방법이다. 현대 언어 모델(BERT, GPT)은 동시 등장 행렬을 직접 사용하지 않지만, Transformer의 Self-Attention이 사실상 동적 동시 등장 관계를 문맥에 따라 계산한다.

📢 **섹션 요약 비유**: BERT의 Self-Attention은 "동적 동시 등장 행렬"이다. 고정된 X_ij 대신, 매 문장마다 문맥에 따라 각 단어 쌍의 관련성 점수를 실시간으로 계산하는 것이 Self-Attention이다. 동시 등장 행렬의 동적 버전이 Attention이라고 이해할 수 있다.

---

## Ⅳ. 실무 적용 및 기술사 판단

메모리 효율화: scipy.sparse 행렬로 저장하면 0 원소를 제외한 실제 데이터만 저장(희소 행렬 포맷). 어휘 크기 100,000에서 dense 행렬은 10GB, sparse 행렬은 실제 비zero 원소만 저장해 수십 MB로 줄어든다. 윈도우 크기 k 선택: k=2~10이 일반적. k가 크면 넓은 문맥을 포착하지만 노이즈 증가. 구문 관계는 좁은 윈도우, 의미 관계는 넓은 윈도우가 효과적이다.

📢 **섹션 요약 비유**: sparse 행렬 저장은 "체크리스트에서 체크된 것만 기록"하는 방법이다. 100만×100만 표(동시 등장 행렬)에서 대부분이 0(만나지 않은 단어 쌍)이다. 0은 저장하지 않고 0이 아닌 칸의 좌표와 값만 저장하면 공간이 1/1000로 줄어든다.

---

## Ⅴ. 기대효과 및 결론

동시 등장 행렬은 단어 임베딩의 근본 원리(분포 가설)를 구현하는 명시적 방법으로, Word2Vec/GloVe의 암묵적 학습이 실제로는 동시 등장 행렬의 특수 분해와 동치임이 이론적으로 증명됐다. 기술사 시험에서 PPMI 변환의 의미, Truncated SVD 차원 축소, sparse 행렬 효율화를 연결해서 서술하면 완성도 높은 답안이 된다.

📢 **섹션 요약 비유**: 동시 등장 행렬은 언어 AI의 "인간관계 지도"다. 모든 단어들이 서로 얼마나 친한지(동시 등장 빈도) 기록한 초대형 인맥 지도이며, 이 지도를 압축(SVD)하면 각 단어의 성격(벡터)이 나온다.

---

### 📌 관련 개념 맵
| 개념 | 연관 키워드 | 관계 |
|:---|:---|:---|
| GloVe | 전역 동시 등장 | 동시 등장 행렬을 목적함수로 분해 |
| LSA (Latent Semantic Analysis) | TF-IDF + SVD | 동시 등장 행렬의 SVD 응용 |
| PPMI | PMI 양수화 | 의미 없는 고빈도 연관 제거 |
| Sparse Matrix | 희소 행렬 | 메모리 효율적 저장 포맷 |

---

### 👶 어린이를 위한 3줄 비유 설명
1. 동시 등장 행렬은 "어떤 단어들이 같은 문장에서 짝꿍이 되는지" 횟수를 기록한 거대한 표예요.
2. "고양이"와 "울음"이 많이 같이 나오면 표의 해당 칸 숫자가 커져요.
3. 이 표를 압축(SVD)하면 각 단어의 의미를 담은 짧은 벡터가 완성돼요!
'''

for filename, content in files.items():
    filepath = os.path.join(TARGET_DIR, filename)
    with open(filepath, 'w', encoding='utf-8') as f:
        f.write(content)
    print(f"Written: {filename}")
print("Done batch 3: 362-366")
