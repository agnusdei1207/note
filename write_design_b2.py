import os

BASE = "/Users/pf/workspace/brainscience/content/studynote/11_design_supervision/06_exam_summary"

def W(fn, content):
    path = os.path.join(BASE, fn)
    if os.path.exists(path):
        print(f"SKIP: {fn}"); return
    with open(path, 'w') as f: f.write(content)
    print(f"OK: {fn}")

# 371-377 already done in gen_design_371_452.py which only defined them but didn't write
# Let's write 371-377 too
W("371_api_gateway_summary.md", '''+++
weight = 371
title = "371. API 게이트웨이 (API Gateway) 인증 라우팅"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: API 게이트웨이 (API Gateway)는 MSA (Microservice Architecture)의 단일 진입점으로, 인증·인가·라우팅·속도 제한을 중앙에서 처리한다.
> 2. **가치**: 각 마이크로서비스가 횡단 관심사 (Cross-Cutting Concerns)를 중복 구현하지 않아도 되어 서비스 응집도가 높아진다.
> 3. **판단 포인트**: 게이트웨이가 단일 장애점 (SPOF, Single Point of Failure)이 되지 않도록 고가용성 (HA, High Availability) 배포와 서킷 브레이커 (Circuit Breaker) 연계가 필수다.

## Ⅰ. 개요 및 필요성

MSA 환경에서 수십~수백 개의 마이크로서비스가 존재할 때, 클라이언트가 각 서비스를 직접 호출하면 엔드포인트 관리·인증 중복·네트워크 비용이 기하급수적으로 증가한다. API 게이트웨이는 이를 해결하는 역방향 프록시 (Reverse Proxy) 계층이다.

**주요 기능:**
- **인증/인가 (AuthN/AuthZ)**: JWT (JSON Web Token), OAuth 2.0 검증 중앙화
- **라우팅 (Routing)**: URL 경로·HTTP 메서드 기준 서비스 전달
- **로드밸런싱 (Load Balancing)**: 복수 인스턴스 간 부하 분산
- **속도 제한 (Rate Limiting)**: 악의적 트래픽 방어
- **응답 집계 (Response Aggregation)**: 여러 서비스 응답 합산
- **관찰가능성 (Observability)**: 중앙화된 로깅·추적·메트릭

📢 **섹션 요약 비유**: API 게이트웨이는 대기업 정문 보안 게이트다. 방문객은 정문에서 신분 확인 후 목적지 부서로 안내된다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│                     API Gateway 아키텍처                        │
├─────────────────────────────────────────────────────────────────┤
│  [Client] ─HTTPS─▶ [API Gateway]                                │
│                     ├─ AuthN/Z (JWT 검증)                       │
│                     ├─ Routing (경로 매핑)                      │
│                     ├─ Rate Limit (슬라이딩 윈도우)             │
│                     └─ Logging/Trace (분산 추적 ID)             │
│                           │                                     │
│              ┌────────────┼────────────┐                        │
│              ▼            ▼            ▼                        │
│         [User SVC]  [Order SVC]  [Payment SVC]                  │
└─────────────────────────────────────────────────────────────────┘
```

| 구성 요소 | 역할 | 대표 구현 |
|:---|:---|:---|
| 인증 플러그인 | JWT 서명 검증 | Kong JWT, AWS Authorizer |
| 라우터 | 경로 패턴 → 업스트림 선택 | Nginx, Envoy Route |
| 속도 제한기 | Token Bucket 카운터 | Redis + Rate Limiter |
| 서킷 브레이커 | 다운스트림 장애 시 빠른 실패 | Hystrix, Resilience4j |
| 분산 추적 | Trace ID 삽입 | Jaeger, Zipkin |

📢 **섹션 요약 비유**: 게이트웨이 내부는 공항 보안 검색대다. 여권 확인→탑승구 배정→인원 제한이 순서대로 처리된다.

## Ⅲ. 비교 및 연결

| 항목 | API 게이트웨이 | BFF (Backend For Frontend) | 서비스 메시 |
|:---|:---|:---|:---|
| 위치 | 외부 엣지 (North-South) | 클라이언트 유형별 | 서비스 사이드카 (East-West) |
| 인증 | ✅ 중앙 처리 | 부분 처리 | mTLS 서비스 간 |
| 속도 제한 | ✅ 전체 적용 | 클라이언트별 | 서비스별 정책 |

📢 **섹션 요약 비유**: API 게이트웨이는 쇼핑몰 정문, BFF는 VIP 전용 입구, 서비스 메시는 내부 복도의 CCTV 망이다.

## Ⅳ. 실무 적용 및 기술사 판단

**고가용성 설계:**
- 게이트웨이 인스턴스 N+1 구성
- L4/L7 로드밸런서가 게이트웨이 앞단에 위치
- 블루-그린 (Blue-Green) 무중단 배포

**감리 체크포인트:**
1. 게이트웨이 자체의 HA 구성 여부
2. 인증 토큰 만료 정책 및 갱신 흐름 명세
3. 속도 제한 임계값 산정 근거 (트래픽 분석 기반)
4. 서킷 브레이커 임계값 및 복구 정책 문서화
5. 분산 추적 ID (Trace ID) 전파 여부

**안티패턴:**
- 게이트웨이에 비즈니스 로직 집중 → 비즈니스 로직은 서비스 내부에
- 단일 게이트웨이에 모든 클라이언트 유형 혼재 → BFF 패턴 분리 권장

📢 **섹션 요약 비유**: 잘 설계된 게이트웨이는 교통 신호 시스템이다. 신호가 고장 나도 폴백 경로가 있다.

## Ⅴ. 기대효과 및 결론

API 게이트웨이는 MSA의 복잡성을 클라이언트로부터 격리하여, 내부 서비스 토폴로지 변경이 외부에 영향을 주지 않는 구조를 만든다. 기술사 시험에서는 SPOF 위험, HA 설계, 서킷 브레이커 연계를 반드시 언급해야 한다.

📢 **섹션 요약 비유**: API 게이트웨이는 도시의 중앙 환승 터미널이다. 어디서 출발하든 이 터미널을 거쳐 목적지에 연결된다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | MSA (Microservice Architecture) | 게이트웨이가 전제하는 아키텍처 |
| 연관 개념 | 서비스 메시 (Service Mesh) | 서비스 간 통신 제어 보완 |
| 연관 개념 | BFF (Backend For Frontend) | 클라이언트별 최적화 계층 |
| 연관 개념 | 서킷 브레이커 | 장애 격리 패턴 |

### 👶 어린이를 위한 3줄 비유 설명

- 여러 가게가 모인 백화점에서 정문 안내 데스크가 어디로 갈지 알려주는 것이에요.
- 신분증 확인, 층별 안내, 입장 인원 제한을 한 곳에서 해결해요.
- 각 가게는 안내 데스크를 믿고 장사에만 집중할 수 있어요.
''')

W("372_service_mesh_summary.md", '''+++
weight = 372
title = "372. 서비스 메시 (Service Mesh) 사이드카 프록시 통신 보안망"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 서비스 메시 (Service Mesh)는 마이크로서비스 간 통신을 애플리케이션 코드 밖에서 제어하는 인프라 계층으로, 사이드카 (Sidecar) 프록시를 통해 mTLS (mutual TLS) 보안·재시도·관찰가능성을 처리한다.
> 2. **가치**: 개발팀이 네트워크 정책·암호화·재시도 로직을 서비스 코드에 구현하지 않아도 되어 비즈니스 로직 집중도와 운영 일관성이 확보된다.
> 3. **판단 포인트**: 사이드카의 메모리·CPU 오버헤드와 컨트롤 플레인 (Control Plane) 단일 장애점 위험을 관리하는 것이 핵심 감리 포인트다.

## Ⅰ. 개요 및 필요성

MSA 환경에서 재시도·타임아웃·서킷 브레이커·mTLS를 각 서비스가 구현하면 코드 중복과 일관성 부재가 생긴다. 서비스 메시는 **데이터 플레인 (Data Plane)**과 **컨트롤 플레인 (Control Plane)**의 분리로 이를 해결한다.

- **데이터 플레인**: 각 서비스 파드(Pod) 옆에 Envoy 사이드카 배치
- **컨트롤 플레인**: Istiod가 정책·인증서·라우팅 규칙을 프록시에 배포

📢 **섹션 요약 비유**: 서비스 메시는 배달원(서비스) 옆에 항상 붙은 전담 교통안내원(사이드카)이다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│             Service Mesh 아키텍처 (Istio + Envoy 기준)          │
├─────────────────────────────────────────────────────────────────┤
│  Control Plane: Istiod (Pilot + Citadel + Galley)               │
│       │ xDS API (정책·인증서 배포)                              │
│  Data Plane:                                                    │
│  ┌──────────────────────┐  mTLS  ┌──────────────────────┐      │
│  │ Service A + Envoy    │◄──────►│ Service B + Envoy    │      │
│  │  App │ Sidecar Proxy │        │  App │ Sidecar Proxy │      │
│  └──────────────────────┘        └──────────────────────┘      │
└─────────────────────────────────────────────────────────────────┘
```

| 기능 | 구현 방식 | 도구 |
|:---|:---|:---|
| mTLS 암호화 | 인증서 자동 교환 | Istio Citadel |
| 트래픽 관리 | 가중치 라우팅, 카나리 | Istio VirtualService |
| 서킷 브레이커 | OutlierDetection 정책 | Envoy + Istio |
| 분산 추적 | Trace ID 자동 전파 | Jaeger, Zipkin |

📢 **섹션 요약 비유**: 컨트롤 플레인은 항공 관제탑, 데이터 플레인은 각 항공기의 자동 항법 장치다.

## Ⅲ. 비교 및 연결

| 항목 | 서비스 메시 | API 게이트웨이 | 라이브러리 접근 |
|:---|:---|:---|:---|
| 적용 위치 | East-West (서비스 간) | North-South (외부 진입) | 애플리케이션 내부 |
| 언어 독립성 | ✅ 완전 독립 | ✅ 독립 | ❌ 언어 종속 |
| 오버헤드 | 사이드카 리소스 | 단일 게이트웨이 | 라이브러리 메모리 |

📢 **섹션 요약 비유**: API 게이트웨이가 국경 검문소라면 서비스 메시는 국내 도로 전체의 스마트 CCTV·신호 시스템이다.

## Ⅳ. 실무 적용 및 기술사 판단

**Istio 도입 체크리스트:**
1. Kubernetes 클러스터 버전 및 리소스 여유 확인
2. 사이드카 자동 주입 (Automatic Injection) 네임스페이스 설정
3. PeerAuthentication으로 mTLS 모드 결정 (STRICT/PERMISSIVE)
4. AuthorizationPolicy로 서비스 간 최소 권한 접근 제어
5. 트래픽 미러링으로 점진 전환

**성능 튜닝:**
- Envoy 연결 풀 크기 (`maxConnections`, `http1MaxPendingRequests`) 조정
- 사이드카 리소스 제한: CPU 50m~200m, Memory 64Mi~128Mi

📢 **섹션 요약 비유**: 서비스 메시 도입은 도시 전체에 스마트 교통 시스템을 까는 것이다. 설치 비용이 크지만 장기 효과가 크다.

## Ⅴ. 기대효과 및 결론

서비스 메시는 MSA 운영 복잡도를 인프라 계층으로 추상화한다. 기술사 시험에서는 데이터 플레인과 컨트롤 플레인의 분리, 사이드카 오버헤드, Zero Trust 보안 모델과의 연계를 강조해야 한다.

📢 **섹션 요약 비유**: 서비스 메시는 서비스들이 서로 믿고 대화하는 신뢰 인프라다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | MSA (Microservice Architecture) | 서비스 메시가 지원하는 아키텍처 |
| 구성 요소 | Envoy, Istiod | 데이터/컨트롤 플레인 구현 |
| 연관 개념 | Zero Trust | 서비스 간 명시적 검증 |
| 연관 개념 | API 게이트웨이 | North-South 트래픽 제어 |

### 👶 어린이를 위한 3줄 비유 설명

- 친구들이 편지를 보낼 때 우체부(사이드카)가 암호화와 배달을 대신해줘요.
- 우체국 본부(컨트롤 플레인)가 배달 규칙을 모든 우체부에게 알려줘요.
- 친구들은 편지 내용에만 집중하면 돼요.
''')

W("373_saga_pattern_compensation.md", '''+++
weight = 373
title = "373. 사가 (Saga) 패턴 보상 트랜잭션 (Compensating Transaction)"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 사가 (Saga) 패턴은 MSA (Microservice Architecture) 환경의 분산 트랜잭션을 일련의 로컬 트랜잭션과 보상 트랜잭션 (Compensating Transaction)으로 대체하여 최종 일관성 (Eventual Consistency)을 달성한다.
> 2. **가치**: 2PC (Two-Phase Commit)의 동기 블로킹 없이 서비스 간 약한 결합을 유지하면서 비즈니스 트랜잭션의 원자성을 보장한다.
> 3. **판단 포인트**: 보상 트랜잭션이 비즈니스적으로 "취소 가능"한지 먼저 분석하고, 멱등성 (Idempotency) 설계가 반드시 수반되어야 한다.

## Ⅰ. 개요 및 필요성

MSA 환경에서 각 서비스가 독립 DB를 가지면, 서비스 A와 B를 하나의 ACID 트랜잭션으로 묶을 수 없다. 2PC는 코디네이터 장애 시 전체 잠금·동기 블로킹으로 성능 저하가 크다.

**사가의 두 구현 방식:**
- **코레오그래피 (Choreography)**: 이벤트로 자율 트리거
- **오케스트레이션 (Orchestration)**: 중앙 오케스트레이터 명령

📢 **섹션 요약 비유**: 사가는 팀 릴레이 달리기다. 누군가 넘어지면 앞선 주자들이 역방향으로 달려 원래 자리로 돌아간다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│            Saga 오케스트레이션 - 주문 처리 예시                 │
├─────────────────────────────────────────────────────────────────┤
│  1. 주문 생성 (Order Service)                                   │
│       ▼                                                         │
│  2. 재고 예약 (Inventory Service) → 성공                        │
│       ▼                                                         │
│  3. 결제 처리 (Payment Service) → 실패!                         │
│       ▼ 보상 트랜잭션                                          │
│  3'. 재고 예약 취소 (Inventory Service)                         │
│  2'. 주문 취소 (Order Service)                                  │
└─────────────────────────────────────────────────────────────────┘
```

| 구분 | 코레오그래피 | 오케스트레이션 |
|:---|:---|:---|
| 조율 방식 | 이벤트 기반 자율 | 중앙 오케스트레이터 |
| 가시성 | 낮음 (분산 흐름) | 높음 (중앙 상태) |
| 결합도 | 느슨 | 오케스트레이터 중심 |
| 적합 규모 | 소규모 MSA | 복잡한 워크플로우 |

📢 **섹션 요약 비유**: 코레오그래피는 악보 없는 재즈, 오케스트레이션은 지휘자 있는 클래식이다.

## Ⅲ. 비교 및 연결

| 항목 | Saga | 2PC (Two-Phase Commit) |
|:---|:---|:---|
| 일관성 | 최종 일관성 | 강한 일관성 |
| 결합도 | 느슨 | 강함 (XA 프로토콜) |
| 성능 | 비동기 고성능 | 동기 블로킹 |
| 장애 대응 | 보상 트랜잭션 | 잠금 해제 대기 |

📢 **섹션 요약 비유**: 2PC는 모든 팀원이 동시에 손을 들어야 회의가 진행되는 방식이다.

## Ⅳ. 실무 적용 및 기술사 판단

**보상 트랜잭션 설계 원칙:**
1. **멱등성**: 보상 트랜잭션 중복 실행 시 동일 결과 보장
2. **취소 불가 작업 분리**: 이메일 발송 등은 보상 로직 전에 배치
3. **Outbox 패턴 연계**: 로컬 TX와 이벤트 발행을 원자적 처리

**구현 프레임워크:**
- Axon Framework (Java)
- Apache Camel Saga
- Outbox 패턴 + Debezium CDC (Change Data Capture)

📢 **섹션 요약 비유**: Outbox 패턴은 편지를 바로 보내는 대신 우체통에 넣는 것이다. 우체부가 반드시 배달한다.

## Ⅴ. 기대효과 및 결론

사가 패턴은 MSA에서 2PC 없이 비즈니스 트랜잭션 원자성을 구현하는 핵심 패턴이다. 기술사 시험에서는 코레오그래피/오케스트레이션 비교, 보상 트랜잭션 멱등성, Outbox 패턴 연계를 명확히 서술해야 한다.

📢 **섹션 요약 비유**: 사가는 분산된 팀의 명확한 "되돌리기 프로토콜"이다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | 분산 트랜잭션 | 사가가 해결하는 문제 |
| 연관 개념 | 최종 일관성 (Eventual Consistency) | 사가의 일관성 모델 |
| 연관 패턴 | Outbox 패턴 | 이벤트 발행 원자성 보장 |
| 대안 개념 | 2PC (Two-Phase Commit) | 강한 일관성 필요 시 |

### 👶 어린이를 위한 3줄 비유 설명

- 여러 친구와 공동 숙제 시 한 명이 실수하면 그 전까지 한 일을 모두 되돌려요.
- 각자 역할을 끝내면 다음 친구에게 알려줘요.
- 실수를 되돌리는 방법(보상)을 미리 정해놔야 혼란이 없어요.
''')

W("374_ddd_bounded_context_aggregate.md", '''+++
weight = 374
title = "374. DDD (Domain-Driven Design) 바운디드 컨텍스트와 애그리게이트"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: DDD (Domain-Driven Design)는 복잡한 비즈니스 도메인을 소프트웨어 모델로 정확히 표현하는 설계 방법론으로, 바운디드 컨텍스트 (Bounded Context)가 마이크로서비스 분할의 자연스러운 경계를 제공한다.
> 2. **가치**: 유비쿼터스 언어 (Ubiquitous Language)로 개발자와 도메인 전문가가 동일하게 소통하면, 코드와 비즈니스 개념의 간극이 사라진다.
> 3. **판단 포인트**: "한 트랜잭션 = 한 애그리게이트 (Aggregate)" 원칙이 설계 핵심으로, 경계 설정 오류 시 분산 트랜잭션이 필요해진다.

## Ⅰ. 개요 및 필요성

기술 계층(Controller-Service-Repository)으로만 코드를 나누면, 비즈니스 규칙이 여러 클래스에 분산되어 복잡성이 증가한다. DDD는 코드를 도메인 모델 중심으로 구성한다.

**DDD 핵심 개념:**
- **유비쿼터스 언어**: 팀 전체 공유 도메인 용어
- **바운디드 컨텍스트**: 도메인 모델이 유효한 명시적 경계
- **엔티티 (Entity)**: 식별자로 구별되는 객체
- **값 객체 (Value Object)**: 속성으로만 구별되는 불변 객체
- **애그리게이트**: 일관성 경계 단위, 루트를 통해서만 접근

📢 **섹션 요약 비유**: DDD는 업계 전문가와 개발자가 같은 지도를 보며 이야기하는 것이다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│                 DDD 바운디드 컨텍스트 맵 예시                   │
├─────────────────────────────────────────────────────────────────┤
│  ┌────────────────┐  Customer/Supplier  ┌────────────────┐      │
│  │  주문 컨텍스트  │◄───────────────────►│  결제 컨텍스트  │      │
│  │  Order (AR)    │                    │  Payment (AR)  │      │
│  │  - OrderId     │                    │  - PaymentId   │      │
│  └────────────────┘                    └────────────────┘      │
│           │ Context Map                                         │
│  ┌────────────────┐                                             │
│  │  재고 컨텍스트  │                                             │
│  │  Product (AR)  │  AR = Aggregate Root                        │
│  └────────────────┘                                             │
└─────────────────────────────────────────────────────────────────┘
```

| 개념 | 설명 | MSA 연관 |
|:---|:---|:---|
| 바운디드 컨텍스트 | 도메인 모델의 명시적 경계 | 서비스 분할 경계 기준 |
| 애그리게이트 | 일관성 경계 단위 | 한 서비스 내 DB 범위 |
| 도메인 이벤트 | 상태 변화 사실 표현 | 서비스 간 통신 수단 |
| 컨텍스트 맵 | 컨텍스트 간 관계 명시 | API 계약, ACL 설계 |

📢 **섹션 요약 비유**: 애그리게이트는 팀 리더가 있는 프로젝트 팀이다. 외부는 반드시 팀 리더를 통해 접근해야 한다.

## Ⅲ. 비교 및 연결

| 항목 | 트랜잭션 스크립트 | 도메인 모델 (DDD) |
|:---|:---|:---|
| 비즈니스 로직 | 서비스 레이어 집중 | 도메인 객체 내부 |
| 복잡성 대응 | 로직 증가 시 스파게티화 | 캡슐화로 관리 |
| 도메인 전문가 소통 | 코드와 불일치 | 유비쿼터스 언어 일치 |
| 적합 규모 | 단순 CRUD | 복잡한 비즈니스 규칙 |

📢 **섹션 요약 비유**: 트랜잭션 스크립트는 요리사 한 명이 모든 요리를 담당, DDD는 각 요리마다 전담 요리사가 레시피를 지키는 분업이다.

## Ⅳ. 실무 적용 및 기술사 판단

**애그리게이트 설계 원칙:**
1. 작게 유지: 엔티티 수 최소화
2. ID로만 참조: 다른 애그리게이트는 ID만으로 연결
3. 한 트랜잭션 = 한 애그리게이트
4. 불변식 (Invariant) 내재화

**이벤트 스토밍 (Event Storming):**
도메인 이벤트 → 커맨드 → 애그리게이트 → 바운디드 컨텍스트 순 식별 워크숍

📢 **섹션 요약 비유**: 이벤트 스토밍은 포스트잇으로 벽에 비즈니스 시나리오를 펼쳐 "누가 책임질지" 찾는 과정이다.

## Ⅴ. 기대효과 및 결론

DDD는 복잡한 비즈니스 도메인을 코드에 정확히 반영하며, MSA와 결합하면 서비스 경계가 비즈니스 경계와 일치하여 변경 영향 범위가 명확해진다. 기술사 시험에서는 바운디드 컨텍스트와 마이크로서비스 분할의 연관성, 애그리게이트 설계 원칙을 구체적으로 서술해야 한다.

📢 **섹션 요약 비유**: DDD는 건물 설계도에서 각 방의 용도를 명확히 정하는 것이다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | 도메인 모델링 | DDD가 속하는 설계 접근법 |
| 핵심 개념 | 바운디드 컨텍스트, 애그리게이트 | DDD 전술 패턴 |
| 연관 개념 | 사가 패턴 | 애그리게이트 간 트랜잭션 처리 |
| 연관 개념 | 이벤트 소싱 (Event Sourcing) | 상태 대신 이벤트로 저장 |

### 👶 어린이를 위한 3줄 비유 설명

- 학교에서 각 반이 자기 규칙으로 운영되는 것처럼 각 컨텍스트는 자기만의 의미로 움직여요.
- 반장(루트 엔티티)을 통해서만 반 내부에 접근할 수 있어요.
- 선생님과 학생이 같은 용어를 써야 오해가 없는 것처럼 개발자와 전문가가 같은 말을 써요.
''')

W("375_acl_legacy_buffer.md", '''+++
weight = 375
title = "375. ACL (Anti-Corruption Layer) 안티-코럽션 레이어"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: ACL (Anti-Corruption Layer)은 서로 다른 도메인 모델이나 레거시 시스템 경계에서, 한 쪽 개념이 다른 쪽을 오염시키지 않도록 변환하는 격리 계층이다.
> 2. **가치**: 신규 시스템이 레거시의 복잡하고 불일치한 모델을 흡수하지 않아도 되어, 깨끗한 도메인 모델을 유지하면서 점진적 현대화가 가능하다.
> 3. **판단 포인트**: ACL 구현 비용이 직접 통합 비용보다 클 수 있으므로, 통합 기간과 복잡성을 고려하여 도입 여부를 판단해야 한다.

## Ⅰ. 개요 및 필요성

DDD (Domain-Driven Design) 컨텍스트 맵에서 레거시와 통합 시, 레거시의 복잡한 데이터 구조·불일치 명명 규칙이 신규 시스템에 유입되면 도메인 모델이 왜곡된다. ACL은 경계에서 번역자(Translator)이자 완충재(Buffer) 역할을 한다.

📢 **섹션 요약 비유**: ACL은 해외 출장의 통역사다. 현지어 모르는 비즈니스맨(신규 시스템)과 현지 파트너(레거시) 사이에서 양쪽이 자기 언어로만 대화하게 한다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│                ACL (Anti-Corruption Layer) 구조                 │
├─────────────────────────────────────────────────────────────────┤
│  [신규 시스템]                                                  │
│  Order, Customer (신규 개념)                                    │
│         │                                                       │
│         ▼                                                       │
│  ┌──────────────────────────────────────┐                       │
│  │  ACL: Facade → Adapter → Translator  │                       │
│  │  LegacyOrderDto ←→ Order             │                       │
│  │  CUST_NUM       ←→ CustomerId        │                       │
│  └──────────────────────────────────────┘                       │
│         │                                                       │
│         ▼                                                       │
│  [레거시 시스템]                                                │
│  CUST_NUM, PRD_CD, ORD_DT (레거시 명명)                         │
└─────────────────────────────────────────────────────────────────┘
```

| ACL 구성 요소 | 역할 | 구현 패턴 |
|:---|:---|:---|
| 파사드 (Facade) | 레거시 인터페이스 단순화 | 퍼사드 패턴 |
| 어댑터 (Adapter) | 인터페이스 형식 변환 | 어댑터 패턴 |
| 번역기 (Translator) | 도메인 개념 매핑 | 데이터 매핑 테이블 |

📢 **섹션 요약 비유**: ACL 내부는 공항 환전소다. 달러(레거시)를 원화(신규 모델)로 바꾸되, 환율표(번역 규칙)가 명확해야 오환이 없다.

## Ⅲ. 비교 및 연결

| 통합 전략 | 오염 위험 | 비용 | 설명 |
|:---|:---|:---|:---|
| ACL 적용 | 없음 | 높음 | 완전 격리 |
| 직접 통합 | 높음 | 낮음 | 레거시 API 직접 호출 |
| 공유 커널 | 중간 | 중간 | 공통 도메인 모델 공유 |
| 적합주의 | 최고 | 최저 | 레거시 모델 그대로 수용 |

📢 **섹션 요약 비유**: 직접 통합은 외국 음식을 날 것으로 먹는 것, ACL은 내 입맛에 맞게 조리해서 먹는 것이다.

## Ⅳ. 실무 적용 및 기술사 판단

**ACL 적용 결정 기준:**
1. 레거시와의 통합 기간이 6개월 이상 지속 예상
2. 레거시 데이터 모델과 신규 도메인 개념이 크게 불일치
3. 레거시 변경이 불가능하거나 제한적 (외부 시스템, 패키지 SW)

**번역 규칙 문서화:**
- 레거시 필드명 ↔ 신규 도메인 용어 매핑 테이블
- 레거시 코드값 ↔ 신규 열거형 (Enum) 매핑
- ACL 내부로 변경 영향 범위 제한

📢 **섹션 요약 비유**: ACL은 외교관이 협약서를 번역할 때 사용하는 공식 번역 프로토콜이다.

## Ⅴ. 기대효과 및 결론

ACL은 레거시 통합에서 신규 시스템의 도메인 모델을 보호하는 핵심 패턴이다. 스트랭글러 피그 (Strangler Fig) 패턴과 함께 사용하면, 레거시를 단계적으로 교체하면서도 깨끗한 모델을 유지할 수 있다.

📢 **섹션 요약 비유**: ACL은 새 건물과 낡은 건물 사이의 방화문이다. 낡은 건물에 불이 나도 새 건물은 안전하다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | DDD 컨텍스트 맵 | ACL이 위치하는 통합 패턴 |
| 연관 패턴 | 스트랭글러 피그 패턴 | 레거시 점진 교체 |
| 구현 패턴 | 어댑터, 퍼사드, 번역기 | ACL 내부 구현 방식 |
| 연관 개념 | 적합주의 (Conformist) | ACL의 반대 접근법 |

### 👶 어린이를 위한 3줄 비유 설명

- 오래된 외국어 책과 새 한국어 책 사이에 통역자가 있는 것처럼 ACL은 번역자예요.
- 통역자 덕분에 새 책은 외국어를 배울 필요가 없어요.
- 외국어 책이 바뀌어도 통역자만 업데이트하면 새 책은 그대로예요.
''')

W("376_strangler_fig_summary.md", '''+++
weight = 376
title = "376. 스트랭글러 피그 (Strangler Fig) 패턴 점진 교체"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 스트랭글러 피그 (Strangler Fig) 패턴은 레거시를 한 번에 교체하는 빅뱅 방식 대신, 새 기능을 점진적으로 신규 시스템에 구축하면서 레거시를 서서히 대체하는 마이그레이션 전략이다.
> 2. **가치**: 비즈니스 연속성을 유지하면서 수십 년 된 레거시도 운영 중단 없이 MSA (Microservice Architecture)로 전환할 수 있다.
> 3. **판단 포인트**: 교체 경계 정의와 레거시·신규 간 데이터 동기화 전략이 성공의 핵심이다.

## Ⅰ. 개요 및 필요성

레거시 현대화의 최대 위험은 "빅뱅 교체 (Big Bang Replacement)"다. 수년 개발 후 한 번에 전환하면 누락 기능·버그·사용자 저항으로 실패 확률이 높다.

스트랭글러 피그는 열대 무화과나무가 숙주 나무를 서서히 감싸는 자연 현상에서 영감받은 패턴 (Martin Fowler, 2004):
1. **변환 (Transform)**: 레거시 옆에 신규 시스템 구축
2. **공존 (Coexist)**: 레거시·신규 동시 운영, 신규 기능은 신규 시스템에만
3. **제거 (Eliminate)**: 모든 기능 이전 완료 후 레거시 종료

📢 **섹션 요약 비유**: 낡은 건물을 허물지 않고 옆에 새 건물을 지어가면서 방 하나씩 이사하는 방식이다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│           Strangler Fig 마이그레이션 아키텍처 (공존 단계)       │
├─────────────────────────────────────────────────────────────────┤
│              [API Gateway / Strangler Facade]                   │
│                        │                                        │
│           ┌────────────┴────────────┐                           │
│           ▼                         ▼                           │
│  ┌─────────────────┐    ┌─────────────────────┐                 │
│  │  레거시 시스템   │    │  신규 마이크로서비스  │                 │
│  │  (기존 기능)    │    │  (이전된 기능)        │                 │
│  └─────────────────┘    └─────────────────────┘                 │
│           │                         │                           │
│           └──────────┬──────────────┘                           │
│                      ▼                                          │
│               데이터 동기화 계층                                 │
└─────────────────────────────────────────────────────────────────┘
```

| 단계 | 활동 | 리스크 관리 |
|:---|:---|:---|
| 변환 | 신규 서비스 구축, 프록시 설치 | 레거시 영향 없음 |
| 공존 | 프록시 트래픽 분기, 이중 기록 | 롤백 가능 상태 유지 |
| 제거 | 레거시 라우팅 제거, 종료 | 점진적 트래픽 전환 (Canary) |

📢 **섹션 요약 비유**: 도로 공사처럼 기존 도로를 닫지 않고 새 도로를 건설하고 차량을 서서히 이전한다.

## Ⅲ. 비교 및 연결

| 마이그레이션 전략 | 위험도 | 비용 | 특징 |
|:---|:---|:---|:---|
| 빅뱅 교체 | 매우 높음 | 높음 | 한 번에 전환 |
| 스트랭글러 피그 | 낮음 | 중간 | 점진적 교체 |
| 리호스팅 (Lift & Shift) | 낮음 | 낮음 | 인프라만 이전 |
| 리팩터링 | 낮음 | 중간 | 코드 개선 |

📢 **섹션 요약 비유**: 빅뱅은 심장 이식 수술, 스트랭글러 피그는 혈관 하나씩 교체하는 치료법이다.

## Ⅳ. 실무 적용 및 기술사 판단

**성공 요인:**
1. **Strangler Facade (API 게이트웨이)**: 트래픽을 레거시/신규로 분기
2. **피처 토글 (Feature Toggle)**: 런타임에 라우팅 제어
3. **데이터 동기화**: 이중 기록 또는 CDC (Change Data Capture)
4. **ACL**: 레거시 데이터 구조 오염 방지

**감리 포인트:**
- 각 단계별 롤백 계획 수립 여부
- 이중 기록 구간의 정합성 검증 방법
- 레거시 종료 기준 및 일정 명세

📢 **섹션 요약 비유**: 핵심은 "언제든 레거시로 돌아갈 수 있는 비상 탈출구"를 항상 열어두는 것이다.

## Ⅴ. 기대효과 및 결론

스트랭글러 피그 패턴은 비즈니스 연속성 유지 속에 레거시를 현대화하는 가장 현실적인 전략이다. 기술사 시험에서는 3단계(변환/공존/제거), Strangler Facade, 데이터 동기화 전략을 구체적으로 서술해야 한다.

📢 **섹션 요약 비유**: 레거시라는 낡은 옷을 양말→바지→상의 순서로 교체하면 추위를 타지 않는다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 연관 패턴 | ACL (Anti-Corruption Layer) | 레거시 오염 방지 |
| 연관 패턴 | 피처 토글 | 트래픽 분기 제어 |
| 연관 전략 | 카나리 배포 | 점진적 트래픽 전환 |
| 상위 개념 | 레거시 현대화 | 패턴의 목표 |

### 👶 어린이를 위한 3줄 비유 설명

- 낡은 레고 성을 부수지 않고 옆에 새 성을 지어가요.
- 새 성의 방이 완성될 때마다 낡은 성의 방을 비워요.
- 모든 방이 이사 완료되면 낡은 성을 치워요.
''')

W("377_serverless_cold_start.md", '''+++
weight = 377
title = "377. 서버리스 (Serverless) FaaS (Function as a Service) 콜드 스타트"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: FaaS (Function as a Service)는 이벤트 발생 시에만 함수를 실행하고 비용을 지불하는 서버리스 모델이지만, 첫 실행 시 컨테이너 초기화 지연인 콜드 스타트 (Cold Start)가 성능 병목이 된다.
> 2. **가치**: 서버 프로비저닝·운영 부담이 없고 자동 스케일링이 기본 제공되어 트래픽 변동이 큰 워크로드에서 비용 효율이 극대화된다.
> 3. **판단 포인트**: 콜드 스타트 허용 여부를 SLA (Service Level Agreement) 기준으로 판단하고, 실시간 서비스는 프로비저닝된 동시 실행 (Provisioned Concurrency)을 검토해야 한다.

## Ⅰ. 개요 및 필요성

전통 서버는 항상 프로세스가 실행 중이어서 요청에 즉시 응답하지만 유휴 시에도 비용이 발생한다. FaaS는 요청 시에만 함수 컨테이너를 생성·실행·종료하여 비용을 최소화한다.

**대표 FaaS 플랫폼:** AWS Lambda, Google Cloud Functions, Azure Functions, Cloudflare Workers

**콜드 스타트 발생 원인:**
1. 일정 시간 이상 미호출 → 컨테이너 제거
2. 급격한 동시 호출 증가 → 새 컨테이너 생성 필요
3. 배포 후 첫 호출

📢 **섹션 요약 비유**: FaaS는 자동판매기다. 선택(이벤트) 시 음료를 제조하는데, 재료가 없어 제조 시간이 걸리는 것이 콜드 스타트다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│             Cold Start vs Warm Start 비교                       │
├─────────────────────────────────────────────────────────────────┤
│  Cold Start:                                                    │
│  [Request] → [컨테이너 생성 100~500ms] → [런타임 초기화 100ms]  │
│  → [코드 로드 50ms] → [핸들러 초기화 50~200ms] → [응답]        │
│  총 지연: 수백ms ~ 수초                                         │
│                                                                 │
│  Warm Start:                                                    │
│  [Request] → [기존 컨테이너 재사용 0ms] → [핸들러 10~50ms]     │
│  → [응답]  총 지연: 10~50ms                                     │
└─────────────────────────────────────────────────────────────────┘
```

| 콜드 스타트 요인 | 영향 | 최적화 방법 |
|:---|:---|:---|
| 런타임 언어 | JVM > Python > Node.js | GraalVM Native, Node.js 선택 |
| 배포 패키지 크기 | 클수록 느림 | 의존성 최소화 |
| VPC 설정 | ENI 생성 지연 | VPC 없는 함수 설계 |
| 메모리 설정 | 512MB+ 권장 | 메모리 ↑ → CPU 비례 증가 |

📢 **섹션 요약 비유**: 웜 스타트는 불 켜진 오븐, 콜드 스타트는 예열부터 기다리는 것이다.

## Ⅲ. 비교 및 연결

| 항목 | 서버리스 FaaS | 컨테이너 | 서버 (EC2/VM) |
|:---|:---|:---|:---|
| 비용 모델 | 실행 시간/횟수 | 컨테이너 실행 시간 | 인스턴스 시간 |
| 콜드 스타트 | 있음 | 없음 | 없음 |
| 스케일링 | 자동 0→무한 | 수동/자동 | 수동/자동 |
| 적합 워크로드 | 이벤트 기반, 간헐적 | 지속 트래픽 | 긴 실행 시간 |

📢 **섹션 요약 비유**: FaaS는 택시(호출 시 비용), 컨테이너는 전세 버스(시간제), 서버는 자가용(항상 유지비)이다.

## Ⅳ. 실무 적용 및 기술사 판단

**콜드 스타트 완화 전략:**
1. **프로비저닝된 동시 실행**: 미리 컨테이너를 웜 상태로 유지
2. **웜 업 스케줄러**: 주기적 함수 호출로 컨테이너 유지
3. **런타임 선택**: GraalVM Native Image로 JVM 시작 시간 제거
4. **패키지 최소화**: 필요 의존성만 포함, Lambda Layer 활용
5. **메모리 증가**: 메모리 ↑ → CPU 비례 증가 → 초기화 속도 향상

**감리 포인트:**
- 실시간성 요구 기능에 FaaS 적용 여부 검토
- 콜드 스타트 최대 지연 시간의 SLA 반영 여부

📢 **섹션 요약 비유**: 겨울에 자동차 예열이 필요한 것처럼, 콜드 스타트는 응답 시간에 부담을 준다.

## Ⅴ. 기대효과 및 결론

서버리스 FaaS는 트래픽 변동이 큰 워크로드에서 인프라 관리 비용을 획기적으로 줄인다. 기술사 시험에서는 FaaS 비용 모델, 콜드 스타트 원인·완화 전략, 컨테이너와의 적용 판단 기준을 명확히 서술해야 한다.

📢 **섹션 요약 비유**: 서버리스는 필요할 때만 켜지는 에어컨이다. 첫 번째 냉각에 시간이 걸리는 것이 콜드 스타트다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | 클라우드 컴퓨팅 서비스 모델 | FaaS의 분류 |
| 연관 개념 | 이벤트 기반 아키텍처 | FaaS의 실행 모델 |
| 연관 개념 | 컨테이너 (Docker) | FaaS 하부 실행 환경 |
| 연관 개념 | Provisioned Concurrency | 콜드 스타트 완화 방법 |

### 👶 어린이를 위한 3줄 비유 설명

- 서버리스는 전기를 켤 때만 요금 내는 전등이에요.
- 콜드 스타트는 오랫동안 안 써서 형광등이 깜빡이며 켜지는 현상이에요.
- 항상 준비해두면(프로비저닝) 바로 켜지지만 그만큼 요금이 더 나와요.
''')

print("371-377 and 378-380 files done")
