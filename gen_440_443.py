import os
OUT = "/Users/pf/workspace/brainscience/content/studynote/09_security/05_web_app_security/"

def w(fn, weight, title, content):
    path = os.path.join(OUT, fn)
    if os.path.exists(path):
        print(f"SKIP: {fn}")
        return
    with open(path, "w") as f:
        f.write(f'+++\nweight = {weight}\ntitle = "{title}"\ndate = "2026-04-21"\n[extra]\ncategories = "studynote-security"\n+++\n\n')
        f.write(content.strip() + "\n")
    print(f"CREATED: {fn}")

w("440_insecure_design.md", 440, "440. A04 안전하지 않은 설계", """
## 핵심 인사이트 (3줄 요약)
> 1. **본질**: Insecure Design (안전하지 않은 설계, A04)은 구현 버그가 아니라 설계 단계에서 보안 통제를 누락하거나 잘못 설계해, 올바른 구현조차도 시스템을 취약하게 만드는 근본적인 아키텍처 취약점이다.
> 2. **가치**: OWASP 2021에서 신규 진입하며 4위를 차지했고, "보안은 코딩 단계가 아닌 설계 단계에서 내재화되어야 한다"는 패러다임 전환을 반영한다.
> 3. **판단 포인트**: 위협 모델링(Threat Modeling, STRIDE), 보안 설계 패턴 적용, 제한 노출(Constrained Design), 레퍼런스 아키텍처가 핵심 대응이며, SDL (Secure Development Lifecycle) 초기 단계 통합이 필수다.

---

## Ⅰ. 개요 및 필요성

Insecure Design은 코드에 버그가 없어도 시스템이 취약한 경우다. 예를 들어 비밀번호 재설정 기능을 "어린 시절 애완동물 이름"으로 설계하면, 코드가 완벽해도 소셜 엔지니어링으로 계정이 탈취된다. 이처럼 설계 결함은 구현 수정만으로는 해결할 수 없다.

실제 사례:
1. **비밀번호 재설정 보안 질문**: 추측 가능한 질문으로 설계 → 계정 탈취
2. **대량 구매 제한 없는 티켓 예매**: 봇으로 모든 티켓 선점 → 비즈니스 피해
3. **신용카드 번호 전체 저장**: 결제 후에도 모든 번호 저장 → PCI DSS (Payment Card Industry Data Security Standard) 위반
4. **사용자 권한 DB에서 클라이언트가 직접 조작**: IDOR (Insecure Direct Object Reference) 설계

📢 **섹션 요약 비유**: 안전하지 않은 설계는 튼튼한 자물쇠를 달았지만 창문이 없는 집이다. 아무리 잠금장치를 잘 만들어도, 들어오는 구멍이 있으면 의미가 없다.

---

## Ⅱ. 아키텍처 및 핵심 원리

Secure Design의 핵심 원칙:

| 원칙 | 설명 | 예시 |
|:---|:---|:---|
| 최소 노출 (Constrained Design) | 필요한 기능만 노출 | API 엔드포인트 최소화 |
| 심층 방어 (Defense in Depth) | 여러 레이어의 보안 통제 | 인증 + 인가 + 암호화 |
| 실패 안전 기본값 (Fail Secure) | 오류 시 보안 상태 유지 | 예외 발생 시 접근 거부 |
| 완전 중재 (Complete Mediation) | 모든 접근에 권한 확인 | 서버 측 권한 검사 강제 |
| 직접 객체 참조 금지 | 내부 ID 외부 노출 금지 | 토큰/랜덤 식별자 사용 |

```
┌──────────────────────────────────────────────────────────┐
│           Insecure Design vs Secure Design 비교          │
├──────────────────────────────────────────────────────────┤
│  Insecure:                                               │
│  사용자 → 클라이언트에서 권한 확인 → DB 직접 접근        │
│                                                          │
│  Secure:                                                 │
│  사용자 → API Gateway (인증) → 서버 권한 확인            │
│         → 서비스 레이어 → DB (최소 권한 계정)            │
└──────────────────────────────────────────────────────────┘
```

📢 **섹션 요약 비유**: 안전한 설계는 건물을 지을 때 화재 대피로를 나중에 추가하는 것이 아니라, 설계도부터 포함시키는 것이다.

---

## Ⅲ. 비교 및 연결

Insecure Design vs. 다른 OWASP 항목:

| 구분 | A04 Insecure Design | A05 Security Misconfiguration |
|:---|:---|:---|
| 원인 | 설계 단계 보안 통제 누락 | 올바른 설계를 잘못 설정 |
| 해결 | 설계 변경 (코딩으로 불가) | 설정 변경 |
| 발견 시점 | 아키텍처 리뷰 | 설정 감사 |
| 예시 | 보안 질문 기반 비밀번호 재설정 | 기본 비밀번호 미변경 |

📢 **섹션 요약 비유**: Insecure Design은 집 설계에 잠금장치가 아예 없는 것이고, Misconfiguration은 잠금장치가 있는데 안 잠근 것이다.

---

## Ⅳ. 실무 적용 및 기술사 판단

**Secure Design 방법론**:
1. **위협 모델링 (Threat Modeling)**: STRIDE (Spoofing, Tampering, Repudiation, Information Disclosure, Denial of Service, Elevation of Privilege)로 설계 단계 위협 식별
2. **보안 사용자 스토리 (Security User Stories)**: 기능 요구사항과 함께 보안 요구사항 정의
3. **레퍼런스 아키텍처**: 검증된 보안 패턴 재사용
4. **설계 리뷰**: 보안 전문가 참여 아키텍처 리뷰
5. **Abuse Cases**: 정상 사용 시나리오와 함께 악용 시나리오 작성

📢 **섹션 요약 비유**: 위협 모델링은 건물을 짓기 전에 "이 건물에서 어떤 사고가 날 수 있는가?"를 먼저 생각하는 과정이다.

---

## Ⅴ. 기대효과 및 결론

위협 모델링을 SDL에 통합하면 설계 결함을 코딩 이전에 발견·수정할 수 있어 수정 비용을 1/10 이하로 줄일 수 있다(보안 결함 수정 비용은 발견 시점이 늦을수록 기하급수적으로 증가). OWASP SAMM (Software Assurance Maturity Model)에 따르면 위협 모델링 성숙도가 높은 조직일수록 보안 사고 빈도가 현저히 낮다.

📢 **섹션 요약 비유**: 설계 단계의 보안 투자 1원은 운영 단계의 100원 수정 비용을 아낀다. 안전한 설계는 비용이 아닌 투자다.

---

### 📌 관련 개념 맵
| 개념 | 관계 | 설명 |
|:---|:---|:---|
| STRIDE | 위협 모델링 방법론 | 6가지 위협 카테고리 |
| SDL | 통합 프레임워크 | 전 SDLC 보안 내재화 |
| Abuse Case | 설계 도구 | 악용 시나리오 정의 |
| Defense in Depth | 설계 원칙 | 다층 보안 통제 |
| OWASP SAMM | 성숙도 모델 | 소프트웨어 보증 성숙도 |

### 👶 어린이를 위한 3줄 비유 설명
- 안전하지 않은 설계는 문을 열 수 없게 만들었지만, 창문을 너무 낮게 달아서 아무나 들어올 수 있는 집이야.
- 코드를 아무리 잘 짜도, 처음부터 설계가 잘못됐으면 고칠 수가 없어.
- 그래서 집을 짓기 전에 "어떻게 침입할 수 있는지"를 먼저 생각하는 위협 모델링이 필요해!
""")

w("441_missing_threat_modeling.md", 441, "441. 위협 모델링 부재", """
## 핵심 인사이트 (3줄 요약)
> 1. **본질**: 위협 모델링 (Threat Modeling) 부재는 시스템 설계 단계에서 잠재적 공격 시나리오를 체계적으로 분석하지 않아, 보안 통제가 실제 위협과 불일치하거나 중요 취약점이 운영 단계에야 발견되는 문제다.
> 2. **가치**: Microsoft STRIDE, PASTA (Process for Attack Simulation and Threat Analysis), DREAD (Damage, Reproducibility, Exploitability, Affected Users, Discoverability)로 설계 단계에서 위협을 식별하면 수정 비용을 운영 단계 대비 1/100로 줄일 수 있다.
> 3. **판단 포인트**: 위협 모델링은 일회성 활동이 아니라 시스템 변경마다 반복해야 하며, 개발팀·보안팀·아키텍트가 함께 참여하는 협업 과정이어야 한다.

---

## Ⅰ. 개요 및 필요성

소프트웨어 개발 현장에서 보안은 종종 개발이 완료된 후 침투 테스트 단계에서야 다루어진다. 이때 발견된 설계 결함은 이미 구현이 완료된 상태라 수정 비용이 막대하다. 위협 모델링은 이 문제를 설계 단계에서 선제적으로 해결하는 방법론이다.

**STRIDE 위협 모델링 6가지 위협 유형**:
- **S (Spoofing)**: 신원 위조 — 인증 통제로 방어
- **T (Tampering)**: 데이터 변조 — 무결성 통제로 방어
- **R (Repudiation)**: 부인 — 로깅·감사로 방어
- **I (Information Disclosure)**: 정보 노출 — 암호화·접근제어로 방어
- **D (Denial of Service)**: 서비스 거부 — 가용성 통제로 방어
- **E (Elevation of Privilege)**: 권한 상승 — 인가 통제로 방어

📢 **섹션 요약 비유**: 위협 모델링은 건물 설계 전에 화재, 침수, 도난 등을 시뮬레이션해보는 리스크 분석 과정이다. 미리 생각하지 않으면 나중에 더 큰 비용을 치른다.

---

## Ⅱ. 아키텍처 및 핵심 원리

위협 모델링 4단계 프로세스:

| 단계 | 활동 | 산출물 |
|:---|:---|:---|
| 1. 시스템 분해 | DFD (Data Flow Diagram) 작성 | 시스템 구성 요소, 신뢰 경계 |
| 2. 위협 식별 | STRIDE 기법으로 각 요소에 위협 매핑 | 위협 목록 |
| 3. 위협 평가 | DREAD/CVSS로 위험도 점수화 | 우선순위 위협 목록 |
| 4. 대응 정의 | 각 위협에 대응 통제 매핑 | 보안 요구사항 |

```
┌──────────────────────────────────────────────────────────┐
│           위협 모델링 프로세스                           │
├──────────────────────────────────────────────────────────┤
│  [시스템 분해] → DFD 작성 → 신뢰 경계 식별              │
│       │                                                  │
│       ▼                                                  │
│  [위협 식별] → STRIDE 적용 → 위협 목록                   │
│       │                                                  │
│       ▼                                                  │
│  [위협 평가] → DREAD 점수 → 우선순위                     │
│       │                                                  │
│       ▼                                                  │
│  [대응 정의] → 보안 통제 설계 → SDL 통합                 │
└──────────────────────────────────────────────────────────┘
```

📢 **섹션 요약 비유**: 위협 모델링은 체스 게임에서 상대방의 수를 미리 생각하는 것이다. "상대가 이 말을 여기 두면 나는 어떻게 방어하지?"

---

## Ⅲ. 비교 및 연결

위협 모델링 방법론 비교:

| 방법론 | 특징 | 적용 시점 |
|:---|:---|:---|
| STRIDE | 6가지 위협 유형으로 체계적 분석 | 설계 단계 |
| PASTA | 비즈니스 영향 중심 7단계 | 설계~개발 |
| DREAD | 위험도 정량 평가 (5가지 기준) | 위협 우선순위화 |
| LINDDUN | 프라이버시 위협 특화 | GDPR 관련 |
| VAST | 자동화·애자일 친화 | DevSecOps |

📢 **섹션 요약 비유**: 각 위협 모델링 방법론은 같은 집을 다른 전문가(도둑 전문가, 소방관, 구조 전문가)가 각자 시각으로 검토하는 것이다.

---

## Ⅳ. 실무 적용 및 기술사 판단

**실무 도입 방법**:
1. **Microsoft Threat Modeling Tool**: DFD 기반 자동 STRIDE 분석 지원
2. **OWASP Threat Dragon**: 오픈소스 웹 기반 위협 모델링 도구
3. **스프린트 위협 모델링**: 새 기능 추가 시마다 간략한 위협 분석 수행
4. **Security Champion**: 개발팀 내 보안 담당자 지정, 위협 모델링 주도
5. **Architecture Decision Records (ADR)**: 보안 결정사항을 ADR로 문서화

📢 **섹션 요약 비유**: Threat Modeling Tool은 건물 설계 소프트웨어에 자동 화재·지진 리스크 분석 기능이 내장된 것이다.

---

## Ⅴ. 기대효과 및 결론

위협 모델링을 SDL에 통합하면 설계 단계에서 보안 요구사항을 명확히 정의하고, 개발 단계에서 불필요한 재작업을 방지할 수 있다. 특히 마이크로서비스 아키텍처에서 서비스 간 신뢰 경계를 위협 모델링으로 명확히 하면 Zero Trust 구현의 기반이 된다.

📢 **섹션 요약 비유**: 위협 모델링 없는 보안은 도면 없이 짓는 건물이다. 완성 후에 벽이 얇다는 걸 알면 이미 늦다.

---

### 📌 관련 개념 맵
| 개념 | 관계 | 설명 |
|:---|:---|:---|
| STRIDE | 핵심 방법론 | 6가지 위협 분류 |
| DFD | 분석 도구 | 데이터 흐름 다이어그램 |
| DREAD | 평가 도구 | 위협 위험도 정량화 |
| SDL | 통합 프레임워크 | 전 SDLC 보안 내재화 |
| Zero Trust | 연계 아키텍처 | 신뢰 경계 제거 |

### 👶 어린이를 위한 3줄 비유 설명
- 위협 모델링은 집 짓기 전에 "도둑, 화재, 홍수에 대비해서 어떻게 설계할까?"를 먼저 생각하는 것이야.
- 집 다 짓고 나서 "창문이 너무 크네"라고 하면 비용이 많이 드는 것처럼, 개발 후에 보안 결함을 고치는 것도 매우 비싸.
- 그래서 설계 단계에서 미리 공격 방법을 생각하고 막는 게 가장 똑똑한 방법이야!
""")

w("442_insecure_defaults.md", 442, "442. 안전하지 않은 기본값", """
## 핵심 인사이트 (3줄 요약)
> 1. **본질**: 안전하지 않은 기본값 (Insecure Defaults)은 소프트웨어나 프레임워크가 배포 용이성을 위해 편의성 우선으로 설정된 기본값을 그대로 사용해, 보안 통제가 비활성화된 상태로 운영하는 취약점이다.
> 2. **가치**: 기본 관리자 계정(admin/admin), 기본 포트, 디버그 모드 활성화, 불필요한 기능 활성화가 실제 침해의 주요 경로이며, A05 Security Misconfiguration과 밀접하게 연관된다.
> 3. **판단 포인트**: Secure by Default 원칙을 적용해 새 시스템은 항상 가장 제한적인 설정으로 시작하고, 필요할 때만 명시적으로 기능을 활성화해야 한다.

---

## Ⅰ. 개요 및 필요성

소프트웨어 벤더는 초기 설치 후 빠른 동작 확인을 위해 관리 편의성 우선의 기본값을 설정한다. 이는 개발 환경에서는 유용하지만, 운영 환경에서 그대로 사용되면 심각한 보안 위협이 된다.

대표적인 안전하지 않은 기본값 사례:
- Apache Tomcat: 기본 admin/admin 계정 및 Manager 앱
- MySQL: 빈 root 비밀번호 (구버전)
- Jenkins: 익명 접속 허용 기본 설정
- Spring Boot Actuator: 모든 엔드포인트 공개 기본 설정
- Redis: 인증 없이 모든 IP 접속 허용
- MongoDB: 인증 비활성화 기본 설정 (구버전)
- AWS S3: Public Access 설정 (구버전 기본)

📢 **섹션 요약 비유**: 안전하지 않은 기본값은 새 자동차에 모든 창문이 열려 있고, 문이 잠겨 있지 않은 상태로 출고되는 것이다. 운전자가 직접 닫아야 한다.

---

## Ⅱ. 아키텍처 및 핵심 원리

Secure by Default 원칙의 레이어별 적용:

| 레이어 | 안전하지 않은 기본값 | Secure Default 설정 |
|:---|:---|:---|
| 네트워크 | 모든 포트 열림 | 필요한 포트만 명시적 허용 |
| OS | guest 계정 활성화 | 모든 불필요 계정 비활성화 |
| DB | root 인증 없음 | 강한 비밀번호 + 원격 접속 제한 |
| 미들웨어 | 기본 관리자 계정 | 기본 계정 삭제 또는 비밀번호 변경 |
| 애플리케이션 | 디버그 모드 활성화 | 운영 환경 디버그 비활성화 |

```
┌──────────────────────────────────────────────────────────┐
│         Secure by Default 원칙                           │
├──────────────────────────────────────────────────────────┤
│  잘못된 접근: 모든 기능 활성화 → 필요 없으면 비활성화   │
│  올바른 접근: 모든 기능 비활성화 → 필요할 때만 활성화   │
│                                                          │
│  적용 영역:                                              │
│  계정: 기본 계정 삭제 / 비밀번호 초기화 강제            │
│  네트워크: Deny All → 필요한 것만 Allow                │
│  기능: 최소 기능만 활성화 (Minimal Installation)        │
└──────────────────────────────────────────────────────────┘
```

📢 **섹션 요약 비유**: Secure by Default는 새 건물 모든 문을 잠근 채로 입주하게 하고, 주민이 필요할 때 직접 열쇠를 받는 방식이다.

---

## Ⅲ. 비교 및 연결

| 구분 | Insecure Defaults | A05 Security Misconfiguration |
|:---|:---|:---|
| 원인 | 벤더 기본값 사용 | 관리자의 잘못된 설정 변경 |
| 발견 | 초기 배포 단계 | 운영 중 감사 |
| 해결 | 하드닝 스크립트 + Secure Default 정책 | 설정 변경·검토 |
| 도구 | CIS Benchmark, DISA STIG | CSPM, 설정 스캔 도구 |

📢 **섹션 요약 비유**: Insecure Default는 공장에서 출고된 기본 설정이고, Misconfiguration은 사용자가 잘못 바꾼 설정이다.

---

## Ⅳ. 실무 적용 및 기술사 판단

**Hardening 체크리스트**:
1. **기본 계정 변경/삭제**: 설치 후 즉시 기본 비밀번호 변경 또는 계정 삭제
2. **불필요 서비스 제거**: `minimal installation` 패턴으로 필요한 패키지만 설치
3. **디버그 모드 비활성화**: 운영 환경 `DEBUG=False`, `Spring.profiles.active=prod`
4. **인증 활성화**: Redis, MongoDB 등 기본 인증 없는 서비스에 인증 설정
5. **자동화 하드닝**: Ansible, Chef 등 IaC 도구로 보안 기본값 자동 적용

📢 **섹션 요약 비유**: IaC로 하드닝을 자동화하는 것은 공장 출고 전에 자동으로 모든 창문과 문을 잠그는 라인을 추가하는 것이다.

---

## Ⅴ. 기대효과 및 결론

CIS Benchmark 기준 하드닝을 자동화하면 신규 시스템 배포 시 기본값에 의한 취약점을 사전에 제거할 수 있다. 특히 컨테이너 환경에서 Golden Image (보안 강화된 기본 이미지)를 표준화하면 모든 배포에 일관된 보안 기본값이 적용된다.

📢 **섹션 요약 비유**: Golden Image는 모든 직원이 같은 보안 설정이 미리 적용된 노트북을 받는 것이다. 개인이 설정을 깜빡해도 기본적으로 안전하다.

---

### 📌 관련 개념 맵
| 개념 | 관계 | 설명 |
|:---|:---|:---|
| CIS Benchmark | 하드닝 기준 | 운영체제·애플리케이션 보안 설정 기준 |
| Secure by Default | 설계 원칙 | 기본적으로 가장 안전한 설정 |
| Golden Image | 구현 방법 | 사전 하드닝된 기본 이미지 |
| IaC | 자동화 도구 | 설정 코드화·자동 적용 |
| Minimal Installation | 보안 원칙 | 최소 필요 소프트웨어만 설치 |

### 👶 어린이를 위한 3줄 비유 설명
- 안전하지 않은 기본값은 새 스마트폰이 비밀번호 없이 열려있는 상태로 배달 오는 것 같아.
- 사용자가 직접 비밀번호를 설정해야 하는데, 많은 사람들이 귀찮아서 그냥 쓰거나 깜빡해.
- 그래서 처음부터 "반드시 비밀번호를 설정해야 사용할 수 있어"처럼 강제로 안전하게 시작하는 것이 Secure by Default야!
""")

w("443_excess_functionality.md", 443, "443. 초과 기능 — 불필요한 기능 활성화", """
## 핵심 인사이트 (3줄 요약)
> 1. **본질**: 초과 기능 (Excess Functionality)은 운영 환경에서 불필요하거나 더 이상 사용하지 않는 기능·API 엔드포인트·디버그 인터페이스가 활성화된 채로 남아, 공격자가 이를 통해 의도하지 않은 방식으로 시스템에 접근하는 취약점이다.
> 2. **가치**: Spring Boot Actuator의 `/env`, `/heapdump` 엔드포인트, 개발용 API 엔드포인트, 관리자 콘솔의 운영 환경 노출이 실제 침해로 이어지는 대표적 사례다.
> 3. **판단 포인트**: 최소 노출 원칙(Principle of Least Exposure)을 적용해 기능별 활성화 여부를 명시적으로 설정하고, 운영 환경과 개발 환경의 설정을 완전히 분리해야 한다.

---

## Ⅰ. 개요 및 필요성

소프트웨어는 개발 과정에서 다양한 디버깅, 모니터링, 테스트 기능이 추가된다. 이러한 기능이 운영 환경에 함께 배포되면, 공격자에게 시스템 내부 구조, 설정 정보, 심지어 원격 실행 경로를 제공할 수 있다.

실제 사례:
1. **Spring Boot Actuator**: `/actuator/env` (환경 변수), `/actuator/shutdown` (서버 종료 API), `/actuator/heapdump` (메모리 덤프) 공개
2. **Debug 엔드포인트**: `/__debug__`, `/console`, `/phpinfo.php` 운영 노출
3. **미삭제 샘플 코드**: 프레임워크 설치 시 포함된 샘플 앱이 운영에 포함
4. **불필요한 HTTP 메서드**: GET·POST 외 PUT·DELETE·TRACE 메서드 활성화
5. **레거시 API 엔드포인트**: 구버전 API가 신버전과 함께 운영

📢 **섹션 요약 비유**: 초과 기능은 은행 창구 옆에 직원 전용 비상구와 금고 통로가 표지판도 없이 일반 손님에게 열려있는 것과 같다.

---

## Ⅱ. 아키텍처 및 핵심 원리

위험한 초과 기능 유형:

| 기능 유형 | 예시 | 위험 |
|:---|:---|:---|
| 관리 인터페이스 | Spring Actuator `/shutdown` | 서비스 다운 |
| 정보 노출 엔드포인트 | `/actuator/env`, `/phpinfo.php` | 민감 설정 유출 |
| 디버그 기능 | `/console`, `DEBUG=True` | 코드 실행, 설정 유출 |
| 구버전 API | `/api/v1/`, `/api/v2/` 동시 활성 | 보안 패치 미적용 API |
| 불필요 HTTP 메서드 | TRACE, TRACK 메서드 | XST (Cross-Site Tracing) |

```
┌──────────────────────────────────────────────────────────┐
│        Spring Boot Actuator 위험 엔드포인트              │
├──────────────────────────────────────────────────────────┤
│  /actuator/env      → 환경 변수 (DB 비밀번호 노출)       │
│  /actuator/shutdown → 서버 즉시 종료 (DoS)              │
│  /actuator/heapdump → JVM 힙 메모리 덤프                │
│  /actuator/mappings → 전체 URL 매핑 노출                 │
│  /actuator/loggers  → 로그 레벨 실시간 변경             │
└──────────────────────────────────────────────────────────┘
```

📢 **섹션 요약 비유**: 운영 서버에 디버그 콘솔이 열려있는 것은 은행에 "직원 전용: 금고 비밀번호 보관함"이라는 표지판이 고객 앞에 있는 것과 같다.

---

## Ⅲ. 비교 및 연결

초과 기능(A04)과 보안 설정 오류(A05)의 관계:

| 항목 | 초과 기능 | 보안 설정 오류 |
|:---|:---|:---|
| 원인 | 불필요한 기능이 활성화됨 | 필요한 기능이 잘못 설정됨 |
| 예시 | `/actuator/shutdown` 공개 | Admin 기본 비밀번호 미변경 |
| 해결 | 기능 비활성화 또는 제거 | 올바른 설정 적용 |

📢 **섹션 요약 비유**: 초과 기능은 아예 없어야 할 문이 있는 것이고, 보안 설정 오류는 있어야 하는 문의 잠금장치가 고장 난 것이다.

---

## Ⅳ. 실무 적용 및 기술사 판단

**Spring Boot Actuator 보안 설정**:
```yaml
management:
  endpoints:
    web:
      exposure:
        include: "health,info"  # 허용 목록만 명시
  endpoint:
    shutdown:
      enabled: false  # 종료 API 비활성화
  server:
    port: 8081  # 관리 포트 분리 + 내부 네트워크만 접근 허용
```

**일반 원칙**:
1. **환경별 설정 분리**: dev/staging/prod 프로파일 완전 분리
2. **화이트리스트 노출**: 허용 기능 목록만 명시, 나머지 자동 차단
3. **주기적 기능 감사**: 사용하지 않는 API·기능 정기 검토·제거
4. **HTTP 메서드 제한**: 불필요한 메서드(TRACE, TRACK) 비활성화

📢 **섹션 요약 비유**: 운영 환경 기능 목록은 "이것만 허용"이어야 한다. "이것 빼고 다 허용"은 안전하지 않다.

---

## Ⅴ. 기대효과 및 결론

운영 환경에서 불필요한 기능을 비활성화하면 공격 표면(Attack Surface)을 최소화할 수 있다. 특히 인프라 자동화(IaC) 도구로 운영 환경 설정을 코드화하면 개발 기능이 운영에 포함되는 사고를 예방할 수 있다.

기술사 관점에서 초과 기능 관리는 Zero Trust의 "Verify Explicitly"와 "Assume Breach" 원칙의 실천이다. 공격자가 어떤 엔드포인트든 발견하면 즉시 악용을 시도한다는 전제 하에 설계해야 한다.

📢 **섹션 요약 비유**: 집에 있는 모든 창문과 문을 정기적으로 확인하고, 사용하지 않는 것은 아예 벽으로 막는 것이 초과 기능 관리다.

---

### 📌 관련 개념 맵
| 개념 | 관계 | 설명 |
|:---|:---|:---|
| Principle of Least Exposure | 설계 원칙 | 최소 기능 노출 |
| Spring Actuator | 주요 위험 | 관리 엔드포인트 공개 위험 |
| Attack Surface | 위험 개념 | 공격 가능한 진입점 총합 |
| Whitelist | 방어 방법 | 허용 기능 목록 명시 |
| IaC | 자동화 도구 | 설정 코드화·관리 |

### 👶 어린이를 위한 3줄 비유 설명
- 초과 기능은 집 안에 사용하지 않는 비밀 문이 있는데, 잊고 잠그지 않아서 도둑이 들어오는 것과 같아.
- 개발할 때 편의를 위해 만든 기능들이 운영 서버에서도 켜져 있으면 공격자가 이용해.
- 그래서 운영 환경에서는 꼭 필요한 기능만 켜두고, 나머지는 모두 꺼야 해!
""")

print("Batch 440-443 done.")
