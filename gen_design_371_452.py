import os

BASE = "/Users/pf/workspace/brainscience/content/studynote/11_design_supervision/06_exam_summary"

files = {}

files["371_api_gateway_summary.md"] = '''+++
weight = 371
title = "371. API 게이트웨이 (API Gateway) 인증 라우팅"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: API 게이트웨이 (API Gateway)는 MSA (Microservice Architecture)의 단일 진입점으로, 클라이언트와 내부 서비스 사이에 위치하여 인증·인가·라우팅·속도 제한을 중앙 집중 처리한다.
> 2. **가치**: 각 마이크로서비스가 공통 관심사를 개별적으로 구현하지 않아도 되므로, 서비스 응집도가 높아지고 횡단 관심사 (Cross-Cutting Concerns)가 한 곳에서 관리된다.
> 3. **판단 포인트**: 게이트웨이가 단일 장애점 (SPOF, Single Point of Failure)이 되지 않도록 고가용성 (HA, High Availability) 배포와 회로 차단기 (Circuit Breaker) 연계가 필수다.

## Ⅰ. 개요 및 필요성

MSA 환경에서 수십~수백 개의 마이크로서비스가 존재할 때, 클라이언트가 각 서비스를 직접 호출하면 엔드포인트 관리, 인증 중복, 네트워크 비용이 기하급수적으로 증가한다. API 게이트웨이 (API Gateway)는 이 문제를 해결하는 역전된 프록시 (Reverse Proxy) 계층이다.

**주요 기능:**
- **인증/인가 (AuthN/AuthZ)**: JWT (JSON Web Token), OAuth 2.0 검증을 중앙에서 처리
- **라우팅 (Routing)**: URL 경로·HTTP 메서드 기준으로 적절한 마이크로서비스로 요청 전달
- **로드밸런싱 (Load Balancing)**: 복수 인스턴스 간 부하 분산
- **속도 제한 (Rate Limiting)**: 악의적 트래픽 및 과부하 방어
- **응답 변환 (Response Aggregation)**: 여러 서비스 응답을 합쳐 클라이언트에 전달
- **관찰가능성 (Observability)**: 중앙화된 로깅·추적·메트릭 수집

📢 **섹션 요약 비유**: API 게이트웨이는 대기업 정문 보안 게이트와 같다. 방문객은 정문에서 신분 확인을 마친 뒤, 방문 목적에 따라 알맞은 부서로 안내받는다. 각 부서가 직접 방문객을 걸러낼 필요가 없다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│                    API Gateway 아키텍처                         │
├─────────────────────────────────────────────────────────────────┤
│  [Client]                                                       │
│     │ HTTPS                                                     │
│     ▼                                                           │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │            API Gateway (Kong / AWS API GW)               │   │
│  │  ┌─────────┐ ┌──────────┐ ┌────────────┐ ┌──────────┐   │   │
│  │  │ AuthN/Z │ │ Routing  │ │Rate Limit  │ │ Logging  │   │   │
│  │  └─────────┘ └──────────┘ └────────────┘ └──────────┘   │   │
│  └──────────────────────────────────────────────────────────┘   │
│           │            │              │                         │
│           ▼            ▼              ▼                         │
│  [User Service]  [Order Service]  [Payment Service]             │
└─────────────────────────────────────────────────────────────────┘
```

| 구성 요소 | 역할 | 대표 구현 |
|:---|:---|:---|
| 인증 플러그인 | JWT 서명 검증, OAuth 토큰 체크 | Kong JWT, AWS Authorizer |
| 라우터 | 경로 패턴 매핑 → 업스트림 선택 | Nginx Location, Envoy Route |
| 속도 제한기 | 슬라이딩 윈도우 카운터 | Redis + Token Bucket |
| 서킷 브레이커 | 다운스트림 장애 시 빠른 실패 | Hystrix, Resilience4j |
| 관찰가능성 | 분산 추적 ID 삽입 | Jaeger, Zipkin |

📢 **섹션 요약 비유**: 게이트웨이 내부는 공항 보안 검색대와 같다. 여권 확인(인증), 탑승구 배정(라우팅), 인원 제한(속도 제한)이 순서대로 처리되고, 이상 탐지 시 비행기 탑승이 막힌다.

## Ⅲ. 비교 및 연결

| 항목 | API 게이트웨이 | BFF (Backend For Frontend) | 서비스 메시 (Service Mesh) |
|:---|:---|:---|:---|
| 위치 | 외부 엣지 | 클라이언트 유형별 | 서비스 사이드카 |
| 주 목적 | 진입점 통합 | 클라이언트 최적화 | 서비스 간 통신 제어 |
| 인증 | ✅ 중앙 처리 | 부분 처리 | mTLS 서비스 간 |
| 속도 제한 | ✅ 전체 적용 | 클라이언트별 | 서비스별 정책 |

📢 **섹션 요약 비유**: API 게이트웨이는 쇼핑몰 정문, BFF는 VIP 전용 입구, 서비스 메시는 쇼핑몰 내부 복도의 CCTV 망이다.

## Ⅳ. 실무 적용 및 기술사 판단

**고가용성 설계:**
- 게이트웨이 인스턴스를 최소 2개 이상 N+1 구성
- L4/L7 로드밸런서가 게이트웨이 앞단에 위치
- 배포 시 블루-그린 (Blue-Green) 전략으로 무중단 배포

**감리 체크포인트:**
1. 게이트웨이 자체의 HA (High Availability) 구성 여부
2. 인증 토큰 만료 정책 및 갱신 흐름 명세 여부
3. 속도 제한 임계값 산정 근거 (트래픽 분석 기반)
4. 서킷 브레이커 임계값 및 복구 정책 문서화
5. 분산 추적 ID (Trace ID) 전파 여부

**안티패턴:**
- 게이트웨이에 비즈니스 로직 집중 → 비즈니스 로직은 마이크로서비스 내부에 위치해야 함
- 단일 게이트웨이에 모든 클라이언트 유형(모바일/웹/IoT) 혼재 → BFF 패턴 분리 권장

📢 **섹션 요약 비유**: 잘 설계된 게이트웨이는 교통 신호 시스템과 같다. 신호가 고장 나도 경찰관(폴백)이 교통을 대신 정리하며, 특정 차선이 막히면 우회로(서킷 브레이커)로 자동 유도한다.

## Ⅴ. 기대효과 및 결론

API 게이트웨이는 MSA의 복잡성을 클라이언트로부터 완전히 격리하여, 내부 서비스 토폴로지 변경이 외부에 영향을 주지 않는 구조를 만든다. 공통 관심사의 중앙화로 개발 속도가 향상되고, 보안 정책을 일관적으로 적용할 수 있다. 기술사 시험에서는 단일 진입점의 SPOF 위험, HA 설계, 서킷 브레이커 연계를 반드시 언급해야 한다.

📢 **섹션 요약 비유**: API 게이트웨이는 도시의 중앙 환승 터미널과 같다. 어디서 출발하든 이 터미널을 거치면 목적지까지 안전하게 연결되며, 터미널 자체는 24시간 운영 체계를 갖춰야 한다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | MSA (Microservice Architecture) | API 게이트웨이가 전제하는 아키텍처 |
| 하위 개념 | 라우팅, 속도 제한, 서킷 브레이커 | 게이트웨이의 핵심 기능 |
| 연관 개념 | 서비스 메시 (Service Mesh) | 서비스 간 통신 제어 보완 |
| 연관 개념 | BFF (Backend For Frontend) | 클라이언트별 최적화 계층 |

### 👶 어린이를 위한 3줄 비유 설명

- 여러 가게가 모인 백화점에서, 정문 안내 데스크가 어디로 가야 할지 알려주는 것과 같아요.
- 신분증 확인, 층별 안내, 입장 인원 제한을 한 곳에서 해결해요.
- 각 가게는 안내 데스크를 믿고 장사에만 집중할 수 있어요.
'''

files["372_service_mesh_summary.md"] = '''+++
weight = 372
title = "372. 서비스 메시 (Service Mesh) 사이드카 프록시 통신"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 서비스 메시 (Service Mesh)는 마이크로서비스 간 통신을 애플리케이션 코드 밖에서 제어하는 인프라 계층으로, 각 서비스 옆에 사이드카 (Sidecar) 프록시를 붙여 통신·보안·관찰가능성을 처리한다.
> 2. **가치**: 개발팀이 네트워크 정책·암호화·재시도 로직을 서비스 코드에 직접 구현하지 않아도 되므로, 비즈니스 로직 집중도가 높아지고 운영 일관성이 확보된다.
> 3. **판단 포인트**: 사이드카 프록시의 메모리·CPU 오버헤드가 실제 서비스 부하와 비교해 허용 가능한지, 그리고 컨트롤 플레인 (Control Plane)의 단일 장애점 위험을 관리하는지가 핵심 감리 포인트다.

## Ⅰ. 개요 및 필요성

MSA 환경에서 서비스 수가 수백 개로 늘어나면 서비스 간 통신 관리가 폭발적으로 복잡해진다. 재시도 (Retry), 타임아웃 (Timeout), 서킷 브레이커 (Circuit Breaker), mTLS (mutual TLS) 암호화를 각 서비스가 개별 구현하면 코드 중복과 일관성 부재가 발생한다.

서비스 메시는 이 문제를 **데이터 플레인 (Data Plane)**과 **컨트롤 플레인 (Control Plane)**의 분리로 해결한다.

- **데이터 플레인**: 각 서비스 파드 (Pod) 옆에 Envoy 같은 사이드카 프록시 배치 → 모든 인바운드/아웃바운드 트래픽 대리 처리
- **컨트롤 플레인**: Istiod 같은 중앙 컨트롤러가 정책·인증서·라우팅 규칙을 프록시에 배포

📢 **섹션 요약 비유**: 서비스 메시는 배달원(서비스) 옆에 항상 붙어 있는 전담 교통안내원(사이드카)과 같다. 배달원은 목적지만 알면 되고, 교통·우회·보안 경비는 안내원이 처리한다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│               Service Mesh 아키텍처 (Istio 기준)                │
├─────────────────────────────────────────────────────────────────┤
│  Control Plane                                                  │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  Istiod  (Pilot + Citadel + Galley)                     │    │
│  │   정책 배포  │  인증서 발급  │  설정 검증                │    │
│  └─────────────────────────────────────────────────────────┘    │
│           │ xDS API                                             │
│  Data Plane                                                     │
│  ┌──────────────────────┐    ┌──────────────────────┐          │
│  │  Service A           │    │  Service B           │          │
│  │  ┌──────────────┐    │    │  ┌──────────────┐    │          │
│  │  │  App Code    │    │    │  │  App Code    │    │          │
│  │  └──────┬───────┘    │    │  └──────┬───────┘    │          │
│  │  ┌──────▼───────┐    │    │  ┌──────▼───────┐    │          │
│  │  │ Envoy Sidecar│◄───┼mTLS┼──│ Envoy Sidecar│    │          │
│  │  └──────────────┘    │    │  └──────────────┘    │          │
│  └──────────────────────┘    └──────────────────────┘          │
└─────────────────────────────────────────────────────────────────┘
```

| 기능 | 구현 방식 | 도구 |
|:---|:---|:---|
| mTLS 암호화 | 사이드카 간 인증서 자동 교환 | Istio Citadel |
| 트래픽 관리 | 가중치 기반 라우팅, 카나리 배포 | Istio VirtualService |
| 서킷 브레이커 | Envoy OutlierDetection 정책 | Envoy + Istio |
| 분산 추적 | Trace ID 자동 전파 | Jaeger, Zipkin |
| 정책 적용 | AuthorizationPolicy, PeerAuthentication | Istio |

📢 **섹션 요약 비유**: 컨트롤 플레인은 항공 관제탑, 데이터 플레인은 각 항공기의 자동 항법 장치다. 관제탑이 비행 규칙을 내리면 각 항법 장치가 자율적으로 실행한다.

## Ⅲ. 비교 및 연결

| 항목 | 서비스 메시 | API 게이트웨이 | 라이브러리 접근 |
|:---|:---|:---|:---|
| 적용 위치 | 서비스 간 (East-West) | 외부 진입 (North-South) | 애플리케이션 코드 내 |
| 언어 독립성 | ✅ 완전 독립 | ✅ 독립 | ❌ 언어 종속 |
| 배포 복잡도 | 높음 (사이드카 관리) | 중간 | 낮음 |
| 오버헤드 | 사이드카 리소스 | 단일 게이트웨이 | 라이브러리 메모리 |

📢 **섹션 요약 비유**: API 게이트웨이가 국경 검문소라면, 서비스 메시는 국내 도로 전체에 깔린 스마트 CCTV·신호 시스템이다.

## Ⅳ. 실무 적용 및 기술사 판단

**Istio 도입 체크리스트:**
1. 쿠버네티스 (Kubernetes) 클러스터 버전 및 리소스 여유 확인
2. 사이드카 자동 주입 (Automatic Injection) 네임스페이스 설정
3. PeerAuthentication으로 mTLS 모드 (STRICT/PERMISSIVE) 결정
4. AuthorizationPolicy로 서비스 간 최소 권한 접근 제어
5. 트래픽 미러링으로 프로덕션 트래픽 분석 후 점진 전환

**성능 튜닝:**
- Envoy 연결 풀 크기 (maxConnections, http1MaxPendingRequests) 워크로드별 조정
- 사이드카 리소스 제한 (CPU 50m~200m, Memory 64Mi~128Mi) 설정

📢 **섹션 요약 비유**: 서비스 메시 도입은 도시 전체에 스마트 교통 시스템을 까는 것과 같다. 처음 설치 비용이 크지만, 이후 교통 체증·사고 감소 효과는 장기적으로 훨씬 크다.

## Ⅴ. 기대효과 및 결론

서비스 메시는 MSA의 운영 복잡도를 인프라 계층으로 추상화하여, 개발팀이 비즈니스 로직에 집중하게 한다. mTLS 기반 서비스 간 암호화, 세밀한 트래픽 제어, 자동화된 관찰가능성이 핵심 가치다. 기술사 시험에서는 데이터 플레인과 컨트롤 플레인의 분리, 사이드카 오버헤드 관리, Zero Trust 보안 모델과의 연계를 강조해야 한다.

📢 **섹션 요약 비유**: 서비스 메시는 서비스들이 서로 믿고 대화하는 '신뢰 인프라'다. 각자 암호화된 통화만 하고, 모든 대화 내역은 자동으로 기록된다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | MSA (Microservice Architecture) | 서비스 메시가 지원하는 아키텍처 |
| 구성 요소 | Envoy, Istio | 대표적인 데이터/컨트롤 플레인 구현 |
| 연관 개념 | Zero Trust | 서비스 간 명시적 검증 원칙 |
| 연관 개념 | API 게이트웨이 | 외부 진입점 제어 (North-South) |

### 👶 어린이를 위한 3줄 비유 설명

- 친구들이 서로 편지를 보낼 때 각자 비밀 코드를 쓰고, 우체부(사이드카)가 코드 해독과 배달을 대신해줘요.
- 우체국 본부(컨트롤 플레인)는 어떤 규칙으로 편지를 보낼지 모든 우체부에게 알려줘요.
- 친구들은 편지 내용에만 집중하면 되고, 보안과 배달은 우체부가 알아서 해요.
'''

files["373_saga_pattern_compensation.md"] = '''+++
weight = 373
title = "373. 사가 (Saga) 패턴 보상 트랜잭션"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 사가 (Saga) 패턴은 MSA (Microservice Architecture) 환경에서 분산 트랜잭션을 ACID (Atomicity, Consistency, Isolation, Durability) 대신 일련의 로컬 트랜잭션과 보상 트랜잭션 (Compensating Transaction)으로 대체하는 패턴이다.
> 2. **가치**: 2PC (Two-Phase Commit)처럼 서비스 간 강한 결합 없이 최종 일관성 (Eventual Consistency)을 달성할 수 있어, 서비스 자율성과 장애 격리가 동시에 가능해진다.
> 3. **판단 포인트**: 보상 트랜잭션이 비즈니스적으로 "취소 가능"한지 먼저 분석해야 하며, 보상이 불가능한 조작(이메일 발송 등)에 대한 멱등성 (Idempotency) 설계가 필수다.

## Ⅰ. 개요 및 필요성

전통적인 모놀리식 (Monolithic) 시스템에서는 하나의 DB 트랜잭션으로 여러 테이블을 원자적으로 갱신할 수 있다. 그러나 MSA 환경에서 각 서비스가 독립된 DB를 가지면, 서비스 A의 변경과 서비스 B의 변경을 하나의 ACID 트랜잭션으로 묶을 수 없다.

2PC (Two-Phase Commit) 프로토콜이 대안이지만, 코디네이터 장애 시 전체 잠금, 동기 블로킹으로 인한 성능 저하가 문제다. 사가 패턴은 각 단계를 로컬 트랜잭션으로 분리하고, 실패 시 이전 단계들을 취소하는 보상 트랜잭션으로 최종 일관성을 달성한다.

**사가의 두 가지 구현 방식:**
- **코레오그래피 (Choreography)**: 이벤트를 통해 서비스가 자율적으로 다음 단계 트리거
- **오케스트레이션 (Orchestration)**: 중앙 사가 오케스트레이터가 각 서비스에 명령 전송

📢 **섹션 요약 비유**: 사가는 팀 릴레이 달리기와 같다. 각 주자가 자기 구간을 완주하면 바통을 넘기고, 누군가 넘어지면 앞선 주자들이 거꾸로 뛰어 돌아가 원래 자리로 되돌아가는 규칙이다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│           Saga 패턴: 주문 처리 예시 (오케스트레이션)            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [Order Service]                                                │
│      │ 1. 주문 생성 (Local TX)                                  │
│      ▼                                                         │
│  [Saga Orchestrator]                                            │
│      │ 2. 재고 예약 요청                                        │
│      ▼                                                         │
│  [Inventory Service] → 성공 →                                  │
│      │ 3. 결제 요청                                             │
│      ▼                                                         │
│  [Payment Service] → 실패! →                                   │
│      │                                                         │
│      ▼ 보상 트랜잭션 실행                                      │
│  [Inventory Service] ← 재고 예약 취소 (Compensate)             │
│  [Order Service]     ← 주문 취소 (Compensate)                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

| 구분 | 코레오그래피 | 오케스트레이션 |
|:---|:---|:---|
| 조율 방식 | 이벤트 기반 자율 | 중앙 오케스트레이터 명령 |
| 결합도 | 느슨함 | 오케스트레이터 중심 |
| 가시성 | 낮음 (분산 흐름) | 높음 (중앙 상태 관리) |
| 복잡도 | 서비스 증가 시 파악 어려움 | 오케스트레이터 복잡도 |
| 적합 규모 | 소규모 MSA | 복잡한 비즈니스 워크플로우 |

📢 **섹션 요약 비유**: 코레오그래피는 악보 없이 연주하는 재즈, 오케스트레이션은 지휘자가 이끄는 클래식 오케스트라다. 재즈는 자유롭지만 복잡해지면 불협화음이 나기 쉽다.

## Ⅲ. 비교 및 연결

| 항목 | Saga | 2PC (Two-Phase Commit) | 로컬 트랜잭션 |
|:---|:---|:---|:---|
| 일관성 모델 | 최종 일관성 | 강한 일관성 | 강한 일관성 |
| 서비스 결합 | 느슨함 | 강함 (XA 프로토콜) | N/A |
| 장애 격리 | 보상으로 처리 | 잠금 해제 대기 | 자동 롤백 |
| 성능 | 비동기로 고성능 | 동기 블로킹 | 가장 빠름 |
| 적용 범위 | MSA 분산 환경 | 중앙 집중 DB | 단일 서비스 |

📢 **섹션 요약 비유**: 2PC는 모든 팀원이 동시에 손을 들어야 회의가 진행되는 방식, Saga는 각자 할 일을 끝내고 다음 사람에게 알려주는 방식이다.

## Ⅳ. 실무 적용 및 기술사 판단

**보상 트랜잭션 설계 원칙:**
1. **멱등성 (Idempotency)**: 보상 트랜잭션이 여러 번 실행되어도 동일 결과 보장
2. **비즈니스 취소 가능성 분석**: 이미 발송된 이메일은 취소 불가 → 보상 로직을 알림 발송 전에 배치
3. **사가 상태 영속화**: 오케스트레이터의 사가 상태를 DB에 저장하여 장애 후 재개 가능

**구현 프레임워크:**
- Axon Framework (Java 기반 이벤트 소싱 + 사가)
- Apache Camel Saga
- 직접 구현 (Outbox 패턴 + 이벤트 브로커)

**Outbox 패턴 연계:**
로컬 트랜잭션과 이벤트 발행을 원자적으로 처리하기 위해 Outbox 테이블에 이벤트를 먼저 저장하고, Debezium 같은 CDC (Change Data Capture) 도구가 이를 읽어 Kafka에 발행한다.

📢 **섹션 요약 비유**: Outbox 패턴은 "편지를 쓴 후 바로 보내는 대신 우체통에 넣는 것"이다. 우체통이 편지를 확실히 받았다면, 나중에 우체부가 반드시 배달한다.

## Ⅴ. 기대효과 및 결론

사가 패턴은 MSA 환경에서 비즈니스 트랜잭션의 원자성을 코드 레벨로 구현하는 핵심 패턴이다. 2PC의 블로킹 문제를 해결하면서 최종 일관성을 달성한다. 기술사 시험에서는 코레오그래피/오케스트레이션의 비교, 보상 트랜잭션의 멱등성, Outbox 패턴과의 연계를 명확히 서술해야 한다.

📢 **섹션 요약 비유**: 사가는 분산된 팀이 공동 작업 시 실수가 생겨도 모든 팀원이 이전 상태로 돌아갈 수 있는 명확한 '되돌리기 프로토콜'을 갖춘 것이다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | 분산 트랜잭션 (Distributed Transaction) | 사가가 해결하는 문제 |
| 연관 개념 | 최종 일관성 (Eventual Consistency) | 사가의 일관성 모델 |
| 연관 개념 | Outbox 패턴 | 이벤트 발행의 원자성 보장 |
| 대안 개념 | 2PC (Two-Phase Commit) | 강한 일관성이 필요한 경우 |

### 👶 어린이를 위한 3줄 비유 설명

- 여러 친구와 공동 숙제를 할 때, 한 명이 실수하면 그 전까지 한 일을 모두 되돌리는 약속이에요.
- 각자 자기 역할을 다 끝내면 다음 친구에게 알려줘요.
- 실수를 되돌리는 방법(보상)을 미리 정해놓아야 혼란이 없어요.
'''

files["374_ddd_bounded_context_aggregate.md"] = '''+++
weight = 374
title = "374. DDD (Domain-Driven Design) 바운디드 컨텍스트와 애그리게이트"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: DDD (Domain-Driven Design)는 복잡한 비즈니스 도메인을 소프트웨어 모델로 정확히 표현하는 설계 방법론으로, 바운디드 컨텍스트 (Bounded Context)가 마이크로서비스 분할의 자연스러운 경계를 제공한다.
> 2. **가치**: 유비쿼터스 언어 (Ubiquitous Language)를 통해 개발자와 도메인 전문가가 동일한 언어로 소통하면, 코드와 비즈니스 개념 사이의 간극이 사라진다.
> 3. **판단 포인트**: 애그리게이트 (Aggregate) 경계 설정이 잘못되면 트랜잭션 범위가 커지거나 분산 트랜잭션이 필요해지므로, "한 트랜잭션 = 한 애그리게이트" 원칙이 설계의 핵심이다.

## Ⅰ. 개요 및 필요성

기업 시스템의 복잡성은 대부분 비즈니스 도메인의 복잡성에서 온다. 코드를 기술 계층(Controller, Service, Repository)으로만 나누면, 비즈니스 규칙이 여러 클래스에 분산되어 이해하기 어려워진다. DDD는 코드를 도메인 모델 중심으로 구성하여 이 문제를 해결한다.

**DDD의 핵심 개념:**
- **유비쿼터스 언어**: 팀 전체가 공유하는 도메인 용어로 코드, 문서, 대화를 통일
- **바운디드 컨텍스트**: 하나의 도메인 모델이 유효한 명시적 경계
- **엔티티 (Entity)**: 고유 식별자로 구별되는 객체 (주문, 고객)
- **값 객체 (Value Object)**: 속성으로만 구별되는 불변 객체 (주소, 금액)
- **애그리게이트**: 연관된 엔티티와 값 객체의 묶음, 루트 엔티티를 통해서만 접근

📢 **섹션 요약 비유**: DDD는 "업계 전문가와 개발자가 같은 지도를 보며 이야기하는" 것이다. 지도(도메인 모델)가 없으면 서로 다른 곳을 가리키며 소통한다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│                 DDD 바운디드 컨텍스트 맵                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────┐    ┌─────────────────────┐            │
│  │  주문 컨텍스트       │    │  결제 컨텍스트       │            │
│  │  Order (Aggregate)  │    │  Payment (Aggregate)│            │
│  │  - OrderId          │    │  - PaymentId        │            │
│  │  - OrderLines       │◄──►│  - Amount           │            │
│  │  - Status           │    │  - Status           │            │
│  └─────────────────────┘    └─────────────────────┘            │
│           │ Context Map: Customer/Supplier                      │
│  ┌─────────────────────┐                                        │
│  │  재고 컨텍스트       │                                        │
│  │  Product (Aggregate)│                                        │
│  │  - ProductId        │                                        │
│  │  - StockLevel       │                                        │
│  └─────────────────────┘                                        │
└─────────────────────────────────────────────────────────────────┘
```

| 개념 | 설명 | 마이크로서비스 연관 |
|:---|:---|:---|
| 바운디드 컨텍스트 | 도메인 모델의 명시적 경계 | 서비스 분할 경계 기준 |
| 애그리게이트 | 일관성 경계 단위 | 한 서비스 내 DB 범위 |
| 도메인 이벤트 | 상태 변화 사실 표현 | 서비스 간 통신 수단 |
| 컨텍스트 맵 | 컨텍스트 간 관계 명시 | API 계약, ACL 설계 |

📢 **섹션 요약 비유**: 애그리게이트는 팀 리더가 있는 프로젝트 팀이다. 외부에서 팀에 요청할 때는 반드시 팀 리더(루트 엔티티)를 통해야 하며, 팀 내부 상태는 팀 리더가 책임진다.

## Ⅲ. 비교 및 연결

| 항목 | 트랜잭션 스크립트 | 도메인 모델 (DDD) |
|:---|:---|:---|
| 비즈니스 로직 위치 | 서비스 레이어에 집중 | 도메인 객체 내부 |
| 복잡성 대응 | 로직 성장 시 스파게티화 | 캡슐화로 복잡성 관리 |
| 도메인 전문가 소통 | 코드와 용어 불일치 | 유비쿼터스 언어 일치 |
| 적합 규모 | 단순 CRUD | 복잡한 비즈니스 규칙 |

📢 **섹션 요약 비유**: 트랜잭션 스크립트는 요리사 한 명이 모든 요리를 처음부터 끝까지 혼자 담당하는 방식, DDD는 각 요리마다 전담 요리사가 레시피(도메인 모델)를 지키며 만드는 방식이다.

## Ⅳ. 실무 적용 및 기술사 판단

**애그리게이트 설계 원칙:**
1. **작게 유지**: 애그리게이트 내 엔티티 수를 최소화
2. **ID로만 참조**: 다른 애그리게이트는 ID 참조로만 연결
3. **한 트랜잭션 = 한 애그리게이트**: 여러 애그리게이트 수정 시 도메인 이벤트 + 사가 패턴 사용
4. **일관성 규칙 내재화**: 불변식 (Invariant)은 애그리게이트 내부에서 강제

**이벤트 스토밍 (Event Storming) 워크숍:**
도메인 이벤트 → 커맨드 → 애그리게이트 → 바운디드 컨텍스트 순으로 식별하는 협업 설계 기법

📢 **섹션 요약 비유**: 이벤트 스토밍은 포스트잇으로 비즈니스 시나리오를 벽에 펼쳐 놓고, 팀 전체가 같이 "이 일은 누가 책임져야 하나?"를 찾는 과정이다.

## Ⅴ. 기대효과 및 결론

DDD는 복잡한 비즈니스 도메인을 코드에 정확히 반영하는 설계 방법론으로, MSA와 결합하면 서비스 경계가 비즈니스 경계와 일치하여 변경의 영향 범위가 명확해진다. 기술사 시험에서는 바운디드 컨텍스트와 마이크로서비스 분할의 연관성, 애그리게이트 설계 원칙, 이벤트 스토밍 기법을 구체적으로 서술해야 한다.

📢 **섹션 요약 비유**: DDD는 건물 설계도에서 각 방의 용도를 명확히 정하는 것이다. 용도가 명확하면 나중에 증축할 때도 어떤 방을 수정할지 분명해진다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | 도메인 모델링 | DDD가 속하는 설계 접근법 |
| 핵심 개념 | 바운디드 컨텍스트, 애그리게이트 | DDD의 핵심 전술 패턴 |
| 연관 개념 | 사가 패턴 | 애그리게이트 간 트랜잭션 처리 |
| 연관 개념 | 이벤트 소싱 (Event Sourcing) | 상태 대신 이벤트로 저장 |

### 👶 어린이를 위한 3줄 비유 설명

- 학교에서 각 반이 자기 규칙으로 운영되는 것처럼, 각 컨텍스트는 자기만의 의미로 운영돼요.
- 반장(루트 엔티티)을 통해서만 반 내부에 접근할 수 있어요.
- 선생님과 학생이 같은 용어로 대화해야 오해가 없는 것처럼, 개발자와 전문가가 같은 말을 써요.
'''

files["375_acl_legacy_buffer.md"] = '''+++
weight = 375
title = "375. ACL (Anti-Corruption Layer) 안티-코럽션 레이어"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: ACL (Anti-Corruption Layer)은 서로 다른 도메인 모델이나 레거시 시스템 간 경계에 위치하여, 한 쪽의 개념이 다른 쪽을 오염시키지 않도록 변환하는 격리 계층이다.
> 2. **가치**: 신규 시스템이 레거시의 복잡하고 불일치한 데이터 모델을 그대로 흡수하지 않아도 되므로, 깨끗한 도메인 모델을 유지하면서 점진적 현대화가 가능해진다.
> 3. **판단 포인트**: ACL 구현 비용이 레거시 직접 통합 비용보다 클 수 있으므로, 통합 기간과 복잡성을 고려하여 도입 여부를 판단해야 한다.

## Ⅰ. 개요 및 필요성

DDD (Domain-Driven Design) 컨텍스트 맵에서 두 바운디드 컨텍스트 (Bounded Context)가 통합될 때, 한 컨텍스트의 도메인 모델이 다른 컨텍스트를 침범하는 "오염 (Corruption)"이 발생할 수 있다. 특히 레거시 시스템과 통합 시, 레거시의 복잡한 데이터 구조와 불일치한 명명 규칙이 신규 시스템에 그대로 유입되면 신규 시스템의 도메인 모델이 왜곡된다.

ACL은 이 경계에서 번역자(Translator)이자 완충재(Buffer) 역할을 한다:
- 레거시 → 신규: 레거시 데이터 구조를 신규 도메인 개념으로 변환
- 신규 → 레거시: 신규 도메인 개념을 레거시가 이해하는 형식으로 변환

📢 **섹션 요약 비유**: ACL은 해외 출장에서 만나는 통역사다. 현지어를 모르는 비즈니스맨(신규 시스템)이 현지 파트너(레거시)와 소통할 때, 통역사가 문화·언어 차이를 모두 흡수하여 양쪽이 자기 언어로만 대화하게 한다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│               ACL (Anti-Corruption Layer) 아키텍처              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [신규 시스템 - 깨끗한 도메인]                                  │
│   Order, Customer, Product (신규 개념)                          │
│           │                                                     │
│           ▼                                                     │
│  ┌─────────────────────────────────────────────┐               │
│  │           ACL (Anti-Corruption Layer)        │               │
│  │  Facade → Adapter → Translator              │               │
│  │  LegacyOrderDto ←→ Order                    │               │
│  │  CUST_NUM      ←→ CustomerId                │               │
│  │  PRD_CD        ←→ ProductCode               │               │
│  └─────────────────────────────────────────────┘               │
│           │                                                     │
│           ▼                                                     │
│  [레거시 시스템 - 복잡한 구조]                                  │
│   CUST_NUM, PRD_CD, ORD_DT (레거시 명명)                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

| ACL 구성 요소 | 역할 | 구현 패턴 |
|:---|:---|:---|
| 파사드 (Facade) | 레거시 인터페이스 단순화 | 퍼사드 패턴 |
| 어댑터 (Adapter) | 인터페이스 형식 변환 | 어댑터 패턴 |
| 번역기 (Translator) | 도메인 개념 매핑 | 데이터 매핑 테이블 |

📢 **섹션 요약 비유**: ACL 내부는 공항 환전소와 같다. 달러(레거시 데이터)를 원화(신규 도메인 모델)로 바꿔주되, 환율표(번역 규칙)가 명확해야 오환이 없다.

## Ⅲ. 비교 및 연결

| 통합 전략 | 설명 | 오염 위험 | 비용 |
|:---|:---|:---|:---|
| ACL 적용 | 변환 계층으로 완전 격리 | 없음 | 높음 |
| 직접 통합 | 레거시 API 직접 호출 | 높음 | 낮음 |
| 공유 커널 (Shared Kernel) | 공통 도메인 모델 공유 | 중간 | 중간 |
| 적합주의 (Conformist) | 레거시 모델 그대로 수용 | 최고 | 최저 |

📢 **섹션 요약 비유**: 직접 통합은 외국 음식을 날 것으로 먹는 것, ACL 적용은 현지 요리를 내 입맛에 맞게 조리해서 먹는 것이다.

## Ⅳ. 실무 적용 및 기술사 판단

**ACL 적용 결정 기준:**
1. 레거시와의 통합 기간이 6개월 이상 지속될 것으로 예상될 때
2. 레거시 데이터 모델이 복잡하거나 신규 도메인 개념과 크게 불일치할 때
3. 레거시 변경이 불가능하거나 제한적일 때 (외부 시스템, 패키지 소프트웨어)

**번역 규칙 문서화:**
- 레거시 필드명 ↔ 신규 도메인 용어 매핑 테이블 관리
- 레거시 코드값 ↔ 신규 열거형 (Enum) 매핑 관리
- 변경 시 영향 범위 ACL 내부로 제한

**감리 포인트:**
- ACL 구현의 단위 테스트 커버리지 확인
- 번역 규칙의 문서화 및 버전 관리 여부
- 레거시 의존성이 ACL 밖으로 유출되지 않았는지

📢 **섹션 요약 비유**: ACL은 외교관이 협약서를 번역할 때 사용하는 공식 번역 프로토콜이다. 한 단어의 번역이 바뀌면 전체 협약에 영향을 주므로, 번역 규칙 변경은 신중하게 관리된다.

## Ⅴ. 기대효과 및 결론

ACL은 레거시 시스템과의 통합에서 신규 시스템의 도메인 모델을 보호하는 핵심 설계 패턴이다. 스트랭글러 피그 (Strangler Fig) 패턴과 함께 사용하면, 레거시를 단계적으로 교체하면서도 신규 시스템의 깨끗한 모델을 유지할 수 있다. 기술사 시험에서는 DDD 컨텍스트 맵에서 ACL의 위치, 구성 요소(Facade/Adapter/Translator), 도입 판단 기준을 명확히 서술해야 한다.

📢 **섹션 요약 비유**: ACL은 새 건물(신규 시스템)과 낡은 건물(레거시) 사이에 설치하는 방화문이다. 방화문이 있으면 낡은 건물에 불이 나도 새 건물은 안전하다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | DDD 컨텍스트 맵 | ACL이 위치하는 통합 패턴 |
| 연관 개념 | 스트랭글러 피그 패턴 | 레거시 점진 교체 전략 |
| 구현 패턴 | 어댑터, 퍼사드, 번역기 | ACL 내부 구현 방식 |
| 연관 개념 | 적합주의 (Conformist) | ACL의 반대 접근법 |

### 👶 어린이를 위한 3줄 비유 설명

- 오래된 외국어 책과 새 한국어 책 사이에 통역자가 있는 것처럼, ACL은 서로 다른 시스템 사이의 번역자예요.
- 통역자 덕분에 새 책은 외국어를 배울 필요가 없어요.
- 외국어 책이 바뀌어도 통역자만 업데이트하면 새 책은 그대로예요.
'''

files["376_strangler_fig_summary.md"] = '''+++
weight = 376
title = "376. 스트랭글러 피그 (Strangler Fig) 패턴"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 스트랭글러 피그 (Strangler Fig) 패턴은 레거시 시스템을 한 번에 교체하는 빅뱅 방식 대신, 새로운 기능을 점진적으로 신규 시스템에 구축하면서 레거시를 서서히 대체하는 마이그레이션 전략이다.
> 2. **가치**: 전체 교체 리스크를 제거하고 비즈니스 연속성을 유지하면서 현대화할 수 있어, 수십 년 된 레거시도 운영 중단 없이 MSA (Microservice Architecture)로 전환 가능하다.
> 3. **판단 포인트**: 교체 경계를 어떻게 정의하고, 레거시와 신규 시스템 간의 데이터 동기화를 어떻게 처리할지가 성공의 핵심이다.

## Ⅰ. 개요 및 필요성

레거시 시스템 현대화의 가장 큰 위험은 "빅뱅 교체 (Big Bang Replacement)"다. 수년에 걸친 개발 후 한 번에 전환하면, 누락된 기능, 예상치 못한 버그, 사용자 저항으로 프로젝트가 실패할 확률이 높다.

스트랭글러 피그는 열대 무화과나무(Strangler Fig)가 숙주 나무를 서서히 감싸 대체하는 자연 현상에서 영감받은 패턴이다. Martin Fowler가 2004년 처음 제안했으며, 다음 단계로 진행된다:

1. **변환 (Transform)**: 레거시 옆에 새 시스템 구축 시작
2. **공존 (Coexist)**: 레거시와 신규가 동시 운영, 신규 기능은 신규 시스템에만 추가
3. **제거 (Eliminate)**: 레거시 기능이 모두 신규로 이전되면 레거시 종료

📢 **섹션 요약 비유**: 스트랭글러 피그는 낡은 건물을 허물지 않고 옆에 새 건물을 지어가면서 방 하나씩 이사하는 방식이다. 모든 방이 이사 완료되면 낡은 건물을 철거한다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│           Strangler Fig 마이그레이션 아키텍처                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Phase 1: 공존                                                  │
│  ┌─────────────────────────────────────────────────────┐        │
│  │                    API Gateway / Proxy              │        │
│  └──────────────────┬──────────────────────────────────┘        │
│                     │                                           │
│          ┌──────────┴──────────┐                               │
│          ▼                     ▼                               │
│  ┌──────────────┐    ┌──────────────────┐                      │
│  │ 레거시 시스템 │    │  신규 마이크로서비스│                      │
│  │ (기존 기능)   │    │ (새 기능 + 이전된 기능)│                  │
│  └──────────────┘    └──────────────────┘                      │
│          │                     │                               │
│          └─────────┬───────────┘                               │
│                    ▼                                           │
│            데이터 동기화 계층                                   │
│                                                                 │
│  Phase 2: 레거시 기능 완전 이전 → 레거시 종료                   │
└─────────────────────────────────────────────────────────────────┘
```

| 단계 | 활동 | 리스크 관리 |
|:---|:---|:---|
| 변환 | 신규 서비스 구축, 프록시 설치 | 레거시 영향 없음 |
| 공존 | 프록시가 트래픽 분기, 데이터 이중 기록 | 롤백 가능 상태 유지 |
| 제거 | 프록시에서 레거시 라우팅 제거, 레거시 종료 | 점진적 트래픽 전환 (Canary) |

📢 **섹션 요약 비유**: 도로 공사처럼 기존 도로를 닫지 않고 새 도로를 건설하고, 새 도로가 완성되면 차량을 서서히 이전 도로로 옮기는 방식이다.

## Ⅲ. 비교 및 연결

| 마이그레이션 전략 | 특징 | 위험도 | 비용 |
|:---|:---|:---|:---|
| 빅뱅 교체 | 한 번에 전환 | 매우 높음 | 높음 |
| 스트랭글러 피그 | 점진적 교체 | 낮음 | 중간 |
| 리호스팅 (Lift & Shift) | 인프라만 이전 | 낮음 | 낮음 |
| 리팩터링 | 코드 개선, 아키텍처 유지 | 낮음 | 중간 |

📢 **섹션 요약 비유**: 빅뱅 교체는 심장 이식 수술, 스트랭글러 피그는 혈관 하나씩 교체하는 치료법이다. 후자가 리스크는 낮지만 시간이 더 걸린다.

## Ⅳ. 실무 적용 및 기술사 판단

**성공 요인:**
1. **프록시 레이어 (Strangler Facade)**: API 게이트웨이 또는 역방향 프록시가 트래픽을 레거시/신규로 분기
2. **피처 토글 (Feature Toggle)**: 특정 기능의 레거시/신규 라우팅을 런타임에 제어
3. **데이터 동기화**: 이중 기록 (Dual Write) 또는 CDC (Change Data Capture)로 두 시스템의 데이터 일관성 유지
4. **ACL (Anti-Corruption Layer)**: 레거시 데이터 구조가 신규 시스템 오염 방지

**감리 포인트:**
- 각 마이그레이션 단계별 롤백 계획 수립 여부
- 데이터 이중 기록 구간의 정합성 검증 방법
- 레거시 종료 기준 및 일정 명세 여부

📢 **섹션 요약 비유**: 스트랭글러 피그 프로젝트의 핵심은 "언제든 레거시로 돌아갈 수 있는 비상 탈출구"를 항상 열어두는 것이다.

## Ⅴ. 기대효과 및 결론

스트랭글러 피그 패턴은 수십 년 된 레거시 시스템을 비즈니스 연속성 유지 속에 현대화하는 가장 현실적인 전략이다. ACL, 사가 패턴, DDD와 함께 사용하면 레거시의 복잡성을 격리하면서 점진적으로 깨끗한 아키텍처로 이전할 수 있다. 기술사 시험에서는 3단계(변환/공존/제거), 프록시 역할, 데이터 동기화 전략을 구체적으로 서술해야 한다.

📢 **섹션 요약 비유**: 스트랭글러 피그는 레거시라는 낡은 옷을 벗으면서 새 옷을 입는 것이다. 먼저 양말부터, 그 다음 바지, 마지막에 상의를 교체하면 추위를 타지 않는다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 연관 패턴 | ACL (Anti-Corruption Layer) | 레거시 오염 방지 |
| 연관 패턴 | 피처 토글 | 트래픽 분기 제어 |
| 연관 전략 | 카나리 배포 | 점진적 트래픽 전환 |
| 상위 개념 | 레거시 현대화 | 스트랭글러 피그의 목표 |

### 👶 어린이를 위한 3줄 비유 설명

- 낡은 레고 성을 부수지 않고 옆에 새 성을 지어가는 것과 같아요.
- 새 성의 방이 하나씩 완성될 때마다 낡은 성의 방을 비워요.
- 모든 방이 이사 완료되면 낡은 성을 치워요.
'''

files["377_serverless_cold_start.md"] = '''+++
weight = 377
title = "377. 서버리스 (Serverless) FaaS 콜드 스타트 (Cold Start)"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 서버리스 (Serverless) FaaS (Function as a Service)는 이벤트 발생 시에만 함수를 실행하고 비용을 지불하는 모델이지만, 첫 실행 시 컨테이너 초기화 지연인 콜드 스타트 (Cold Start)가 성능 병목이 된다.
> 2. **가치**: 서버 프로비저닝·운영 부담이 없고 자동 스케일링이 기본으로 제공되어, 트래픽 변동이 큰 워크로드에서 비용 효율이 극대화된다.
> 3. **판단 포인트**: 콜드 스타트 지연(수백 ms~수 초)이 허용되지 않는 실시간 서비스는 웜 업 (Warm-up) 전략 또는 프로비저닝된 동시 실행 (Provisioned Concurrency)으로 대응해야 한다.

## Ⅰ. 개요 및 필요성

전통적 서버 모델은 항상 프로세스가 실행 중이어서 요청에 즉시 응답하지만, 사용량이 없을 때도 비용이 발생한다. FaaS 모델은 요청이 있을 때만 함수 컨테이너를 생성·실행하고 종료하여 비용을 최소화한다.

**주요 FaaS 플랫폼:**
- AWS Lambda
- Google Cloud Functions
- Azure Functions
- Cloudflare Workers (V8 Isolate 기반, 콜드 스타트 거의 없음)

**콜드 스타트 발생 원인:**
1. 일정 시간 이상 함수가 호출되지 않아 컨테이너가 제거됨
2. 동시 호출 수가 급증하여 새 컨테이너 생성 필요
3. 배포 후 첫 호출

📢 **섹션 요약 비유**: FaaS는 자동판매기와 같다. 음료를 선택하면(이벤트) 그때 기계가 음료를 만들어 내보낸다. 음료가 없어 제조 시간이 걸리는 것이 콜드 스타트다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│              FaaS 콜드 스타트 vs 웜 스타트                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Cold Start (첫 호출):                                          │
│  [Request] → [컨테이너 생성] → [런타임 초기화] → [코드 로드]   │
│               100~500ms        100~200ms        50~100ms       │
│  → [핸들러 초기화] → [응답] 총 지연: 수백ms ~ 수초             │
│     50~200ms                                                    │
│                                                                 │
│  Warm Start (재사용):                                           │
│  [Request] → [기존 컨테이너 재사용] → [핸들러 실행] → [응답]  │
│                   0ms                    10~50ms               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

| 콜드 스타트 요인 | 영향 | 최적화 방법 |
|:---|:---|:---|
| 런타임 언어 | JVM > Python > Node.js | GraalVM Native, Node.js 선택 |
| 배포 패키지 크기 | 클수록 느림 | 의존성 최소화, 레이어 분리 |
| VPC 설정 | ENI 생성 지연 | VPC 없는 함수 설계 |
| 메모리 설정 | 512MB+ 권장 | 메모리 ↑ → CPU 비례 증가 |

📢 **섹션 요약 비유**: 웜 스타트는 이미 불이 켜진 오븐에 피자를 넣는 것, 콜드 스타트는 오븐을 예열하는 시간부터 기다리는 것이다.

## Ⅲ. 비교 및 연결

| 항목 | 서버리스 FaaS | 컨테이너 (항상 실행) | 서버 (EC2/VM) |
|:---|:---|:---|:---|
| 비용 모델 | 실행 시간/횟수 | 컨테이너 실행 시간 | 인스턴스 시간 |
| 콜드 스타트 | 있음 | 없음 (항상 실행) | 없음 |
| 스케일링 | 자동 (0→무한) | 수동/자동 | 수동/자동 |
| 관리 부담 | 없음 | 중간 | 높음 |
| 적합 워크로드 | 이벤트 기반, 간헐적 | 지속 트래픽 | 긴 실행 시간 |

📢 **섹션 요약 비유**: FaaS는 택시(호출 시 비용), 컨테이너는 전세 버스(시간제 임대), 서버는 자가용(항상 유지비)이다.

## Ⅳ. 실무 적용 및 기술사 판단

**콜드 스타트 완화 전략:**
1. **프로비저닝된 동시 실행 (Provisioned Concurrency)**: AWS Lambda에서 미리 컨테이너를 웜 상태로 유지 (비용 증가)
2. **웜 업 스케줄러**: EventBridge로 주기적으로 함수 호출하여 컨테이너 유지
3. **런타임 선택**: JVM 대신 Node.js/Python, 또는 GraalVM Native Image로 JVM 시작 시간 제거
4. **배포 패키지 최소화**: 필요한 의존성만 포함, Lambda Layer 활용
5. **메모리 증가**: Lambda 메모리 ↑ → 할당 CPU 비례 증가 → 초기화 속도 향상

**감리 포인트:**
- 실시간성이 요구되는 기능에 FaaS 적용 여부 검토
- 콜드 스타트 최대 지연 시간의 SLA (Service Level Agreement) 반영 여부
- 프로비저닝된 동시 실행 비용 대비 효과 분석

📢 **섹션 요약 비유**: 콜드 스타트 문제는 겨울에 자동차 시동을 걸기 전 예열이 필요한 것과 같다. 급하면 예열을 건너뛰지만 엔진에 부담이 되듯, 콜드 스타트는 응답 시간에 부담을 준다.

## Ⅴ. 기대효과 및 결론

서버리스 FaaS는 트래픽 변동이 큰 워크로드에서 인프라 관리 비용을 획기적으로 줄이는 패러다임이다. 콜드 스타트 문제는 워크로드 특성과 비용을 고려한 전략으로 완화할 수 있다. 기술사 시험에서는 FaaS의 비용 모델, 콜드 스타트 발생 원인과 완화 전략, 그리고 서버리스 vs 컨테이너 적용 판단 기준을 명확히 서술해야 한다.

📢 **섹션 요약 비유**: 서버리스는 필요할 때만 켜지는 에어컨이다. 항상 켜두면 비용이 낭비되지만, 첫 번째 켤 때 냉각에 시간이 걸리는 것이 콜드 스타트다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | 클라우드 컴퓨팅 서비스 모델 | FaaS가 속하는 분류 |
| 하위 개념 | 콜드 스타트, 웜 스타트 | FaaS의 성능 특성 |
| 연관 개념 | 이벤트 기반 아키텍처 | FaaS의 실행 모델 |
| 연관 개념 | 컨테이너 (Docker) | FaaS 하부 실행 환경 |

### 👶 어린이를 위한 3줄 비유 설명

- 서버리스는 전기를 켤 때만 요금을 내는 전등 같아요. 꺼두면 전기가 안 들어요.
- 콜드 스타트는 전등을 오랫동안 안 써서 형광등이 깜빡이며 켜지는 현상이에요.
- 항상 준비해두면(프로비저닝) 바로 켜지지만 그만큼 전기요금은 더 나와요.
'''

print("First batch done - creating more files...")
