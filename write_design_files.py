import os

BASE = "/Users/pf/workspace/brainscience/content/studynote/11_design_supervision/06_exam_summary"
os.makedirs(BASE, exist_ok=True)

def write_file(filename, content):
    path = os.path.join(BASE, filename)
    if os.path.exists(path):
        print(f"SKIP (exists): {filename}")
        return
    with open(path, 'w', encoding='utf-8') as f:
        f.write(content)
    print(f"CREATED: {filename}")

# ─────────────────────────────────────────────────────────────────
# 378 - Factory Method
# ─────────────────────────────────────────────────────────────────
write_file("378_factory_method_summary.md", '''+++
weight = 378
title = "378. 팩토리 메서드 (Factory Method) 패턴"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 팩토리 메서드 (Factory Method) 패턴은 객체 생성 로직을 서브클래스에 위임하여, 슈퍼클래스가 "어떤 객체를 만들지"를 결정하지 않고 "언제 만들지"만 결정하는 생성 패턴이다.
> 2. **가치**: 클라이언트 코드가 구체 클래스에 의존하지 않아 OCP (Open-Closed Principle)를 충족하며, 새로운 제품 추가 시 기존 코드를 수정하지 않아도 된다.
> 3. **판단 포인트**: 제품 타입이 런타임에 결정되거나 프레임워크가 사용자 정의 확장을 허용해야 할 때 팩토리 메서드가 적합하다.

## Ⅰ. 개요 및 필요성

소프트웨어 시스템에서 객체 생성 로직이 클라이언트 코드에 직접 박혀 있으면 (`new ConcreteProduct()`), 새로운 제품 타입이 추가될 때마다 클라이언트 코드를 수정해야 한다. 이는 OCP (Open-Closed Principle) 위반이다.

팩토리 메서드 패턴은 객체 생성을 별도 메서드로 캡슐화하여 이 문제를 해결한다. GoF (Gang of Four)의 23가지 디자인 패턴 중 하나로, 생성(Creational) 패턴에 속한다.

**구성 요소:**
- `Creator` (추상 클래스): `factoryMethod()` 선언
- `ConcreteCreator`: `factoryMethod()` 구현, 구체 제품 반환
- `Product` (인터페이스): 제품의 추상 타입
- `ConcreteProduct`: 실제 생성될 제품

📢 **섹션 요약 비유**: 팩토리 메서드는 본사(슈퍼클래스)가 "빵을 만들어라"고만 지시하고, 각 지점(서브클래스)이 자기 레시피로 빵을 굽는 것이다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│              Factory Method 패턴 구조                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  <<abstract>>                                                   │
│  Creator                                                        │
│  + createProduct(): Product  ◄── 팩토리 메서드 (추상)           │
│  + doSomething(): void       ◄── 팩토리 메서드 활용             │
│       △                                                         │
│       │                                                         │
│  ConcreteCreatorA        ConcreteCreatorB                       │
│  + createProduct()       + createProduct()                      │
│    → new ProductA()        → new ProductB()                     │
│                                                                 │
│  <<interface>>                                                  │
│  Product                                                        │
│  + use(): void                                                  │
│       △                                                         │
│  ProductA          ProductB                                     │
│  + use()           + use()                                      │
└─────────────────────────────────────────────────────────────────┘
```

| 구성 요소 | 역할 | 변경 영향 |
|:---|:---|:---|
| Creator | 팩토리 메서드 선언, 알고리즘 뼈대 | 변경 없음 |
| ConcreteCreator | 구체 제품 생성 | 새 제품 추가 시 새 서브클래스 추가 |
| Product | 제품 추상화 | 변경 없음 |
| ConcreteProduct | 실제 제품 | 독립적으로 추가 가능 |

📢 **섹션 요약 비유**: 팩토리 메서드는 프랜차이즈 계약서와 같다. 본사(Creator)는 "메뉴판을 따르라"고만 하고, 각 가맹점(ConcreteCreator)이 지역 특색에 맞게 실제 음식을 만든다.

## Ⅲ. 비교 및 연결

| 패턴 | 생성 주체 | 생성 범위 | 유연성 |
|:---|:---|:---|:---|
| 팩토리 메서드 | 서브클래스 | 단일 제품 | 상속으로 확장 |
| 추상 팩토리 | 팩토리 인터페이스 | 제품 패밀리 | 팩토리 교체로 확장 |
| 빌더 | 빌더 객체 | 복잡한 단일 객체 | 단계별 조합 |
| 싱글톤 | 클래스 자체 | 하나의 인스턴스 | 확장 없음 |

📢 **섹션 요약 비유**: 팩토리 메서드가 "케이크 한 종류"를 맞춤 제작한다면, 추상 팩토리는 "케이크 세트(케이크+음료+포크)"를 통째로 맞춤 제작한다.

## Ⅳ. 실무 적용 및 기술사 판단

**실무 적용 예시:**
- Java `Iterator` 패턴: `Collection.iterator()` → 구체 컬렉션이 구체 Iterator 반환
- Spring Framework: `BeanFactory.getBean()` → 빈 생성 위임
- JDBC `DriverManager.getConnection()` → DB 드라이버별 Connection 생성

**적용 원칙:**
1. 생성될 클래스를 미리 알 수 없을 때
2. 서브클래스가 어떤 객체를 생성할지 결정해야 할 때
3. 제품 생성 책임을 여러 보조 서브클래스 중 하나에 위임하고 지식을 국소화할 때

📢 **섹션 요약 비유**: JDBC에서 MySQL, PostgreSQL, Oracle 중 어떤 DB에 연결할지를 팩토리 메서드가 결정한다. 클라이언트 코드는 `Connection` 인터페이스만 알면 된다.

## Ⅴ. 기대효과 및 결론

팩토리 메서드 패턴은 객체 생성의 유연성을 상속 계층으로 확보하는 패턴으로, 프레임워크 설계에서 특히 강력하다. 기술사 시험에서는 OCP 준수, 구성 요소(Creator/ConcreteCreator/Product/ConcreteProduct) 관계, 추상 팩토리와의 차이를 명확히 서술해야 한다.

📢 **섹션 요약 비유**: 팩토리 메서드는 "어떤 상품을 만들지는 현장(서브클래스)이 결정하고, 상품 만드는 절차는 본사(슈퍼클래스)가 관리하는" 분업 시스템이다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | GoF 생성 패턴 | 팩토리 메서드가 속하는 분류 |
| 연관 패턴 | 추상 팩토리 | 제품 패밀리로 확장 |
| 연관 원칙 | OCP (Open-Closed Principle) | 팩토리 메서드가 준수하는 원칙 |
| 연관 패턴 | 템플릿 메서드 | 동일한 위임 구조의 행동 패턴 |

### 👶 어린이를 위한 3줄 비유 설명

- "장난감을 만들어"라고 지시는 하지만 어떤 장난감인지는 공장마다 달라요.
- 블록 공장은 블록을 만들고, 인형 공장은 인형을 만들지만, 모두 "장난감"이에요.
- 새 장난감이 추가되어도 기존 공장을 건드리지 않고 새 공장을 세우기만 해요.
''')

# ─────────────────────────────────────────────────────────────────
# 379 - Abstract Factory
# ─────────────────────────────────────────────────────────────────
write_file("379_abstract_factory_summary.md", '''+++
weight = 379
title = "379. 추상 팩토리 (Abstract Factory) 패턴"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 추상 팩토리 (Abstract Factory) 패턴은 관련 객체들의 패밀리 (Family)를 구체 클래스를 지정하지 않고 생성하는 인터페이스를 제공하며, 제품군 간의 일관성을 보장한다.
> 2. **가치**: 호환되지 않는 제품들이 혼용되는 것을 방지하고 (예: Windows 버튼 + Mac 텍스트박스 혼용 방지), 팩토리 교체만으로 전체 UI 테마나 DB 구현을 한 번에 변경할 수 있다.
> 3. **판단 포인트**: 제품 패밀리가 확장될 때는 기존 팩토리 인터페이스 자체를 변경해야 하므로, 패밀리 내 제품 수가 안정적일 때 적합하다.

## Ⅰ. 개요 및 필요성

팩토리 메서드가 단일 제품 생성을 서브클래스에 위임한다면, 추상 팩토리는 "연관된 제품 세트" 전체를 일관성 있게 생성하는 팩토리 인터페이스를 제공한다.

**전형적인 문제:**
- GUI 라이브러리에서 Windows용 Button, Checkbox, TextField와 Mac용 Button, Checkbox, TextField가 있을 때
- 클라이언트는 어떤 OS인지에 따라 일관성 있는 UI 컴포넌트 세트를 받아야 함
- Windows 버튼 + Mac 체크박스처럼 혼용되면 안 됨

추상 팩토리는 `GUIFactory` 인터페이스로 이 일관성을 강제한다.

📢 **섹션 요약 비유**: 추상 팩토리는 가구 세트 매장과 같다. "모던 스타일" 팩토리에서 소파, 테이블, 의자를 주문하면 전부 모던 스타일로 일관성 있게 나온다. 혼합 주문 불가!

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│              Abstract Factory 패턴 구조 (GUI 예시)              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  <<interface>> GUIFactory                                       │
│  + createButton(): Button                                       │
│  + createCheckbox(): Checkbox                                   │
│         △                    △                                  │
│  WinFactory             MacFactory                              │
│  +createButton()        +createButton()                         │
│  → new WinButton()      → new MacButton()                       │
│  +createCheckbox()      +createCheckbox()                       │
│  → new WinCheckbox()    → new MacCheckbox()                     │
│                                                                 │
│  Button ──△── WinButton, MacButton                              │
│  Checkbox ──△── WinCheckbox, MacCheckbox                        │
│                                                                 │
│  [Client] → GUIFactory 주입 → 팩토리 메서드 호출               │
│             일관된 제품 패밀리 생성 보장                        │
└─────────────────────────────────────────────────────────────────┘
```

| 구성 요소 | 역할 |
|:---|:---|
| AbstractFactory | 제품 생성 인터페이스 선언 |
| ConcreteFactory | 특정 제품 패밀리 생성 구현 |
| AbstractProduct | 제품 타입 인터페이스 |
| ConcreteProduct | 각 패밀리의 구체 제품 |
| Client | AbstractFactory와 AbstractProduct만 사용 |

📢 **섹션 요약 비유**: 추상 팩토리는 IKEA 매장과 같다. "Billy 시리즈"를 선택하면 모든 가구가 Billy 스타일로 맞춰 나온다. 다른 시리즈와 섞이지 않는다.

## Ⅲ. 비교 및 연결

| 항목 | 추상 팩토리 | 팩토리 메서드 |
|:---|:---|:---|
| 생성 범위 | 제품 패밀리 전체 | 단일 제품 |
| 확장 방식 | 새 팩토리 클래스 추가 | 새 서브클래스 추가 |
| 패밀리 확장 어려움 | 인터페이스 변경 필요 | 해당 없음 |
| 결합도 | 팩토리 인터페이스에 느슨 | 크리에이터 서브클래스에 느슨 |

📢 **섹션 요약 비유**: 추상 팩토리는 전체 인테리어 패키지, 팩토리 메서드는 맞춤 가구 하나다.

## Ⅳ. 실무 적용 및 기술사 판단

**실무 적용 예시:**
- Java Swing의 `LookAndFeel` (각 OS별 UI 팩토리)
- JDBC `ConnectionFactory` (DB 종류별 연결/쿼리 팩토리)
- Spring `ApplicationContext` (다양한 팩토리 전략 지원)

**사용 시점:**
1. 시스템이 여러 제품 패밀리 중 하나를 사용해야 할 때
2. 제품들이 함께 사용되도록 설계될 때 (일관성 강제)
3. 라이브러리를 제공하되 내부 구현을 숨기고 싶을 때

📢 **섹션 요약 비유**: 다크 테마 ↔ 라이트 테마 전환 시 버튼, 텍스트, 배경색이 모두 한 번에 바뀌는 것이 추상 팩토리의 힘이다.

## Ⅴ. 기대효과 및 결론

추상 팩토리는 제품 패밀리의 일관성을 컴파일 타임에 강제하며, 팩토리 교체만으로 전체 구현을 전환할 수 있는 강력한 패턴이다. 기술사 시험에서는 팩토리 메서드와의 차이(단일 제품 vs 패밀리), OCP 준수, 패밀리 확장의 한계를 명확히 서술해야 한다.

📢 **섹션 요약 비유**: 추상 팩토리는 팀 유니폼 세트와 같다. 팀마다 상의, 바지, 신발이 세트로 정해져 있어 혼용하면 경기에 못 나간다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | GoF 생성 패턴 | 추상 팩토리가 속하는 분류 |
| 하위 개념 | 팩토리 메서드 | 추상 팩토리의 구성 요소 |
| 연관 개념 | 빌더 패턴 | 단일 복잡 객체 생성 |
| 연관 원칙 | DIP (Dependency Inversion Principle) | 추상에 의존하는 원칙 |

### 👶 어린이를 위한 3줄 비유 설명

- 레고 세트처럼, "스타워즈 세트"를 사면 모든 블록이 스타워즈 스타일이에요.
- "해리포터 세트"와 섞이지 않도록 팩토리가 보장해줘요.
- 새 테마 세트를 추가해도 기존 세트는 변하지 않아요.
''')

# ─────────────────────────────────────────────────────────────────
# 380 - Builder Pattern
# ─────────────────────────────────────────────────────────────────
write_file("380_builder_pattern_summary.md", '''+++
weight = 380
title = "380. 빌더 (Builder) 패턴"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 빌더 (Builder) 패턴은 복잡한 객체의 구성 과정을 표현과 분리하여, 동일한 생성 절차로 서로 다른 표현을 만들 수 있는 패턴으로, 점층적 생성자 안티패턴 (Telescoping Constructor Anti-Pattern)을 해결한다.
> 2. **가치**: 선택적 매개변수가 많은 객체 생성 시 `new Object(a, null, null, d, null)` 같은 가독성 최악의 코드 대신 유창한 API (Fluent API) 스타일의 명확한 코드를 제공한다.
> 3. **판단 포인트**: 객체의 불변성 (Immutability)을 보장하면서 복잡한 초기화를 처리해야 할 때 빌더가 최선이며, Java의 `Lombok @Builder`나 `record` 타입도 이 패턴을 구현한다.

## Ⅰ. 개요 및 필요성

생성자 매개변수가 4개 이상이면 점층적 생성자 패턴의 가독성이 급격히 떨어진다:
```
new Pizza(size, cheese, pepperoni, mushrooms, onions, bacon)
```
어느 인수가 무엇인지 직관적으로 알기 어렵고, 선택 매개변수 조합마다 새 생성자가 필요하다.

JavaBeans 패턴 (`setXxx()` 체인)은 가독성을 높이지만, 설정 중 객체가 불완전한 상태에 있을 수 있어 스레드 안전성이 무너진다.

빌더 패턴은 이 두 문제를 동시에 해결한다: 유창한 API + 불변 객체.

📢 **섹션 요약 비유**: 빌더는 맞춤 자동차 주문서와 같다. "색상: 빨강, 엔진: V8, 선루프: 있음"처럼 원하는 옵션만 선택하고 마지막에 "주문 완료"를 누르면 완성된 차가 나온다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│                  Builder 패턴 구조                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Pizza pizza = new Pizza.Builder(size)  // 필수 매개변수        │
│      .cheese(true)                      // 선택 매개변수        │
│      .pepperoni(true)                   // 선택 매개변수        │
│      .mushrooms(false)                  // 선택 매개변수        │
│      .build();                          // 불변 객체 생성       │
│                                                                 │
│  Builder (inner static class)                                   │
│  + cheese(bool): Builder                                        │
│  + pepperoni(bool): Builder                                     │
│  + build(): Pizza  ◄── 검증 후 불변 객체 반환                   │
│                                                                 │
│  Pizza (불변)                                                   │
│  - final size, cheese, pepperoni, mushrooms                     │
│  생성자: private (Builder만 접근 가능)                          │
└─────────────────────────────────────────────────────────────────┘
```

| 구분 | 특징 | 단점 |
|:---|:---|:---|
| 점층적 생성자 | 컴파일 타임 안전 | 매개변수 많을수록 가독성 급락 |
| JavaBeans 패턴 | 가독성 좋음 | 불완전 상태 가능, 불변 불가 |
| 빌더 패턴 | 가독성 + 불변성 | 코드량 증가 |

📢 **섹션 요약 비유**: JavaBeans는 조립식 가구를 부품별로 하나씩 끼우는 것, 빌더는 주문서를 완성한 후 공장에서 완제품으로 배송하는 것이다.

## Ⅲ. 비교 및 연결

| 항목 | 빌더 패턴 | 팩토리 메서드 | 추상 팩토리 |
|:---|:---|:---|:---|
| 목적 | 복잡한 단일 객체 조립 | 단일 객체 생성 위임 | 제품 패밀리 생성 |
| 매개변수 | 선택적 다수 | 일반적으로 적음 | 제품마다 다름 |
| 불변성 | ✅ 보장 가능 | 제품에 따라 다름 | 제품에 따라 다름 |

📢 **섹션 요약 비유**: 빌더는 "내 입맛대로 서브웨이 샌드위치 만들기"처럼 재료를 단계별로 선택하는 과정이다.

## Ⅳ. 실무 적용 및 기술사 판단

**실무 패턴:**
- `Lombok @Builder` 어노테이션으로 보일러플레이트 제거
- `java.lang.StringBuilder` (문자열 빌더)
- HTTP 클라이언트: `OkHttpClient.Builder()`
- JPA Criteria API: `CriteriaBuilder`

**build() 시 검증:**
```
if (size <= 0) throw new IllegalStateException("size must be positive");
```
빌더의 `build()` 메서드에서 필수 조건 검증 → 불완전 객체 생성 원천 차단

📢 **섹션 요약 비유**: build()는 주방장이 요리를 완성하기 전에 모든 재료가 준비됐는지 최종 점검하는 과정이다.

## Ⅴ. 기대효과 및 결론

빌더 패턴은 매개변수가 많은 복잡한 객체의 생성을 가독성 있고 안전하게 처리하는 핵심 패턴이다. 불변 객체 설계와 결합하면 멀티스레드 환경에서도 안전한 객체를 만들 수 있다. 기술사 시험에서는 점층적 생성자 문제, 유창한 API 설계, `build()` 검증 로직을 구체적으로 설명해야 한다.

📢 **섹션 요약 비유**: 빌더는 건축 설계도를 다 그린 후 한 번에 완공하는 방식이다. 시공 중간에 "창문을 더 추가해"는 없다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | GoF 생성 패턴 | 빌더가 속하는 분류 |
| 연관 개념 | 불변 객체 (Immutable Object) | 빌더와 함께 사용 |
| 연관 개념 | 유창한 API (Fluent API) | 빌더의 인터페이스 스타일 |
| 연관 패턴 | 복합체 패턴 | 복잡한 구조 구성 |

### 👶 어린이를 위한 3줄 비유 설명

- 햄버거를 주문할 때 빵, 패티, 소스, 채소를 각각 고르고 마지막에 "완성!"을 외치는 것이에요.
- 고르는 동안 불완전한 햄버거가 나오지 않아요.
- 한번 완성되면 내용물을 바꿀 수 없어요 (불변성).
''')

print("Batch 1 (378-380) done")
