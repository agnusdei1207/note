import os

BASE = "/Users/pf/workspace/brainscience/content/studynote/09_security/05_web_app_security"

files = {
"451_credential_stuffing.md": (451, "451. 크리덴셜 스터핑 (Credential Stuffing)", """
## 핵심 인사이트 (3줄 요약)
> 1. **본질**: 크리덴셜 스터핑 (Credential Stuffing)은 다른 서비스에서 유출된 아이디·비밀번호 조합을 자동화 도구로 대량 시도해 계정을 탈취하는 공격이다.
> 2. **가치**: 피해자가 여러 사이트에 동일한 비밀번호를 재사용하는 한, 공격자는 별도의 취약점 없이도 합법적 인증 절차만으로 계정을 점령한다.
> 3. **판단 포인트**: 방어의 핵심은 MFA (Multi-Factor Authentication) 강제 적용과 비정상 로그인 패턴 탐지이며, 비밀번호 재사용을 차단하는 사용자 교육이 병행되어야 한다.

---

## Ⅰ. 개요 및 필요성

크리덴셜 스터핑 (Credential Stuffing)은 브루트 포스 (Brute Force) 공격과 달리 무작위 패스워드를 추측하지 않는다. 대신 다크웹에서 구매하거나 공개적으로 유출된 **콤보리스트 (Combo List)** — 아이디와 비밀번호가 쌍으로 묶인 목록 — 을 이용해, 다른 서비스의 로그인 엔드포인트에 대입하는 방식이다.

전 세계 사용자의 약 65%가 동일한 비밀번호를 여러 서비스에서 재사용한다는 통계가 존재한다. 이 때문에 A서비스에서 유출된 크리덴셜이 B·C·D서비스에도 그대로 작동하며, 공격자는 수억 건의 계정 정보를 저렴한 비용으로 시도할 수 있다. 대규모 크리덴셜 스터핑 캠페인에서는 하루 1억 건 이상의 로그인 요청이 관찰된 사례도 있다.

```text
┌─────────────────────────────────────────────────────────────┐
│             크리덴셜 스터핑 공격 흐름                        │
├─────────────────────────────────────────────────────────────┤
│  1. 다크웹에서 콤보리스트 구매 (유출된 ID + PW 수억 건)       │
│  2. 자동화 봇 (OpenBullet 등)으로 대상 사이트 공격            │
│     ┌────────────┐  POST /login  ┌────────────────┐         │
│     │ 공격자 봇  │ ────────────▶ │ 대상 서버       │         │
│     │ (콤보리스트│               │ (은행, 쇼핑몰 등)│         │
│     │  반복 대입)│ ◀──────────── │ 200 OK / 401    │         │
│     └────────────┘               └────────────────┘         │
│  3. 성공 건 추출 → 계정 탈취 → 금전 피해 또는 재판매          │
└─────────────────────────────────────────────────────────────┘
```

공격 도구는 OpenBullet, SentryMBA 같은 오픈소스 툴이 대표적이며, 프록시 회전 (Proxy Rotation)으로 IP 차단을 우회한다. 공격이 성공하면 계정 내 포인트 탈취, 개인정보 수집, 결제 수단 악용, 계정 판매 등 다양한 2차 피해로 이어진다.

📢 **섹션 요약 비유**: 크리덴셜 스터핑은 마치 마스터키 세트를 들고 아파트 단지를 돌아다니는 것이다. 열쇠 자체는 진짜지만, 자물쇠 주인이 여러 집에 같은 자물쇠를 쓴 게 문제다.

---

## Ⅱ. 아키텍처 및 핵심 원리

### 공격 인프라 구성

| 구성 요소 | 역할 | 설명 |
|:---|:---|:---|
| 콤보리스트 (Combo List) | 공격 원재료 | 유출된 ID·PW 쌍 |
| 자동화 봇 | 대입 실행 | OpenBullet, SentryMBA |
| 프록시 풀 (Proxy Pool) | IP 우회 | 수천 개 IP 순환 사용 |
| CAPTCHA 우회 서비스 | 탐지 회피 | 사람 풀 또는 ML 솔버 |
| 결과 분석기 | 성공 건 추출 | HTTP 200/302 패턴 필터 |

```text
┌──────────────────────────────────────────────────────────────┐
│               방어 계층 구조                                   │
├──────────────────────────────────────────────────────────────┤
│  레이어 1: 유출 모니터링 (HaveIBeenPwned API 연동)            │
│  레이어 2: 로그인 시도 속도 제한 (Rate Limiting)              │
│  레이어 3: 장치 지문 (Device Fingerprint) 이상 탐지           │
│  레이어 4: MFA (Multi-Factor Authentication) 강제             │
│  레이어 5: 행동 분석 (Behavioral Analytics) 봇 탐지           │
└──────────────────────────────────────────────────────────────┘
```

크리덴셜 스터핑과 브루트 포스는 다르다. 브루트 포스는 하나의 계정에 수많은 패스워드를 시도하므로 계정 잠금 정책으로 방어 가능하다. 반면 크리덴셜 스터핑은 각 계정에 한두 번만 시도하므로 임계값 기반 잠금이 효과적이지 않다.

📢 **섹션 요약 비유**: 방어 계층은 아파트 보안 시스템과 같다. 로비 카메라, 엘리베이터 카드키, 현관 도어락, 방범 창문까지 여러 겹을 두어야 침입자를 막을 수 있다.

---

## Ⅲ. 비교 및 연결

| 구분 | 크리덴셜 스터핑 | 브루트 포스 | 패스워드 스프레이 |
|:---|:---|:---|:---|
| 시도 방식 | 유출 크리덴셜 대입 | 무작위 패스워드 반복 | 공통 패스워드 다수 계정 |
| 계정당 시도 수 | 1~2회 | 수천~수백만 회 | 1~5회 |
| 탐지 난이도 | 높음 | 낮음 (속도 이상) | 중간 |
| 방어 핵심 | 유출 모니터링 + MFA | 계정 잠금 | 속도 제한 |

HaveIBeenPwned (HIBP)와 같은 서비스는 기업이 자사 사용자의 이메일이 유출된 적 있는지 API로 확인할 수 있게 해준다. 또한 NIST (National Institute of Standards and Technology) SP 800-63B에서는 패스워드 변경 시 알려진 유출 패스워드 목록과 비교하도록 권고한다.

📢 **섹션 요약 비유**: 세 공격은 마치 복사 열쇠(크리덴셜 스터핑), 픽킹 도구(브루트 포스), 공용 마스터키 시도(패스워드 스프레이)처럼 다른 전략이지만 목적은 같다.

---

## Ⅳ. 실무 적용 및 기술사 판단

**시나리오**: 대형 전자상거래 플랫폼에서 갑자기 새벽 2~4시 사이 로그인 실패율이 평소 대비 30배 상승했다. 특정 IP는 아니지만 수천 개 분산 IP에서 초당 수십 건의 로그인 시도가 감지됐다.

**판단 포인트**:
1. **즉시 대응**: SIEM (Security Information and Event Management) 경보 발동 → 해당 시간대 성공 로그인 계정 목록 추출 → 강제 비밀번호 재설정
2. **중기 대응**: 모든 계정에 MFA 강제 적용, Device Fingerprint 도입
3. **장기 대응**: HIBP API 연동으로 회원가입/로그인 시 유출 여부 실시간 확인, 행동 기반 봇 탐지 (Behavioral Bot Detection) 시스템 도입

**비용 계산**: 계정 탈취 한 건당 평균 피해액 vs. MFA 구현 비용을 비교하면, MFA 도입이 항상 ROI (Return on Investment) 양의 값을 보인다.

📢 **섹션 요약 비유**: 실무 대응은 화재 진압이 아니라 방화 설계다. 불이 난 뒤 끄는 것보다 스프링클러를 미리 설치하는 것이 훨씬 경제적이다.

---

## Ⅴ. 기대효과 및 결론

크리덴셜 스터핑 방어 체계를 갖추면 계정 탈취로 인한 금전 피해, 개인정보 침해, 브랜드 손상을 크게 줄일 수 있다. 핵심은 단일 방어선이 아닌 **다층 방어 (Defense in Depth)** 전략이다.

OWASP (Open Web Application Security Project) Top 10 2021에서 A07 인증 실패 (Identification and Authentication Failures) 항목에 크리덴셜 스터핑이 포함되며, 이는 인증 설계의 취약성을 체계적으로 다루어야 함을 시사한다. 패스키 (Passkey) 및 FIDO2 (Fast IDentity Online 2) 기반 패스워드리스 인증으로 전환하면 크리덴셜 유출 자체의 의미가 없어지므로, 장기적으로는 이 방향이 근본 해결책이다.

📢 **섹션 요약 비유**: 크리덴셜 스터핑 대응의 완성은 자물쇠를 강화하는 것이 아니라, 자물쇠 자체가 필요 없는 생체인식 출입 시스템으로 바꾸는 것이다.

---

### 📌 관련 개념 맵
| 개념 | 관계 | 설명 |
|:---|:---|:---|
| 브루트 포스 (Brute Force) | 혼동 주의 | 크리덴셜 스터핑과 다른 공격 방식 |
| MFA (Multi-Factor Authentication) | 핵심 방어 | 유출 크리덴셜 무력화 |
| HIBP (HaveIBeenPwned) | 탐지 도구 | 유출 DB 조회 서비스 |
| CAPTCHA | 보조 방어 | 봇 차단, 한계 있음 |
| FIDO2 | 장기 해결책 | 패스워드리스 인증 |

### 👶 어린이를 위한 3줄 비유 설명
- 크리덴셜 스터핑은 다른 집에서 훔친 열쇠 묶음으로 우리 집 문을 열어보는 것이에요.
- 많은 사람이 여러 집에 같은 열쇠를 쓰기 때문에 이 공격이 통해요.
- 집마다 다른 열쇠를 쓰거나(비밀번호 다르게), 지문 인식기(MFA)를 달면 막을 수 있어요!
"""),

"452_software_integrity_failure.md": (452, "452. A08 소프트웨어 무결성 실패 (Software Integrity Failure)", """
## 핵심 인사이트 (3줄 요약)
> 1. **본질**: OWASP A08 소프트웨어 무결성 실패 (Software and Data Integrity Failures)는 코드·데이터·CI/CD 파이프라인이 신뢰되지 않은 소스에서 검증 없이 실행될 때 발생한다.
> 2. **가치**: 공급망 공격 (Supply Chain Attack)이 현대 위협의 핵심으로 부상함에 따라, "내가 배포한 코드가 진짜인가"를 검증하는 무결성 보장이 필수가 됐다.
> 3. **판단 포인트**: SolarWinds, Log4Shell 사건이 보여주듯 빌드 파이프라인 하나의 침해가 수천 개 조직을 동시에 위협할 수 있으므로, 서명 검증과 SBOM (Software Bill of Materials) 관리가 핵심이다.

---

## Ⅰ. 개요 및 필요성

OWASP (Open Web Application Security Project) Top 10 2021에서 새로 등장한 A08은 소프트웨어 업데이트, 플러그인, CDN (Content Delivery Network) 라이브러리, CI/CD (Continuous Integration/Continuous Delivery) 파이프라인의 무결성이 검증되지 않을 때 발생하는 취약점 범주다.

전통적 보안은 "외부 공격자가 내부를 침투하는" 시나리오에 집중했다. 그러나 현대 소프트웨어는 수백 개의 오픈소스 의존성을 포함하며, 이 의존성 중 하나라도 악성 코드가 삽입되면 이를 신뢰한 모든 애플리케이션이 즉시 피해를 입는다. 2020년 SolarWinds 오리온 플랫폼 빌드 서버 침해가 18,000개 이상 기관에 영향을 미친 것이 대표적 사례다.

```text
┌──────────────────────────────────────────────────────────────┐
│              공급망 공격 경로                                  │
├──────────────────────────────────────────────────────────────┤
│  소스코드 저장소 ──▶ 빌드 서버 ──▶ 아티팩트 레지스트리        │
│       │                 │                   │                 │
│   [코드 삽입]       [빌드 스크립트       [패키지 교체]         │
│   취약점           변조]               취약점                  │
│       └─────────────────────────────────────┘                 │
│                     ▼                                         │
│               최종 사용자 시스템 (악성 코드 실행)              │
└──────────────────────────────────────────────────────────────┘
```

대표적 공격 유형으로는 의존성 혼동 (Dependency Confusion), 타이포스쿼팅 (Typosquatting), 빌드 서버 침해, 안전하지 않은 역직렬화, CDN 스크립트 변조 등이 있다.

📢 **섹션 요약 비유**: 소프트웨어 무결성 실패는 식재료 납품 업체가 바뀌었는데 주방장이 맛만 보고 음식을 서빙하는 것이다. 납품 경로 전체를 믿을 수 있어야 안전한 요리가 된다.

---

## Ⅱ. 아키텍처 및 핵심 원리

### 주요 공격 벡터

| 공격 유형 | 설명 | 예시 |
|:---|:---|:---|
| 의존성 혼동 (Dependency Confusion) | 내부 패키지명과 같은 공개 패키지 등록 | npm/PyPI에 동명 악성 패키지 |
| 타이포스쿼팅 (Typosquatting) | 오타 유도 패키지명 | lodahs vs lodash |
| 빌드 파이프라인 침해 | CI/CD 서버 직접 공격 | SolarWinds 사례 |
| CDN 스크립트 변조 | 서드파티 JS 파일 교체 | SRI 미적용 시 위험 |
| 안전하지 않은 역직렬화 | 검증 없는 직렬화 데이터 실행 | Java/Python 역직렬화 |

```text
┌──────────────────────────────────────────────────────────────┐
│               무결성 검증 체계                                 │
├──────────────────────────────────────────────────────────────┤
│  SRI (Subresource Integrity) 해시 검증                        │
│  ┌─────────────┐   SHA-256 해시   ┌─────────────────┐        │
│  │ HTML 파일   │ ──────────────▶  │ 브라우저 검증    │        │
│  │ (기대 해시) │                  │ (일치 시 실행)   │        │
│  └─────────────┘                  └─────────────────┘        │
│                                                              │
│  코드 서명 체계: 빌드 → 서명 → 배포 → 서명 검증 후 실행      │
└──────────────────────────────────────────────────────────────┘
```

SRI (Subresource Integrity)는 HTML `<script>` 태그에 `integrity` 속성으로 해시값을 명시해, CDN 파일이 변조됐을 때 브라우저가 실행을 거부하게 한다. SLSA (Supply chain Levels for Software Artifacts) 프레임워크는 빌드 무결성을 4단계로 인증한다.

📢 **섹션 요약 비유**: 무결성 검증은 택배를 받을 때 봉인 씰(해시 서명)을 확인하는 것이다. 씰이 뜯겨 있으면 내용물을 믿으면 안 된다.

---

## Ⅲ. 비교 및 연결

| 항목 | 무결성 실패 | 보안 설정 오류 (A05) | 취약한 컴포넌트 (A06) |
|:---|:---|:---|:---|
| 초점 | 신뢰 체인 파괴 | 설정 부재 | 알려진 취약점 방치 |
| 공격 경로 | 공급망, 빌드 파이프라인 | 노출된 관리 인터페이스 | 패치 미적용 라이브러리 |
| 방어 핵심 | 서명·해시 검증 | 설정 강화 | 의존성 업데이트 |

SBOM (Software Bill of Materials)은 소프트웨어에 포함된 모든 컴포넌트와 의존성 목록이다. 미국 행정명령 14028(2021)은 연방정부 납품 소프트웨어에 SBOM 제출을 의무화했다.

📢 **섹션 요약 비유**: SBOM은 식품 성분표다. 무엇이 들어갔는지 알아야 리콜 때 빠르게 대응할 수 있다.

---

## Ⅳ. 실무 적용 및 기술사 판단

**시나리오**: npm 프로젝트의 의존성 `event-stream` 패키지에 악성 코드가 삽입돼 암호화폐 지갑을 탈취하는 사건(2018)이 발생했다. 수백만 개의 프로젝트가 이를 설치한 상태였다.

**방어 전략**:
1. **패키지 잠금 파일**: `package-lock.json`, `yarn.lock`으로 정확한 버전·해시 고정
2. **Sigstore/Cosign**: 컨테이너 이미지 및 패키지에 투명성 로그 기반 서명
3. **의존성 감사**: `npm audit`, `pip-audit`, Dependabot 자동화
4. **Least Privilege CI/CD**: 빌드 서버의 시크릿 접근 최소화, 읽기 전용 토큰 사용
5. **SBOM 생성 자동화**: Syft, CycloneDX 도구로 릴리스마다 SBOM 생성

📢 **섹션 요약 비유**: 공급망 보안은 식당의 HACCP (Hazard Analysis Critical Control Points) 시스템처럼, 재료가 들어오는 모든 경로를 기록하고 검증한다.

---

## Ⅴ. 기대효과 및 결론

소프트웨어 무결성 관리 체계를 구축하면 공급망 공격, 악성 의존성 삽입, 빌드 파이프라인 침해를 조기에 탐지하고 차단할 수 있다. SLSA 레벨 3 이상의 빌드 무결성 달성은 국제적으로 인정받는 공급망 보안 성숙도 지표다.

미래에는 소프트웨어 서명 생태계(Sigstore, Notary v2)가 npm·PyPI·Docker Hub에 통합되어 모든 패키지 설치 시 자동으로 서명이 검증되는 방향으로 발전할 것이다.

📢 **섹션 요약 비유**: 소프트웨어 무결성 완성은 모든 식재료에 QR코드를 붙여 생산지부터 조리까지 전 과정을 추적하는 스마트 주방 시스템을 만드는 것이다.

---

### 📌 관련 개념 맵
| 개념 | 관계 | 설명 |
|:---|:---|:---|
| SBOM (Software Bill of Materials) | 핵심 도구 | 컴포넌트 목록 관리 |
| SRI (Subresource Integrity) | 방어 기술 | CDN 파일 해시 검증 |
| SLSA | 성숙도 프레임워크 | 빌드 무결성 4단계 |
| Sigstore | 서명 인프라 | 투명성 로그 기반 서명 |
| CI/CD 보안 | 연관 주제 | 빌드 파이프라인 보호 |

### 👶 어린이를 위한 3줄 비유 설명
- 소프트웨어 무결성 실패는 마트에서 산 쿠키 봉지 안에 모르는 사람이 다른 것을 몰래 넣은 것이에요.
- 포장이 뜯긴 것처럼 보여도 모르고 먹으면 큰일 나요.
- 그래서 봉인 씰(코드 서명)을 확인하고, 성분표(SBOM)를 꼭 읽어야 해요!
"""),

"453_cicd_security.md": (453, "453. CI/CD 보안 (Continuous Integration Continuous Delivery Security)", """
## 핵심 인사이트 (3줄 요약)
> 1. **본질**: CI/CD (Continuous Integration/Continuous Delivery) 보안은 자동화된 소프트웨어 빌드·테스트·배포 파이프라인이 악용되지 않도록 각 단계의 무결성과 최소 권한을 보장하는 것이다.
> 2. **가치**: 파이프라인은 소스 코드에서 프로덕션까지 모든 것을 제어하므로, 여기가 침해되면 단일 지점 공격으로 전체 인프라를 장악할 수 있다.
> 3. **판단 포인트**: 시크릿 관리, 최소 권한 원칙, 코드 서명, 파이프라인 자체 코드 리뷰가 CI/CD 보안의 4대 기둥이다.

---

## Ⅰ. 개요 및 필요성

현대 소프트웨어 개발에서 CI/CD 파이프라인은 코드 병합부터 프로덕션 배포까지를 몇 분 안에 자동 처리한다. 이 속도와 자동화는 생산성의 핵심이지만, 동시에 공격 표면 (Attack Surface)을 크게 넓힌다.

파이프라인은 소스 코드 저장소, 시크릿(API 키, DB 패스워드), 클라우드 자격증명, 프로덕션 배포 권한을 모두 한 곳에 집중시킨다. 2021년 SolarWinds 침해와 2022년 CircleCI 시크릿 유출 사고는 파이프라인 침해의 파급력이 얼마나 큰지를 보여준다.

```text
┌──────────────────────────────────────────────────────────────┐
│               CI/CD 파이프라인 보안 위협 지점                  │
├──────────────────────────────────────────────────────────────┤
│  [소스 저장소] ─▶ [빌드 서버] ─▶ [레지스트리] ─▶ [배포]       │
│       │               │               │              │        │
│  코드 삽입       시크릿 노출     이미지 변조      권한 악용     │
│  악성 PR         빌드스크립트    공개 이미지       과도한 IAM   │
│                  주입            신뢰 문제        권한          │
└──────────────────────────────────────────────────────────────┘
```

OWASP Top 10 CI/CD Security Risks에서는 파이프라인 취약점을 10가지로 분류하며, 불충분한 흐름 제어, 부족한 ID·인증 관리, 의존성 체인 남용 등이 상위에 위치한다.

📢 **섹션 요약 비유**: CI/CD 파이프라인은 공장의 자동 컨베이어벨트다. 벨트가 빠를수록 생산은 많아지지만, 불량 부품이 한 번 들어가면 모든 제품에 퍼진다.

---

## Ⅱ. 아키텍처 및 핵심 원리

### CI/CD 보안 위협 10대 항목 (OWASP)

| 순위 | 위협 | 설명 |
|:---|:---|:---|
| 1 | 불충분한 흐름 제어 | PR 승인 없이 배포 가능 |
| 2 | 부족한 ID·인증 | 파이프라인 토큰 과도한 권한 |
| 3 | 의존성 체인 남용 | 악성 의존성 자동 설치 |
| 4 | 파이프라인 코드 주입 | Jenkinsfile 악성 코드 삽입 |
| 5 | 불충분한 아티팩트 무결성 | 서명 없는 이미지 배포 |
| 6 | 불충분한 자격증명 위생 | 시크릿 하드코딩 |
| 7 | 시스템 설정 오류 | 퍼블릭 파이프라인 실행기 |
| 8 | 미검증 서드파티 | 신뢰 없는 Action/Plugin 사용 |
| 9 | 부적절한 아티팩트 보존 | 오래된 취약 이미지 유지 |
| 10 | 불충분한 로깅 | 파이프라인 감사 로그 부재 |

```text
┌──────────────────────────────────────────────────────────────┐
│               시크릿 관리 아키텍처 (올바른 방식)               │
├──────────────────────────────────────────────────────────────┤
│  ❌ 잘못된 방식:                                              │
│  Jenkinsfile: echo $DB_PASSWORD  ← 로그에 시크릿 노출         │
│                                                              │
│  ✅ 올바른 방식:                                              │
│  HashiCorp Vault / AWS Secrets Manager                       │
│  ┌──────────┐  동적 크리덴셜   ┌──────────────┐              │
│  │빌드 에이전│ ◀─────────────  │ 시크릿 관리자 │              │
│  │트 (신원) │  TTL 60초        │ (동적 발급)   │              │
│  └──────────┘                  └──────────────┘              │
└──────────────────────────────────────────────────────────────┘
```

📢 **섹션 요약 비유**: 시크릿 관리는 식당 금고처럼, 직원마다 필요한 재료만 꺼낼 수 있는 개별 서랍을 두고 사용 후 자동으로 잠그는 것이다.

---

## Ⅲ. 비교 및 연결

| 보안 원칙 | CI/CD 적용 | 미적용 시 위험 |
|:---|:---|:---|
| 최소 권한 (Least Privilege) | 빌드 토큰에 필요한 권한만 | 전체 클라우드 장악 가능 |
| 코드 서명 | 모든 커밋·이미지 서명 | 변조 배포 탐지 불가 |
| 브랜치 보호 | PR 리뷰 필수 + Status Check | 악성 코드 직접 병합 |
| 격리 실행 | 빌드마다 새 컨테이너 | 빌드 간 오염 |
| 감사 로그 | 모든 파이프라인 이벤트 기록 | 침해 사후 분석 불가 |

GitHub Actions에서는 `permissions: read-all` 기본 설정과 `OIDC (OpenID Connect)` 기반 클라우드 인증으로 장기 자격증명 없이 배포가 가능하다.

📢 **섹션 요약 비유**: CI/CD 보안 원칙들은 병원의 멸균 절차처럼, 각 단계마다 교차 오염을 막는 격리와 검증의 연속이다.

---

## Ⅳ. 실무 적용 및 기술사 판단

**시나리오**: GitHub Actions 워크플로우에서 `pull_request_target` 이벤트를 사용하는데 외부 포크(fork)에서 악성 PR을 보내 시크릿에 접근하는 공격(PWN Request)이 가능한 상황이다.

**대응 전략**:
1. `pull_request_target` 대신 `pull_request` 사용 (포크에 시크릿 미전달)
2. 외부 Action 버전 핀닝: `actions/checkout@v3` → `actions/checkout@SHA해시`
3. `CODEOWNERS` 설정으로 파이프라인 파일 보호
4. Dependabot으로 Action 버전 자동 업데이트
5. OIDC로 AWS/GCP 인증 — 장기 시크릿 제거

📢 **섹션 요약 비유**: 파이프라인 보안 강화는 자동차 공장에서 로봇 팔의 동작 범위를 정확히 제한하는 것이다. 팔이 필요 이상으로 움직이면 사고가 난다.

---

## Ⅴ. 기대효과 및 결론

CI/CD 보안이 성숙해지면 공급망 공격 노출면을 크게 줄이고, 침해 발생 시 빠른 원인 추적이 가능하다. SLSA (Supply chain Levels for Software Artifacts) 레벨 4 달성 시 모든 빌드가 변조 불가한 서명 체인으로 보증된다.

DevSecOps (Development Security Operations) 문화에서 CI/CD 보안은 별도 게이트가 아니라 파이프라인의 내장 기능이 되어야 한다. "Shift Left Security" 원칙처럼 보안 검사를 개발 초기 단계로 옮길수록 수정 비용이 낮아진다.

📢 **섹션 요약 비유**: CI/CD 보안의 완성은 자동차 제조 공장에서 매 단계마다 품질 검사가 내장된 스마트 조립 라인을 갖추는 것이다 — 완성차 출구에서만 검사하는 것이 아니라.

---

### 📌 관련 개념 맵
| 개념 | 관계 | 설명 |
|:---|:---|:---|
| SLSA | 성숙도 기준 | 빌드 무결성 레벨 |
| OIDC (OpenID Connect) | 인증 방식 | 장기 시크릿 없는 인증 |
| DevSecOps | 문화적 맥락 | 보안 내재화 개발 문화 |
| HashiCorp Vault | 시크릿 관리 | 동적 크리덴셜 발급 |
| Dependabot | 의존성 관리 | 자동 취약 의존성 업데이트 |

### 👶 어린이를 위한 3줄 비유 설명
- CI/CD 파이프라인은 레고 공장의 자동 조립 기계예요.
- 그런데 나쁜 부품이 하나 들어가면 모든 레고에 들어가버려요.
- 그래서 재료마다 검사하고(코드 서명), 조립기마다 잠금장치(최소 권한)를 달아야 해요!
"""),
}

# Write files
for filename, (weight, title, content) in files.items():
    filepath = os.path.join(BASE, filename)
    if os.path.exists(filepath):
        print(f"SKIP (exists): {filename}")
        continue
    with open(filepath, 'w', encoding='utf-8') as f:
        f.write(f'+++\nweight = {weight}\ntitle = "{title}"\ndate = "2026-04-21"\n[extra]\ncategories = "studynote-security"\n+++\n')
        f.write(content)
    print(f"CREATED: {filename}")

print("Done with batch 1")
