import os
OUT = "/Users/pf/workspace/brainscience/content/studynote/09_security/05_web_app_security/"

def w(fn, weight, title, content):
    path = os.path.join(OUT, fn)
    if os.path.exists(path):
        print(f"SKIP: {fn}")
        return
    with open(path, "w") as f:
        f.write(f'+++\nweight = {weight}\ntitle = "{title}"\ndate = "2026-04-21"\n[extra]\ncategories = "studynote-security"\n+++\n\n')
        f.write(content.strip() + "\n")
    print(f"CREATED: {fn}")

w("428_injection_overview.md", 428, "428. A03 인젝션 개요", """
## 핵심 인사이트 (3줄 요약)
> 1. **본질**: OWASP A03:2021 Injection (인젝션)은 신뢰할 수 없는 사용자 입력이 인터프리터(DB, OS, LDAP 등)에 명령으로 전달되어 의도하지 않은 명령이 실행되는 취약점 군이다.
> 2. **가치**: 2013년부터 2017년까지 OWASP Top 1이었으며, 2021년 3위로 하락했지만 SQL, NoSQL, OS Command, LDAP, XPath, EL (Expression Language) 등 수십 가지 유형이 존재한다.
> 3. **판단 포인트**: 모든 인젝션 공격의 공통 근본 원인은 **데이터와 코드의 혼용**이며, 파라미터화/이스케이프/화이트리스트 검증으로 구조적으로 분리해야 한다.

---

## Ⅰ. 개요 및 필요성

인젝션은 가장 오래되고 이해하기 쉬우면서도 여전히 광범위하게 발생하는 취약점이다. 이름이 "인젝션"인 이유는 공격자가 애플리케이션의 쿼리나 명령 구조에 악의적인 데이터를 "주입(Inject)"하기 때문이다.

취약성이 지속되는 이유:
1. **레거시 코드**: 파라미터화 쿼리 없이 문자열 연결로 쿼리를 만드는 오래된 코드
2. **복잡한 입력 경로**: 직접 입력뿐 아니라 쿠키, HTTP 헤더, 파일 이름 등 다양한 경로
3. **다양한 인터프리터**: SQL 외에도 OS, LDAP, XML, JSON, 템플릿 엔진 등 수많은 인터프리터
4. **우회 기법 다양성**: 인코딩, 공백 변형, 주석 삽입 등 필터 우회 방법이 무수히 많음

📢 **섹션 요약 비유**: 인젝션은 주문 폼에 "햄버거 주세요. 아 그리고 창고 문도 열어주세요"라고 쓰는 것이다. 컴퓨터가 주문과 명령을 구분하지 못하면 둘 다 실행한다.

---

## Ⅱ. 아키텍처 및 핵심 원리

인젝션 공격의 공통 구조와 예방 원칙:

| 인젝션 유형 | 대상 | 공격 예시 |
|:---|:---|:---|
| SQL 인젝션 | 관계형 DB | `' OR 1=1--` |
| NoSQL 인젝션 | MongoDB 등 | `{"$gt": ""}` |
| OS Command | 운영체제 셸 | `; ls -la /etc` |
| LDAP 인젝션 | 디렉토리 서비스 | `*(|(mail=*))` |
| XPath 인젝션 | XML 데이터 | `' or '1'='1` |
| EL 인젝션 | Java EE 표현식 | `${7*7}` |
| SSTI | 템플릿 엔진 | `{{7*7}}` |

```
┌──────────────────────────────────────────────────────────┐
│         인젝션 취약점 공통 구조                          │
├──────────────────────────────────────────────────────────┤
│  사용자 입력 → [애플리케이션] → 인터프리터              │
│                    │                                     │
│  취약: 입력을 코드에 직접 연결 → 구조 파괴              │
│  안전: 파라미터화 → 데이터와 코드 분리                  │
│                                                          │
│  공통 방어: 파라미터화 / 이스케이프 / 화이트리스트       │
└──────────────────────────────────────────────────────────┘
```

📢 **섹션 요약 비유**: 모든 인젝션 공격의 치료법은 같다. 사용자 입력은 항상 데이터로, 절대 코드의 일부로 취급하지 않는 것이다.

---

## Ⅲ. 비교 및 연결

| 인젝션 유형 | 탐지 난이도 | 잠재적 피해 | 방어 난이도 |
|:---|:---:|:---:|:---:|
| SQL 인젝션 | 쉬움 | 최상 | 낮음 (파라미터화) |
| OS Command | 중간 | 최상 (서버 장악) | 중간 |
| SSTI | 어려움 | 높음 (RCE 가능) | 높음 |
| NoSQL 인젝션 | 중간 | 높음 | 중간 |
| LDAP 인젝션 | 어려움 | 중간 | 중간 |

📢 **섹션 요약 비유**: 인젝션 유형마다 타깃이 다르지만 전략은 같다. 칼로 오는지 총으로 오는지 달라도 방탄복(파라미터화)은 모든 공격에 효과적이다.

---

## Ⅳ. 실무 적용 및 기술사 판단

**설계 원칙**:
1. **Parameterization First**: 모든 인터프리터 호출에 파라미터화 우선 적용
2. **Input Validation**: 화이트리스트 기반 입력 검증 (블랙리스트는 우회 가능)
3. **Least Privilege**: 인터프리터 계정(DB 계정, OS 계정)에 최소 권한만 부여
4. **Error Handling**: 인터프리터 에러 메시지를 사용자에게 노출하지 않음
5. **SAST/DAST**: 코드 레벨(SAST, Static Application Security Testing)과 런타임 레벨(DAST, Dynamic Application Security Testing) 이중 스캔

📢 **섹션 요약 비유**: SAST는 설계도(코드)에서 함정을 찾는 것이고, DAST는 실제 건물에 들어가 보안을 테스트하는 것이다.

---

## Ⅴ. 기대효과 및 결론

인젝션 취약점은 현대 프레임워크와 ORM (Object-Relational Mapping), 파라미터화 API를 올바르게 사용하면 구조적으로 제거할 수 있다. SDLC (Software Development Lifecycle) 전반에 걸쳐 Secure Coding 교육과 자동화 스캔을 통합하면 신규 발생을 최소화할 수 있다.

기술사 관점에서 인젝션은 **데이터와 코드의 분리(Data-Code Separation)**라는 근본 원칙 위반이다. 이 원칙은 단순히 SQL에 국한되지 않고 모든 인터프리터를 다루는 설계에서 일관되게 적용되어야 한다.

📢 **섹션 요약 비유**: "사용자 입력을 절대 직접 실행하지 마라"는 보안의 황금률이다. 이 한 가지 원칙이 수십 가지 인젝션 공격을 동시에 방어한다.

---

### 📌 관련 개념 맵
| 개념 | 관계 | 설명 |
|:---|:---|:---|
| Parameterized Query | 핵심 방어 | 데이터·코드 분리 |
| OWASP A03:2021 | 상위 카테고리 | 인젝션 취약점 군 |
| SAST/DAST | 탐지 도구 | 정적·동적 취약점 스캔 |
| ORM | 방어 기술 | 자동 이스케이프 제공 |
| Input Validation | 방어 계층 | 화이트리스트 검증 |

### 👶 어린이를 위한 3줄 비유 설명
- 인젝션은 주문서에 몰래 다른 명령을 끼워 넣어 컴퓨터가 실수로 실행하게 만드는 방법이야.
- SQL, 명령줄, 템플릿 등 컴퓨터가 명령을 받는 곳이면 어디든 이런 공격이 가능해.
- 사용자가 입력한 내용은 항상 "데이터로만 취급"하고, 절대 명령으로 실행하면 안 돼!
""")

w("429_sql_injection_deep.md", 429, "429. SQL 인젝션 심화", """
## 핵심 인사이트 (3줄 요약)
> 1. **본질**: SQL (Structured Query Language) 인젝션 심화는 단순 로그인 우회를 넘어 Union-based, Stacked Queries, Out-of-Band 기법을 활용해 전체 DB (Database) 스키마 덤프, 파일 시스템 접근, OS 명령 실행까지 가능한 고급 공격 기법이다.
> 2. **가치**: sqlmap 같은 자동화 도구가 보편화되어 기술 수준이 낮은 공격자도 고급 SQLi 공격을 자동으로 수행할 수 있어, 모든 웹 애플리케이션이 심화 방어를 적용해야 한다.
> 3. **판단 포인트**: Union-based 차단만으로 부족하며 Blind/Time-based 인젝션도 동시에 방어해야 하고, WAF (Web Application Firewall)와 파라미터화 쿼리를 병행해야 한다.

---

## Ⅰ. 개요 및 필요성

기본 SQL 인젝션 패턴을 이해하더라도 실제 공격이 어떤 단계로 진행되는지 이해하는 것이 방어 설계의 핵심이다. 공격자는 일반적으로 다음 단계로 진행한다.

**공격 단계 (Attack Phases)**:
1. **정보 수집**: DB 유형, 버전 확인 (`@@version`, `VERSION()`)
2. **스키마 파악**: `information_schema.tables`로 테이블 목록 추출
3. **데이터 추출**: 민감 테이블에서 레코드 덤프
4. **권한 상승**: `xp_cmdshell` (MS SQL), `LOAD_FILE()` / `INTO OUTFILE` (MySQL) 사용
5. **지속성 확보**: 백도어 계정 생성, 웹쉘 업로드

sqlmap은 이 모든 단계를 자동화해 취약한 파라미터 발견부터 전체 DB 덤프까지 수 분 내 완료한다.

📢 **섹션 요약 비유**: 기초 SQLi는 문을 여는 것이고, 심화 SQLi는 건물 전체를 장악해 설계도까지 훔치는 것이다.

---

## Ⅱ. 아키텍처 및 핵심 원리

Union-based SQL 인젝션 공격 흐름:

| 단계 | 쿼리 예시 | 목적 |
|:---|:---|:---|
| 컬럼 수 확인 | `ORDER BY 1--`, `ORDER BY 2--` | UNION 컬럼 맞추기 |
| 표시 컬럼 찾기 | `UNION SELECT NULL,NULL,NULL--` | 출력 가능 컬럼 위치 |
| DB 버전 확인 | `UNION SELECT 1,@@version,3--` | DB 유형/버전 |
| 테이블 목록 | `UNION SELECT 1,table_name,3 FROM information_schema.tables--` | 스키마 정보 |
| 데이터 추출 | `UNION SELECT 1,password,3 FROM users--` | 크리덴셜 탈취 |

```
┌──────────────────────────────────────────────────────────┐
│           Union-based SQLi 공격 구조                     │
├──────────────────────────────────────────────────────────┤
│  원래 쿼리: SELECT name, price FROM products WHERE id=1  │
│                                                          │
│  공격 입력: 1 UNION SELECT username, password            │
│                              FROM admin_users--          │
│                                                          │
│  결과: 원본 상품 데이터 + admin 계정 비밀번호 함께 반환  │
└──────────────────────────────────────────────────────────┘
```

📢 **섹션 요약 비유**: UNION은 두 개의 다른 창고 목록을 하나의 주문서에 합치는 것이다. 공격자는 상품 목록 대신 비밀 창고 열쇠 목록을 함께 가져온다.

---

## Ⅲ. 비교 및 연결

SQLi 기법별 적용 조건:

| 기법 | 필요 조건 | 특징 |
|:---|:---|:---|
| Union-based | 출력 결과가 페이지에 표시 | 빠른 데이터 추출 |
| Error-based | 에러 메시지 노출 | 에러에서 데이터 추출 |
| Boolean Blind | 참/거짓 결과 차이 존재 | 느리지만 출력 없어도 가능 |
| Time-based Blind | SLEEP 함수 허용 | 완전 Blind 환경에서 사용 |
| Out-of-Band | DNS/HTTP 외부 접속 가능 | 방화벽 환경 우회 |

📢 **섹션 요약 비유**: 환경에 따라 공격자는 다른 도구를 선택한다. 정문이 막히면 창문으로, 창문도 없으면 환기구로 들어가는 방식이다.

---

## Ⅳ. 실무 적용 및 기술사 판단

**심화 방어 전략**:
1. **에러 메시지 완전 억제**: DB 에러를 일반화된 메시지로 교체
2. **WAF + 파라미터화 이중화**: WAF는 알려진 패턴 차단, 파라미터화는 근본 해결
3. **DB 사용자 최소 권한**: SELECT만 필요한 기능 계정에 EXECUTE, FILE 권한 제거
4. **응답 시간 정규화**: Time-based SQLi 탐지 방해
5. **IDS 룰셋 적용**: sqlmap 특징 트래픽 탐지

📢 **섹션 요약 비유**: WAF는 경비원이고, 파라미터화 쿼리는 금고 자체다. 경비원이 뚫려도 금고가 버텨야 한다.

---

## Ⅴ. 기대효과 및 결론

심화 SQLi 방어는 단순히 특수 문자를 차단하는 것이 아니라, 애플리케이션 아키텍처 전반에서 데이터와 코드를 분리하는 원칙을 관철하는 것이다. ORM 전면 도입, DB 계정 최소 권한, WAF + DAST 자동화가 현대적 방어 체계의 핵심이다.

📢 **섹션 요약 비유**: 심화 SQLi 방어는 성의 성벽(WAF)과 내부 금고(파라미터화)를 동시에 강화하는 것이다. 어느 한쪽만 있으면 약점이 된다.

---

### 📌 관련 개념 맵
| 개념 | 관계 | 설명 |
|:---|:---|:---|
| Union-based SQLi | 기법 | 결과 셋 합치기로 데이터 추출 |
| sqlmap | 자동화 도구 | SQLi 자동 탐지·익스플로잇 |
| information_schema | 공격 대상 | DB 메타데이터 저장소 |
| Blind SQLi | 확장 기법 | 출력 없는 환경에서의 공격 |
| ORM | 방어 기술 | 자동 파라미터화 |

### 👶 어린이를 위한 3줄 비유 설명
- SQLi 심화는 문을 여는 것에서 끝나지 않고, 집 안의 모든 방을 뒤지는 것이야.
- sqlmap이라는 자동 도구는 공격자 대신 알아서 집 전체를 뒤져 비밀을 찾아줘.
- 그래서 "이 방법은 막았으니 괜찮겠지"가 아니라, 모든 방어선을 함께 세워야 해!
""")

w("430_error_based_sqli.md", 430, "430. Error-based SQL Injection", """
## 핵심 인사이트 (3줄 요약)
> 1. **본질**: Error-based SQL Injection (에러 기반 SQL 인젝션)은 DB (Database) 에러 메시지에 데이터를 포함시켜 반환하게 하는 공격으로, 에러 메시지를 그대로 사용자에게 노출하는 취약한 애플리케이션에서 유효하다.
> 2. **가치**: 에러 메시지에서 DB 유형, 버전, 테이블 구조, 심지어 데이터 값까지 추출할 수 있어 공격 속도와 정보 획득량이 Blind 방식보다 빠르다.
> 3. **판단 포인트**: `extractvalue()`, `updatexml()`, `floor()` 등 MySQL 전용 함수와 MS SQL의 `convert()` 에러를 이용한 공격이 대표적이며, 에러 메시지 숨김이 핵심 방어다.

---

## Ⅰ. 개요 및 필요성

에러 기반 인젝션은 의도적으로 DB에서 에러를 발생시키고, 그 에러 메시지 안에 원하는 데이터를 포함시키는 기법이다. DB가 에러 발생 시 실행 중이던 쿼리나 함수 인수를 에러 메시지에 포함해 반환하는 특성을 악용한다.

MySQL (My Structured Query Language) Error-based 대표 기법:
```sql
-- extractvalue() 악용: XPath 에러에 데이터 포함
' AND extractvalue(1, concat(0x7e, (SELECT version())))--
-- 에러 메시지: XPATH syntax error: '~8.0.26' (버전 노출)

-- updatexml() 악용
' AND updatexml(1, concat(0x7e, (SELECT user())), 1)--
-- 에러 메시지: XPATH syntax error: '~root@localhost'
```

이 기법이 위험한 이유는 에러 메시지만 확인하면 DB의 모든 정보를 순서대로 추출할 수 있기 때문이다.

📢 **섹션 요약 비유**: DB가 에러가 날 때 "지금 이런 걸 처리하다가 실패했어요"라고 상세히 알려주면, 공격자는 그 설명에서 비밀 정보를 꺼낸다.

---

## Ⅱ. 아키텍처 및 핵심 원리

Error-based 인젝션 주요 기법:

| DB 유형 | 기법 | 특징 |
|:---|:---|:---|
| MySQL | `extractvalue()`, `updatexml()` | XPath 에러에 데이터 포함 |
| MySQL | `floor(rand(0)*2)` GROUP BY | 중복 에러로 데이터 노출 |
| MS SQL | `CONVERT(int, user)` | 타입 변환 에러 |
| Oracle | `ctxsys.drithsx.sn()` | 패키지 에러 |
| PostgreSQL | CAST 에러 | 타입 오류 |

```
┌──────────────────────────────────────────────────────────┐
│           Error-based SQLi 데이터 추출 흐름              │
├──────────────────────────────────────────────────────────┤
│  공격자 입력: ' AND extractvalue(1,                      │
│             concat(0x7e,(SELECT pw FROM admin)))--       │
│       │                                                  │
│       ▼                                                  │
│  DB 에러 발생                                            │
│  에러 메시지: XPATH syntax error: '~P@ssw0rd!'           │
│       │                                                  │
│       ▼                                                  │
│  공격자: 에러 메시지에서 비밀번호 추출 성공              │
└──────────────────────────────────────────────────────────┘
```

📢 **섹션 요약 비유**: DB가 에러 설명서에 "이 비밀번호를 처리하다 실패함: P@ssw0rd!"라고 적어 돌려주는 것과 같다.

---

## Ⅲ. 비교 및 연결

| 구분 | Error-based | Boolean Blind | Time-based |
|:---|:---|:---|:---|
| 출력 필요 여부 | 에러 메시지 필요 | 참/거짓 차이 필요 | 응답 시간 차이 |
| 공격 속도 | 빠름 | 느림 | 가장 느림 |
| 방어 | 에러 메시지 숨김 | 응답 균일화 | SLEEP 함수 제한 |

📢 **섹션 요약 비유**: Error-based는 잡힌 사람이 실수로 비밀을 말하는 것이고, Blind는 침묵하는 사람에게서 예/아니오만 뽑아내는 것이다.

---

## Ⅳ. 실무 적용 및 기술사 판단

**핵심 방어책**:
1. **에러 메시지 일반화**: DB 에러를 "오류가 발생했습니다"로 교체, 스택 트레이스 숨김
2. **커스텀 에러 페이지**: 500 에러 전용 페이지 설정
3. **로깅 분리**: 에러 상세 정보는 서버 로그에만 기록
4. **파라미터화 쿼리**: 근본 해결책 — SQL 구조를 변경하는 입력 자체를 방지

```python
# 취약한 패턴 (에러 노출)
try:
    cursor.execute("SELECT * FROM users WHERE id=" + uid)
except Exception as e:
    return str(e)   # DB 에러 메시지 직접 반환

# 안전한 패턴 (에러 숨김 + 파라미터화)
try:
    cursor.execute("SELECT * FROM users WHERE id=%s", (uid,))
except Exception:
    return "처리 중 오류가 발생했습니다."
```

📢 **섹션 요약 비유**: 에러 메시지 숨김은 "지금 어떤 작업을 했다가 실패했는지"를 공격자에게 알려주지 않는 것이다.

---

## Ⅴ. 기대효과 및 결론

에러 메시지 일반화만으로 Error-based SQLi를 완전 차단할 수 있다. 그러나 이것은 공격을 "더 어렵게" 만드는 것이지, 취약점 자체를 제거하는 것이 아니다. 파라미터화 쿼리 적용이 근본 해결책이며, 에러 메시지 숨김은 그에 더해지는 심층 방어 레이어다.

📢 **섹션 요약 비유**: 에러 메시지 숨김은 비밀 창고 지도를 찢어버리는 것이다. 창고 자물쇠(파라미터화)는 여전히 필요하다.

---

### 📌 관련 개념 맵
| 개념 | 관계 | 설명 |
|:---|:---|:---|
| extractvalue() | 공격 함수 | MySQL XPath 에러 유발 |
| updatexml() | 공격 함수 | MySQL XML 에러 유발 |
| Custom Error Page | 방어 | DB 에러 노출 차단 |
| Parameterized Query | 근본 방어 | SQL 구조 변경 방지 |
| WAF | 보완 방어 | 알려진 패턴 차단 |

### 👶 어린이를 위한 3줄 비유 설명
- Error-based SQLi는 컴퓨터가 실수를 설명하다가 비밀을 말해버리는 것이야.
- "이 비밀번호를 처리하다 실패했어요: admin123"처럼 에러 메시지에 비밀이 새어나와.
- 그래서 에러가 나면 "오류가 발생했습니다"라고만 말하고 자세한 내용은 숨겨야 해!
""")

w("431_blind_sql_injection.md", 431, "431. Blind SQL Injection", """
## 핵심 인사이트 (3줄 요약)
> 1. **본질**: Blind SQL Injection (블라인드 SQL 인젝션)은 DB (Database) 쿼리 결과나 에러 메시지가 직접 반환되지 않는 환경에서, 참(True)과 거짓(False)에 따른 애플리케이션 응답 차이를 이용해 데이터를 한 비트씩 추출하는 공격이다.
> 2. **가치**: 에러 메시지와 쿼리 결과가 모두 숨겨져도 공격이 가능해, "출력이 없으면 안전하다"는 잘못된 인식을 깨트린다.
> 3. **판단 포인트**: Boolean-based Blind와 Time-based Blind가 대표적이며, 두 방식 모두 sqlmap으로 자동화되어 전체 DB를 추출할 수 있다.

---

## Ⅰ. 개요 및 필요성

많은 개발자가 "에러를 숨겼으니 SQL 인젝션은 막혔다"고 오해한다. 하지만 Blind SQLi는 에러 메시지 없이도 동작한다. 애플리케이션이 조건이 참일 때와 거짓일 때 다른 응답(내용, HTTP 상태 코드, 응답 시간)을 보여준다면 공격자는 이 차이를 이용해 데이터를 추출할 수 있다.

**Boolean-based Blind 원리**:
```sql
-- 첫 번째 문자가 'a'인지 확인
' AND SUBSTRING((SELECT password FROM admin LIMIT 1),1,1)='a'--

-- 참이면: 정상 페이지 반환
-- 거짓이면: 빈 페이지 또는 다른 내용 반환
```

공격자는 각 문자 위치마다 이진 탐색(Binary Search)으로 효율화하면 문자당 약 7회 요청으로 ASCII 값을 특정할 수 있다.

📢 **섹션 요약 비유**: Blind SQLi는 "이 글자가 맞아요?"라고 물어보며 천천히 비밀을 알아내는 것이다. 느리지만 결국 전체를 읽어낼 수 있다.

---

## Ⅱ. 아키텍처 및 핵심 원리

Boolean-based Blind SQLi 추출 과정:

| 단계 | 쿼리 | 결과 |
|:---|:---|:---|
| 1. 비밀번호 길이 확인 | `AND LENGTH(password)>5--` | 참이면 페이지 정상 |
| 2. 첫 글자 범위 좁히기 | `AND ASCII(SUBSTR(pw,1,1))>64--` | 64 이상이면 참 |
| 3. 정확한 ASCII 값 | `AND ASCII(SUBSTR(pw,1,1))=65--` | 65='A' → 참 |
| 4. 반복 | 각 문자마다 반복 | 전체 비밀번호 추출 |

```
┌──────────────────────────────────────────────────────────┐
│       Boolean-based Blind SQLi 이진 탐색                 │
├──────────────────────────────────────────────────────────┤
│  목표: password[1] 값 찾기 (ASCII 범위 32~127)           │
│                                                          │
│  1차: >79 → 참 (범위: 80~127)                           │
│  2차: >103 → 거짓 (범위: 80~103)                        │
│  3차: >91 → 참 (범위: 92~103)                           │
│  ...7번 반복 → 정확한 문자 특정                          │
└──────────────────────────────────────────────────────────┘
```

📢 **섹션 요약 비유**: Up/Down 게임에서 숫자를 맞추듯, 공격자는 각 문자의 ASCII 코드를 이진 탐색으로 찾아낸다.

---

## Ⅲ. 비교 및 연결

| 구분 | Boolean Blind | Time-based Blind |
|:---|:---|:---|
| 탐지 방식 | 응답 내용/크기 차이 | 응답 시간 차이 |
| 적용 환경 | 응답 차이가 있는 경우 | 완전히 동일한 응답 시 |
| 속도 | 느림 | 더 느림 |
| 서버 부하 | 낮음 | SLEEP으로 인한 부하 |

📢 **섹션 요약 비유**: Boolean은 문을 두드려 다른 소리로 구분하는 것이고, Time-based는 문이 열리는 시간이 얼마나 걸리는지로 구분하는 것이다.

---

## Ⅳ. 실무 적용 및 기술사 판단

**탐지 및 방어**:
1. **응답 균일화**: 참/거짓에 관계없이 동일한 응답 반환
2. **Rate Limiting**: 동일 IP에서 초당 요청 수 제한 (Blind SQLi는 대량 요청 필요)
3. **WAF 룰셋**: Boolean 조건문 패턴, SLEEP 함수 패턴 탐지
4. **파라미터화 쿼리**: 근본 해결책
5. **CAPTCHA**: 자동화 도구의 대량 요청 차단

📢 **섹션 요약 비유**: Rate Limiting은 "하루에 일정 횟수 이상 노크하는 사람은 들어올 수 없다"는 규칙이다. Blind SQLi는 수천 번 시도해야 하므로 이 규칙이 효과적이다.

---

## Ⅴ. 기대효과 및 결론

Blind SQLi의 근본적 방어는 파라미터화 쿼리다. 에러 숨김, 응답 균일화, Rate Limiting은 보조 방어 계층이다. 특히 sqlmap의 보편화로 Blind SQLi 공격 진입 장벽이 크게 낮아졌기 때문에, 파라미터화 쿼리 미적용 시 기술적 난이도와 관계없이 침해가 가능하다는 점을 명심해야 한다.

📢 **섹션 요약 비유**: Blind SQLi 방어는 "예/아니오"조차 말하지 않게 하는 것이다. 어떤 질문을 해도 같은 대답만 받는다면 공격자는 아무것도 얻을 수 없다.

---

### 📌 관련 개념 맵
| 개념 | 관계 | 설명 |
|:---|:---|:---|
| Boolean Blind | 기법 | 참/거짓 응답 차이 이용 |
| Time-based Blind | 기법 | SLEEP 함수 응답 시간 이용 |
| sqlmap | 자동화 도구 | Blind SQLi 자동 추출 |
| Rate Limiting | 보조 방어 | 대량 요청 차단 |
| Parameterized Query | 근본 방어 | SQL 구조 변경 방지 |

### 👶 어린이를 위한 3줄 비유 설명
- Blind SQLi는 답이 맞으면 초록불, 틀리면 빨간불이 켜지는 게임처럼 하나씩 맞춰가며 비밀을 알아내는 방법이야.
- 직접 답을 볼 수 없어도 반응의 차이만 있으면 결국 전체를 알아낼 수 있어.
- 그래서 어떤 질문을 해도 항상 같은 반응을 보이게 해야 안전해!
""")

print("Batch 428-431 done.")
