import os
TARGET_DIR = "/Users/pf/workspace/brainscience/content/studynote/10_ai/05_exam_summary"
files = {}

files["355_random_forest_feature_importance.md"] = '''+++
weight = 355
title = "355. 랜덤 포레스트 변수 중요도 (Feature Importance) - 불순도 감소 합산"
date = "2026-04-21"
[extra]
categories = "studynote-ai"
+++

## 핵심 인사이트 (3줄 요약)
> 1. **본질**: 랜덤 포레스트(Random Forest)의 변수 중요도(Feature Importance)는 각 특성(Feature)이 모든 트리에서 분할 시 불순도(Gini/Entropy)를 얼마나 감소시켰는지의 가중 평균으로, 예측에 가장 기여한 특성을 수치화한다.
> 2. **가치**: 블랙박스 앙상블 모델에서 어떤 변수가 중요한지 자동으로 파악할 수 있어, 특성 선택(Feature Selection)과 도메인 검증(Domain Validation)에 활용된다.
> 3. **판단 포인트**: MDI(Mean Decrease Impurity)는 연속형 변수나 고유값 많은 범주형 변수에 편향(Bias)되므로, 순열 중요도(Permutation Importance, MDA)를 함께 사용해야 신뢰성이 높다.

---

## Ⅰ. 개요 및 필요성

랜덤 포레스트는 수백~수천 개의 결정 트리를 배깅(Bagging)으로 앙상블한 모델이다. 각 트리가 훈련 데이터의 부트스트랩(Bootstrap) 샘플로 독립 학습하므로 단독 트리보다 분산이 낮다. 이 수백 개 트리에서 "어떤 특성이 분할에 가장 자주, 효과적으로 사용됐는가?"를 종합하면 데이터 구조를 드러내는 변수 중요도 지도(Feature Importance Map)가 완성된다.

📢 **섹션 요약 비유**: 랜덤 포레스트의 변수 중요도는 "500명의 심사위원(트리)이 매긴 평점 평균"이다. 500명이 각자 심사(분할)할 때 "이 기준(특성)을 쓰면 심사가 훨씬 쉬워졌다"고 투표한 횟수와 효과를 합산한 것이 중요도다.

---

## Ⅱ. 아키텍처 및 핵심 원리

```
┌──────────────────────────────────────────────────────────┐
│       MDI (Mean Decrease Impurity) 계산 과정             │
├──────────────────────────────────────────────────────────┤
│  for 각 트리 t in 랜덤 포레스트:                        │
│    for 각 분할 노드 v in 트리 t (특성 j 사용):          │
│      ΔI(v) = I(부모) - [Nₗ/N·I(왼쪽) + Nᵣ/N·I(오른쪽)]│
│      → 불순도 감소량 계산                               │
│                                                          │
│  특성 j의 중요도:                                       │
│  VI(j) = (1/|T|) · Σₜ Σᵥ(특성j 사용) ΔI(v) / N(v)     │
│                                                          │
│  정규화: VI(j) = VI(j) / Σⱼ VI(j)  (합이 1이 되도록)  │
└──────────────────────────────────────────────────────────┘
```

| 방법 | 계산 방식 | 장점 | 단점 |
|:---|:---|:---|:---|
| MDI (Mean Decrease Impurity) | 트리 훈련 중 불순도 감소 합산 | 빠름, 훈련 비용 없음 | 고유값 많은 변수 편향 |
| MDA (Mean Decrease Accuracy) | 변수 순열 후 OOB 성능 변화 | 공정, 상관 변수 처리 가능 | 느림, 계산 비용 큼 |
| SHAP | 샤플리 값 기반 기여도 | 개별 예측 설명 가능 | 매우 느림 |

📢 **섹션 요약 비유**: MDI는 "경기 중 기록한 어시스트 통계"이고 MDA(Permutation Importance)는 "그 선수가 없을 때 팀 성적이 얼마나 떨어지는지"다. 어시스트 숫자(MDI)는 빠르지만 왜곡될 수 있고, 없을 때 성적 하락(MDA)이 진짜 기여도다.

---

## Ⅲ. 비교 및 연결

OOB(Out-Of-Bag) 오차: 배깅에서 각 트리는 전체 데이터의 약 63.2%만 사용하므로, 나머지 36.8% (OOB 샘플)로 검증이 자동 수행된다. 교차 검증 없이 훈련과 동시에 검증 점수를 얻는 효율적 기법이다. 부스팅(Boosting) 계열(XGBoost, LightGBM)도 유사한 Feature Importance를 제공하지만, gain(불순도 감소), weight(분할 횟수), cover(샘플 커버) 등 더 다양한 방식을 지원한다.

📢 **섹션 요약 비유**: OOB 오차는 "일부 선수를 경기에 안 쓰고 관중석에서 보게 한 뒤 그 선수의 평가 점수로 활용"하는 것이다. 훈련에 안 쓴 선수(OOB 샘플)를 관찰자로 활용해 별도 검증 세트 없이 모델 성능을 평가한다.

---

## Ⅳ. 실무 적용 및 기술사 판단

변수 중요도 실무 사용 시 주의 사항: ① 높은 상관관계를 가진 두 변수(A, B)는 MDI 중요도를 나눠가져 둘 다 낮게 나올 수 있다. ② 범주형 변수의 더미 인코딩 방식(원핫 vs 레이블)이 중요도 계산에 영향을 준다. ③ 재현성(Reproducibility)을 위해 random_state 고정 필수. SHAP(SHapley Additive exPlanations) 값은 개별 예측에서 각 특성의 기여도를 섀플리 값으로 계산하여 가장 신뢰성 높은 설명을 제공한다.

📢 **섹션 요약 비유**: 상관 변수의 MDI 분산은 "한 팀에서 쌍둥이 선수의 어시스트 통계"다. 쌍둥이 선수 A, B가 서로 패스를 주고받으면 어시스트가 절반씩 나뉘어 둘 다 중간 정도로 나온다. 실제론 한 명만 있어도 충분하지만 MDI는 이를 구분 못한다.

---

## Ⅴ. 기대효과 및 결론

랜덤 포레스트 변수 중요도는 블랙박스 모델에서 해석가능성(XAI)을 얻는 가장 간단한 방법이다. 불필요한 특성을 제거해 모델 경량화, 데이터 수집 비용 절감, 도메인 전문가와의 소통에 활용된다. SHAP과 결합하면 전역 중요도(Global Importance)와 개별 예측 설명(Local Explanation) 모두 달성할 수 있다.

📢 **섹션 요약 비유**: 변수 중요도는 "팀 공헌도 점수표"다. 랜덤 포레스트라는 팀에서 500번의 경기(트리) 동안 어떤 선수(특성)가 팀 승리(불순도 감소)에 가장 많이 기여했는지 집계한 MVP 점수표다.

---

### 📌 관련 개념 맵
| 개념 | 연관 키워드 | 관계 |
|:---|:---|:---|
| 배깅 (Bagging) | 부트스트랩, 앙상블 | 랜덤 포레스트의 기반 |
| SHAP | 섀플리 값 | 가장 정교한 Feature Importance |
| OOB (Out-Of-Bag) | 검증 | 별도 검증 세트 없는 평가 |
| XGBoost / LightGBM | 부스팅 | 유사한 Feature Importance 제공 |

---

### 👶 어린이를 위한 3줄 비유 설명
1. 랜덤 포레스트는 500명의 전문가가 각자 데이터를 보고 투표하는 AI예요.
2. 변수 중요도는 500명이 "이 정보가 결정에 얼마나 도움됐나요?"라고 평가한 평균 점수예요.
3. 점수가 높은 특성이 AI의 예측에 가장 큰 영향을 미치는 핵심 정보예요!
'''

files["356_mahalanobis_distance.md"] = '''+++
weight = 356
title = "356. 마할라노비스 거리 (Mahalanobis Distance) - 공분산 고려 다차원 거리"
date = "2026-04-21"
[extra]
categories = "studynote-ai"
+++

## 핵심 인사이트 (3줄 요약)
> 1. **본질**: 마할라노비스 거리(Mahalanobis Distance)는 공분산 행렬(Covariance Matrix)의 역행렬로 데이터 분포의 상관관계와 스케일 차이를 보정한 뒤 계산하는 다차원 거리이며, 이상치(Outlier) 탐지와 다변량 분류의 기준 거리다.
> 2. **가치**: 키(cm)와 몸무게(kg)처럼 단위가 다르거나 상관관계가 있는 변수들 사이의 거리를 공정하게 측정해, 유클리드 거리(Euclidean Distance)가 놓치는 "통계적 비정상"을 정확히 탐지한다.
> 3. **판단 포인트**: D_M(x) = √((x-μ)ᵀ Σ⁻¹ (x-μ)) 수식에서 Σ⁻¹이 공분산을 정규화하며, 데이터가 다변량 정규 분포(Multivariate Normal Distribution)를 따를 때 D_M²은 카이제곱 분포(Chi-squared Distribution)를 따른다.

---

## Ⅰ. 개요 및 필요성

키 170cm, 몸무게 65kg인 사람이 "평균(키 170, 몸무게 70)"과 유클리드 거리로는 5 단위 차이다. 그런데 "키 145, 몸무게 70"인 사람도 유클리드 거리로 25 단위 차이다. 하지만 키 145cm는 통계적으로 매우 드문(이상치) 반면, 몸무게 65는 정상 범위다. 유클리드 거리는 단위와 분산 차이를 무시해 "이상함"을 제대로 측정 못한다. 마할라노비스 거리는 각 변수의 표준 편차로 정규화하고, 변수 간 상관관계를 제거(de-correlation)하여 통계적 이상 정도를 정확히 측정한다.

📢 **섹션 요약 비유**: 유클리드 거리는 "직선 줄자"고 마할라노비스 거리는 "통계적 이상함 측정기"다. 직선 줄자는 단위를 모르니 cm와 kg을 같이 더하는 실수를 한다. 마할라노비스는 "이 값이 전체 분포에서 얼마나 특이한가?"를 σ 단위로 측정한다.

---

## Ⅱ. 아키텍처 및 핵심 원리

```
┌──────────────────────────────────────────────────────────┐
│         마할라노비스 거리 계산 구조                       │
├──────────────────────────────────────────────────────────┤
│  수식:  D_M(x) = √((x-μ)ᵀ · Σ⁻¹ · (x-μ))             │
│                                                          │
│  단계:  1. 중심화: d = x - μ  (평균으로부터의 편차 벡터) │
│         2. Σ⁻¹ 계산: 공분산 행렬의 역행렬               │
│         3. 이차 형식: D²_M = dᵀ · Σ⁻¹ · d             │
│         4. 제곱근: D_M = √D²_M                          │
│                                                          │
│  Σ⁻¹의 역할:                                           │
│  ┌──────────────────────────────────┐                   │
│  │ 분산이 큰 방향 → 거리 축소       │                   │
│  │ 분산이 작은 방향 → 거리 확대      │                   │
│  │ 상관된 변수 → 독립 축으로 회전   │                   │
│  └──────────────────────────────────┘                   │
│  D_M² ~ χ²(p) when x ~ N(μ, Σ)  (카이제곱 분포)       │
└──────────────────────────────────────────────────────────┘
```

| 거리 | 수식 | 상관 보정 | 스케일 보정 |
|:---|:---|:---|:---|
| 유클리드 (Euclidean) | √Σ(xᵢ-μᵢ)² | ❌ | ❌ |
| 표준화 유클리드 | √Σ((xᵢ-μᵢ)/σᵢ)² | ❌ | ✅ |
| 마할라노비스 (Mahalanobis) | √(x-μ)ᵀΣ⁻¹(x-μ) | ✅ | ✅ |

📢 **섹션 요약 비유**: Σ⁻¹(역공분산 행렬)는 "왜곡된 공간 복원 렌즈"다. 변수들이 상관관계로 인해 타원형으로 분포할 때, 이 렌즈로 보면 타원이 원으로 변환되어 모든 방향의 거리가 공정하게 측정된다.

---

## Ⅲ. 비교 및 연결

이상치 탐지 시 D_M² > χ²(p, 0.975) (97.5% 신뢰구간)를 임계값으로 사용한다. 예를 들어 2차원 데이터에서 χ²(2, 0.975) ≈ 7.38이므로 D_M > 2.72인 점은 이상치로 분류한다. QDA(Quadratic Discriminant Analysis, 이차 판별 분석)는 마할라노비스 거리를 분류 결정 경계로 사용하는 확률적 분류기다.

📢 **섹션 요약 비유**: 마할라노비스 거리의 이상치 탐지는 "인체 건강 검진"이다. 키와 몸무게가 같이 변해야 하는 상관관계를 고려해, 키는 크고 몸무게가 극단적으로 낮다면 통계적 이상(이상치)으로 탐지한다.

---

## Ⅳ. 실무 적용 및 기술사 판단

금융 사기 탐지에서 거래 금액, 빈도, 위치 등 다변량 특성의 마할라노비스 거리를 실시간 계산해 임계값 초과 거래를 즉시 차단한다. Minimum Covariance Determinant(MCD) 추정으로 이상치에 강건(Robust)한 공분산 행렬을 사용하면 이상치가 많은 데이터에서도 안정적이다. 차원이 높을수록 Σ⁻¹ 계산의 수치적 불안정성이 증가하므로 PCA로 차원 축소 후 적용을 권장한다.

📢 **섹션 요약 비유**: MCD 추정은 "이상치를 제외하고 정상 데이터만으로 평균과 공분산을 추정"하는 방법이다. 사기 거래(이상치)가 섞인 데이터로 공분산을 추정하면 정상 패턴이 왜곡되므로, 정상치 중심의 MCD로 더 정확한 기준을 잡는다.

---

## Ⅴ. 기대효과 및 결론

마할라노비스 거리는 다변량 이상치 탐지, 패턴 인식, 클러스터 분류에서 유클리드 거리의 근본적 한계(단위 의존, 상관 무시)를 극복하는 통계적으로 우월한 거리 척도다. 기술사 시험에서 D_M 수식의 Σ⁻¹ 역할(스케일 정규화 + 탈상관)과 D_M² ~ χ² 분포 관계를 명확히 서술하면 높은 점수를 받을 수 있다.

📢 **섹션 요약 비유**: 마할라노비스 거리는 "문화권을 고려한 키 측정"이다. 평균 키가 190cm인 나라에서 180cm는 '작은 편'이지만, 평균 160cm인 나라에서 180cm는 '큰 편'이다. 마할라노비스는 각 데이터의 분포(공분산)를 기준으로 얼마나 특이한지를 측정한다.

---

### 📌 관련 개념 맵
| 개념 | 연관 키워드 | 관계 |
|:---|:---|:---|
| 공분산 행렬 (Covariance Matrix) | PCA | Σ⁻¹의 기반 |
| 이상치 탐지 (Anomaly Detection) | 금융 사기, 제조 불량 | 마할라노비스 거리 주요 응용 |
| QDA (Quadratic Discriminant Analysis) | 확률적 분류 | 마할라노비스 거리 기반 분류 |
| 카이제곱 분포 (Chi-squared) | 통계 검정 | D_M² 분포 |

---

### 👶 어린이를 위한 3줄 비유 설명
1. 유클리드 거리는 "직선 거리"고 마할라노비스 거리는 "이 점이 얼마나 특이한가"를 재는 특별한 자예요.
2. 키와 몸무게처럼 서로 관련된 숫자들의 상관관계까지 고려해서 거리를 측정해요.
3. 이 거리가 너무 크면 "이상한 점"(이상치)으로 찾아내는 데 사용할 수 있어요!
'''

files["357_dbscan.md"] = '''+++
weight = 357
title = "357. DBSCAN (Density-Based Spatial Clustering) - EPS·MinPts 밀도 군집화"
date = "2026-04-21"
[extra]
categories = "studynote-ai"
+++

## 핵심 인사이트 (3줄 요약)
> 1. **본질**: DBSCAN(Density-Based Spatial Clustering of Applications with Noise, 노이즈 포함 밀도 기반 공간 군집화)은 반경 ε 내에 최소 MinPts 이상의 점이 밀집한 영역을 군집으로 연결하는 밀도 기반 군집화 알고리즘으로, 군집 개수 K를 사전에 지정할 필요가 없다.
> 2. **가치**: 원형이 아닌 임의 형태의 군집(초승달, 도넛, 나선형)을 탐지하고, 어느 군집에도 속하지 않는 이상치(Noise Point)를 자동 분리하여 K-Means가 실패하는 복잡한 데이터 구조를 처리한다.
> 3. **판단 포인트**: ε(epsilon, 반경)와 MinPts(최소 점 수) 두 하이퍼파라미터가 군집 품질을 결정하며, k-NN 거리 그래프(k-distance graph)의 엘보우(elbow) 지점에서 적절한 ε를 추정한다.

---

## Ⅰ. 개요 및 필요성

위성 사진에서 도시 군집을 탐지하거나 소셜 미디어 게시물의 지역 이벤트를 군집화할 때, 도시 모양은 원형이 아닌 복잡한 형태다. K-Means는 원형 군집만 탐지할 수 있어 이런 상황에서 완전히 실패한다. DBSCAN은 "이 점의 주변 반경 ε 안에 점이 충분히 많으면(≥MinPts) 군집이다"라는 직관적 원리로 임의 형태 군집을 탐지하고, 주변에 점이 없는 고립된 이상치를 자동으로 -1(노이즈)로 표시한다.

📢 **섹션 요약 비유**: DBSCAN은 "친구 연결망 탐색기"다. 반경 ε 안에 친구(MinPts)가 충분히 있는 사람은 '핵심 멤버'이고, 핵심 멤버와 연결된 사람은 같은 그룹(군집)이 된다. 아무와도 연결되지 않은 고립된 사람은 이상치(Noise)로 표시된다.

---

## Ⅱ. 아키텍처 및 핵심 원리

```
┌──────────────────────────────────────────────────────────┐
│             DBSCAN 점 분류 규칙                           │
├──────────────────────────────────────────────────────────┤
│  입력 파라미터: ε (반경), MinPts (최소 이웃 수)          │
│                                                          │
│  ε-이웃 (ε-Neighborhood):                               │
│  N_ε(p) = {q | dist(p,q) ≤ ε}                          │
│                                                          │
│  점 분류:                                               │
│  ┌─────────────────────────────────────────────────┐    │
│  │ 핵심점 (Core Point):  |N_ε(p)| ≥ MinPts         │    │
│  │ 경계점 (Border Point): 핵심점의 이웃, 자신은 아님│    │
│  │ 노이즈 (Noise Point):  핵심점 이웃 아님          │    │
│  └─────────────────────────────────────────────────┘    │
│                                                          │
│  군집 형성: 핵심점 → 핵심점으로 밀도 연결(Density      │
│  Connected) 전파 → 같은 군집                           │
│  시간 복잡도: O(n log n) with spatial indexing         │
└──────────────────────────────────────────────────────────┘
```

| 알고리즘 | K 사전 지정 | 군집 형태 | 이상치 처리 | 계산 복잡도 |
|:---|:---|:---|:---|:---|
| K-Means | ✅ 필요 | 원형만 | ❌ 없음 | O(nKI) |
| DBSCAN | ❌ 불필요 | 임의 형태 | ✅ 자동 | O(n log n) |
| 계층적 군집화 | ❌ 불필요 | 임의 형태 | ❌ 없음 | O(n³) |

📢 **섹션 요약 비유**: DBSCAN 핵심점은 "인기인"이다. 반경 ε 안에 친구가 MinPts 명 이상인 사람이 핵심점이다. 이 핵심인들이 서로 연결되면 같은 그룹이 되고, 혼자 고립된 사람은 "이상한 사람(노이즈)"으로 분류된다.

---

## Ⅲ. 비교 및 연결

HDBSCAN(Hierarchical DBSCAN): DBSCAN의 단점인 단일 ε 값으로 밀도가 다른 군집을 처리 못하는 문제를 계층적 밀도 기반 접근으로 해결한다. 밀도가 높은 군집과 낮은 군집을 동시에 탐지할 수 있어 실무에서 DBSCAN보다 선호된다. OPTICS(Ordering Points To Identify the Clustering Structure)는 ε 없이 도달 거리(reachability distance)로 군집 구조를 시각화한다.

📢 **섹션 요약 비유**: HDBSCAN은 "여러 밀도의 도시를 동시에 탐지"한다. DBSCAN이 하나의 단일 밀도 기준으로만 보는 반면, HDBSCAN은 도심(고밀도)과 교외(저밀도)를 동시에 다른 기준으로 군집화한다.

---

## Ⅳ. 실무 적용 및 기술사 판단

ε 선택: 데이터의 k번째(k=MinPts) 최근접 이웃 거리를 정렬하여 k-distance 그래프를 그리고, 거리가 급격히 증가하는 엘보우(knee/elbow) 지점을 ε로 선택한다. MinPts 선택: 일반적으로 차원 d의 2배(MinPts ≥ 2d)를 권장한다. 지리 정보 데이터에서 Haversine 거리를 사용하면 위도/경도 기반 지역 군집화가 가능하다. 데이터 스케일링 필수(유클리드 거리 기반이므로).

📢 **섹션 요약 비유**: k-distance 그래프의 엘보우는 "물이 넘치기 직전의 수위"다. ε가 너무 작으면 모두 노이즈, 너무 크면 모두 하나의 군집이 된다. 엘보우 지점이 "딱 적당한 수위"다.

---

## Ⅴ. 기대효과 및 결론

DBSCAN은 군집 형태 제약 없음, 이상치 자동 탐지, K 사전 지정 불필요라는 3대 강점으로 지리정보, 이상치 탐지, 의료 이미지 분석에서 K-Means 대비 압도적 우위를 보인다. 단, 고차원 데이터(차원의 저주)와 밀도가 균일하지 않은 데이터에는 HDBSCAN이나 스펙트럼 군집화(Spectral Clustering)가 더 적합하다.

📢 **섹션 요약 비유**: DBSCAN은 군집화 세계의 "자유로운 탐험가"다. K-Means가 "원 모양으로만 찢어줘!"라고 명령받은 로봇이라면, DBSCAN은 "네가 알아서 지형 따라 자연스럽게 묶어봐!"라고 방목된 탐험가다. 복잡한 지형에서는 탐험가가 훨씬 유연하다.

---

### 📌 관련 개념 맵
| 개념 | 연관 키워드 | 관계 |
|:---|:---|:---|
| K-Means | 원형 군집 | DBSCAN이 극복하는 한계 |
| HDBSCAN | 계층적 밀도 | DBSCAN의 개선 버전 |
| 이상치 탐지 (Anomaly Detection) | 노이즈 포인트 | DBSCAN 핵심 부산물 |
| k-NN (k-Nearest Neighbors) | 거리 계산 | ε 선택을 위한 k-distance 사용 |

---

### 👶 어린이를 위한 3줄 비유 설명
1. DBSCAN은 "주변에 친구가 많은 사람을 리더로 정하는 그룹 만들기"예요.
2. 리더들이 서로 가까우면 같은 그룹, 아무 리더와도 가깝지 않으면 "외톨이(이상치)"로 표시돼요.
3. K-Means처럼 그룹 수를 미리 정하지 않아도 되니까 더 자유롭고 유연한 AI예요!
'''

files["358_hierarchical_clustering.md"] = '''+++
weight = 358
title = "358. 계층적 군집화 (Hierarchical Clustering) - 덴드로그램 분석"
date = "2026-04-21"
[extra]
categories = "studynote-ai"
+++

## 핵심 인사이트 (3줄 요약)
> 1. **본질**: 계층적 군집화(Hierarchical Clustering)는 데이터 포인트들을 단계적으로 병합(Agglomerative, 상향식) 또는 분할(Divisive, 하향식)하여 트리 형태의 덴드로그램(Dendrogram)을 생성하고, 이를 특정 높이에서 자르면 원하는 군집 수를 얻는 알고리즘이다.
> 2. **가치**: 군집 수 K를 사전에 정하지 않아도 되고, 덴드로그램으로 데이터의 전체 계층 구조를 시각화하여 도메인 전문가가 적절한 군집 수를 사후에 결정할 수 있다.
> 3. **판단 포인트**: 연결 기준(Linkage)으로 단순 연결(Single, 연쇄 효과), 완전 연결(Complete, 컴팩트), 평균 연결(Average, 균형), 워드 연결(Ward, 분산 최소화)이 있으며 Ward가 실무 기본값이다.

---

## Ⅰ. 개요 및 필요성

유전체 발현 데이터에서 유사한 발현 패턴을 가진 유전자 그룹을 찾거나, 회사 조직도처럼 계층적 관계가 있는 데이터를 군집화할 때 K-Means 같은 평면적 방법은 계층 구조를 표현하지 못한다. 계층적 군집화는 가장 유사한 두 포인트를 묶고, 다시 가장 가까운 두 군집을 묶는 과정을 반복하여 최종적으로 모든 데이터를 잇는 이진 트리(덴드로그램)를 만든다.

📢 **섹션 요약 비유**: 계층적 군집화는 "족보 만들기"다. 가장 비슷한 두 사람을 먼저 같은 가족으로 묶고, 가족들 중 가장 비슷한 두 가족을 씨족으로 묶고, 씨족들을 부족으로 묶는 과정을 반복해 전체 인류의 족보(덴드로그램)가 완성된다.

---

## Ⅱ. 아키텍처 및 핵심 원리

```
┌────────────────────────────────────────────────────────────┐
│  병합적 계층 군집화 (Agglomerative Hierarchical Clustering) │
├────────────────────────────────────────────────────────────┤
│  데이터: {A, B, C, D, E}                                   │
│                                                            │
│  Step 1: 거리 행렬 계산 후 가장 가까운 쌍 병합             │
│          {A,B} 병합 → 새 군집 AB                          │
│  Step 2: AB와 다른 군집 거리 재계산 (Linkage 적용)        │
│          {C,D} 병합                                        │
│  Step 3: {AB,CD} 병합                                     │
│  Step 4: {ABCD, E} 병합                                   │
│                                                            │
│  덴드로그램 (Dendrogram):                                  │
│      E ──────────────────────┐                            │
│      A ──┐                   │                            │
│      B ──┘──────┐            │                            │
│      C ──┐      ├────────────┘                            │
│      D ──┘──────┘                                         │
│  높이(거리)┌─────────────────────────                     │
│  자르면 K개의 군집 선택 가능!                              │
└────────────────────────────────────────────────────────────┘
```

| 연결 기준 | 군집 간 거리 정의 | 특성 | 문제점 |
|:---|:---|:---|:---|
| 단순 연결 (Single) | 최소 거리 | 비구형 탐지 가능 | 연쇄 효과(Chaining) |
| 완전 연결 (Complete) | 최대 거리 | 컴팩트 구형 군집 | 이상치 영향 큼 |
| 평균 연결 (Average) | 평균 거리 | 균형 잡힌 성능 | - |
| 워드 연결 (Ward) | 분산 증가 최소화 | 구형, 균일 크기 | 이상치 민감 |

📢 **섹션 요약 비유**: 연결 기준은 "두 도시 간 거리 측정 방식"이다. 단순 연결은 두 도시 중 가장 가까운 두 변두리 간 거리, 완전 연결은 가장 먼 두 끝점 간 거리, 워드는 합쳐지면 내부 분산이 얼마나 늘어나는지로 결정한다.

---

## Ⅲ. 비교 및 연결

코팬틱 상관 계수(Cophenetic Correlation Coefficient): 덴드로그램에서 각 쌍의 병합 높이와 실제 거리 행렬의 상관관계로 덴드로그램의 질을 평가(1에 가까울수록 좋음). 코팬틱 계수 > 0.75면 좋은 덴드로그램으로 간주한다. UPGMA(Unweighted Pair Group Method with Arithmetic Mean)는 평균 연결(Average Linkage)의 진화 생물학 버전으로 계통수(Phylogenetic Tree) 구성에 사용된다.

📢 **섹션 요약 비유**: 코팬틱 상관은 "지도의 정확도 점수"다. 실제 땅 거리(원래 데이터)와 지도에 그려진 거리(덴드로그램)가 얼마나 잘 맞는지 점수로 평가한다. 1점 만점에 0.75 이상이면 믿을만한 지도다.

---

## Ⅳ. 실무 적용 및 기술사 판단

계층적 군집화의 치명적 단점은 시간 복잡도 O(n² log n)~O(n³)다. 데이터가 10,000개를 넘으면 계산이 불가능해진다. 대규모 데이터에서는 Mini-batch K-Means로 초기 군집을 만든 뒤, 군집 중심(centroid)에 계층적 군집화를 적용하는 하이브리드 전략을 사용한다. scipy의 linkage/dendrogram 함수와 scikit-learn의 AgglomerativeClustering으로 구현 가능하다.

📢 **섹션 요약 비유**: 계층적 군집화를 대규모 데이터에 직접 쓰는 것은 "100만 명의 족보를 손으로 그리는 것"이다. 대신 먼저 K-Means로 100개 가문(군집)을 만든 뒤, 100개 가문 간 족보(계층 군집화)를 그리는 2단계 전략이 현실적이다.

---

## Ⅴ. 기대효과 및 결론

계층적 군집화는 데이터의 내재적 계층 구조를 시각적으로 탐색하는 데 탁월하다. 유전체학, 문서 분류, 마케팅 고객 세분화에서 K개를 몰라도 전체 구조를 파악한 후 적절한 수준에서 자를 수 있다는 유연성이 핵심 강점이다. 기술사 시험에서 4가지 연결 기준의 수식과 장단점, 코팬틱 상관으로 품질 평가하는 방법을 함께 서술하면 완성도 높은 답안이 된다.

📢 **섹션 요약 비유**: 덴드로그램은 AI의 "진화 계통수"다. 모든 데이터의 조상부터 개별 후손까지 전체 연결 관계가 한 눈에 보인다. 어느 높이에서 자르느냐에 따라 "과(科)" 수준의 큰 그룹이 될 수도, "종(種)" 수준의 세밀한 그룹이 될 수도 있다.

---

### 📌 관련 개념 맵
| 개념 | 연관 키워드 | 관계 |
|:---|:---|:---|
| 덴드로그램 (Dendrogram) | 트리 구조 시각화 | 계층적 군집화의 핵심 출력 |
| 워드 연결 (Ward Linkage) | 분산 최소화 | 실무 기본 연결 기준 |
| K-Means | 평면 군집화 | 계층 구조 표현 불가, 대비 비교 |
| 코팬틱 상관 | 덴드로그램 품질 | 군집 구조 신뢰도 평가 |

---

### 👶 어린이를 위한 3줄 비유 설명
1. 계층적 군집화는 "세상에서 가장 비슷한 친구 두 명을 먼저 짝지어주는 AI"예요.
2. 짝들을 계속 더 큰 그룹으로 묶어나가면 전체 연결 트리(덴드로그램)가 완성돼요.
3. 이 트리를 원하는 높이에서 자르면 "3개 그룹", "5개 그룹"처럼 원하는 만큼 군집을 만들 수 있어요!
'''

files["359_cosine_similarity_math.md"] = '''+++
weight = 359
title = "359. 코사인 유사도 (Cosine Similarity) - 텍스트 임베딩 방향성 유사도"
date = "2026-04-21"
[extra]
categories = "studynote-ai"
+++

## 핵심 인사이트 (3줄 요약)
> 1. **본질**: 코사인 유사도(Cosine Similarity)는 두 벡터 사이의 각도(θ)의 코사인 값으로 방향적 유사성을 측정하며, 벡터의 크기(magnitude)를 정규화하므로 문서 길이에 무관한 텍스트 유사도 측정에 최적화된 거리 척도다.
> 2. **가치**: "AI"와 "인공지능"처럼 의미는 같지만 하나는 2자, 다른 하나는 4자인 경우 TF-IDF 벡터의 L2 정규화 후 코사인 유사도를 계산하면 크기 차이를 무시하고 의미적 방향만 비교해 높은 유사도를 얻는다.
> 3. **판단 포인트**: cos(θ) = (A·B)/(|A||B|) = ΣAᵢBᵢ/√(ΣAᵢ²·ΣBᵢ²) 범위는 [-1, 1]이며, L2 정규화된 벡터 간의 내적(Dot Product)이 코사인 유사도와 동치다.

---

## Ⅰ. 개요 및 필요성

검색 엔진에서 "기계 학습"이라는 쿼리에 가장 유사한 문서를 찾을 때, 짧은 문서(100단어)와 긴 문서(1,000단어)를 유클리드 거리로 비교하면 긴 문서의 단어 빈도가 절대적으로 크므로 불공평하다. 코사인 유사도는 두 벡터의 방향만 비교하므로 문서 길이(크기)에 무관하게 의미적 유사도를 측정한다. Transformer 기반 모델의 임베딩 공간에서 의미론적 유사도 검색(Semantic Similarity Search)의 핵심 지표다.

📢 **섹션 요약 비유**: 코사인 유사도는 "나침반 방향 비교"다. 두 사람이 걷는 거리(벡터 크기)는 달라도, 같은 방향(북쪽)을 향하면 코사인 유사도 = 1(동일 방향)이다. AI는 문서의 "방향성(주제)"만 보고 유사도를 결정한다.

---

## Ⅱ. 아키텍처 및 핵심 원리

```
┌─────────────────────────────────────────────────────────┐
│           코사인 유사도 수식 및 기하학적 의미            │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  cos(θ) = (A·B) / (|A|·|B|)                           │
│         = ΣᵢAᵢBᵢ / √(ΣᵢAᵢ²) · √(ΣᵢBᵢ²)             │
│                                                         │
│  θ=0°  → cos=1.0  → 완전 동일 방향 (최고 유사)        │
│  θ=90° → cos=0.0  → 직교 (무관)                       │
│  θ=180°→ cos=-1.0 → 반대 방향 (최저 유사)             │
│                                                         │
│  L2 정규화 후 내적:                                    │
│  â = A/|A|,  b̂ = B/|B|   (단위 벡터)                  │
│  cos(θ) = â · b̂          (정규화된 내적)              │
│                                                         │
│  ANN 검색: FAISS/Milvus에서 코사인 거리 =1-cos(θ)     │
└─────────────────────────────────────────────────────────┘
```

| 유사도/거리 | 수식 | 크기 영향 | 주요 용도 |
|:---|:---|:---|:---|
| 코사인 유사도 | A·B/\|A\|\|B\| | ❌ 무관 | 텍스트, 임베딩 |
| 유클리드 거리 | √Σ(Aᵢ-Bᵢ)² | ✅ 영향 있음 | K-Means, KNN |
| 내적 (Dot Product) | Σ AᵢBᵢ | ✅ 영향 있음 | Attention Score |
| 맨해튼 거리 (L1) | Σ\|Aᵢ-Bᵢ\| | ✅ 영향 있음 | 희소 데이터 |

📢 **섹션 요약 비유**: L2 정규화 후 내적 = 코사인 유사도는 "모든 선수를 같은 체급(단위 벡터)으로 맞춘 뒤 방향 비교"다. 체급(크기)을 통일하면 내적만으로 방향 유사도를 공정하게 측정할 수 있다.

---

## Ⅲ. 비교 및 연결

PPMI(Positive Pointwise Mutual Information, 양의 점별 상호정보)와 코사인 유사도의 결합: TF-IDF 벡터에 PPMI 가중치를 적용하면 희귀 단어의 공동 등장 패턴을 더 잘 포착한다. 벡터 데이터베이스(Vector Database, Milvus, Pinecone, Weaviate)에서 ANN(Approximate Nearest Neighbor, 근사 최근접 이웃) 검색은 FAISS의 HNSW(Hierarchical Navigable Small World) 인덱스로 수십억 벡터에서 밀리초 내 코사인 유사도 검색을 지원한다.

📢 **섹션 요약 비유**: 벡터 DB에서 코사인 유사도 검색은 "도서관 사서 없는 초고속 도서 추천"이다. 수십억 권의 책(벡터) 중에서 "이 책과 방향이 비슷한 책 10권"을 밀리초 만에 찾아주는 FAISS가 ChatGPT의 RAG(Retrieval-Augmented Generation) 시스템 뒤에서 돌아간다.

---

## Ⅳ. 실무 적용 및 기술사 판단

추천 시스템에서 사용자 임베딩 벡터와 아이템 임베딩 벡터의 코사인 유사도 상위 K개를 추천한다(Item-based CF). Sentence-BERT(SBERT) 같은 문장 임베딩 모델은 코사인 유사도로 의미론적 문장 검색(Semantic Search)을 구현한다. 주의: 이진 특성 데이터(0/1 벡터)에서는 자카드 유사도(Jaccard Similarity)가 더 적합하고, 고차원 희소 데이터(TF-IDF)에서는 코사인이 적합하며 밀집 임베딩(BERT 출력)도 코사인이 적합하다.

📢 **섹션 요약 비유**: 코사인 유사도는 "유튜브 추천 알고리즘의 심장"이다. 내가 본 영상의 임베딩 벡터와 방향이 가장 비슷한 임베딩을 가진 영상 10개를 추천한다. 영상 길이(크기)가 달라도 주제 방향이 비슷하면 추천된다.

---

## Ⅴ. 기대효과 및 결론

코사인 유사도는 자연어 처리(NLP)와 정보 검색(IR) 분야의 표준 유사도 척도로, Transformer 임베딩 공간에서 의미론적 검색의 핵심 수식이다. L2 정규화 벡터에서 내적이 코사인과 동치라는 사실은 GPU에서 행렬 곱으로 수십억 쌍의 유사도를 효율적으로 계산할 수 있게 해준다.

📢 **섹션 요약 비유**: 코사인 유사도는 AI 세계의 "나침반 거리"다. 얼마나 멀리 걸어왔는지(벡터 크기)가 아니라, 같은 방향(주제/의미)을 향하고 있는지만 측정한다. ChatGPT의 RAG부터 유튜브 추천까지, 이 단순한 삼각함수 공식이 현대 AI 검색의 기반이다.

---

### 📌 관련 개념 맵
| 개념 | 연관 키워드 | 관계 |
|:---|:---|:---|
| TF-IDF | 텍스트 벡터화 | 코사인 유사도 입력 |
| 벡터 DB (Milvus, FAISS) | ANN 검색 | 코사인 유사도 대규모 검색 |
| SBERT | 문장 임베딩 | 코사인 기반 의미 검색 |
| RAG (Retrieval-Augmented Generation) | LLM 지식 검색 | 코사인 유사도로 관련 문서 검색 |

---

### 👶 어린이를 위한 3줄 비유 설명
1. 코사인 유사도는 "두 사람이 같은 방향을 바라보고 있는가"를 측정하는 거예요.
2. 얼마나 멀리 있는지(거리)가 아니라, 같은 곳을 향하고 있는지(방향)만 보는 게 핵심이에요.
3. 검색 AI가 "이 질문과 가장 관련 있는 문서"를 찾을 때 이 방법으로 수십억 개 중 1초 만에 찾아요!
'''

files["360_gmm_em_algorithm.md"] = '''+++
weight = 360
title = "360. GMM (Gaussian Mixture Model) 과 EM 알고리즘 - E-step/M-step"
date = "2026-04-21"
[extra]
categories = "studynote-ai"
+++

## 핵심 인사이트 (3줄 요약)
> 1. **본질**: GMM(Gaussian Mixture Model, 가우시안 혼합 모델)은 데이터가 K개의 가우시안 분포(정규 분포) 혼합으로 생성됐다고 가정하는 생성 모델이며, EM 알고리즘(Expectation-Maximization Algorithm, 기대값-최대화 알고리즘)으로 파라미터를 추정한다.
> 2. **가치**: K-Means처럼 하드 배정(0 또는 1)이 아닌 소프트 배정(soft assignment, 확률값)으로 각 포인트가 여러 군집에 속할 수 있어, 겹치는 군집과 타원형 군집을 자연스럽게 표현한다.
> 3. **판단 포인트**: E-step(소속 확률 계산)과 M-step(파라미터 μ, Σ, π 갱신)을 수렴까지 반복하며, BIC(Bayesian Information Criterion, 베이즈 정보 기준)나 AIC(Akaike Information Criterion, 아카이케 정보 기준)로 최적 K를 선택한다.

---

## Ⅰ. 개요 및 필요성

키 분포를 보면 남성(평균 175cm)과 여성(평균 162cm)이 섞여있어 이중 봉우리(bimodal) 분포를 보인다. 단일 가우시안으로는 이 분포를 표현 못한다. GMM은 "이 데이터는 2개의 가우시안 분포가 혼합된 것"으로 모델링하여, 남성 분포와 여성 분포를 동시에 추정한다. 각 데이터 포인트는 "80% 확률로 남성 분포, 20% 확률로 여성 분포"처럼 소프트하게 배정된다.

📢 **섹션 요약 비유**: GMM은 "두 종류의 물감이 섞인 그림"을 분리하는 AI다. 파란색과 노란색이 섞인 그림(데이터)을 보고, "이 부분은 70% 파랑, 30% 노랑"으로 분리 추정하는 것이 GMM이고, 이를 반복적으로 정확히 추정하는 과정이 EM 알고리즘이다.

---

## Ⅱ. 아키텍처 및 핵심 원리

```
┌──────────────────────────────────────────────────────────┐
│           GMM + EM 알고리즘 반복 구조                    │
├──────────────────────────────────────────────────────────┤
│  GMM 모델:  p(x) = Σₖ πₖ · N(x | μₖ, Σₖ)             │
│  πₖ: 혼합 가중치, Σπₖ=1                                │
│  μₖ: k번째 분포의 평균                                  │
│  Σₖ: k번째 분포의 공분산 행렬                           │
│                                                          │
│  E-step (Expectation):                                   │
│  γ(zₙₖ) = πₖN(xₙ|μₖ,Σₖ) / Σⱼπⱼ N(xₙ|μⱼ,Σⱼ)         │
│  → 각 포인트 xₙ이 군집 k에 속할 사후 확률(책임감)      │
│                                                          │
│  M-step (Maximization):                                  │
│  Nₖ = Σₙ γ(zₙₖ)                                       │
│  μₖ = (1/Nₖ) Σₙ γ(zₙₖ) xₙ                            │
│  Σₖ = (1/Nₖ) Σₙ γ(zₙₖ)(xₙ-μₖ)(xₙ-μₖ)ᵀ               │
│  πₖ = Nₖ/N                                              │
│                                                          │
│  반복: E→M→E→M→... 로그 우도 수렴까지                  │
└──────────────────────────────────────────────────────────┘
```

| 알고리즘 | 배정 방식 | 군집 형태 | 겹침 허용 |
|:---|:---|:---|:---|
| K-Means | 하드 (0/1) | 구형 | ❌ |
| GMM + EM | 소프트 (확률) | 타원형 | ✅ |

📢 **섹션 요약 비유**: EM 알고리즘의 E-step/M-step은 "닭이 먼저냐 달걀이 먼저냐" 문제를 반복으로 푸는 방법이다. 군집을 모르면 파라미터 못 추정(E-step), 파라미터 모르면 군집 못 결정(M-step). 둘을 번갈아 반복하면 결국 수렴한다.

---

## Ⅲ. 비교 및 연결

EM 알고리즘의 일반화: GMM은 EM의 한 응용이며, EM은 잠재 변수(Latent Variable)가 있는 모든 확률 모델에 적용 가능하다. HMM(Hidden Markov Model, 은닉 마르코프 모델) 학습(Baum-Welch 알고리즘)도 EM의 특수 케이스다. K를 자동 결정하는 방법: BIC = -2logL + k·log(n), AIC = -2logL + 2k (k: 파라미터 수). BIC가 낮은 K를 선택한다.

📢 **섹션 요약 비유**: BIC/AIC로 K 선택은 "모델 복잡도와 성능의 균형 점수"다. K가 크면 훈련 우도는 올라가지만 BIC에서 파라미터 수 패널티가 커진다. 패널티를 고려한 최적 K가 진짜 군집 수다.

---

## Ⅳ. 실무 적용 및 기술사 판단

금융 수익률 모델링에서 정상 시장(정규 분포)과 위기 시장(꼬리 두꺼운 분포)을 2-component GMM으로 모델링하면 리스크를 더 정확히 추정한다. 이상치 탐지: 낮은 사후 확률(모든 K에서 낮은 γ)을 가진 샘플 = 이상치. sklearn.mixture.GaussianMixture로 구현 가능. 초기화 방법: k-means++로 초기 μ를 설정하면 EM 수렴이 빠르다.

📢 **섹션 요약 비유**: GMM 이상치 탐지는 "어느 동호회에도 어울리지 않는 사람 찾기"다. 모든 가우시안 군집에서 소속 확률이 낮은 데이터 포인트가 이상치다. "어느 그룹 분위기에도 전혀 안 어울리는 사람"을 확률로 자동 식별한다.

---

## Ⅴ. 기대효과 및 결론

GMM은 K-Means보다 표현력이 높고 확률적 해석이 가능하다는 점에서 연속형 데이터의 밀도 추정, 이상치 탐지, 클러스터 겹침 허용이 필요한 모든 상황에 적합하다. EM 알고리즘의 이해는 HMM, VAE, 베이즈 네트워크 등 잠재 변수 모델 전반의 토대가 되어 AI 수학의 핵심 축이다.

📢 **섹션 요약 비유**: GMM은 K-Means의 "확률적 업그레이드"다. K-Means가 "어느 팀이냐 딱 결정해!"라면 GMM은 "너는 팀A에 70%, 팀B에 30% 소속이야"라고 유연하게 답한다. 현실 데이터는 경계가 모호하므로 GMM이 더 자연스럽다.

---

### 📌 관련 개념 맵
| 개념 | 연관 키워드 | 관계 |
|:---|:---|:---|
| K-Means | 하드 군집화 | GMM의 특수 케이스 (Σ=σ²I) |
| HMM (Hidden Markov Model) | 시계열 | EM 알고리즘의 다른 응용 |
| BIC / AIC | 모델 선택 | 최적 K 결정 기준 |
| VAE (Variational Autoencoder) | 생성 모델 | 잠재 변수 모델 패밀리 |

---

### 👶 어린이를 위한 3줄 비유 설명
1. GMM은 "여러 종류의 콩이 섞인 콩 주머니"를 각 콩 종류(가우시안)로 분리하는 AI예요.
2. "이 콩은 검은콩일 확률 80%, 완두콩일 확률 20%"처럼 소프트하게 분류해요.
3. E-step에서 확률 계산, M-step에서 각 콩 종류의 평균/모양 업데이트를 반복하면 완벽히 분리돼요!
'''

files["361_multicollinearity_vif.md"] = '''+++
weight = 361
title = "361. 다중 공선성 (Multicollinearity) 과 VIF (Variance Inflation Factor)"
date = "2026-04-21"
[extra]
categories = "studynote-ai"
+++

## 핵심 인사이트 (3줄 요약)
> 1. **본질**: 다중 공선성(Multicollinearity)은 회귀 모델에서 독립 변수들 간에 강한 선형 관계가 존재할 때 발생하는 현상으로, 계수 추정이 불안정해지고 해석이 불가능해진다. VIF(Variance Inflation Factor, 분산 팽창 지수)는 이를 수치화한 진단 도구다.
> 2. **가치**: VIF > 10인 변수는 다중 공선성이 심각하다고 판단하여 Ridge 정규화(L2), PCA 차원 축소, 또는 변수 제거로 대응한다. 트리 계열 모델(랜덤 포레스트, XGBoost)은 분할 기반이라 다중 공선성에 면역이다.
> 3. **판단 포인트**: VIF_j = 1/(1-R²_j) (j번째 변수를 나머지 변수로 회귀한 R²). VIF=1이면 무관, VIF=10이면 분산이 10배 팽창(표준 오차 √10 ≈ 3.16배 증가).

---

## Ⅰ. 개요 및 필요성

주택 가격 예측 모델에서 "방 개수"와 "거실 면적"이 모두 특성으로 들어갔을 때 이 둘은 강한 상관관계를 가진다. 회귀 계수를 추정할 때 "(방 개수 효과) + (거실 면적 효과)"를 분리하기 어려워, 작은 데이터 변동에도 계수가 크게 요동친다. 이것이 다중 공선성이다. 해석 관점에서 "방 개수를 1개 늘리면 가격이 500만원 오른다"는 결론을 내릴 수 없게 된다.

📢 **섹션 요약 비유**: 다중 공선성은 "두 탐정이 서로 같은 증거만 제출하는 상황"이다. 탐정 A(방 개수)와 탐정 B(거실 면적)가 거의 같은 증거(상관관계)를 제출하면, 판사(회귀 모델)가 "둘 중 누가 진짜 결정적 증인인가?"를 판단하지 못하고 계수가 불안정해진다.

---

## Ⅱ. 아키텍처 및 핵심 원리

```
┌──────────────────────────────────────────────────────────┐
│         VIF (Variance Inflation Factor) 계산             │
├──────────────────────────────────────────────────────────┤
│  VIF_j = 1 / (1 - R²_j)                                │
│                                                          │
│  R²_j: 특성 j를 나머지 특성들로 선형 회귀한 결정 계수   │
│                                                          │
│  해석:                                                   │
│  VIF = 1.0  → 다중 공선성 없음 (완전 독립)             │
│  VIF = 5.0  → 주의 필요 (R² = 0.80)                   │
│  VIF = 10.0 → 심각한 다중 공선성 (R² = 0.90)          │
│  VIF = ∞   → 완전 공선성 (R² = 1.00)                  │
│                                                          │
│  분산 팽창 의미:                                        │
│  Var(β̂_j) = VIF_j · σ²/(n·Var(Xj))                   │
│  → VIF가 크면 계수 분산 ↑ → 표준 오차 ↑ → t값 ↓      │
│  → 통계적 유의성 상실                                   │
└──────────────────────────────────────────────────────────┘
```

| VIF 값 | 공선성 정도 | 대응 방법 |
|:---|:---|:---|
| 1 ~ 5 | 없음~약함 | 문제 없음 |
| 5 ~ 10 | 보통 | 모니터링 필요 |
| > 10 | 심각 | Ridge/PCA/변수 제거 |
| > 100 | 극심 | 즉시 조치 |

📢 **섹션 요약 비유**: VIF는 "공선성 온도계"다. 온도(VIF)가 1°C면 건강(무관), 37°C면 미열(주의), 40°C면 고열(위험)이다. 열이 나는 원인(공선성 변수)을 찾아 약(Ridge/변수 제거)을 처방한다.

---

## Ⅲ. 비교 및 연결

Ridge 회귀(L2 정규화): 비용 함수에 ||β||² 페널티를 추가하면 (XᵀX + λI)를 역행렬로 사용해 완전 공선성(XᵀX 특이 행렬)에서도 안정적으로 역행렬 계산이 가능하다. 트리 기반 모델(결정 트리, 랜덤 포레스트, XGBoost)은 특성을 독립적으로 분할하므로 다중 공선성의 영향을 받지 않는다. 단, 변수 중요도 해석에서 상관 변수들이 중요도를 나눠 가지는 문제는 여전히 존재한다.

📢 **섹션 요약 비유**: Ridge vs 트리 모델의 공선성 대응: Ridge는 "공선성 탐정들을 공평하게 처리"하는 반면, 트리는 "증인 한 명만 채택해 판결"하므로 공선성 자체가 문제되지 않는다. 하지만 어느 탐정이 더 중요한지 해석은 여전히 어렵다.

---

## Ⅳ. 실무 적용 및 기술사 판단

공선성 탐지 체크리스트: ① VIF 계산 → ② 상관 행렬(Correlation Matrix) 히트맵 시각화 → ③ 조건 지수(Condition Index) > 30 체크. 대응 전략 선택: 해석가능성 중요 → Ridge + 변수 제거, 예측 성능 중요 → PCA 차원 축소 + Ridge, 블랙박스 허용 → 트리 기반 모델. 표준화(Standardization) 후 VIF 계산이 수치 안정성을 높인다.

📢 **섹션 요약 비유**: 공선성 대응 전략은 "겹치는 과목 정리"와 같다. 수학과 물리가 90% 겹친다면(공선성) ① 물리 제거(변수 제거), ② 두 과목 평균 점수 사용(PCA), ③ Ridge로 둘 다 약하게 반영, 중 하나를 선택하는 것이다.

---

## Ⅴ. 기대효과 및 결론

다중 공선성 진단과 VIF는 통계 모델의 신뢰성을 보장하는 핵심 전처리 단계다. 기업 의사결정 지원 모델에서 계수 해석의 오류를 방지하고, 규제 환경(금융 모델 감사)에서 모델의 안정성을 입증하는 데 필수적이다. 기술사 시험에서 VIF 수식, VIF>10 기준, Ridge와의 연결을 명확히 서술하면 고득점이다.

📢 **섹션 요약 비유**: VIF 검사는 AI 모델의 "건강 검진"이다. 회귀 모델을 의사에게 데리고 가서 "계수들 사이에 너무 비슷한 게 있어서 판단이 흔들리지 않나요?"를 VIF로 체크받는 과정이다. 고열(VIF>10)이 발견되면 약(Ridge/PCA)을 처방해 건강한 모델을 만든다.

---

### 📌 관련 개념 맵
| 개념 | 연관 키워드 | 관계 |
|:---|:---|:---|
| Ridge 회귀 (L2 정규화) | λI 추가 | 공선성 해결의 표준 처방 |
| PCA (Principal Component Analysis) | 차원 축소 | 공선성 제거 + 차원 축소 |
| OLS (Ordinary Least Squares) | 선형 회귀 | 공선성에 가장 취약한 방법 |
| 트리 기반 모델 (Tree-based Models) | 랜덤 포레스트 | 공선성 면역 |

---

### 👶 어린이를 위한 3줄 비유 설명
1. 다중 공선성은 "두 친구가 항상 붙어다니는 상황"이에요. AI가 "A가 문제야? B가 문제야?"를 구분 못해요.
2. VIF는 "얼마나 붙어다니는지" 측정하는 온도계예요. VIF > 10이면 너무 붙어다녀 문제가 생겨요.
3. Ridge 정규화나 PCA로 이 문제를 해결하거나, 트리 AI처럼 애초에 이 문제가 없는 방법을 쓰면 돼요!
'''

for filename, content in files.items():
    filepath = os.path.join(TARGET_DIR, filename)
    with open(filepath, 'w', encoding='utf-8') as f:
        f.write(content)
    print(f"Written: {filename}")

print("Done batch 1: 355-361")
