{
  "name": "studynote",
  "scripts": {
    "build": "zola build && npx pagefind --site public --glob \"**/*.html\" --force-language ko",
    "serve": "zola serve",
    "check": "bash scripts/check.sh"
  },
  "devDependencies": {
    "pagefind": "^1.4.0"
  }
}