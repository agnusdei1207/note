import os
OUT = "/Users/pf/workspace/brainscience/content/studynote/09_security/05_web_app_security/"

def w(fn, weight, title, content):
    path = os.path.join(OUT, fn)
    if os.path.exists(path):
        print(f"SKIP: {fn}")
        return
    with open(path, "w") as f:
        f.write(f'+++\nweight = {weight}\ntitle = "{title}"\ndate = "2026-04-21"\n[extra]\ncategories = "studynote-security"\n+++\n\n')
        f.write(content.strip() + "\n")
    print(f"CREATED: {fn}")

w("448_authentication_failures.md", 448, "448. A07 인증 실패", """
## 핵심 인사이트 (3줄 요약)
> 1. **본질**: A07 Identification and Authentication Failures (인증 실패)는 OWASP 2021에서 3위에서 7위로 하락했지만 여전히 만연하며, 크리덴셜 스터핑(Credential Stuffing), 브루트 포스(Brute Force), 약한 세션 관리, 취약한 비밀번호 저장이 핵심 문제다.
> 2. **가치**: HaveIBeenPwned 데이터베이스에는 100억 개 이상의 유출 계정이 있으며, 크리덴셜 스터핑 자동화 도구로 수분 내에 수천 개 계정에 대입 공격이 가능하다.
> 3. **판단 포인트**: MFA (Multi-Factor Authentication) 강제 적용만으로 계정 탈취 공격의 99.9%를 차단할 수 있으며, 비밀번호 저장은 반드시 bcrypt·Argon2 등 느린 해시를 사용해야 한다.

---

## Ⅰ. 개요 및 필요성

인증 실패 취약점은 공격자가 다른 사용자의 계정 권한으로 시스템에 접근하게 만드는 모든 약점을 포함한다. 인증(Authentication, "누구인가")과 인가(Authorization, "무엇을 할 수 있는가")를 구분하는 것이 이해의 출발점이다.

**주요 인증 실패 패턴**:
1. **크리덴셜 스터핑**: 다른 사이트 유출 DB 재사용 공격
2. **브루트 포스**: 가능한 모든 비밀번호 자동 시도
3. **비밀번호 스프레잉**: 흔한 비밀번호를 많은 계정에 시도
4. **취약한 비밀번호 정책**: 짧거나 단순한 비밀번호 허용
5. **취약한 비밀번호 저장**: MD5, SHA-1 평문 해시 사용
6. **취약한 세션 관리**: 예측 가능한 세션 ID, 긴 세션 만료
7. **안전하지 않은 비밀번호 재설정**: 추측 가능한 보안 질문

📢 **섹션 요약 비유**: 인증 실패는 은행 금고에 자물쇠는 있지만, 비밀번호가 "1234"이고 틀려도 횟수 제한 없이 시도할 수 있는 상태다.

---

## Ⅱ. 아키텍처 및 핵심 원리

인증 방어 다계층 구조:

| 방어 계층 | 기술 | 차단 대상 |
|:---|:---|:---|
| 1차: MFA | TOTP, FIDO2, 푸시 알림 | 크리덴셜 스터핑, 브루트 포스 |
| 2차: Rate Limiting | Account Lockout, CAPTCHA | 자동화 공격 |
| 3차: 비밀번호 정책 | 최소 12자, 복잡도, 유출 비밀번호 차단 | 취약 비밀번호 |
| 4차: 안전한 해시 | bcrypt(cost=12), Argon2id | 해시 크래킹 |
| 5차: 세션 보안 | 재발급, HttpOnly, SameSite | 세션 하이재킹 |

```
┌──────────────────────────────────────────────────────────┐
│           크리덴셜 스터핑 공격 vs 방어                   │
├──────────────────────────────────────────────────────────┤
│  공격:                                                   │
│  유출 DB → 자동화 도구 → 대규모 로그인 시도 → 성공       │
│                                                          │
│  방어:                                                   │
│  1. Rate Limiting: 5회 실패 → 30분 잠금                  │
│  2. MFA: 비밀번호 맞아도 OTP 추가 요구                   │
│  3. 유출 비밀번호 탐지: HaveIBeenPwned API 연동           │
└──────────────────────────────────────────────────────────┘
```

📢 **섹션 요약 비유**: MFA는 자동 열쇠 복사기로 집 열쇠를 복사해도, 지문 인식이 없으면 들어올 수 없게 만드는 것이다.

---

## Ⅲ. 비교 및 연결

인증(A07)과 접근 제어(A01)의 관계:

| 구분 | A07 인증 실패 | A01 접근 제어 실패 |
|:---|:---|:---|
| 핵심 질문 | "당신이 누구인가?" | "무엇을 할 수 있는가?" |
| 공격 예시 | 크리덴셜 스터핑 | IDOR, 권한 초과 API 접근 |
| 방어 | MFA, 강한 비밀번호, 세션 관리 | RBAC, 서버 측 권한 검증 |
| 연계 | 인증 성공이 인가의 전제 | 인증 후 인가가 적용됨 |

📢 **섹션 요약 비유**: 인증은 입장 티켓 확인이고, 인가는 좌석 등급 확인이다. 티켓이 위조됐으면(인증 실패) 좌석 등급 확인(인가)은 의미가 없다.

---

## Ⅳ. 실무 적용 및 기술사 판단

**NIST SP 800-63B 권장사항**:
1. **비밀번호 최소 길이**: 8자 이상 (사용자 선택), 최대 64자 이상 허용
2. **보안 질문 폐기**: 비밀번호 재설정에 보안 질문 사용 금지
3. **유출 비밀번호 차단**: HaveIBeenPwned API 또는 로컬 유출 비밀번호 DB와 비교
4. **정기 비밀번호 변경 폐기**: 정기 강제 변경은 취약한 비밀번호 패턴 유발 → 비추천
5. **SMS OTP 주의**: SIM 스와핑 공격에 취약, TOTP 또는 FIDO2 권장

📢 **섹션 요약 비유**: NIST는 "비밀번호를 자주 바꾸게 강요하면 오히려 'Summer2024!'처럼 패턴 있는 비밀번호를 쓰게 된다"는 연구를 반영해 정기 변경 폐기를 권고했다.

---

## Ⅴ. 기대효과 및 결론

MFA 적용, Rate Limiting, 안전한 비밀번호 저장을 조합하면 실용적인 인증 방어 체계가 완성된다. 특히 FIDO2/Passkey 기반 패스워드리스 인증으로 전환하면 비밀번호 자체를 없애 크리덴셜 스터핑을 구조적으로 차단할 수 있다.

기술사 관점에서 인증 실패는 제로 트러스트 아키텍처에서 "항상 인증, 항상 검증" 원칙으로 발전하며, 단일 인증 지점(Single Point of Authentication)을 넘어 지속적 인증(Continuous Authentication)으로 진화하고 있다.

📢 **섹션 요약 비유**: 패스워드리스 인증은 집 열쇠를 없애고 지문과 얼굴만으로 문을 여는 것이다. 열쇠를 훔칠 수 없으니 열쇠 도난 문제가 근본적으로 사라진다.

---

### 📌 관련 개념 맵
| 개념 | 관계 | 설명 |
|:---|:---|:---|
| MFA | 핵심 방어 | 다중 인증 요소 |
| Credential Stuffing | 공격 유형 | 유출 계정 재사용 |
| bcrypt/Argon2 | 비밀번호 저장 | 느린 해시 함수 |
| FIDO2/Passkey | 진화 방향 | 비밀번호 없는 인증 |
| NIST SP 800-63B | 표준 | 인증 가이드라인 |

### 👶 어린이를 위한 3줄 비유 설명
- 인증 실패는 집 비밀번호가 너무 쉽거나, 여러 번 틀려도 잠기지 않아서 누군가 들어오는 것이야.
- MFA는 비밀번호를 맞혀도 핸드폰으로 한 번 더 확인하게 만들어, 훨씬 안전해.
- 비밀번호는 절대 간단한 해시(MD5)로 저장하면 안 되고, 아주 느린 해시(bcrypt)로 저장해야 해!
""")

w("449_session_management_failure.md", 449, "449. 세션 관리 실패", """
## 핵심 인사이트 (3줄 요약)
> 1. **본질**: 세션 관리 실패 (Session Management Failure)는 사용자 인증 상태를 추적하는 세션 토큰의 생성·전송·저장·만료 과정에서 보안 결함으로 공격자가 정당한 세션을 탈취해 인증을 우회하는 취약점이다.
> 2. **가치**: 세션 하이재킹(Session Hijacking), 세션 고정(Session Fixation), 크로스 사이트 요청 위조(CSRF, Cross-Site Request Forgery)의 근본 원인이 세션 관리 실패에 있으며, 이는 A07 인증 실패와 밀접하게 연관된다.
> 3. **판단 포인트**: 128비트 이상 랜덤 세션 ID, 로그인 시 재발급, `HttpOnly·Secure·SameSite=Strict` 쿠키 속성, 절대 URL 파라미터 전달 금지가 4대 필수 통제다.

---

## Ⅰ. 개요 및 필요성

HTTP는 Stateless 프로토콜이다. 사용자 로그인 상태를 유지하려면 서버가 세션을 생성하고, 클라이언트가 매 요청마다 세션 식별자를 전달해야 한다. 이 과정에서 세션 ID가 탈취·위조·예측되면 공격자가 피해자의 인증 상태를 사용할 수 있다.

**주요 세션 공격 유형**:
1. **세션 하이재킹**: 네트워크 스니핑, XSS로 세션 쿠키 탈취
2. **세션 고정 (Session Fixation)**: 공격자가 미리 생성한 세션 ID로 피해자가 로그인하도록 유도
3. **세션 예측**: 짧거나 순차적인 세션 ID 추측
4. **세션 만료 없음**: 오래된 세션이 영구 유효
5. **URL 세션 ID 노출**: 세션 ID가 URL에 포함되어 Referer 헤더로 유출

📢 **섹션 요약 비유**: 세션 관리 실패는 호텔 방 열쇠 카드를 분실했는데 프론트에서 복제해서 낯선 사람이 쓸 수 있게 두는 것이다.

---

## Ⅱ. 아키텍처 및 핵심 원리

안전한 세션 관리 원칙:

| 항목 | 취약한 방식 | 안전한 방식 |
|:---|:---|:---|
| 세션 ID 생성 | 순차적, 예측 가능 | 128비트 이상 CSPRNG |
| 세션 ID 위치 | URL 파라미터 | HttpOnly 쿠키만 |
| 로그인 후 처리 | 기존 세션 ID 유지 | 새 세션 ID 재발급 |
| 세션 만료 | 없음 또는 매우 김 | 비활성 15~30분, 절대 만료 |
| 전송 | HTTP 평문 | HTTPS + Secure 속성 |
| CSRF 방어 | 없음 | SameSite=Strict + CSRF 토큰 |

```
┌──────────────────────────────────────────────────────────┐
│           세션 고정 (Session Fixation) 공격 흐름         │
├──────────────────────────────────────────────────────────┤
│  1. 공격자가 서버에서 세션 ID 획득: SID=ABC123           │
│  2. 피해자에게 URL 전송: /login?SID=ABC123               │
│  3. 피해자가 로그인 (서버가 SID 재발급 안 함)            │
│  4. 피해자 세션: SID=ABC123 (공격자도 동일)              │
│  5. 공격자가 SID=ABC123으로 피해자 계정 접근             │
└──────────────────────────────────────────────────────────┘
```

📢 **섹션 요약 비유**: 세션 고정은 공격자가 호텔 키 카드를 미리 만들어 고객에게 주고, 고객이 체크인하면 그 키 카드가 작동하도록 만드는 것이다.

---

## Ⅲ. 비교 및 연결

| 구분 | 세션 기반 인증 | JWT (JSON Web Token) 기반 |
|:---|:---|:---|
| 저장 위치 | 서버 (Session Store) | 클라이언트 (localStorage/cookie) |
| 취소 | 즉시 가능 (세션 삭제) | 만료 전 취소 어려움 |
| 확장성 | 세션 공유 필요 (Redis) | Stateless (확장 쉬움) |
| 보안 리스크 | 세션 하이재킹 | JWT 탈취, alg none 공격 |

📢 **섹션 요약 비유**: 세션은 호텔 프론트가 관리하는 방 열쇠이고, JWT는 고객이 보관하는 암호화된 계약서다. 둘 다 분실하면 문제가 된다.

---

## Ⅳ. 실무 적용 및 기술사 판단

**쿠키 보안 속성 설정**:
```http
Set-Cookie: sessionid=abc123xyz;
            HttpOnly;
            Secure;
            SameSite=Strict;
            Path=/;
            Max-Age=1800
```

- **HttpOnly**: JavaScript에서 쿠키 접근 불가 → XSS로 탈취 방지
- **Secure**: HTTPS에서만 전송 → 평문 전송 방지
- **SameSite=Strict**: 교차 사이트 요청에 쿠키 전송 차단 → CSRF 방지
- **Max-Age=1800**: 30분 후 자동 만료

**로그인 후 세션 재발급 (Java Spring)**:
```java
request.getSession().invalidate();
request.getSession(true); // 새 세션 생성
```

📢 **섹션 요약 비유**: HttpOnly는 비밀 서랍에 열쇠를 잠가두는 것이고, Secure는 열쇠를 안전한 봉투에만 배달하는 것이다.

---

## Ⅴ. 기대효과 및 결론

안전한 세션 관리 구현은 세션 하이재킹, 세션 고정, CSRF를 동시에 방어할 수 있다. 특히 현대 프레임워크(Spring Security, Django, Express-session)는 안전한 세션 관리를 기본으로 제공하므로, 이를 올바르게 설정하고 사용하는 것이 핵심이다.

기술사 관점에서 세션 관리는 Zero Trust 아키텍처에서 "지속적 인증(Continuous Authentication)"으로 발전한다. 정적 세션 대신 매 요청마다 컨텍스트 기반 신뢰 재평가가 이루어지는 방향으로 진화하고 있다.

📢 **섹션 요약 비유**: 좋은 세션 관리는 호텔이 체크인마다 새 카드를 발급하고, 일정 시간 후 자동으로 비활성화하며, 카드 정보를 누구도 훔쳐볼 수 없게 암호화하는 것이다.

---

### 📌 관련 개념 맵
| 개념 | 관계 | 설명 |
|:---|:---|:---|
| Session Fixation | 공격 유형 | 고정 세션 ID 강제 |
| Session Hijacking | 공격 유형 | 세션 쿠키 탈취 |
| CSRF | 연관 공격 | 세션 기반 상태 변경 공격 |
| HttpOnly | 방어 속성 | JS 쿠키 접근 차단 |
| SameSite=Strict | 방어 속성 | 교차 사이트 쿠키 전송 차단 |

### 👶 어린이를 위한 3줄 비유 설명
- 세션 관리 실패는 놀이공원 입장 팔찌가 누구나 복사할 수 있거나, 잃어버려도 아무 조치가 없는 것이야.
- 공격자가 팔찌(세션 쿠키)를 훔치면 그 사람으로 위장해 모든 서비스를 이용할 수 있어.
- 그래서 팔찌는 암호화하고, 일정 시간 후 자동으로 무효화하고, 로그인할 때마다 새 팔찌를 줘야 해!
""")

w("450_brute_force.md", 450, "450. 브루트 포스", """
## 핵심 인사이트 (3줄 요약)
> 1. **본질**: 브루트 포스 (Brute Force, 무차별 대입 공격)는 가능한 모든 비밀번호·PIN·암호화 키 조합을 순차적으로 시도해 정답을 찾아내는 공격이며, 클라우드 기반 GPU 크래킹으로 짧고 단순한 비밀번호는 수초 내에 해독된다.
> 2. **가치**: MD5·SHA-1로 저장된 비밀번호는 레인보우 테이블 + GPU 가속으로 8자 이하는 수초~수분 내 해독 가능하며, 이것이 bcrypt·Argon2 같은 느린 해시 함수가 표준이 된 이유다.
> 3. **판단 포인트**: 계정 잠금(Account Lockout), Rate Limiting, CAPTCHA, MFA (Multi-Factor Authentication), 느린 해시 함수가 브루트 포스 방어의 5대 핵심 통제다.

---

## Ⅰ. 개요 및 필요성

브루트 포스는 가장 단순한 공격이지만, 현대 하드웨어(GPU, 클라우드)와 자동화 도구의 발전으로 여전히 매우 효과적이다. 공격 유형에 따라 대상과 방법이 달라진다.

**브루트 포스 유형**:
1. **온라인 브루트 포스**: 로그인 폼에 직접 시도 (느리지만 해시 불필요)
2. **오프라인 브루트 포스**: 유출된 해시 파일을 로컬에서 크래킹 (매우 빠름)
3. **비밀번호 스프레잉**: 흔한 비밀번호를 여러 계정에 분산 시도 (잠금 우회)
4. **사전 공격 (Dictionary Attack)**: 흔한 단어 목록으로 시도
5. **레인보우 테이블 공격**: 사전 계산된 해시 역산 테이블 사용

**해시별 초당 시도 횟수 (RTX 4090 기준)**:
- MD5: 164 GH/s (1640억 회/초)
- SHA-1: 72 GH/s (720억 회/초)
- bcrypt(cost=10): 49,000 H/s (4만 9천 회/초)
- Argon2id: 더 느림

MD5와 bcrypt의 속도 차이는 약 330만 배다.

📢 **섹션 요약 비유**: MD5로 비밀번호를 저장하는 것은 도어락 비밀번호를 포스트잇에 써서 문에 붙이는 것이다. bcrypt는 비밀번호를 탱크 금고에 넣는 것이다.

---

## Ⅱ. 아키텍처 및 핵심 원리

브루트 포스 방어 다계층 구조:

| 계층 | 방어 기술 | 효과 |
|:---|:---|:---|
| 서버 측 | Rate Limiting, 계정 잠금 | 온라인 공격 차단 |
| 프론트엔드 | CAPTCHA, Progressive Delay | 자동화 차단 |
| 저장 | bcrypt/Argon2 (느린 해시) | 오프라인 크래킹 저지 |
| 인증 | MFA | 비밀번호 해독해도 접근 불가 |
| 모니터링 | 비정상 로그인 패턴 탐지 | 공격 감지 |

```
┌──────────────────────────────────────────────────────────┐
│           브루트 포스 vs bcrypt 방어 효과                │
├──────────────────────────────────────────────────────────┤
│  MD5("password123") 크래킹 시간:                         │
│  → GPU 4090으로 약 0.001초 (즉시)                       │
│                                                          │
│  bcrypt(cost=12, "password123") 크래킹 시간:             │
│  → GPU 4090으로 약 2분 (동일 비밀번호!)                  │
│                                                          │
│  bcrypt + 12자 무작위 비밀번호: 수십억 년 이상            │
└──────────────────────────────────────────────────────────┘
```

📢 **섹션 요약 비유**: bcrypt의 느린 해시는 자물쇠를 열어보려는 사람에게 시도마다 1분씩 기다리게 만드는 것이다. 같은 시간에 시도할 수 있는 횟수가 330만분의 1로 줄어든다.

---

## Ⅲ. 비교 및 연결

| 구분 | 온라인 브루트 포스 | 오프라인 브루트 포스 |
|:---|:---|:---|
| 조건 | 로그인 API 직접 접근 | 해시 파일 탈취 필요 |
| 속도 | 매우 느림 (네트워크 제한) | 매우 빠름 (로컬 GPU) |
| 방어 | Rate Limiting, 계정 잠금 | 느린 해시 함수, 솔트 |
| 위협 수준 | 낮음 (탐지 쉬움) | 높음 (DB 탈취 후 발생) |

📢 **섹션 요약 비유**: 온라인은 자물쇠 직접 시도(경비원이 봄), 오프라인은 자물쇠를 가져가서 집에서 복사(아무도 모름). 후자가 훨씬 위험하다.

---

## Ⅳ. 실무 적용 및 기술사 판단

**계정 잠금 vs 비밀번호 스프레잉 딜레마**:
- 계정 잠금(5회 실패 시 30분)은 브루트 포스를 방어하지만, 비밀번호 스프레잉은 계정당 1~2회만 시도하므로 잠금 없이 진행 가능
- **해결책**: 계정별 잠금 + IP별 Rate Limiting + MFA 조합

**비밀번호 정책 (NIST SP 800-63B 기준)**:
1. **최소 8자** (사용자 생성), 최소 6자 (시스템 생성)
2. **최대 64자** 이상 허용 (길이 제한 완화)
3. **유출 비밀번호 차단**: HaveIBeenPwned API 연동
4. **정기 변경 강제 비추천**: 패턴 있는 약한 비밀번호로 이어짐

📢 **섹션 요약 비유**: "비밀번호 90일마다 변경 강제"는 오히려 "Summer2024! → Fall2024!"처럼 패턴을 만들어 오히려 취약하게 한다.

---

## Ⅴ. 기대효과 및 결론

느린 해시 함수(bcrypt/Argon2) + MFA + Rate Limiting의 조합은 브루트 포스 공격의 현실적 성공 가능성을 근본적으로 줄인다. 특히 Passkey/FIDO2 기반 패스워드리스 인증으로 전환하면 비밀번호 자체가 없어져 모든 비밀번호 기반 공격이 무의미해진다.

기술사 관점에서 브루트 포스 방어는 암호학적 강도(느린 해시)와 운영 통제(Rate Limiting, MFA)가 함께 작동하는 심층 방어의 대표 사례다. 둘 중 하나만으로는 충분하지 않다.

📢 **섹션 요약 비유**: 브루트 포스 방어는 두꺼운 금고문(bcrypt)과 침입 감지 시스템(Rate Limiting), 그리고 지문 인식(MFA)을 모두 갖추는 것이다.

---

### 📌 관련 개념 맵
| 개념 | 관계 | 설명 |
|:---|:---|:---|
| bcrypt/Argon2 | 핵심 방어 | 의도적으로 느린 해시 |
| Rate Limiting | 온라인 방어 | 시도 횟수 제한 |
| MFA | 계층 방어 | 비밀번호 해독해도 접근 불가 |
| CAPTCHA | 자동화 차단 | 봇과 사람 구분 |
| Passkey | 미래 방향 | 비밀번호 없는 인증 |

### 👶 어린이를 위한 3줄 비유 설명
- 브루트 포스는 금고 비밀번호를 0000부터 9999까지 하나씩 다 눌러보는 것이야.
- bcrypt는 각 번호를 누를 때마다 5초씩 기다리게 해서, 만 번을 다 시도하려면 13시간이 넘게 걸리게 만들어.
- 여기에 MFA를 추가하면 비밀번호를 맞춰도 핸드폰 인증까지 통과해야 하니, 거의 완벽하게 막을 수 있어!
""")

print("All remaining files done.")
