+++
title = "501. XSS 유형 (Reflected, Stored, DOM-based)"
date = 2026-04-21
weight = 501
description = "반사형, 저장형, DOM-based XSS의 차이"
taxonomy = ""
tags = ["Software Engineering", "Security", "XSS", "Reflected XSS", "DOM XSS"]
+++

## 핵심 인사이트 (3줄 요약)
> 1. **본질**: XSS는 유입 경로에 따라 반사형, 저장형, DOM-based로 나뉜다.
> 2. **가치**: 공격 흐름을 알면 방어 포인트가 보인다.
> 3. **판단 포인트**: 서버 문제인지, 클라이언트 문제인지 구분한다.

---

## Ⅰ. 개요 및 필요성

XSS 유형을 구분하면 테스트와 방어가 쉬워진다. 모두 같은 공격처럼 보여도 유입과 실행 지점이 다르다.

이 차이를 알아야 진단이 정확해진다.

- **📢 섹션 요약 비유**: 불이 났을 때 부엌, 거실, 마당 중 어디서 시작했는지 아는 것과 같다.

---

## Ⅱ. 아키텍처 및 핵심 원리

세 가지 유형은 흐름이 다르다.

```text
Reflected: 요청 -> 즉시 응답에 반영
Stored: 요청 -> 서버 저장 -> 다른 사용자에게 노출
DOM-based: 브라우저 JS -> DOM 조작으로 실행
```

| 유형 | 특징 |
|:---|:---|
| Reflected XSS | 요청 반사 |
| Stored XSS | 서버 저장 |
| DOM-based XSS | 클라이언트 조작 |

- **📢 섹션 요약 비유**: 메아리, 메모장, 조립 장난감이 각각 다르게 작동하는 것과 같다.

---

## Ⅲ. 비교 및 연결

세 유형 모두 결국 출력과 실행의 문제지만, 방어 위치가 다르다.

| 구분 | Reflected | Stored | DOM-based |
|:---|:---|:---|:---|
| 흐름 | 요청-응답 | 저장-재노출 | 브라우저 내부 |
| 주된 방어 | 입력/출력 인코딩 | 저장 전 검증 | 안전한 DOM API |
| 위험 | 즉시 실행 | 광범위 노출 | 클라이언트 취약 |

XSS 방어는 코드 리뷰와 테스트 시나리오 설계에도 중요하다.

- **📢 섹션 요약 비유**: 같은 도둑이라도 창문, 창고, 안방 중 어디로 들어오는지 다르다.

---

## Ⅳ. 실무 적용 및 기술사 판단

실무에서는 로그, 검색 결과, 댓글, URL 파라미터, 프론트엔드 렌더링을 점검한다.

점검 포인트는 다음과 같다.
1. 사용자 입력이 어디서 다시 보이는가?
2. 브라우저에서 JS가 문자열을 조립하는가?
3. 저장 데이터가 다른 사용자에게 전달되는가?

- **📢 섹션 요약 비유**: 같은 물건도 어디에 놓느냐에 따라 위험이 달라진다.

---

## Ⅴ. 기대효과 및 결론

XSS 유형을 구분하면 맞춤형 방어가 가능하다.

결론적으로 이 항목은 "실행 경로별 XSS 분류"다.

- **📢 섹션 요약 비유**: 병도 증상에 따라 다르게 치료해야 한다.

### 📌 관련 개념 맵

| 개념 | 연결 포인트 |
|:---|:---|
| Reflected XSS | 즉시 반영 |
| Stored XSS | 저장 기반 |
| DOM-based XSS | 브라우저 기반 |

### 👶 어린이를 위한 3줄 비유 설명

1. XSS는 생기는 길이 여러 가지예요.
2. 바로 튀어나오기도 하고, 저장되었다가 나오기도 해요.
3. 어디서 생기는지 알아야 막을 수 있어요.

