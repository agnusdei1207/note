+++
title = "469. 모델 기반 테스팅 (MBT, Model-Based Testing)"
date = 2026-04-21
weight = 469
description = "시스템 모델에서 테스트 케이스를 자동으로 만드는 테스트 기법"
taxonomy = ""
tags = ["Software Engineering", "Testing", "MBT", "Model-Based Testing", "UML"]
+++

## 핵심 인사이트 (3줄 요약)
> 1. **본질**: 모델 기반 테스팅(MBT, Model-Based Testing)은 모델에서 테스트를 자동 생성하는 방식이다.
> 2. **가치**: 복잡한 흐름을 구조적으로 커버하고, 테스트 생성의 일관성을 높인다.
> 3. **판단 포인트**: 모델이 정확해야 테스트도 의미가 있다.

---

## Ⅰ. 개요 및 필요성

MBT는 UML 같은 모델을 기준으로 테스트 케이스를 만든다. 즉, 구현이 아니라 설계 모델을 테스트의 출발점으로 삼는다.

모델이 있으면 복잡한 상태 전이와 흐름을 체계적으로 커버할 수 있다.

- **📢 섹션 요약 비유**: 지도에서 길을 뽑아 여행 계획을 만드는 것이다.

---

## Ⅱ. 아키텍처 및 핵심 원리

모델에서 상태, 전이, 조건을 읽고 테스트 경로를 자동 생성한다.

| 요소 | 의미 |
|:---|:---|
| 모델 | 테스트의 원천 |
| 전이 | 상태 변화 |
| 생성기 | 테스트 케이스 생산 |

```text
모델 -> 경로 추출 -> 테스트 케이스 생성 -> 실행
```

수동 작성보다 일관성이 좋고, 복잡한 상태 시스템에 강하다.

- **📢 섹션 요약 비유**: 게임 규칙표에서 가능한 플레이를 모두 뽑아내는 것이다.

---

## Ⅲ. 비교 및 연결

MBT는 사람 손으로 하나씩 쓰는 테스트보다 구조적이다. 다만 모델이 틀리면 테스트도 틀릴 수 있다.

| 구분 | 수동 작성 | MBT |
|:---|:---|:---|
| 생성 | 사람 | 모델 기반 자동 |
| 일관성 | 편차 가능 | 높음 |
| 위험 | 누락 | 모델 오류 |

상태 다이어그램, 시퀀스 다이어그램, 요구사항 모델과 연결된다.

- **📢 섹션 요약 비유**: 레시피가 정확해야 자동 조리 기계도 제대로 만든다.

---

## Ⅳ. 실무 적용 및 기술사 판단

실무에서는 프로토콜, 상태 전이, 업무 흐름이 복잡한 시스템에 적합하다. 모델 관리가 성패를 가른다.

체크 포인트는 다음과 같다.
1. 모델의 정확성을 검증한다.
2. 자동 생성 범위를 정한다.
3. 생성된 테스트를 재검토한다.

- **📢 섹션 요약 비유**: 설계도가 정확해야 집도 제대로 나온다.

---

## Ⅴ. 기대효과 및 결론

MBT는 복잡한 시스템의 테스트를 구조화한다. 자동화와 모델 품질이 결합되면 효과가 크다.

결론적으로 이 개념은 "모델에서 테스트를 뽑아내는 방법"이다.

- **📢 섹션 요약 비유**: 지도 한 장에서 필요한 길들을 자동으로 골라 내는 것이다.

### 📌 관련 개념 맵

| 개념 | 연결 포인트 |
|:---|:---|
| UML | 모델 원천 |
| 상태 전이 | 주요 대상 |
| 자동 생성 | 핵심 가치 |

### 👶 어린이를 위한 3줄 비유 설명

1. 모델 기반 테스팅은 그림에서 테스트를 만들어요.
2. 그래서 자동으로 케이스가 나와요.
3. 하지만 그림이 맞아야 해요.

