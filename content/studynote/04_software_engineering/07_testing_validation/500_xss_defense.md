+++
title = "500. 크로스 사이트 스크립팅 (XSS) 방어"
date = 2026-04-21
weight = 500
description = "입/출력값 인코딩과 CSP 헤더로 XSS를 방어하는 방법"
taxonomy = ""
tags = ["Software Engineering", "Security", "XSS", "Encoding", "CSP"]
+++

## 핵심 인사이트 (3줄 요약)
> 1. **본질**: XSS (Cross-Site Scripting)는 악성 스크립트가 브라우저에서 실행되는 취약점이다.
> 2. **가치**: 출력 인코딩과 CSP (Content Security Policy)로 피해를 크게 줄인다.
> 3. **판단 포인트**: 입력을 막는 것만이 아니라 출력 문맥을 맞춘다.

---

## Ⅰ. 개요 및 필요성

XSS는 사용자 입력이 HTML, JavaScript 문맥에 섞여 실행될 때 생긴다. 쿠키 탈취, 세션 악용, UI 위조로 이어질 수 있다.

웹 보안에서 가장 기본적으로 막아야 할 문제다.

- **📢 섹션 요약 비유**: 편지에 남의 낙서를 그대로 보여 주면 오해가 생기는 것과 같다.

---

## Ⅱ. 아키텍처 및 핵심 원리

핵심 방어는 입력의 원천보다 출력의 문맥을 보호하는 것이다.

```text
입력 -> 저장/전달 -> 출력 인코딩 -> 브라우저 렌더링
```

| 방어 | 설명 |
|:---|:---|
| HTML Encoding | 태그 해석 방지 |
| JavaScript Encoding | 스크립트 문맥 보호 |
| CSP | 허용된 스크립트만 실행 |

- **📢 섹션 요약 비유**: 같은 말도 교실, 문자, 방송에서 전달 방식이 달라야 한다.

---

## Ⅲ. 비교 및 연결

입력 검증은 XSS 방어의 일부지만, 충분조건은 아니다.

| 구분 | 안전한 방식 | 위험한 방식 |
|:---|:---|:---|
| 출력 | 문맥별 인코딩 | 원문 삽입 |
| 스크립트 | CSP 적용 | 모든 실행 허용 |
| 저장 | 안전한 텍스트 | HTML 혼합 |

Stored XSS, Reflected XSS, DOM-based XSS와 모두 연결된다.

- **📢 섹션 요약 비유**: 불쏘시개를 집에 두지 않는 것만으로는 부족하고, 불이 붙을 길도 막아야 한다.

---

## Ⅳ. 실무 적용 및 기술사 판단

실무에서는 템플릿 엔진 기본 이스케이프, CSP 헤더, 안전한 DOM 조작이 중요하다.

점검 포인트는 다음과 같다.
1. 출력 시 문맥별 인코딩을 하는가?
2. 위험한 innerHTML 사용을 피하는가?
3. CSP로 외부 스크립트 실행을 제한하는가?

- **📢 섹션 요약 비유**: 글씨를 크게 쓰는 것보다, 벽에 낙서가 안 되게 해야 한다.

---

## Ⅴ. 기대효과 및 결론

XSS를 막으면 세션 보호와 사용자 신뢰를 지킬 수 있다.

결론적으로 이 항목은 "출력 문맥을 지키는 방어"다.

- **📢 섹션 요약 비유**: 말은 있어도 전달 방식이 안전해야 한다.

### 📌 관련 개념 맵

| 개념 | 연결 포인트 |
|:---|:---|
| CSP | 실행 제한 |
| Encoding | 핵심 방어 |
| DOM | 브라우저 조작 |

### 👶 어린이를 위한 3줄 비유 설명

1. XSS는 나쁜 글이 화면에서 실행되는 거예요.
2. 그래서 보여 줄 때 안전하게 바꿔야 해요.
3. 그리고 나쁜 스크립트는 못 실행하게 해야 해요.

