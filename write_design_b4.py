import os
BASE = "/Users/pf/workspace/brainscience/content/studynote/11_design_supervision/06_exam_summary"
def W(fn, c):
    p = os.path.join(BASE, fn)
    if os.path.exists(p): print(f"SKIP:{fn}"); return
    with open(p,'w') as f: f.write(c)
    print(f"OK:{fn}")

W("387_facade_pattern_summary.md",'''+++
weight = 387
title = "387. 퍼사드 (Facade) 패턴 서브시스템 단일 통합 뷰"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 퍼사드 (Facade) 패턴은 복잡한 서브시스템을 단순한 하나의 인터페이스로 제공하여, 클라이언트가 서브시스템 내부 구조를 알지 않아도 사용할 수 있게 한다.
> 2. **가치**: 서브시스템 간의 의존성을 줄이고 계층화를 통해 시스템 구조를 명확히 하며, API 게이트웨이·SDK·라이브러리 디자인에서 핵심 역할을 한다.
> 3. **판단 포인트**: 퍼사드가 너무 많은 기능을 집중시키면 "갓 클래스 (God Class)" 안티패턴으로 변질되므로 서브시스템을 직접 사용하는 경로도 열어두어야 한다.

## Ⅰ. 개요 및 필요성

홈 씨어터 시스템을 예로 들면: 영화를 보려면 프로젝터 켜기, 스크린 내리기, 조명 낮추기, 음향 켜기, 플레이어 시작 등 여러 서브시스템을 순서대로 제어해야 한다. 퍼사드는 `homeTheater.watchMovie("Inception")` 하나로 이 모든 것을 처리한다.

**구성 요소:**
- `Facade`: 단순화된 인터페이스 제공
- `Subsystem classes`: 복잡한 내부 구현
- `Client`: Facade만 사용

📢 **섹션 요약 비유**: 퍼사드는 리모컨 하나로 TV·에어컨·조명·음악을 모두 제어하는 홈 자동화 시스템이다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│              Facade 패턴 구조 (홈 씨어터 예시)                  │
├─────────────────────────────────────────────────────────────────┤
│  Client                                                         │
│    │ homeTheater.watchMovie()                                   │
│    ▼                                                            │
│  HomeTheaterFacade                                              │
│  + watchMovie(movie)                                            │
│    → projector.on()                                             │
│    → screen.down()                                              │
│    → lights.dim(10)                                             │
│    → soundSystem.on()                                           │
│    → player.play(movie)                                         │
│  + endMovie()                                                   │
│    → 역순으로 종료                                              │
│                                                                 │
│  [Projector] [Screen] [Lights] [SoundSystem] [Player]           │
│  (서브시스템 - 클라이언트가 직접 알 필요 없음)                  │
└─────────────────────────────────────────────────────────────────┘
```

| 비교 | 퍼사드 | 어댑터 | API 게이트웨이 |
|:---|:---|:---|:---|
| 목적 | 인터페이스 단순화 | 인터페이스 변환 | 외부 진입점 통합 |
| 내부 숨김 | ✅ 복잡성 은닉 | ❌ 변환만 | ✅ 서비스 토폴로지 |
| 직접 접근 | ✅ 가능 | ✅ 가능 | ⚠️ 우회 가능 |

📢 **섹션 요약 비유**: 어댑터는 "플러그 모양을 바꾸는 것", 퍼사드는 "전기 패널 전체를 하나의 스위치로 제어하는 것"이다.

## Ⅲ. 비교 및 연결

| 항목 | 퍼사드 | 중재자 (Mediator) |
|:---|:---|:---|
| 단방향성 | ✅ 클라이언트 → 서브시스템 | ❌ 양방향 조율 |
| 서브시스템 인지 | ❌ 퍼사드만 인지 | ✅ 서로 인지 |
| 목적 | 단순화 | 결합도 감소 |

📢 **섹션 요약 비유**: 퍼사드는 단순화된 메뉴판, 중재자는 협력 회의에서 모든 대화를 조율하는 사회자다.

## Ⅳ. 실무 적용 및 기술사 판단

**계층화 아키텍처에서의 역할:**
- Controller Layer ↔ Service Layer ↔ Repository Layer 각 계층이 퍼사드로 하위 계층 복잡성을 숨김
- MSA의 BFF (Backend For Frontend): 여러 마이크로서비스를 집계하는 퍼사드

**안티패턴 주의:**
- 모든 비즈니스 로직을 퍼사드에 집중 → 갓 클래스 (God Class) 위험
- 서브시스템을 완전히 차단하면 고급 사용자가 직접 접근 불가

📢 **섹션 요약 비유**: 좋은 퍼사드는 "기본 사용자를 위한 간단한 입구"이지, 모든 문을 잠가버리는 것이 아니다.

## Ⅴ. 기대효과 및 결론

퍼사드 패턴은 복잡한 서브시스템을 단순화하여 사용성과 유지보수성을 높인다. 기술사 시험에서는 홈 씨어터·계층화 아키텍처 예시, 어댑터와의 차이, 갓 클래스 위험을 명확히 서술해야 한다.

📢 **섹션 요약 비유**: 퍼사드는 뷔페 레스토랑의 직원이다. 손님이 원하는 음식을 말하면 부엌의 복잡한 요리 과정 없이 접시에 담아준다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | GoF 구조 패턴 | 퍼사드의 분류 |
| 연관 패턴 | 어댑터 | 인터페이스 변환 vs 단순화 |
| 연관 개념 | API 게이트웨이 | MSA에서의 퍼사드 |
| 연관 패턴 | 중재자 | 양방향 조율 |

### 👶 어린이를 위한 3줄 비유 설명

- 스마트홈 앱 하나로 TV, 조명, 에어컨을 모두 켜는 것처럼 퍼사드는 복잡한 것을 간단하게 해줘요.
- 각 기기(서브시스템)는 그대로 있고, 앱(퍼사드)이 조율해줘요.
- 원하면 각 기기를 직접 제어할 수도 있어요.
''')

W("388_flyweight_pattern_summary.md",'''+++
weight = 388
title = "388. 플라이웨이트 (Flyweight) 패턴 고유 상태 공유"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 플라이웨이트 (Flyweight) 패턴은 수많은 유사 객체의 메모리 사용을 최적화하기 위해, 공유 가능한 고유 상태 (Intrinsic State)를 캐싱하고 컨텍스트별 외부 상태 (Extrinsic State)는 클라이언트가 전달하도록 한다.
> 2. **가치**: 수만~수백만 개의 유사 객체(게임 나뭇잎, 텍스트 편집기 문자)를 생성할 때 메모리를 수십 배 절감할 수 있다.
> 3. **판단 포인트**: 내부 상태/외부 상태의 명확한 분리가 핵심이며, 플라이웨이트 팩토리를 통해 공유 객체를 캐싱·재사용한다.

## Ⅰ. 개요 및 필요성

게임에서 숲을 표현하기 위해 수백만 개의 나무 객체가 필요하다. 각 나무마다 텍스처·메시·색상 데이터를 독립적으로 저장하면 메모리가 폭발한다. 하지만 같은 종류의 나무는 텍스처·메시·색상이 동일하다 (고유 상태). 위치·크기만 다르다 (외부 상태).

플라이웨이트는 텍스처·메시·색상을 공유 플라이웨이트 객체로 캐싱하고, 위치·크기는 클라이언트(게임 세계)가 전달한다.

📢 **섹션 요약 비유**: 플라이웨이트는 도서관이다. 같은 책(고유 상태)을 여러 권 사지 않고 한 권을 공유하되, 어느 자리에서 읽는지(외부 상태)는 독자마다 다르다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│              Flyweight 패턴 구조 (나무 예시)                    │
├─────────────────────────────────────────────────────────────────┤
│  TreeFactory (Flyweight Factory)                                │
│  - cache: Map<String, TreeType>                                 │
│  + getTreeType(name, texture, color): TreeType                  │
│    → 캐시 히트 시 기존 반환, 미스 시 생성 후 캐싱              │
│                                                                 │
│  TreeType (Flyweight - 공유 객체)                               │
│  - name: String       ◄── 고유 상태 (Intrinsic)                 │
│  - texture: Bitmap    ◄── 고유 상태                             │
│  - color: Color       ◄── 고유 상태                             │
│  + draw(x, y, age)    ◄── 외부 상태(x,y,age)는 파라미터        │
│                                                                 │
│  Tree (Context)                                                 │
│  - x, y, age          ◄── 외부 상태 (Extrinsic) - 인스턴스별   │
│  - type: TreeType     ◄── 플라이웨이트 참조                     │
└─────────────────────────────────────────────────────────────────┘
```

| 상태 유형 | 설명 | 저장 위치 |
|:---|:---|:---|
| 고유 상태 (Intrinsic) | 객체 간 공유 가능, 불변 | 플라이웨이트 내부 |
| 외부 상태 (Extrinsic) | 컨텍스트별 가변 | 클라이언트 전달 |

📢 **섹션 요약 비유**: 고유 상태는 책의 내용, 외부 상태는 책을 읽는 사람의 위치와 필기 내용이다.

## Ⅲ. 비교 및 연결

| 항목 | 플라이웨이트 | 싱글톤 | 프로토타입 |
|:---|:---|:---|:---|
| 목적 | 메모리 최적화 (다수 객체) | 인스턴스 유일성 | 객체 복제 최적화 |
| 인스턴스 수 | 고유 상태별 하나 | 클래스당 하나 | 복제 시마다 새 객체 |

📢 **섹션 요약 비유**: 싱글톤은 "전 세계에 교장 한 명", 플라이웨이트는 "같은 교과서를 여러 반이 공유"하는 것이다.

## Ⅳ. 실무 적용 및 기술사 판단

**Java에서의 사례:**
- `Integer.valueOf()`: -128~127 정수 캐싱 (Integer Cache)
- `String.intern()`: 문자열 풀 (String Pool) 공유
- 폰트 렌더링: 동일 글자의 비트맵 공유

**성능 분석:**
- 메모리 절감: 공유 객체 수 × (고유 상태 크기)
- 오버헤드: 팩토리 캐시 조회 비용, 외부 상태 전달 비용

📢 **섹션 요약 비유**: Java String Pool은 플라이웨이트의 완벽한 예시다. "hello"를 100번 써도 JVM은 하나의 문자열 객체만 유지한다.

## Ⅴ. 기대효과 및 결론

플라이웨이트 패턴은 대량의 유사 객체를 다루는 시스템에서 메모리를 획기적으로 절감하는 패턴이다. 기술사 시험에서는 고유 상태/외부 상태 분리, 플라이웨이트 팩토리 캐싱, Java String Pool과의 연계를 명확히 서술해야 한다.

📢 **섹션 요약 비유**: 플라이웨이트는 "공유 자전거 서비스"다. 자전거 자체(고유 상태)는 공유하고, 내가 언제 어디서 타는지(외부 상태)는 나만의 정보다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | GoF 구조 패턴 | 플라이웨이트의 분류 |
| 연관 개념 | 캐싱 (Caching) | 플라이웨이트 팩토리의 핵심 |
| 연관 개념 | Java String Pool | 플라이웨이트 구현 예시 |
| 연관 패턴 | 싱글톤 | 인스턴스 관리 |

### 👶 어린이를 위한 3줄 비유 설명

- 학교 도서관에서 같은 책은 한 권만 있어도 여러 학생이 돌아가며 읽어요.
- 책의 내용(고유 상태)은 공유하고, 각자 읽는 페이지(외부 상태)는 달라요.
- 책을 학생마다 한 권씩 사면 공간이 부족하지만, 도서관은 공간을 절약해요.
''')

W("389_proxy_pattern_summary.md",'''+++
weight = 389
title = "389. 프록시 (Proxy) 패턴 접근 제어 대리인"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 프록시 (Proxy) 패턴은 실제 객체에 대한 접근을 제어하거나 기능을 추가하기 위한 대리 객체(Surrogate)를 제공하며, 가상 프록시·보호 프록시·원격 프록시·캐싱 프록시 등으로 분류된다.
> 2. **가치**: 실제 객체의 생성을 지연하거나 (지연 초기화), 접근 권한을 통제하거나, 원격 객체를 로컬처럼 다루거나, 결과를 캐싱하는 등 다양한 횡단 관심사를 투명하게 처리할 수 있다.
> 3. **판단 포인트**: 프록시는 실제 객체와 동일한 인터페이스를 구현하므로 클라이언트는 프록시 여부를 알 필요가 없지만, 간접 계층 추가로 인한 성능 오버헤드를 고려해야 한다.

## Ⅰ. 개요 및 필요성

초고해상도 이미지를 항상 메모리에 로드해 두면 메모리를 낭비한다. 가상 프록시 (Virtual Proxy)는 실제 이미지 객체를 사용 시점에만 로드하는 지연 초기화를 구현한다.

**4가지 프록시 유형:**
1. **가상 프록시 (Virtual)**: 비용이 큰 객체의 지연 초기화
2. **보호 프록시 (Protection)**: 접근 권한 제어 (권한 없으면 차단)
3. **원격 프록시 (Remote)**: 원격 객체를 로컬처럼 다룸 (RPC)
4. **캐싱 프록시 (Caching)**: 동일 요청 결과 캐싱

📢 **섹션 요약 비유**: 프록시는 비서다. 사장(실제 객체)에게 모든 방문자가 직접 접근하지 않고 비서(프록시)가 먼저 필터링하고 처리한다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│              Proxy 패턴 구조                                    │
├─────────────────────────────────────────────────────────────────┤
│  <<interface>> Service                                          │
│  + operation(): result                                          │
│         △                    △                                  │
│  RealService            ServiceProxy                            │
│  (실제 객체)             - realService: RealService             │
│  + operation()          + operation()                           │
│                           → 전처리 (권한 확인, 캐시 조회)       │
│                           → realService.operation()             │
│                           → 후처리 (캐시 저장, 로깅)            │
│                                                                 │
│  Client → Service (인터페이스만 알면 됨)                        │
│         → 프록시인지 실제 객체인지 모름                         │
└─────────────────────────────────────────────────────────────────┘
```

| 프록시 유형 | 목적 | 실무 사례 |
|:---|:---|:---|
| 가상 (Virtual) | 지연 초기화 | 대용량 이미지 지연 로드 |
| 보호 (Protection) | 접근 제어 | 권한별 기능 제한 |
| 원격 (Remote) | 원격 객체 로컬화 | RPC, gRPC 스텁 |
| 캐싱 (Caching) | 결과 재사용 | HTTP 프록시 서버 |

📢 **섹션 요약 비유**: 가상 프록시는 영화 예고편(실제 영화는 나중에), 보호 프록시는 18세 미만 관람 불가 스크린, 원격 프록시는 해외 서버를 국내처럼 다루는 VPN이다.

## Ⅲ. 비교 및 연결

| 패턴 | 인터페이스 동일 | 목적 |
|:---|:---|:---|
| 프록시 | ✅ 동일 | 접근 제어, 지연 초기화 |
| 데코레이터 | ✅ 동일 | 기능 추가 |
| 어댑터 | ❌ 변환 | 인터페이스 호환 |
| 퍼사드 | ❌ 단순화 | 복잡성 은닉 |

📢 **섹션 요약 비유**: 프록시와 데코레이터의 구조는 거의 같지만, 프록시는 "접근 제어", 데코레이터는 "기능 추가"가 목적이다.

## Ⅳ. 실무 적용 및 기술사 판단

**Spring AOP와 프록시:**
Spring의 `@Transactional`, `@Cacheable`은 JDK 동적 프록시 또는 CGLIB 프록시를 통해 구현됨 = 런타임 프록시 생성

**Java `java.lang.reflect.Proxy`:**
인터페이스 기반 동적 프록시 생성으로 로깅·트랜잭션·권한 체크를 투명하게 추가

**게이트웨이 패턴과의 연계:**
API 게이트웨이는 원격 프록시 + 캐싱 프록시 + 보호 프록시를 결합한 구조

📢 **섹션 요약 비유**: Spring @Transactional은 메서드 호출을 프록시가 가로채서 트랜잭션을 시작하고, 완료되면 커밋 또는 롤백한다.

## Ⅴ. 기대효과 및 결론

프록시 패턴은 횡단 관심사를 투명하게 처리하는 핵심 패턴으로, Spring AOP의 기반이 된다. 기술사 시험에서는 4가지 프록시 유형, 데코레이터와의 차이, Spring AOP 구현 방식을 명확히 서술해야 한다.

📢 **섹션 요약 비유**: 프록시는 조직의 중간 관리자다. 실제 결정자(사장)를 대신해 직원들의 요청을 처리하고, 필요한 것만 사장에게 전달한다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | GoF 구조 패턴 | 프록시의 분류 |
| 실무 구현 | Spring AOP | 동적 프록시 기반 |
| 연관 패턴 | 데코레이터 | 구조 유사, 목적 다름 |
| 연관 개념 | 지연 초기화 | 가상 프록시의 핵심 |

### 👶 어린이를 위한 3줄 비유 설명

- 교장 선생님 대신 부교장이 학생들의 요청을 먼저 받아서 처리해줘요.
- 중요한 일만 교장에게 전달하고 나머지는 부교장이 해결해요.
- 학생들은 부교장이 대신 처리하는지 교장이 처리하는지 몰라도 돼요.
''')

W("390_observer_pattern_summary.md",'''+++
weight = 390
title = "390. 옵저버 (Observer) 패턴 1:N 상태 구독 알림"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 옵저버 (Observer) 패턴은 객체의 상태 변화를 관심 있는 모든 구독자에게 자동으로 알려주는 1:N 의존 관계 패턴으로, MVC (Model-View-Controller) 패턴의 핵심을 이루고 이벤트 기반 아키텍처의 기반이 된다.
> 2. **가치**: 발행자 (Publisher)가 구독자 (Subscriber)에 대해 알 필요 없어 느슨한 결합을 달성하며, 런타임에 구독자를 동적으로 추가·제거할 수 있다.
> 3. **판단 포인트**: 구독자 수가 많거나 알림 빈도가 높으면 성능 문제가 발생할 수 있고, 순환 알림 (Circular Notification)이 발생하지 않도록 주의해야 한다.

## Ⅰ. 개요 및 필요성

뉴스 구독 서비스: 독자가 신문사를 구독하면, 새 기사가 발행될 때마다 독자에게 알림이 간다. 신문사(발행자)는 구독자 목록만 관리하고, 구독자가 누구인지 알 필요가 없다.

**구성 요소:**
- `Subject` (Publisher): 상태를 가지며 Observer 목록 관리
- `Observer` (Subscriber): 상태 변화 알림을 받는 인터페이스
- `ConcreteSubject`: 실제 상태 관리, 변화 시 notify
- `ConcreteObserver`: 실제 업데이트 처리

📢 **섹션 요약 비유**: 옵저버는 유튜브 채널 구독이다. 채널(Subject)이 새 영상을 올리면 구독자(Observer) 모두에게 자동 알림이 간다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│              Observer 패턴 구조                                 │
├─────────────────────────────────────────────────────────────────┤
│  <<interface>> Subject                                          │
│  + attach(Observer)                                             │
│  + detach(Observer)                                             │
│  + notify()                                                     │
│         △                                                       │
│  ConcreteSubject                                                │
│  - state: T                                                     │
│  - observers: List<Observer>                                    │
│  + setState(newState)                                           │
│    → this.state = newState                                      │
│    → notify() → observers.forEach(o → o.update(this))          │
│                                                                 │
│  <<interface>> Observer                                         │
│  + update(subject)                                              │
│         △                                                       │
│  ConcreteObserverA    ConcreteObserverB                         │
│  + update() → 화면 갱신  + update() → 로그 기록                │
└─────────────────────────────────────────────────────────────────┘
```

| 푸시 vs 풀 방식 | 특징 | 장단점 |
|:---|:---|:---|
| 푸시 (Push) | Subject가 변경 데이터를 전달 | 구독자가 필요 없는 데이터도 받을 수 있음 |
| 풀 (Pull) | 구독자가 Subject에서 직접 조회 | 필요한 데이터만 조회, 최신 상태 보장 |

📢 **섹션 요약 비유**: 푸시는 신문이 집으로 배달되는 것, 풀은 독자가 직접 신문사에 가서 가져오는 것이다.

## Ⅲ. 비교 및 연결

| 항목 | 옵저버 (동기) | 이벤트 버스 (비동기) | 메시지 큐 |
|:---|:---|:---|:---|
| 실행 방식 | 동기, 순차 | 비동기 | 비동기, 분산 |
| 결합도 | 느슨 | 매우 느슨 | 완전 분리 |
| 성능 | 단일 프로세스 | 멀티스레드 | 분산 시스템 |
| 예시 | Java Observable | Spring EventBus | Kafka, RabbitMQ |

📢 **섹션 요약 비유**: 옵저버는 같은 건물 내 내선 전화, 이벤트 버스는 사내 메신저, 메시지 큐는 국제 우편이다.

## Ⅳ. 실무 적용 및 기술사 판단

**MVC 패턴과의 연계:**
- Model = Subject (상태 변화 시 notify)
- View = Observer (update 시 화면 갱신)
- Controller는 Model과 View를 연결

**Spring EventBus:**
`@EventListener` + `ApplicationEventPublisher`로 스프링 빈 간 이벤트 발행/구독

**MSA 도메인 이벤트:**
Aggregate 상태 변화 시 도메인 이벤트 발행 → Kafka/RabbitMQ → 다른 서비스 구독 = 분산 옵저버

�� **섹션 요약 비유**: MSA의 이벤트 기반 아키텍처는 옵저버 패턴을 서비스 경계 너머로 확장한 것이다.

## Ⅴ. 기대효과 및 결론

옵저버 패턴은 느슨한 결합과 동적 구독 관리로 확장성 있는 이벤트 기반 시스템의 기반을 제공한다. 기술사 시험에서는 MVC와의 연계, 푸시/풀 방식 비교, MSA 이벤트 기반 아키텍처로의 확장을 명확히 서술해야 한다.

📢 **섹션 요약 비유**: 옵저버 패턴은 단톡방과 같다. 방장(Subject)이 메시지를 보내면 모든 참가자(Observer)가 동시에 받는다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | GoF 행동 패턴 | 옵저버의 분류 |
| 연관 패턴 | MVC | 옵저버 패턴 응용 |
| 연관 개념 | 이벤트 기반 아키텍처 | 옵저버의 분산 확장 |
| 연관 개념 | 메시지 큐 (Kafka) | MSA에서의 비동기 옵저버 |

### 👶 어린이를 위한 3줄 비유 설명

- 유튜브에서 좋아하는 채널을 구독하면 새 영상이 올라올 때마다 알림이 와요.
- 채널은 구독자가 누구인지 몰라도 알림을 보낼 수 있어요.
- 언제든지 구독 취소(detach)를 하면 더 이상 알림이 오지 않아요.
''')

W("391_strategy_pattern_summary.md",'''+++
weight = 391
title = "391. 전략 (Strategy) 패턴 알고리즘 교체 캡슐화"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 전략 (Strategy) 패턴은 알고리즘을 캡슐화하여 런타임에 교체 가능하게 하는 행동 패턴으로, 알고리즘 변화가 클라이언트에 영향을 주지 않도록 분리한다.
> 2. **가치**: `if-else`나 `switch`로 알고리즘을 선택하는 코드를 전략 객체 교체로 대체하여 OCP (Open-Closed Principle)를 준수하고 테스트 용이성을 높인다.
> 3. **판단 포인트**: 전략 수가 적고 변경이 거의 없다면 단순 `if-else`가 오히려 명확할 수 있으므로, 전략의 수와 변경 빈도를 고려해야 한다.

## Ⅰ. 개요 및 필요성

정렬 알고리즘 선택: 데이터 크기에 따라 버블 정렬, 퀵 정렬, 병합 정렬을 선택해야 할 때, `if (size < 10) bubble else if (size < 1000) quick else merge` 같은 조건문은 알고리즘 추가 시마다 수정이 필요하다.

전략 패턴은 각 알고리즘을 `SortStrategy` 인터페이스를 구현하는 별도 클래스로 캡슐화하여, 컨텍스트에 전략을 주입하면 알고리즘이 교체된다.

**구성 요소:**
- `Strategy` 인터페이스: 알고리즘 메서드 선언
- `ConcreteStrategy`: 구체 알고리즘 구현
- `Context`: Strategy 참조, 클라이언트 요청을 전략에 위임

📢 **섹션 요약 비유**: 전략 패턴은 내비게이션 앱의 경로 탐색 전략이다. "최단 거리", "최소 시간", "무료 도로만" 버튼을 누르면 같은 내비게이션이 다른 알고리즘으로 경로를 계산한다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│              Strategy 패턴 구조 (정렬 예시)                     │
├─────────────────────────────────────────────────────────────────┤
│  <<interface>> SortStrategy                                     │
│  + sort(data[]): data[]                                         │
│         △                                                       │
│  BubbleSort  QuickSort  MergeSort  TimSort                      │
│  + sort()    + sort()   + sort()   + sort()                     │
│                                                                 │
│  Context (Sorter)                                               │
│  - strategy: SortStrategy ◄── 전략 주입 (DI)                    │
│  + setStrategy(SortStrategy)                                    │
│  + executeSort(data[])                                          │
│    → strategy.sort(data)                                        │
│                                                                 │
│  Client:                                                        │
│  sorter.setStrategy(new QuickSort())                            │
│  sorter.executeSort(data) // 런타임 교체                        │
└─────────────────────────────────────────────────────────────────┘
```

| 비교 | 전략 패턴 | 템플릿 메서드 |
|:---|:---|:---|
| 알고리즘 교체 | 구성 (Composition), 런타임 | 상속, 컴파일 타임 |
| 알고리즘 선택 | 클라이언트가 선택 | 슈퍼클래스 뼈대 내 서브클래스 |
| 유연성 | 높음 | 중간 |

📢 **섹션 요약 비유**: 전략은 운전 방법(전략)을 바꾸는 것, 템플릿 메서드는 운전 순서(뼈대)를 유지하면서 세부 행동을 바꾸는 것이다.

## Ⅲ. 비교 및 연결

| 항목 | 전략 | 상태 (State) |
|:---|:---|:---|
| 목적 | 알고리즘 캡슐화/교체 | 상태별 행동 위임 |
| 교체 주체 | 클라이언트 (외부) | Context (내부 상태 변화) |
| 전략 간 전이 | 없음 | 상태 간 전이 있음 |

📢 **섹션 요약 비유**: 전략은 "내가 원하는 레시피를 선택", 상태는 "음식 상태(냉동/해동/조리)에 따라 자동으로 레시피가 바뀌는 것"이다.

## Ⅳ. 실무 적용 및 기술사 판단

**결제 수단 전략:**
`PaymentStrategy` 인터페이스 → `CreditCard`, `KakaoPay`, `NaverPay`, `Toss` 전략 구현 → 런타임에 사용자 선택에 따라 교체

**Spring의 전략 패턴:**
- `HttpMessageConverter`: 미디어 타입에 따라 변환 전략 교체
- `TransactionManager`: DB 종류에 따라 트랜잭션 전략 교체

📢 **섹션 요약 비유**: 결제 수단 선택은 전략 패턴의 완벽한 예시다. 카드, 계좌이체, 페이 중 선택만 바꾸면 동일한 결제 프로세스가 다른 방식으로 처리된다.

## Ⅴ. 기대효과 및 결론

전략 패턴은 알고리즘의 변화를 클라이언트로부터 격리하는 핵심 패턴이다. 기술사 시험에서는 `if-else` 대체, OCP 준수, 브리지/상태 패턴과의 차이를 명확히 서술해야 한다.

📢 **섹션 요약 비유**: 전략 패턴은 무기를 교체할 수 있는 게임 캐릭터와 같다. 캐릭터(Context)는 그대로이고 무기(전략)만 바뀐다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | GoF 행동 패턴 | 전략의 분류 |
| 연관 패턴 | 템플릿 메서드 | 상속 기반 알고리즘 뼈대 |
| 연관 패턴 | 상태 패턴 | 내부 상태에 의한 행동 위임 |
| 연관 원칙 | OCP, DIP | 전략 패턴이 준수하는 원칙 |

### 👶 어린이를 위한 3줄 비유 설명

- 같은 로봇이지만 레이저 팔, 드릴 팔, 그래플링 팔로 교체할 수 있어요.
- 팔(전략)을 교체해도 로봇(Context) 자체는 바뀌지 않아요.
- 새 팔 종류가 생겨도 로봇을 다시 만들 필요 없어요 (OCP).
''')

print("387-391 done")
