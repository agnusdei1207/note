import os
BASE = "/Users/pf/workspace/brainscience/content/studynote/11_design_supervision/06_exam_summary"
def W(fn, c):
    p = os.path.join(BASE, fn)
    if os.path.exists(p): print(f"SKIP:{fn}"); return
    with open(p,'w') as f: f.write(c)
    print(f"OK:{fn}")

W("381_prototype_pattern_summary.md",'''+++
weight = 381
title = "381. 프로토타입 (Prototype) 패턴 객체 복제"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 프로토타입 (Prototype) 패턴은 기존 객체를 복제(Clone)하여 새 인스턴스를 만드는 생성 패턴으로, 객체 초기화 비용이 클 때 성능을 최적화한다.
> 2. **가치**: DB 조회·복잡한 계산으로 초기화되는 객체를 매번 새로 만들지 않고, 기존 객체를 복제하여 수정함으로써 비용을 절감한다.
> 3. **판단 포인트**: 얕은 복사 (Shallow Copy)와 깊은 복사 (Deep Copy)의 차이를 명확히 이해하고, 참조 타입 필드가 있는 경우 반드시 깊은 복사를 구현해야 한다.

## Ⅰ. 개요 및 필요성

복잡한 초기화 과정이 필요한 객체(예: DB에서 대량 데이터를 로드하는 객체)를 매번 `new`로 생성하면 성능이 저하된다. 프로토타입 패턴은 이런 객체를 미리 하나 만들어두고, 필요 시 복제하여 사용한다.

**Java에서의 구현:**
- `Cloneable` 인터페이스 구현 + `clone()` 메서드 오버라이드
- 복사 생성자 (Copy Constructor) 사용
- 직렬화 (Serialization) + 역직렬화 방식

📢 **섹션 요약 비유**: 프로토타입은 문서 양식(Template)을 복사해서 내용을 채우는 것이다. 매번 새 양식을 그리는 것보다 훨씬 빠르다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│              Prototype 패턴 구조                                │
├─────────────────────────────────────────────────────────────────┤
│  <<interface>> Prototype                                        │
│  + clone(): Prototype                                           │
│         △                                                       │
│  ConcretePrototype1    ConcretePrototype2                        │
│  + clone()             + clone()                                │
│    → this.copy()         → this.copy()                          │
│                                                                 │
│  Client:                                                        │
│  prototype1 = registry.get("type1")  // 원본 조회               │
│  newObj = prototype1.clone()          // 복제                   │
│  newObj.setName("custom")             // 수정                   │
└─────────────────────────────────────────────────────────────────┘
```

| 복사 유형 | 설명 | 주의사항 |
|:---|:---|:---|
| 얕은 복사 (Shallow Copy) | 기본 타입은 복사, 참조 타입은 참조 공유 | 참조 객체 변경 시 원본 영향 |
| 깊은 복사 (Deep Copy) | 모든 중첩 객체까지 재귀적 복사 | 구현 복잡, 성능 비용 |

📢 **섹션 요약 비유**: 얕은 복사는 집 설계도를 복사했지만 같은 가구를 공유하는 것, 깊은 복사는 가구까지 모두 새로 만드는 것이다.

## Ⅲ. 비교 및 연결

| 항목 | 프로토타입 | 팩토리 메서드 | new 연산자 |
|:---|:---|:---|:---|
| 생성 방식 | 기존 객체 복제 | 서브클래스에 위임 | 직접 생성 |
| 초기화 비용 | 최초 1회만 | 매번 발생 | 매번 발생 |
| 유연성 | 런타임 원형 교체 가능 | 컴파일 타임 결정 | 없음 |

📢 **섹션 요약 비유**: 팩토리는 주문 시마다 요리, 프로토타입은 요리된 음식을 그대로 복사(포장)하는 것이다.

## Ⅳ. 실무 적용 및 기술사 판단

**게임 개발에서의 활용:**
몬스터 객체를 DB에서 로드하여 프로토타입으로 등록하고, 게임 진행 중 몬스터 생성 시 복제하여 사용

**프로토타입 레지스트리 (Registry):**
복제할 원형 객체들을 Map으로 관리, `registry.get("key").clone()` 형태로 사용

**주의사항:**
- 순환 참조가 있는 객체의 깊은 복사 처리
- `clone()` 메서드의 가시성 (protected → public)
- 불변 객체와의 결합 (불변 객체는 복제가 불필요)

📢 **섹션 요약 비유**: 프로토타입 레지스트리는 붕어빵 틀 창고다. 틀(원형)을 꺼내 반죽(데이터)만 바꿔 넣으면 된다.

## Ⅴ. 기대효과 및 결론

프로토타입 패턴은 초기화 비용이 큰 객체의 생성을 최적화하는 실용적인 패턴이다. 기술사 시험에서는 얕은 복사 vs 깊은 복사의 차이, Cloneable 인터페이스, 프로토타입 레지스트리 패턴을 명확히 서술해야 한다.

📢 **섹션 요약 비유**: 프로토타입은 마스터 열쇠를 만들어두고 필요한 만큼 복사하는 것이다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | GoF 생성 패턴 | 프로토타입의 분류 |
| 연관 개념 | 얕은 복사/깊은 복사 | 복제 유형 구분 |
| 연관 개념 | 싱글톤 | 인스턴스 관리의 또 다른 접근 |
| 연관 패턴 | 프로토타입 레지스트리 | 원형 객체 관리 |

### 👶 어린이를 위한 3줄 비유 설명

- 그림을 그릴 때 매번 처음부터 그리지 않고 이미 그린 그림을 복사하는 것이에요.
- 복사한 그림에 원하는 색을 다시 칠해도 원본은 변하지 않아요 (깊은 복사).
- 그림 복사가 처음부터 그리는 것보다 훨씬 빠르고 쉬워요.
''')

W("382_singleton_summary.md",'''+++
weight = 382
title = "382. 싱글톤 (Singleton) 유일 인스턴스 전역"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 싱글톤 (Singleton) 패턴은 클래스의 인스턴스를 오직 하나만 보장하고 전역 접근점을 제공하는 생성 패턴으로, 설정 관리·로깅·캐시 같은 공유 리소스에 주로 사용된다.
> 2. **가치**: 불필요한 객체 중복 생성을 방지하고 전역 상태를 일관성 있게 관리하지만, 잘못 사용하면 전역 상태로 인한 테스트 어려움과 강한 결합을 유발한다.
> 3. **판단 포인트**: 멀티스레드 환경에서 DCL (Double-Checked Locking)이나 열거형 (Enum) 방식으로 구현해야 하며, 단위 테스트 어려움을 감안해야 한다.

## Ⅰ. 개요 및 필요성

시스템 전체에서 하나의 객체만 존재해야 하는 경우(DB 연결 풀, 설정 객체, 로거 등)에 싱글톤이 필요하다. 단순 전역 변수와 달리 싱글톤은 생성 시점을 제어하고 초기화를 보장한다.

**문제점 (안티패턴 측면):**
- 전역 상태로 인한 테스트 어려움 (Mock 교체 불가)
- 강한 결합 (클라이언트가 구체 클래스에 직접 의존)
- 클래스 책임 증가 (SRP 위반 가능성)

📢 **섹션 요약 비유**: 싱글톤은 회사의 도장(인감)과 같다. 하나뿐이어야 공식성이 보장되며, 누구나 접근할 수 있지만 관리는 한 곳에서 한다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│             Singleton 구현 방식 비교                            │
├─────────────────────────────────────────────────────────────────┤
│  1. 이른 초기화 (Eager Initialization):                         │
│  private static final instance = new Singleton();              │
│  → 스레드 안전, 클래스 로드 시 즉시 생성                        │
│                                                                 │
│  2. DCL (Double-Checked Locking):                               │
│  if (instance == null) {                                        │
│    synchronized(Singleton.class) {                              │
│      if (instance == null) instance = new Singleton();          │
│    }                                                            │
│  }                                                              │
│  → volatile 키워드 필수 (명령어 재정렬 방지)                    │
│                                                                 │
│  3. Enum 방식 (권장):                                           │
│  public enum Singleton { INSTANCE; }                            │
│  → JVM이 인스턴스 유일성 보장, 직렬화 안전                      │
└─────────────────────────────────────────────────────────────────┘
```

| 구현 방식 | 스레드 안전 | 지연 초기화 | 직렬화 안전 | 권장 |
|:---|:---|:---|:---|:---|
| 이른 초기화 | ✅ | ❌ | ❌ | 단순 환경 |
| DCL + volatile | ✅ | ✅ | ❌ | Java 5+ |
| Enum 방식 | ✅ | ❌ | ✅ | ✅ 최선 |
| Holder 패턴 | ✅ | ✅ | ❌ | 대안 |

📢 **섹션 요약 비유**: DCL은 문이 잠겼는지 두 번 확인하는 이중 자물쇠이고, Enum은 아예 문이 하나뿐인 구조다.

## Ⅲ. 비교 및 연결

| 항목 | 싱글톤 | 정적 클래스 (Static Class) |
|:---|:---|:---|
| 인터페이스 구현 | ✅ 가능 | ❌ 불가능 |
| 지연 초기화 | ✅ 가능 | ❌ 클래스 로드 시 |
| 상속 | ✅ 가능 | ❌ 어려움 |
| 테스트 | 어려움 | 더 어려움 |

📢 **섹션 요약 비유**: 싱글톤은 "하나뿐인 사장님", 정적 클래스는 "회사 자체가 기능"인 구조다.

## Ⅳ. 실무 적용 및 기술사 판단

**적절한 사용 사례:**
- 설정 관리자 (Configuration Manager)
- 로그 관리자 (Log Manager)
- 연결 풀 (Connection Pool)
- 캐시 관리자

**Spring Framework에서 싱글톤:**
기본 빈 스코프가 싱글톤 → 하지만 DI (Dependency Injection) 컨테이너가 관리 → 테스트 시 Mock 교체 가능 → 안티패턴 측면이 완화됨

📢 **섹션 요약 비유**: Spring의 싱글톤은 DI 컨테이너라는 관리인이 있어, 필요 시 다른 직원(Mock)으로 교체 가능한 사장님이다.

## Ⅴ. 기대효과 및 결론

싱글톤은 공유 리소스 관리에 효과적이지만 전역 상태로 인한 부작용을 주의해야 한다. 기술사 시험에서는 DCL과 Enum 구현 방식의 차이, 멀티스레드 안전성, Spring DI 컨테이너에서의 싱글톤 관리를 명확히 서술해야 한다.

📢 **섹션 요약 비유**: 싱글톤은 학교의 교장 선생님이다. 한 명뿐이어야 권위가 있지만, 모든 반이 교장을 알고 있으면 학교 전체가 교장에 의존하게 된다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | GoF 생성 패턴 | 싱글톤의 분류 |
| 연관 개념 | DCL (Double-Checked Locking) | 스레드 안전 구현 |
| 연관 개념 | DI (Dependency Injection) | 싱글톤의 테스트 어려움 해결 |
| 연관 원칙 | SRP (Single Responsibility Principle) | 싱글톤이 위반할 수 있는 원칙 |

### 👶 어린이를 위한 3줄 비유 설명

- 학교에 교장 선생님은 한 명뿐이에요. 두 명이면 명령이 충돌해요.
- 누구나 교장실에 찾아갈 수 있지만 교장 선생님은 한 명이에요.
- 교장 선생님이 너무 많은 일을 맡으면 학교 운영이 교장에 지나치게 의존해요.
''')

W("383_adapter_pattern_summary.md",'''+++
weight = 383
title = "383. 어댑터 (Adapter) 패턴 인터페이스 호환"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 어댑터 (Adapter) 패턴은 호환되지 않는 두 인터페이스를 연결하는 변환 패턴으로, 기존 클래스를 수정하지 않고 새 인터페이스로 감싸서 클라이언트가 기대하는 인터페이스를 제공한다.
> 2. **가치**: 레거시 코드·외부 라이브러리·서드파티 API를 변경하지 않고도 현재 시스템과 통합할 수 있어 OCP (Open-Closed Principle)를 준수한다.
> 3. **판단 포인트**: 객체 어댑터 (Object Adapter)는 구성(Composition)을 사용하여 유연성이 높고, 클래스 어댑터 (Class Adapter)는 다중 상속으로 간결하지만 Java에서는 지원 안 됨을 고려해야 한다.

## Ⅰ. 개요 및 필요성

서드파티 라이브러리나 레거시 코드가 우리 시스템이 기대하는 인터페이스와 다를 때, 직접 수정이 불가능한 경우가 많다. 어댑터는 이 불일치를 해결하는 "변환 어댑터"다.

**실제 사례:**
- `java.util.Arrays.asList()`: 배열을 List로 변환
- `InputStreamReader`: InputStream을 Reader로 변환
- `Collections.enumeration()`: Collection을 Enumeration으로 변환

📢 **섹션 요약 비유**: 어댑터는 220V 전원에 110V 기기를 꽂을 때 사용하는 전압 변환기다. 전원(인터페이스)도 기기(구현)도 바꾸지 않고 중간에서 변환한다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│             Adapter 패턴 구조 (객체 어댑터)                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Client ──uses──► <<interface>> Target                          │
│                   + request(): void                             │
│                         △                                       │
│                   Adapter                                       │
│                   - adaptee: Adaptee  ◄── 구성(Composition)     │
│                   + request()                                   │
│                     → adaptee.specificRequest()                 │
│                                                                 │
│  Adaptee (변경 불가)                                            │
│  + specificRequest(): void                                      │
└─────────────────────────────────────────────────────────────────┘
```

| 어댑터 유형 | 구현 방식 | Java 지원 | 유연성 |
|:---|:---|:---|:---|
| 객체 어댑터 | 구성 (Composition) | ✅ | 높음 |
| 클래스 어댑터 | 다중 상속 | ❌ (단일 상속) | 낮음 |

📢 **섹션 요약 비유**: 객체 어댑터는 통역사가 외국어 책을 번역하는 것, 클래스 어댑터는 통역사 자신이 두 언어 모두 가진 것이다.

## Ⅲ. 비교 및 연결

| 항목 | 어댑터 | 퍼사드 | 데코레이터 |
|:---|:---|:---|:---|
| 목적 | 인터페이스 변환 | 인터페이스 단순화 | 기능 추가 |
| 인터페이스 변경 | ✅ 변환 | ✅ 새 인터페이스 | ❌ 동일 유지 |
| 내부 구현 수정 | ❌ 수정 안 함 | ❌ 수정 안 함 | ❌ 수정 안 함 |

📢 **섹션 요약 비유**: 어댑터는 플러그 모양을 바꾸는 것, 퍼사드는 복잡한 전자 기기를 리모컨 하나로 조작하게 하는 것, 데코레이터는 전자 기기에 기능을 추가하는 것이다.

## Ⅳ. 실무 적용 및 기술사 판단

**레거시 통합:**
- 기존 결제 모듈(Adaptee)을 새 결제 인터페이스(Target)에 맞게 어댑터 작성
- DDD의 ACL (Anti-Corruption Layer)은 어댑터 패턴을 기반으로 구현

**마이크로서비스 통합:**
서로 다른 데이터 형식(JSON vs XML)을 가진 서비스 간 통신 시 어댑터가 변환 처리

📢 **섹션 요약 비유**: ACL은 여러 어댑터를 조합한 복잡한 변환 계층이다.

## Ⅴ. 기대효과 및 결론

어댑터 패턴은 기존 코드의 재사용성을 극대화하면서 인터페이스 불일치를 해결하는 핵심 패턴이다. 기술사 시험에서는 객체 어댑터 vs 클래스 어댑터, OCP 준수, ACL과의 관계를 명확히 서술해야 한다.

📢 **섹션 요약 비유**: 어댑터는 두 퍼즐 조각의 모양이 다를 때 중간에 끼우는 연결 조각이다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | GoF 구조 패턴 | 어댑터의 분류 |
| 연관 패턴 | 퍼사드 (Facade) | 인터페이스 단순화 |
| 연관 패턴 | 데코레이터 (Decorator) | 기능 추가 |
| 연관 개념 | ACL (Anti-Corruption Layer) | 어댑터 기반 격리 계층 |

### 👶 어린이를 위한 3줄 비유 설명

- 해외 전자기기를 국내 콘센트에 꽂을 때 사용하는 어댑터와 같아요.
- 콘센트(인터페이스)도 기기(구현)도 바꾸지 않아요.
- 중간에 변환 플러그(어댑터 클래스)만 추가해요.
''')

W("384_bridge_pattern_summary.md",'''+++
weight = 384
title = "384. 브리지 (Bridge) 패턴 추상-구현 분리"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 브리지 (Bridge) 패턴은 추상(Abstraction)과 구현(Implementation)을 독립적으로 변화할 수 있도록 분리하여, 상속의 조합 폭발 (Cartesian Product) 문제를 구성 (Composition)으로 해결한다.
> 2. **가치**: `Shape × Color`처럼 두 차원이 독립적으로 증가하는 경우, 상속으로는 M×N개의 클래스가 필요하지만 브리지로는 M+N개만 필요하다.
> 3. **판단 포인트**: 추상 계층과 구현 계층 모두 확장이 필요할 때 브리지가 적합하며, 런타임에 구현을 교체할 수 있다는 점이 전략 패턴과의 유사점이다.

## Ⅰ. 개요 및 필요성

"빨간 원", "파란 원", "빨간 사각형", "파란 사각형"을 상속으로 표현하면 4개의 클래스가 필요하다. 색상과 도형이 각각 3가지씩 추가되면 9개가 된다. 브리지는 도형 계층과 색상 계층을 분리하여 이 폭발을 막는다.

**구성 요소:**
- `Abstraction`: 상위 수준 제어 로직, Implementor 참조
- `RefinedAbstraction`: Abstraction 확장
- `Implementor`: 구현 인터페이스
- `ConcreteImplementor`: 실제 구현

📢 **섹션 요약 비유**: 브리지는 리모컨(추상)과 TV(구현)의 분리다. 삼성 TV, LG TV 어떤 TV도 같은 리모컨으로 작동한다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│              Bridge 패턴 구조                                   │
├─────────────────────────────────────────────────────────────────┤
│  Abstraction(Shape)                                             │
│  - impl: ColorImplementor ◄── 브리지(구성)                      │
│  + draw()                                                       │
│       △                                                         │
│  Circle      Rectangle                                          │
│  + draw()    + draw()                                           │
│  → impl.applyColor()                                            │
│                                                                 │
│  <<interface>> ColorImplementor                                 │
│  + applyColor()                                                 │
│       △                                                         │
│  RedColor    BlueColor    GreenColor                            │
│                                                                 │
│  Shape(2) × Color(3) → 클래스 2+3=5개 (상속 시 6개)            │
└─────────────────────────────────────────────────────────────────┘
```

| 항목 | 상속 접근 | 브리지 패턴 |
|:---|:---|:---|
| 클래스 수 | M × N | M + N |
| 독립 확장 | 불가능 | ✅ 가능 |
| 런타임 교체 | 불가능 | ✅ 가능 |
| 복잡도 | 조합 폭발 | 선형 증가 |

📢 **섹션 요약 비유**: 상속은 모든 색상·도형 조합을 직접 만드는 것, 브리지는 색상 팔레트와 도형 세트를 따로 관리하고 원할 때 조합하는 것이다.

## Ⅲ. 비교 및 연결

| 패턴 | 목적 | 관계 유형 |
|:---|:---|:---|
| 브리지 | 추상-구현 분리 | 구성 (has-a) |
| 전략 | 알고리즘 교체 | 구성 (has-a) |
| 어댑터 | 인터페이스 변환 | 구성 (has-a) |
| 데코레이터 | 기능 추가 | 구성 (has-a) |

📢 **섹션 요약 비유**: 브리지와 전략의 구조는 비슷하지만, 브리지는 설계 시점에 추상-구현을 분리하고, 전략은 런타임에 알고리즘을 교체한다.

## Ⅳ. 실무 적용 및 기술사 판단

**실무 예시:**
- JDBC 드라이버: SQL 처리(추상) + DB별 드라이버(구현)
- Java AWT: 컴포넌트(추상) + OS별 피어(구현)
- 로거: 로거 인터페이스(추상) + 파일/콘솔/DB 출력(구현)

📢 **섹션 요약 비유**: JDBC는 SQL 쿼리를 작성하는 추상 계층과 MySQL/Oracle/PostgreSQL 드라이버라는 구현 계층이 브리지로 연결된 대표 사례다.

## Ⅴ. 기대효과 및 결론

브리지 패턴은 두 독립적인 차원이 함께 증가하는 시스템에서 클래스 폭발을 방지하는 핵심 패턴이다. 기술사 시험에서는 조합 폭발 문제, M+N vs M×N 비교, JDBC 예시를 구체적으로 서술해야 한다.

📢 **섹션 요약 비유**: 브리지는 강의 양쪽 둑(추상과 구현)을 연결하되, 각 둑은 독립적으로 확장할 수 있는 구조다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | GoF 구조 패턴 | 브리지의 분류 |
| 연관 패턴 | 전략 패턴 | 유사 구조, 다른 의도 |
| 연관 개념 | 구성 (Composition) | 브리지의 핵심 원리 |
| 연관 개념 | OCP | 추상-구현 독립 확장 |

### 👶 어린이를 위한 3줄 비유 설명

- 리모컨(추상)과 TV(구현)를 따로 만들면 어떤 TV도 리모컨으로 조작할 수 있어요.
- 새 TV가 나와도 리모컨은 그대로, 새 리모컨을 만들어도 TV는 그대로예요.
- 리모컨과 TV를 하나로 붙이면 조합 수만큼 새로 만들어야 해요.
''')

W("385_composite_pattern_summary.md",'''+++
weight = 385
title = "385. 컴포지트 (Composite) 패턴 부분-전체 트리 구조"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 컴포지트 (Composite) 패턴은 단일 객체(Leaf)와 복합 객체(Composite)를 같은 인터페이스로 다룰 수 있게 하여, 트리 구조의 부분과 전체를 균일하게 처리한다.
> 2. **가치**: 클라이언트가 개별 객체인지 컨테이너 객체인지 구분하지 않아도 되어, 파일 시스템·UI 컴포넌트·조직도 같은 재귀적 구조를 단순하게 표현할 수 있다.
> 3. **판단 포인트**: 리프 노드와 컴포지트 노드에 동일 인터페이스를 강제하면 리프에 불필요한 메서드(add/remove)가 생기므로, 안전성(Safety)과 투명성(Transparency) 트레이드오프를 고려해야 한다.

## Ⅰ. 개요 및 필요성

파일 시스템에서 파일과 폴더를 다룰 때, 파일과 폴더가 서로 다른 인터페이스를 가지면 클라이언트 코드에 `if (isFile) ... else if (isFolder)...` 분기가 넘쳐난다. 컴포지트 패턴은 파일(Leaf)과 폴더(Composite)를 동일한 `FileSystemComponent` 인터페이스로 다루어 이 문제를 해결한다.

**구성 요소:**
- `Component`: 공통 인터페이스
- `Leaf`: 자식이 없는 기본 객체 (파일)
- `Composite`: 자식을 가지는 컨테이너 (폴더), 자식의 `operation()` 위임 호출

📢 **섹션 요약 비유**: 컴포지트는 레고 블록과 같다. 작은 블록(Leaf)과 블록 조합(Composite)을 모두 "블록"으로 다루어 어떻게 조합하든 같은 방법으로 다룬다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│              Composite 패턴 구조 (파일 시스템 예시)             │
├─────────────────────────────────────────────────────────────────┤
│  <<interface>> FileSystemComponent                              │
│  + getSize(): int                                               │
│  + print(indent)                                                │
│       △                    △                                    │
│  File (Leaf)         Folder (Composite)                         │
│  + getSize()         - children: List<Component>                │
│    → fileSize          + getSize() → sum(children.getSize())    │
│  + print()           + add(Component)                           │
│                      + remove(Component)                        │
│                      + print() → 재귀 호출                      │
│                                                                 │
│  트리 구조:                                                     │
│  Root/                                                          │
│  ├── docs/ (Composite)                                          │
│  │   ├── a.txt (Leaf)                                           │
│  │   └── b.txt (Leaf)                                           │
│  └── img.png (Leaf)                                             │
└─────────────────────────────────────────────────────────────────┘
```

| 접근 방식 | 투명성 | 안전성 | 설명 |
|:---|:---|:---|:---|
| 투명성 우선 | ✅ 높음 | ❌ 낮음 | 리프에도 add/remove 포함 |
| 안전성 우선 | ❌ 낮음 | ✅ 높음 | Composite에만 add/remove |

📢 **섹션 요약 비유**: 투명성은 "모든 조직원이 팀장 역할도 할 수 있지만 팀이 없으면 아무 의미 없는" 것, 안전성은 "팀장 역할은 팀장만"인 것이다.

## Ⅲ. 비교 및 연결

| 항목 | 컴포지트 | 데코레이터 |
|:---|:---|:---|
| 목적 | 트리 구조 통일 처리 | 단일 객체 기능 추가 |
| 구조 | 트리 (재귀) | 체인 (선형) |
| 자식 | 여러 자식 | 하나의 컴포넌트 |

📢 **섹션 요약 비유**: 컴포지트는 조직도, 데코레이터는 양파 껍질처럼 겹겹이 쌓는 구조다.

## Ⅳ. 실무 적용 및 기술사 판단

**실무 적용 예시:**
- Java Swing/AWT: `Container` (Composite) + `Button`, `Label` (Leaf)
- XML/HTML DOM: `Element` (Composite) + 텍스트 노드 (Leaf)
- 조직도: 팀 (Composite) + 직원 (Leaf)

**재귀 합산 활용:**
폴더 전체 크기 = 하위 모든 파일 크기의 합을 `getSize()` 재귀로 계산

📢 **섹션 요약 비유**: 회사 전체 인원 수 계산을 본사 → 사업부 → 팀 → 직원 순으로 재귀 합산하면 컴포지트다.

## Ⅴ. 기대효과 및 결론

컴포지트 패턴은 트리 구조의 재귀적 처리를 단순화하는 핵심 패턴이다. 기술사 시험에서는 단일/복합 객체의 균일 처리, 투명성 vs 안전성 트레이드오프, 파일 시스템 예시를 명확히 서술해야 한다.

📢 **섹션 요약 비유**: 컴포지트는 러시아 마트료시카 인형이다. 큰 인형 안에 작은 인형이 있고, 모든 인형을 동일하게 다룬다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | GoF 구조 패턴 | 컴포지트의 분류 |
| 연관 패턴 | 데코레이터 | 체인 구조 vs 트리 구조 |
| 연관 패턴 | 반복자 (Iterator) | 트리 구조 순회 |
| 연관 개념 | 재귀 (Recursion) | 컴포지트의 핵심 원리 |

### 👶 어린이를 위한 3줄 비유 설명

- 폴더 안에 파일도 있고 폴더도 있는데, 모두 "파일 시스템 항목"으로 똑같이 다뤄요.
- 폴더 크기를 계산할 때 안에 있는 모든 파일 크기를 더하면 돼요 (재귀).
- 새 종류의 항목을 추가해도 공통 인터페이스만 지키면 기존 코드를 수정할 필요가 없어요.
''')

W("386_decorator_pattern_summary.md",'''+++
weight = 386
title = "386. 데코레이터 (Decorator) 패턴 동적 책임 추가"
date = "2026-04-21"
[extra]
categories = "studynote-design-supervision"
+++

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 데코레이터 (Decorator) 패턴은 객체에 동적으로 새로운 책임(기능)을 추가하는 구조 패턴으로, 서브클래싱보다 유연한 기능 확장 수단이다.
> 2. **가치**: 여러 기능 조합이 필요할 때 `new LoggingDecorator(new CompressionDecorator(new FileDataSource()))` 처럼 체인으로 조합하여, 상속의 조합 폭발 없이 기능을 쌓을 수 있다.
> 3. **판단 포인트**: 데코레이터는 래핑 대상과 동일한 인터페이스를 유지하므로 클라이언트는 데코레이터 여부를 알 필요가 없지만, 많이 쌓이면 디버깅이 어려울 수 있다.

## Ⅰ. 개요 및 필요성

커피 주문 시스템을 예로 들면: 에스프레소, 에스프레소+우유, 에스프레소+설탕, 에스프레소+우유+설탕을 모두 서브클래스로 만들면 조합 수만큼 클래스가 필요하다. 데코레이터는 에스프레소 위에 우유, 설탕을 동적으로 추가하여 이 폭발을 방지한다.

**구성 요소:**
- `Component` 인터페이스
- `ConcreteComponent`: 기본 객체
- `Decorator`: Component를 감싸는 추상 데코레이터
- `ConcreteDecorator`: 실제 기능을 추가하는 데코레이터

📢 **섹션 요약 비유**: 데코레이터는 아이스크림 위에 시럽, 견과류, 초코 소스를 순서대로 올리는 것이다. 아이스크림 자체는 바뀌지 않고 그 위에 기능이 쌓인다.

## Ⅱ. 아키텍처 및 핵심 원리

```text
┌─────────────────────────────────────────────────────────────────┐
│              Decorator 패턴 구조                                │
├─────────────────────────────────────────────────────────────────┤
│  <<interface>> DataSource                                       │
│  + writeData(data)                                              │
│  + readData(): data                                             │
│       △                    △                                    │
│  FileDataSource      DataSourceDecorator                        │
│  (ConcreteComponent)  - wrappee: DataSource ◄── 핵심            │
│  + writeData()        + writeData()                             │
│  + readData()           → wrappee.writeData()                   │
│                                △                               │
│                       EncryptionDecorator  CompressionDecorator │
│                       + writeData():       + writeData():       │
│                         encrypt → super      compress → super   │
│                                                                 │
│  사용:                                                          │
│  source = new EncryptionDecorator(                              │
│             new CompressionDecorator(                           │
│               new FileDataSource("file.txt")))                  │
└─────────────────────────────────────────────────────────────────┘
```

| 비교 항목 | 데코레이터 | 상속 |
|:---|:---|:---|
| 기능 추가 시점 | 런타임 (동적) | 컴파일 타임 |
| 조합 가능성 | 체인으로 자유 조합 | 클래스 수 폭발 |
| 기존 코드 수정 | 불필요 (OCP 준수) | 가능성 있음 |

📢 **섹션 요약 비유**: 상속은 "우유 에스프레소" 메뉴를 미리 만드는 것, 데코레이터는 에스프레소에 우유를 동적으로 붓는 것이다.

## Ⅲ. 비교 및 연결

| 패턴 | 인터페이스 변경 | 기능 추가 | 재귀 가능 |
|:---|:---|:---|:---|
| 데코레이터 | ❌ 동일 유지 | ✅ 동적 | ✅ 체인 |
| 어댑터 | ✅ 변환 | ❌ | ❌ |
| 컴포지트 | ❌ 동일 유지 | ❌ | ✅ 트리 |

📢 **섹션 요약 비유**: 데코레이터는 양파 껍질처럼 층층이 쌓이고, 안쪽이 실제 값이다.

## Ⅳ. 실무 적용 및 기술사 판단

**Java I/O 스트림:**
```
BufferedInputStream(new GZIPInputStream(new FileInputStream("file")))
```
Java I/O는 데코레이터 패턴의 교과서적 구현이다.

**Spring AOP (Aspect-Oriented Programming):**
트랜잭션·로깅·보안을 원본 메서드 호출 전후에 동적으로 추가 = 데코레이터 원리

📢 **섹션 요약 비유**: Spring @Transactional은 메서드를 트랜잭션 데코레이터로 감싸는 것이다.

## Ⅴ. 기대효과 및 결론

데코레이터 패턴은 OCP를 준수하면서 런타임에 기능을 동적으로 조합하는 핵심 패턴이다. 기술사 시험에서는 Java I/O 스트림 예시, 체인 조합의 유연성, 상속 대비 장점을 구체적으로 서술해야 한다.

📢 **섹션 요약 비유**: 데코레이터는 생일 케이크에 초, 장식, 촛불을 순서대로 추가하는 것이다. 케이크 자체는 변하지 않는다.

### 📌 관련 개념 맵

| 관계 | 개념 | 설명 |
|:---|:---|:---|
| 상위 개념 | GoF 구조 패턴 | 데코레이터의 분류 |
| 연관 개념 | OCP (Open-Closed Principle) | 데코레이터가 준수하는 원칙 |
| 연관 패턴 | 컴포지트 | 동일 인터페이스, 트리 구조 |
| 실무 사례 | Java I/O, Spring AOP | 데코레이터 구현 |

### 👶 어린이를 위한 3줄 비유 설명

- 기본 아이스크림에 초코, 딸기, 카라멜 소스를 원하는 대로 추가해요.
- 아이스크림 자체는 바뀌지 않아요.
- 소스를 추가하는 순서를 바꾸면 다른 맛이 나와요.
''')

print("381-386 done")
